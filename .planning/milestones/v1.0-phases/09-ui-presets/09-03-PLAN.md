---
phase: 09-ui-presets
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - source/FactoryPresets.h
  - source/FactoryPresets.cpp
  - source/PluginProcessor.cpp
  - resources/presets/*.xml
  - tests/PresetTest.cpp
  - tests/PluginEditorTest.cpp
  - CMakeLists.txt
  - .planning/phases/09-ui-presets/VERIFICATION.md
autonomous: true
requirements:
  - PRST-01
  - PRST-02
must_haves:
  truths:
    - "8 factory presets embedded in bundle"
    - "Preset load/save round-trip preserves all parameter values"
    - "pluginval 7 gate documented"
  artifacts:
    - path: source/FactoryPresets.cpp
      provides: "PRST-01 factory presets"
    - path: tests/PresetTest.cpp
      provides: "PRST-02 round-trip proofs"
    - path: .planning/phases/09-ui-presets/VERIFICATION.md
      provides: "Phase 9 verification record"
---

<objective>
Eight factory XML presets, program API, round-trip tests, pluginval 7, VERIFICATION.md (PRST-01, PRST-02).
</objective>

<context>
@.planning/phases/09-ui-presets/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Factory presets and round-trip tests</name>
  <files>source/FactoryPresets.h, source/FactoryPresets.cpp, resources/presets/*.xml, CMakeLists.txt, source/PluginProcessor.cpp, tests/PresetTest.cpp</files>
  <action>
Create 8 factory preset XML files in resources/presets/.
Embed via juce_add_binary_data(SendBloomPresets).
FactoryPresets: load by index, return names, apply to APVTS.
Wire PluginProcessor getNumPrograms/setCurrentProgram/getProgramName to FactoryPresets.
PresetTest: load each factory preset, save state, restore, verify all params match.
  </action>
  <verify>
    <automated>ctest --test-dir Builds -R "Preset|preset" --output-on-failure</automated>
  </verify>
  <done>8 presets load; round-trip tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Editor smoke test and verification doc</name>
  <files>tests/PluginEditorTest.cpp, .planning/phases/09-ui-presets/VERIFICATION.md, README.md</files>
  <action>
PluginEditorTest: instantiate editor headless, verify child component count and size 340×520.
Write VERIFICATION.md with test counts and human_needed for DAW UI smoke.
Add README Phase 9 DAW smoke checklist.
Run full ctest suite.
  </action>
  <verify>
    <automated>ctest --test-dir Builds --output-on-failure</automated>
  </verify>
  <done>Full suite GREEN; VERIFICATION.md written.</done>
</task>

<task type="checkpoint:human-verify" gate="human-needed">
  <name>Task 3: DAW UI and preset audition smoke</name>
  <what-built>Full pedal UI with 8 factory presets</what-built>
  <how-to-verify>
1. Build Release AU/VST3
2. Load SendBloom in DAW
3. Verify pedal layout, pressure pad bloom, clip LED on hot input
4. Open Advanced drawer; confirm disabled Extended Stereo / Dirt OS
5. Load each factory preset from menu; audition
Reply `approved` when complete.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<success_criteria>
PRST-01/02 complete; automated gates pass; human DAW smoke documented as pending.
</success_criteria>
