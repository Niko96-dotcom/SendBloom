---
phase: 06-wet-overdrive-dry-integrity
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - source/GatedBloomChain.h
  - tests/DryPathIntegrityTest.cpp
  - tests/ChainTestHelpers.h
autonomous: true
requirements:
  - OD-01
  - OD-03
must_haves:
  truths:
    - "GatedBloomChain uses WetOverdrive instead of PlaceholderWetDirt"
    - "Dry-path THD unchanged at distn=1 level=1"
    - "Phase 3/4/5 routing tests still pass"
  artifacts:
    - path: source/GatedBloomChain.h
      provides: "WetOverdrive swap preserving topology"
    - path: tests/DryPathIntegrityTest.cpp
      provides: "OD-03 dry-tap THD regression"
---

<objective>
Swap placeholder dirt in chain; add dry-path THD integrity test.
</objective>

<context>
@source/GatedBloomChain.h
@source/PluginProcessor.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chain swap and dry THD test</name>
  <files>source/GatedBloomChain.h, tests/DryPathIntegrityTest.cpp, tests/ChainTestHelpers.h</files>
  <action>
Replace PlaceholderWetDirt with WetOverdrive in GatedBloomChain.
Add Goertzel THD helper to ChainTestHelpers.
Add DryPathIntegrityTest: extract dry (output−wet) at distn=0 vs distn=1, level=1; THD must match.
Run routing regression: GatedBloomChain, DryNeverGated, PluginBasics chain tests.
  </action>
  <verify>
    <automated>ctest --test-dir Builds -R "GatedBloomChain|DryNeverGated|DryPath|chain|routing|thd" --output-on-failure</automated>
  </verify>
  <done>OD-03 passes; routing intact.</done>
</task>

</tasks>

<success_criteria>
WetOverdrive in chain; dry-path THD test passes; routing regression green.
</success_criteria>
