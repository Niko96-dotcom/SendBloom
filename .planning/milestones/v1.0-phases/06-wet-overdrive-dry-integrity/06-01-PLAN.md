---
phase: 06-wet-overdrive-dry-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - source/WetOverdrive.h
  - tests/WetOverdriveTest.cpp
autonomous: true
requirements:
  - OD-01
  - OD-02
must_haves:
  truths:
    - "WetOverdrive applies asymmetric tanh at fixed drive"
    - "distnBlend=0 passes clean wet; distnBlend=1 passes fully driven wet"
    - "Positive input produces stronger drive than negative (asymmetry)"
  artifacts:
    - path: source/WetOverdrive.h
      provides: "OD-01 fixed asymmetric circuit"
    - path: tests/WetOverdriveTest.cpp
      provides: "OD-01/02 unit proofs"
---

<objective>
WetOverdrive asymmetric tanh circuit with unit tests (TDD).
</objective>

<context>
@.planning/phases/06-wet-overdrive-dry-integrity/06-CONTEXT.md
@.planning/phases/06-wet-overdrive-dry-integrity/06-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — WetOverdrive unit tests</name>
  <files>tests/WetOverdriveTest.cpp</files>
  <behavior>
    - distnBlend=0 returns input unchanged
    - distnBlend=1 returns asymmetricTanh(input)
    - Positive sample drives harder than negative (asymmetry vs symmetric tanh)
    - Blend at 0.5 is midpoint between clean and driven
  </behavior>
  <action>Create failing tests tagged [od][WetOverdrive].</action>
  <verify>
    <automated>ctest --test-dir Builds -R "WetOverdrive|od" --output-on-failure 2>&1; test $? -ne 0</automated>
  </verify>
  <done>Tests exist and fail RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — WetOverdrive implementation</name>
  <files>source/WetOverdrive.h</files>
  <behavior>
    - Fixed drive 3.0, positive asymmetry 1.12
    - process(wet, distnBlend) linear blend clean↔driven
  </behavior>
  <action>Implement header-only WetOverdrive per RESEARCH. Run tests GREEN.</action>
  <verify>
    <automated>ctest --test-dir Builds -R "WetOverdrive|od" --output-on-failure</automated>
  </verify>
  <done>OD-01/02 unit tests GREEN.</done>
</task>

</tasks>

<success_criteria>
WetOverdrive implemented with asymmetric tanh and blend unit tests passing.
</success_criteria>
