---
phase: 01-scaffold-passthrough
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - .github/workflows/build_and_test.yml
autonomous: true
requirements:
  - SCAF-03
  - SCAF-04
must_haves:
  truths:
    - "GitHub Actions matrix builds and tests on macOS, Windows, and Linux"
    - "pluginval runs at strictness level 5 against VST3 on all platforms"
    - "macOS workflow builds AU; no code-signing steps run without secrets"
  artifacts:
    - path: .github/workflows/build_and_test.yml
      provides: "Three-platform CI with ctest and pluginval"
      contains: "STRICTNESS_LEVEL"
  key_links:
    - from: .github/workflows/build_and_test.yml
      to: cmake/GitHubENV.cmake
      via: "Read .env step exports VST3_PATH for pluginval"
      pattern: "pluginval"
    - from: .github/workflows/build_and_test.yml
      to: Builds/SendBloom_artefacts
      via: "cmake build produces VST3 artifact path"
      pattern: "VST3_PATH"
---

<objective>
Ship slimmed GitHub Actions workflow: three-platform build matrix, ctest, and pluginval strictness 5 (SCAF-03, SCAF-04).

Purpose: Proves cross-platform reproducibility and binary validation before DAW manual smoke.
Output: `.github/workflows/build_and_test.yml` adapted from pamplejuce without signing/packaging/IPP/CLAP.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-scaffold-passthrough/01-02-SUMMARY.md
@.planning/phases/01-scaffold-passthrough/01-RESEARCH.md
@.planning/repo-samples/sudara-pamplejuce/.github/workflows/build_and_test.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create slimmed build_and_test.yml matrix workflow</name>
  <files>.github/workflows/build_and_test.yml</files>
  <action>
Adapt pamplejuce build_and_test.yml per 01-RESEARCH.md Pattern 3 and Pitfalls 1/3/7:

Set workflow name SendBloom. Keep triggers: push, pull_request, workflow_dispatch. Matrix: ubuntu-22.04, macos-14, windows-latest with pluginval-binary paths from research.

env: BUILD_TYPE Release, BUILD_DIR Builds, STRICTNESS_LEVEL 5 (SCAF-04 — override pamplejuce default 10), DISPLAY :0 for Linux.

Steps through ctest: checkout with submodules recursive, configure with Ninja, build, ctest verbose, read .env from GitHubENV.cmake.

REMOVE or skip for Phase 1: Intel IPP apt/nuget install steps, Codesign, notarize, pkgbuild, installer.iss, Azure signing, artifact upload/release jobs (optional keep upload for debugging — planner discretion: omit to keep slim).

Linux: install JUCE deps + xvfb per upstream workflow but WITHOUT Intel oneAPI repo. Start Xvfb before pluginval.

Windows: msvc-dev-cmd, clang-cl, ninja via choco.

macOS: brew ninja; AU builds natively.

Do not reference CLAP, Standalone, or AUv3 artifact paths in env setup (FORMATS AU VST3 only). AU_PATH only used on macOS if needed later; pluginval AU on CI is optional/deferred per research Pitfall 2 — validate VST3 only in pluginval step.
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/build_and_test.yml')); assert d['env'].get('STRICTNESS_LEVEL')==5 or d['env'].get('STRICTNESS_LEVEL')=='5'; assert 'ubuntu' in str(d['jobs']['build_and_test']['strategy']['matrix'])" 2>/dev/null || grep -q 'STRICTNESS_LEVEL: 5' .github/workflows/build_and_test.yml && grep -q 'ubuntu-22.04' .github/workflows/build_and_test.yml && grep -q 'macos-14' .github/workflows/build_and_test.yml && grep -q 'windows-latest' .github/workflows/build_and_test.yml</automated>
  </verify>
  <done>Workflow file exists with three OS matrix entries and STRICTNESS_LEVEL 5; no signing steps without secret guards.</done>
</task>

<task type="auto">
  <name>Task 2: Add pluginval validation step at strictness 5</name>
  <files>.github/workflows/build_and_test.yml</files>
  <action>
Add Pluginval step after ctest per research: download pluginval v1.0.3 release zip for runner.os, extract with 7z, run with --strictness-level from env.STRICTNESS_LEVEL (5), --verbose, --validate against env.VST3_PATH.

Ensure Set additional env vars step computes VST3_PATH as Builds/SendBloom_artefacts/Release/VST3/SendBloom.vst3 (or PRODUCT_NAME from .env).

Linux: pluginval runs after Xvfb started. Fail job on pluginval exit code 1.

Do NOT set strictness 10 (violates SCAF-04 Phase 1 requirement).
  </action>
  <verify>
    <automated>grep -v '^#' .github/workflows/build_and_test.yml | grep -c 'strictness-level' | xargs test 1 -le</automated>
  </verify>
  <done>pluginval step references strictness-level and VST3_PATH; uses release v1.0.3 URL pattern.</done>
</task>

<task type="auto">
  <name>Task 3: Push workflow and verify CI config locally</name>
  <files>.github/workflows/build_and_test.yml</files>
  <action>
Validate YAML structure. If remote exists, push branch to trigger workflow; otherwise document in SUMMARY that CI verification requires first push to GitHub.

Run local dry-run equivalent: cmake -B Builds -DCMAKE_BUILD_TYPE=Release, build VST3, download pluginval for host OS manually if available, or note MISSING for local pluginval and rely on CI.

Record workflow run URL or pending-push note in 01-03-SUMMARY.md.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target SendBloom_VST3 2>/dev/null || cmake --build Builds --config Release</automated>
  </verify>
  <done>Local VST3 build succeeds; workflow ready for GHA; SUMMARY documents CI status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → pluginval zip download | External binary from GitHub releases |
| CI logs | Must not echo signing secrets (none in Phase 1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Tampering | pluginval download URL | high | mitigate | Pin v1.0.3 release URL from Tracktion/pluginval official repo |
| T-01-07 | Information Disclosure | GHA logs | low | accept | No secrets in Phase 1 workflow |
| T-01-08 | Denial of Service | Linux headless | medium | mitigate | Xvfb DISPLAY :0 before pluginval per pamplejuce pattern |
</threat_model>

<verification>
CI gate: GitHub Actions build_and_test workflow green on all three matrix legs with pluginval strictness 5 passing.
</verification>

<success_criteria>
- SCAF-03: macOS, Windows, Linux matrix defined and passing (after push)
- SCAF-04: pluginval strictness 5 in CI env and CLI args
</success_criteria>

<output>
Create `.planning/phases/01-scaffold-passthrough/01-03-SUMMARY.md` when done
</output>
