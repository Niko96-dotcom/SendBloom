---
phase: 01-scaffold-passthrough
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - source/PluginProcessor.h
  - source/PluginProcessor.cpp
  - source/PluginEditor.h
  - source/PluginEditor.cpp
  - tests/PluginBasics.cpp
autonomous: true
requirements:
  - SCAF-02
  - SCAF-05
must_haves:
  truths:
    - "processBlock passes audio unchanged (explicit passthrough, no allocation on audio thread)"
    - "Plugin reports name SendBloom and builds as AU and VST3"
    - "Catch2 passthrough and plugin-name tests pass locally"
  artifacts:
    - path: source/PluginProcessor.cpp
      provides: "sendbloom passthrough processBlock"
      contains: "processBlock"
    - path: tests/PluginBasics.cpp
      provides: "GREEN passthrough identity tests"
  key_links:
    - from: source/PluginProcessor.cpp
      to: JUCE audio callback
      via: "processBlock clears extra outputs, leaves input samples intact"
      pattern: "ScopedNoDenormals"
    - from: tests/PluginBasics.cpp
      to: source/PluginProcessor.cpp
      via: "offline processBlock call in Catch2"
      pattern: "processBlock"
---

<objective>
Implement explicit passthrough DSP and minimal editor so SendBloom builds as AU+VST3 and Catch2 tests go GREEN (SCAF-02).

Purpose: Delivers the first loadable plugin artifact — audio in equals audio out — proving the JUCE processor graph works.
Output: Working passthrough processor, minimal editor, all PluginBasics tests passing, local Release build of AU+VST3+Tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-scaffold-passthrough/01-01-SUMMARY.md
@.planning/phases/01-scaffold-passthrough/01-RESEARCH.md
@.planning/phases/01-scaffold-passthrough/SKELETON.md
@.planning/repo-samples/sudara-pamplejuce/source/PluginProcessor.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement sendbloom passthrough processBlock</name>
  <files>source/PluginProcessor.h, source/PluginProcessor.cpp</files>
  <behavior>
    - Passthrough test passes: output samples equal input samples for stereo 512-block sine
    - Extra output channels cleared when numOutput > numInput
    - No heap allocation, locks, or logging in processBlock
  </behavior>
  <action>
Implement sendbloom::PluginProcessor::processBlock per 01-RESEARCH.md Pattern 2: juce::ScopedNoDenormals, ignoreUnused midiMessages, clear output channels beyond numInput, leave input channel samples untouched (optional explicit copyFrom for clarity when counts match).

Remove any intentional test-breaking stub from Plan 01 (e.g., gain attenuation).

Ensure isBusesLayoutSupported allows mono and stereo main bus layouts common to guitar hosts.

Verify getName() returns SendBloom via JucePlugin_Name / PRODUCT_NAME chain (SCAF-05).

Confirm prepareToPlay and releaseResources are no-op safe for passthrough.
  </action>
  <verify>
    <automated>cmake --build Builds --target Tests && ctest --test-dir Builds -R "Passthrough preserves audio" --output-on-failure</automated>
  </verify>
  <done>Passthrough Catch2 test passes; processBlock is realtime-safe passthrough.</done>
</task>

<task type="auto">
  <name>Task 2: Minimal PluginEditor and plugin-name test GREEN</name>
  <files>source/PluginEditor.h, source/PluginEditor.cpp, tests/PluginBasics.cpp</files>
  <action>
Implement minimal sendbloom::PluginEditor: default size ~400x300, optional version label from VERSION file or CMAKE VERSION define, no custom graphics required. createEditor returns new PluginEditor.

Ensure PluginBasics "Plugin instance" name test expects SendBloom and passes.

Run full PluginBasics test file to confirm all instance tests green.
  </action>
  <verify>
    <automated>ctest --test-dir Builds -R "Plugin instance|Passthrough" --output-on-failure</automated>
  </verify>
  <done>Plugin name test and passthrough test both pass.</done>
</task>

<task type="auto">
  <name>Task 3: Local Release build of AU, VST3, and full ctest suite</name>
  <files>source/PluginProcessor.cpp</files>
  <action>
Build Release targets for the main plugin (AU+VST3 on macOS; VST3 on other platforms if testing cross-platform locally). Run full ctest suite verbose.

On macOS, confirm artifacts appear under Builds/SendBloom_artefacts/Release/VST3/SendBloom.vst3 and AU/SendBloom.component. Document artifact paths in 01-02-SUMMARY.md for Plan 04 DAW smoke reference.

Do not add effect DSP, parameters, or preset logic.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release && ctest --test-dir Builds --output-on-failure</automated>
  </verify>
  <done>Release build succeeds; ctest full suite green; VST3 artifact path known.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host → processBlock | Untrusted buffer sizes from DAW; JUCE AudioBuffer contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Denial of Service | processBlock | medium | mitigate | Use buffer.getNumSamples() bounds; clear only valid channel indices |
| T-01-05 | Tampering | Audio buffer | low | accept | Passthrough does not mutate beyond clear-extra-channels; host owns buffers |
</threat_model>

<verification>
Local gate: `cmake --build Builds --config Release && ctest --test-dir Builds --verbose --output-on-failure` all pass.
</verification>

<success_criteria>
- SCAF-02: Passthrough plugin builds; Catch2 proves identity; AU+VST3 artifacts exist on macOS
- SCAF-05: getName() returns SendBloom
</success_criteria>

<output>
Create `.planning/phases/01-scaffold-passthrough/01-02-SUMMARY.md` when done
</output>
