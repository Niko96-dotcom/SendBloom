---
phase: 01-scaffold-passthrough
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CMakeLists.txt
  - .gitmodules
  - .gitignore
  - VERSION
  - .clang-format
  - README.md
  - source/PluginProcessor.h
  - source/PluginProcessor.cpp
  - source/PluginEditor.h
  - source/PluginEditor.cpp
  - tests/Catch2Main.cpp
  - tests/PluginBasics.cpp
  - tests/helpers/test_helpers.h
autonomous: true
requirements:
  - SCAF-01
  - SCAF-05
must_haves:
  truths:
    - "CMake configures SendBloom with JUCE 8, C++20, AU+VST3 formats, and NkMo/SbLm metadata"
    - "Catch2 passthrough and plugin-name tests exist and fail before DSP implementation"
    - "git submodule init produces JUCE and cmake-includes directories"
  artifacts:
    - path: CMakeLists.txt
      provides: "juce_add_plugin SendBloom with AU VST3, NkMo, SbLm, Niko Audio Labs"
      contains: "PLUGIN_MANUFACTURER_CODE NkMo"
    - path: .gitmodules
      provides: "JUCE 8 and cmake-includes submodule URLs"
    - path: tests/PluginBasics.cpp
      provides: "Failing RED tests for passthrough identity and SendBloom name"
      contains: "[passthrough]"
    - path: README.md
      provides: "Submodule init and local build commands"
  key_links:
    - from: CMakeLists.txt
      to: cmake/Tests.cmake
      via: "include(Tests) after SharedCode wiring"
      pattern: "include\\(Tests\\)"
    - from: tests/PluginBasics.cpp
      to: source/PluginProcessor.h
      via: "Tests target links SharedCode"
      pattern: "PluginProcessor"
---

## Phase Goal

**As a** developer, **I want to** build, CI-verify, and load a legal SendBloom passthrough plugin in AU and VST3, **so that** I have a verified foundation for all subsequent DSP phases.

<objective>
Bootstrap the pamplejuce-style repository skeleton with SendBloom metadata and failing Catch2 acceptance tests (RED phase).

Purpose: Establishes the walking skeleton build graph and test contracts before passthrough DSP ships (SCAF-01, SCAF-05).
Output: Configurable CMake project, submodule layout, stub processor sources, failing passthrough + name tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/ADR-001-fork-decision.md
@.planning/phases/01-scaffold-passthrough/SKELETON.md
@.planning/phases/01-scaffold-passthrough/01-RESEARCH.md
@.planning/repo-samples/sudara-pamplejuce/CMakeLists.txt
@.planning/repo-samples/sudara-pamplejuce/.gitmodules
@.planning/repo-samples/sudara-pamplejuce/tests/PluginBasics.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Copy pamplejuce scaffold and customize SendBloom CMake metadata</name>
  <files>CMakeLists.txt, .gitmodules, .gitignore, VERSION, .clang-format, source/PluginProcessor.h, source/PluginProcessor.cpp, source/PluginEditor.h, source/PluginEditor.cpp, tests/Catch2Main.cpp, tests/helpers/test_helpers.h</files>
  <behavior>
    - CMake configure succeeds after submodule init
    - PROJECT_NAME SendBloom, PRODUCT_NAME SendBloom, COMPANY_NAME Niko Audio Labs, BUNDLE_ID com.nikoaudiolabs.sendbloom
    - FORMATS AU VST3 only (no Standalone, AUv3, CLAP)
    - PLUGIN_MANUFACTURER_CODE NkMo, PLUGIN_CODE SbLm
    - C++20 via target_compile_features after include(SharedCodeDefaults)
    - No demo template names remain in CMake or PRODUCT_NAME_WITHOUT_VERSION define
  </behavior>
  <action>
Copy essential files from `.planning/repo-samples/sudara-pamplejuce/` into repo root (not as a submodule): CMakeLists.txt, VERSION, .clang-format, .gitignore, source/*, tests/Catch2Main.cpp, tests/helpers/test_helpers.h. Do NOT copy packaging/, benchmarks/, or nightly workflow.

Customize CMakeLists.txt per 01-RESEARCH.md Pattern 1: set SendBloom metadata (SCAF-05), FORMATS AU VST3, remove clap-juce-extensions add_subdirectory and clap_juce_extensions_plugin call, comment out include(PamplejuceIPP), keep melatonin_inspector submodule optional (include if low cost). Set PRODUCT_NAME_WITHOUT_VERSION compile definition to SendBloom.

After include(SharedCodeDefaults), add target_compile_features(SharedCode INTERFACE cxx_std_20) to override pamplejuce C++23 default per PROJECT.md.

Create .gitmodules with three submodules: JUCE (juce-framework/JUCE, pin branch to a stable 8.0.x tag such as 8.0.12 in README checkout instructions), cmake (sudara/cmake-includes), modules/melatonin_inspector. Omit clap-juce-extensions submodule for Phase 1.

Wrap processor/editor declarations in namespace sendbloom in headers; keep JUCE macro class names PluginProcessor/PluginEditor inside the namespace.

Strip all upstream demo product strings from copied sources and CMake (Pamplejuce, Pamp, P001, demo company names). Replace LICENSE with repo-appropriate MIT notice referencing pamplejuce upstream if retaining their structure.
  </action>
  <verify>
    <automated>git submodule update --init --recursive && cmake -B Builds -DCMAKE_BUILD_TYPE=Release && test -f Builds/CMakeCache.txt</automated>
  </verify>
  <done>CMake configures without errors; CMakeLists.txt contains NkMo, SbLm, SendBloom, and cxx_std_20 override; no upstream demo template identifiers in CMake or source tree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add failing Catch2 passthrough and plugin-name tests</name>
  <files>tests/PluginBasics.cpp</files>
  <behavior>
    - Test case "Passthrough preserves audio" with tag [dsp][passthrough] fails before explicit passthrough implementation
    - Test case "Plugin instance" name section expects getName() == "SendBloom"
    - Tests target compiles and links against SharedCode
  </behavior>
  <action>
Create tests/PluginBasics.cpp following 01-RESEARCH.md Pattern 4. Include sendbloom::PluginProcessor (or PluginProcessor within sendbloom namespace per Task 1).

TEST_CASE "Passthrough preserves audio" [dsp][passthrough]: instantiate processor, prepareToPlay(48000, 512), fill stereo buffer with deterministic sine samples, copy to expected buffer, call processBlock, REQUIRE each sample equals expected. With default empty processBlock this may pass accidentally on some JUCE versions — intentionally break passthrough in stub (e.g., multiply by 0.5 in temporary stub) so test FAILS RED until Plan 02 implements true passthrough. Remove the intentional break in Plan 02 only.

TEST_CASE "Plugin instance" [instance]: CHECK getName() equals SendBloom string.

Remove pamplejuce dummy "one is equal to one" test and IPP version test blocks.

Ensure cmake/Tests.cmake is included and Tests target builds.
  </action>
  <verify>
    <automated>cmake --build Builds --target Tests && ctest --test-dir Builds -R "Passthrough|Plugin instance" --output-on-failure; test $? -ne 0</automated>
  </verify>
  <done>Tests compile; passthrough test fails (RED); plugin name test fails if name not yet SendBloom (acceptable RED) or passes if metadata already correct.</done>
</task>

<task type="auto">
  <name>Task 3: Document local build workflow in README</name>
  <files>README.md</files>
  <action>
Write README.md with: project description (SendBloom passthrough scaffold), prerequisites (CMake 3.25+, Xcode on macOS for AU, git), submodule init command, configure/build/ctest commands matching 01-RESEARCH.md installation block. Document JUCE pin procedure (checkout 8.0.12 tag after submodule init). Note AU builds macOS-only; VST3 all platforms. Do not reference third-party trademarked product names in marketing copy.
  </action>
  <verify>
    <automated>grep -q "git submodule update --init --recursive" README.md && grep -q "ctest" README.md</automated>
  </verify>
  <done>README contains submodule init, cmake configure, build, and ctest instructions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git submodule fetch | External CMake/JUCE source crosses into build tree |
| CPM Catch2 download | Network-fetched test dependency at configure time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | JUCE/cmake submodules | high | mitigate | Pin JUCE to 8.0.x tag; official github.com URLs only; Package Legitimacy Audit approved |
| T-01-02 | Tampering | CPM Catch2 fetch | medium | mitigate | Pin gh:catchorg/Catch2@3.8.1 per cmake/Tests.cmake |
| T-01-03 | Information Disclosure | README/metadata | low | mitigate | SCAF-05 metadata only; no secrets in repo |
| T-01-SC | Tampering | submodule/CPM installs | high | mitigate | Legitimacy audit complete; no SLOP packages |
</threat_model>

<verification>
After Plan 01: `git submodule update --init --recursive && cmake -B Builds && cmake --build Builds --target Tests` succeeds; `ctest -R passthrough` fails RED.
</verification>

<success_criteria>
- SCAF-01: Repository scaffold files exist with JUCE 8 + CMake + C++20
- SCAF-05: CMake metadata block uses SendBloom / NkMo / SbLm / Niko Audio Labs
- Failing Catch2 tests define acceptance contract for Plan 02
</success_criteria>

<output>
Create `.planning/phases/01-scaffold-passthrough/01-01-SUMMARY.md` when done
</output>
