---
phase: 01-scaffold-passthrough
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - scripts/check-legal-metadata.sh
  - README.md
  - .github/workflows/build_and_test.yml
autonomous: false
requirements:
  - SCAF-05
must_haves:
  truths:
    - "No banned third-party trademark strings in product metadata, source, or docs"
    - "Developer documents DAW load smoke test with passthrough audio confirmation"
    - "AU and VST3 load in a DAW and pass audio unchanged (human verified)"
  artifacts:
    - path: scripts/check-legal-metadata.sh
      provides: "Automated legal string audit for CI and local"
    - path: README.md
      provides: "DAW smoke test procedure section"
  key_links:
    - from: scripts/check-legal-metadata.sh
      to: CMakeLists.txt
      via: "grep scan includes metadata files"
      pattern: "SendBloom"
    - from: README.md
      to: Builds/SendBloom_artefacts
      via: "DAW scan paths for .vst3 and .component"
      pattern: "DAW"
---

<objective>
Legal metadata audit automation, DAW smoke documentation, and human verification that AU/VST3 passthrough loads in a real host (ROADMAP success criterion 1, SCAF-05).

Purpose: Closes the walking skeleton with legal guardrails and the only gate CI cannot automate — host load + audible passthrough.
Output: check-legal-metadata.sh in CI, README DAW smoke section, documented human approval.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-scaffold-passthrough/01-03-SUMMARY.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-scaffold-passthrough/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add legal metadata audit script and CI step</name>
  <files>scripts/check-legal-metadata.sh, .github/workflows/build_and_test.yml</files>
  <action>
Create scripts/check-legal-metadata.sh (executable) that scans repo text files for banned trademark tokens from LEG-01 / PROJECT.md out-of-scope list: the hardware maker name, product line name, and third-party persona name referenced in planning docs (use the three terms from REQUIREMENTS LEG-01 description, not hardcoded in comments here — script should read a BANNED_TERMS array at top of script).

Also fail if upstream demo template identifiers appear: Pamplejuce, Pamp, P001 in CMake or source (case-insensitive).

Pass if SendBloom, NkMo, SbLm, Niko Audio Labs present in CMakeLists.txt.

Add CI step after build (before or after pluginval) running bash scripts/check-legal-metadata.sh; exit 1 on match.

Extend README with Legal/metadata section noting placeholder codes must be verified unique before commercial ship (SCAF-05).
  </action>
  <verify>
    <automated>bash scripts/check-legal-metadata.sh && test -x scripts/check-legal-metadata.sh</automated>
  </verify>
  <done>Script passes on clean tree; CI includes audit step; banned terms absent from metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Document DAW passthrough smoke procedure</name>
  <files>README.md</files>
  <action>
Add "DAW Smoke Test" section to README with numbered steps:

1. Build Release and locate VST3 (all platforms) and AU .component (macOS only) under Builds/SendBloom_artefacts/Release/.
2. Rescan plugins in host (Logic/Reaper/etc.).
3. Insert SendBloom on audio track; play guitar or sine source.
4. Confirm output matches bypass (passthrough); toggle plugin off/on — level unchanged.
5. Verify plugin displays as SendBloom by Niko Audio Labs in plugin manager.

Note macOS Gatekeeper may require ad-hoc codesign for local unsigned builds (research Assumption A4).

Reference phase completion gate from ROADMAP: document results in SUMMARY or verification note.
  </action>
  <verify>
    <automated>grep -q "DAW Smoke Test" README.md && grep -q "SendBloom" README.md</automated>
  </verify>
  <done>README contains reproducible DAW smoke steps for AU and VST3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human DAW load and passthrough confirmation</name>
  <files>README.md</files>
  <action>Pause for human verification per README DAW Smoke Test section. No code changes unless issues found.</action>
  <what-built>SendBloom AU and VST3 Release artifacts, CI green with pluginval 5, legal audit passing</what-built>
  <how-to-verify>
1. Open your DAW (Logic, REAPER, or Cubase) on macOS for AU+VST3 (or VST3-only on Windows/Linux).
2. Load SendBloom from Builds/SendBloom_artefacts/Release/ (or system plugin folder if COPY_PLUGIN_AFTER_BUILD copied it).
3. Play audio through the plugin — confirm passthrough (no level change, no effect).
4. Open plugin UI — confirm title shows SendBloom.
5. Check plugin manager metadata: manufacturer Niko Audio Labs, codes NkMo/SbLm if visible.
Expected: Audio passes unchanged; plugin loads without crash; metadata correct.
  </how-to-verify>
  <verify>
    <human-check>User confirms DAW load and passthrough per steps above</human-check>
  </verify>
  <done>Human types "approved" or lists issues; passthrough audible in at least one AU or VST3 host</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Product metadata → DAW/plugin manager | Public-facing strings visible to users and hosts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09 | Repudiation | Plugin metadata | high | mitigate | check-legal-metadata.sh in CI; SCAF-05 SendBloom/NkMo/SbLm only |
| T-01-10 | Tampering | Source/docs grep bypass | medium | mitigate | CI runs script on every push; scan CMake, source, README, tests |
</threat_model>

<verification>
Phase 1 complete when: legal script passes, CI green, human approves DAW passthrough smoke.
</verification>

<success_criteria>
- SCAF-05: Metadata correct; no banned trademark strings
- ROADMAP criterion 1: AU and VST3 load in DAW, audio unchanged (human approved)
</success_criteria>

<output>
Create `.planning/phases/01-scaffold-passthrough/01-04-SUMMARY.md` when done
</output>
