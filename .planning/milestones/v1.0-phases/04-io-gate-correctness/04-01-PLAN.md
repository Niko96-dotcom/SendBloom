---
phase: 04-io-gate-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - source/InputStage.h
  - source/OutputStage.h
  - tests/InputStageTest.cpp
  - tests/OutputStageTest.cpp
  - tests/MonoBusTest.cpp
autonomous: true
requirements:
  - IO-01
  - IO-02
  - IO-03
must_haves:
  truths:
    - "InputStage applies gain with soft clip at -3 dBFS knee"
    - "Clip-hold flag stays active for 50 ms after clipping"
    - "OutputStage trims by output gain linear multiplier"
    - "Stereo channels sum to mono for wet-path feed"
  artifacts:
    - path: source/InputStage.h
      provides: "Input gain, soft clip, clip-hold flag (IO-01)"
    - path: source/OutputStage.h
      provides: "Output gain trim (IO-02)"
    - path: tests/MonoBusTest.cpp
      provides: "Stereo sum mono contract (IO-03)"
---

<objective>
Implement InputStage and OutputStage with unit tests. Mono bus contract tested offline.
</objective>

<context>
@.planning/phases/04-io-gate-correctness/04-CONTEXT.md
@.planning/phases/04-io-gate-correctness/04-RESEARCH.md
@source/ParameterCurves.h
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — IO stage unit tests</name>
  <files>tests/InputStageTest.cpp, tests/OutputStageTest.cpp, tests/MonoBusTest.cpp</files>
  <behavior>
    - InputStage: unity gain passes sample; high gain triggers clip hold; hold persists ~50 ms after clip ends
    - OutputStage: -6 dB halves amplitude
    - MonoBus: stereo L+R average helper matches processor contract
  </behavior>
  <action>Create failing tests tagged [io][InputStage], [io][OutputStage], [io][mono].</action>
  <verify>
    <automated>cmake --build Builds --target Tests 2>&1; test $? -ne 0</automated>
  </verify>
  <done>IO tests exist and fail RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — InputStage and OutputStage headers</name>
  <files>source/InputStage.h, source/OutputStage.h</files>
  <behavior>
    - InputStage: prepare(sr), processSample(in, gainLinear), isClipHoldActive()
    - Soft clip tanh knee at 0.707946f (-3 dBFS); 50 ms hold counter
    - OutputStage: static processSample(sample, gainLinear)
  </behavior>
  <action>Implement header-only classes per RESEARCH Pattern 1-2. Run IO tests GREEN.</action>
  <verify>
    <automated>ctest --test-dir Builds -R "InputStage|OutputStage|mono" --output-on-failure</automated>
  </verify>
  <done>IO-01, IO-02, IO-03 unit tests GREEN.</done>
</task>

</tasks>

<success_criteria>
InputStage and OutputStage implemented with automated IO tests passing.
</success_criteria>
