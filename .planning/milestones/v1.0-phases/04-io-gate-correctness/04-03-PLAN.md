---
phase: 04-io-gate-correctness
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - source/GatedBloomChain.h
  - source/PluginProcessor.h
  - source/PluginProcessor.cpp
  - tests/GatedBloomChainTest.cpp
  - tests/DryNeverGatedTest.cpp
  - source/StubInputEnvelope.h
  - source/StubNoiseGate.h
autonomous: true
requirements:
  - GATE-04
  - IO-01
  - IO-02
  - IO-03
must_haves:
  truths:
    - "GatedBloomChain uses EnvelopeDetector and NoiseGate not stubs"
    - "PluginProcessor wires InputStage before chain, OutputStage after mix"
    - "Dry tap never affected by gate closure"
    - "All Phase 3 GatedBloomChain routing tests still pass"
  artifacts:
    - path: source/GatedBloomChain.h
      provides: "Real gate integration preserving topology"
    - path: tests/DryNeverGatedTest.cpp
      provides: "GATE-04 dry-never-gated proof"
---

<objective>
Swap stubs in chain and processor; preserve Phase 3 routing proofs; add dry-never-gated test.
</objective>

<context>
@source/GatedBloomChain.h
@source/PluginProcessor.cpp
@tests/GatedBloomChainTest.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap stubs in GatedBloomChain</name>
  <files>source/GatedBloomChain.h, source/StubInputEnvelope.h, source/StubNoiseGate.h</files>
  <action>
Replace StubInputEnvelope with EnvelopeDetector, StubNoiseGate with NoiseGate (PreSoft/PostHard profiles).
Keep processSample signature and routing order identical.
Delete stub headers after swap.
Update GatedBloomChainTest envelope test to use EnvelopeDetector.
  </action>
  <verify>
    <automated>ctest --test-dir Builds -R "GatedBloomChain|envelope" --output-on-failure</automated>
  </verify>
  <done>Chain uses real gate classes; Phase 3 chain tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Processor IO wiring + dry-never-gated test</name>
  <files>source/PluginProcessor.h, source/PluginProcessor.cpp, tests/DryNeverGatedTest.cpp</files>
  <action>
Add InputStage and OutputStage members.
processBlock: dry copy unchanged; mono sum → InputStage → chain; mix → OutputStage.
Remove inline inputGain/outputGain multiply from inner loop (use stages).
Create DryNeverGatedTest: gate closed wet silent, dry mix equals dry tap.
  </action>
  <verify>
    <automated>ctest --test-dir Builds -R "GatedBloomChain|DryNever|chain" --output-on-failure</automated>
  </verify>
  <done>Processor wired; GATE-04 test GREEN; full chain regression pass.</done>
</task>

</tasks>

<success_criteria>
Real IO and gates integrated; Phase 3 routing preserved; dry-never-gated proven.
</success_criteria>
