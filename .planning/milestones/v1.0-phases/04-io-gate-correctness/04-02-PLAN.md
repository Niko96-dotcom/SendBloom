---
phase: 04-io-gate-correctness
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - source/EnvelopeDetector.h
  - source/NoiseGate.h
  - tests/EnvelopeDetectorTest.cpp
  - tests/NoiseGateTest.cpp
autonomous: true
requirements:
  - GATE-01
  - GATE-02
  - GATE-03
  - GATE-05
must_haves:
  truths:
    - "EnvelopeDetector peak follower with configurable attack/release"
    - "PreSoft profile: 150 ms release, soft floor hum silencer"
    - "PostHard profile: 7 ms release, hard floor"
    - "3 dB hysteresis on threshold open/close"
  artifacts:
    - path: source/EnvelopeDetector.h
      provides: "Peak envelope follower (GATE-01)"
    - path: source/NoiseGate.h
      provides: "Dual-profile noise gate with hysteresis (GATE-02,03,05)"
---

<objective>
Implement EnvelopeDetector and NoiseGate replacing stub algorithms with real profiles and hysteresis.
</objective>

<context>
@.planning/phases/04-io-gate-correctness/04-RESEARCH.md
@source/StubInputEnvelope.h
@source/StubNoiseGate.h
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — envelope and gate unit tests</name>
  <files>tests/EnvelopeDetectorTest.cpp, tests/NoiseGateTest.cpp</files>
  <behavior>
    - Envelope rises on burst, decays in silence
    - PreSoft: low envelope yields soft floor gain not zero
    - PostHard: low envelope yields ~0 gain
    - Hysteresis: envelope between open and close thresholds does not chatter
  </behavior>
  <action>Create failing tests tagged [gate][EnvelopeDetector], [gate][NoiseGate].</action>
  <verify>
    <automated>cmake --build Builds --target Tests 2>&1; test $? -ne 0</automated>
  </verify>
  <done>Gate tests exist RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — EnvelopeDetector and NoiseGate</name>
  <files>source/EnvelopeDetector.h, source/NoiseGate.h</files>
  <behavior>
    - EnvelopeDetector: prepare(sr, attackMs, releaseMs), process(input)
    - NoiseGate: GateProfile PreSoft/PostHard, process(inputEnvelope, thresholdDb)
    - 3 dB hysteresis; profile-specific release and floor
  </behavior>
  <action>Implement per RESEARCH Pattern 3-4. Tests GREEN.</action>
  <verify>
    <automated>ctest --test-dir Builds -R "EnvelopeDetector|NoiseGate" --output-on-failure</automated>
  </verify>
  <done>GATE-01 through GATE-05 unit tests GREEN.</done>
</task>

</tasks>

<success_criteria>
EnvelopeDetector and NoiseGate implemented with profile and hysteresis tests passing.
</success_criteria>
