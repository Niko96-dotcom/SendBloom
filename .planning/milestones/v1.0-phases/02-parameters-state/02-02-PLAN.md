---
phase: 02-parameters-state
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - source/ParameterLayout.h
  - source/ParameterLayout.cpp
  - source/PluginProcessor.h
  - source/PluginProcessor.cpp
  - tests/ParameterLayoutTest.cpp
  - tests/PluginBasics.cpp
autonomous: true
requirements:
  - PARM-01
  - PARM-02
must_haves:
  truths:
    - "createParameterLayout exposes all 15 public parameters with correct ranges, defaults, and skew per RESEARCH derived table"
    - "PluginProcessor owns APVTS constructed with createParameterLayout"
    - "getStateInformation/setStateInformation round-trip preserves parameter values via apvts.copyState/replaceState"
    - "getBypassParameter returns bypass bool parameter for host integration"
  artifacts:
    - path: source/ParameterLayout.cpp
      provides: "Full APVTS parameter layout"
    - path: source/PluginProcessor.cpp
      provides: "APVTS member, state persistence, bypass param accessor"
    - path: tests/ParameterLayoutTest.cpp
      provides: "Layout count and range verification"
  key_links:
    - from: source/ParameterLayout.cpp
      to: source/ParameterIDs.h
      via: "ParameterID { IDs::*, 1 } versioning"
      pattern: "createParameterLayout"
    - from: source/PluginProcessor.cpp
      to: apvts
      via: "initializer list + get/setStateInformation"
      pattern: "copyState"
---

## Phase Goal

**As a** DAW user, **I want to** see and automate all effect parameters in my host, **so that** plugin state saves and restores correctly across sessions.

<objective>
Wire full APVTS layout and host state persistence so all architecture-defined parameters appear in automation lanes and preset XML (PARM-01, PARM-02).

Purpose: Host-visible parameters are the vertical slice that proves the plugin is automation-ready before audio processing changes.
Output: ParameterLayout, APVTS in PluginProcessor, layout tests, state round-trip smoke in PluginBasics.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-parameters-state/02-CONTEXT.md
@.planning/phases/02-parameters-state/02-RESEARCH.md
@source/ParameterIDs.h
@source/PluginProcessor.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — APVTS layout and state round-trip tests</name>
  <files>tests/ParameterLayoutTest.cpp, tests/PluginBasics.cpp</files>
  <behavior>
    - Layout test: processor exposes exactly 15 automatable parameters
    - Layout test: size param range 0..1 default 0.5; output_gain range -12..+12 default 0
    - PluginBasics: set several params, getStateInformation, new instance setStateInformation, raw values match within float epsilon
    - getBypassParameter returns non-null and points to bypass ID
  </behavior>
  <action>
Add tests/ParameterLayoutTest.cpp tagged [parm][layout]. Instantiate sendbloom::PluginProcessor offline; iterate apvts.getParameterLayout() or count parameters via processor.getParameters().size() expecting 15. Spot-check size and output_gain NormalisableRange endpoints and defaults per 02-RESEARCH derived table [ASSUMED defaults where noted].

Extend tests/PluginBasics.cpp with TEST_CASE "APVTS state round-trip" [parm][state]: mutate input_gain, distn, bypass via apvts.getParameter()->setValueNotifyingHost or raw atomic; serialize with getStateInformation; deserialize into fresh processor; verify values restored.

Add assertion that getBypassParameter() is non-null — required for PARM-06 host path (implementation in Plan 04).

Tests must fail RED before layout exists.
  </action>
  <verify>
    <automated>cmake --build Builds --target Tests 2>&1; ! ctest --test-dir Builds -R "layout|state round-trip" --output-on-failure</automated>
  </verify>
  <done>Layout and state tests exist and fail before APVTS wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — ParameterLayout and APVTS processor wiring</name>
  <files>source/ParameterLayout.h, source/ParameterLayout.cpp, source/PluginProcessor.h, source/PluginProcessor.cpp</files>
  <behavior>
    - All 15 parameters registered with correct types (float/bool/choice)
    - Split input_gain and input_threshold per CROSS_AGENT mandate
    - gate_pre_post choice: PreSoft=0, PostHard=1
    - send_feel choice: Firm=0, Soft=1
    - Processor still passthrough in processBlock until Plan 03
  </behavior>
  <action>
Create source/ParameterLayout.h declaring sendbloom::createParameterLayout() returning AudioProcessorValueTreeState::ParameterLayout.

Implement source/ParameterLayout.cpp per 02-RESEARCH derived table: all float ranges, skew on size (0.5 skew ≈ pow 2.4 feel), input_threshold skew, output_gain dB range, bool defaults, choice strings. Use ParameterID { IDs::name, 1 } for every param per locked CONTEXT immutable ID decision.

Update PluginProcessor.h: add public apvts member, static createParameterLayout declaration, override getBypassParameter() returning bypass param pointer, accessor getAPVTS() if needed for tests.

Update PluginProcessor constructor initializer: apvts (*this, nullptr, "SendBloomParams", createParameterLayout()).

Implement getStateInformation/setStateInformation using MemoryOutputStream + copyState XML and replaceState fromXml pattern in 02-RESEARCH.md. Keep processBlock passthrough unchanged.

Expose createParameterLayout as static method on PluginProcessor delegating to free function if cleaner for tests.
  </action>
  <verify>
    <automated>cmake --build Builds --target Tests && ctest --test-dir Builds -R "layout|state round-trip|Passthrough" --output-on-failure</automated>
  </verify>
  <done>15 parameters in APVTS; state round-trip GREEN; passthrough still passes.</done>
</task>

<task type="auto">
  <name>Task 3: Verify host parameter metadata and Release build</name>
  <files>source/ParameterLayout.cpp, source/PluginProcessor.cpp</files>
  <action>
Run full ctest suite. Build Release AU+VST3 targets to confirm APVTS does not break plugin packaging.

Grep PluginProcessor.cpp processBlock to confirm no parameter reads added yet (snapshot deferred to Plan 03 per locked CONTEXT ParameterSnapshot decision).

Document parameter count and state XML format note in 02-02-SUMMARY.md for Plan 05 DAW smoke.
  </action>
  <verify>
    <automated>ctest --test-dir Builds --output-on-failure && cmake --build Builds --config Release</automated>
  </verify>
  <done>Full test suite green; Release build succeeds with APVTS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host→APVTS | Untrusted automation values cross into atomics |
| preset XML→replaceState | Untrusted preset bytes on load |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Tampering | APVTS float params | medium | mitigate | NormalisableRange clamps all float params per JUCE AudioParameterFloat |
| T-02-04 | Tampering | setStateInformation | medium | mitigate | ValueTree::fromXml only; no custom eval; invalid XML ignored safely |
| T-02-05 | Spoofing | bypass param | medium | mitigate | getBypassParameter returns registered AudioParameterBool |
| T-02-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages |
</threat_model>

<verification>
ctest layout and state tests pass. Passthrough test still passes. 15 parameters visible via processor.getParameters().
</verification>

<success_criteria>
All architecture-defined parameters in host automation and plugin state XML; PARM-02 satisfied.
</success_criteria>

<output>
Create `.planning/phases/02-parameters-state/02-02-SUMMARY.md` when done
</output>
