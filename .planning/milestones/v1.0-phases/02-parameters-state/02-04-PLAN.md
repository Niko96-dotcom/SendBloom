---
phase: 02-parameters-state
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - source/BypassCrossfade.h
  - source/DummyDspHooks.h
  - source/PluginProcessor.h
  - source/PluginProcessor.cpp
  - tests/BypassCrossfadeTest.cpp
  - tests/DummyDspHooksTest.cpp
  - tests/PluginBasics.cpp
autonomous: true
requirements:
  - PARM-06
must_haves:
  truths:
    - "Bypass toggles with 5 ms clickless wet/dry crossfade using SmoothedValue wet gain (dry = 1 - wet)"
    - "getBypassParameter integrated; single processBlock path checks bypass param per JUCE guidance"
    - "DummyDspHooks apply audible placeholder processing driven by smoothed params: input gain, size tone, distn tanh blend, level wet scale, send gain, dark HF rolloff, output trim"
    - "Changing distn or size produces measurably different output buffer vs passthrough"
  artifacts:
    - path: source/BypassCrossfade.h
      provides: "5 ms per-sample wet/dry mix"
    - path: source/DummyDspHooks.h
      provides: "Minimal audible automation hooks"
    - path: source/PluginProcessor.cpp
      provides: "Full dry copy → dummy wet → bypass crossfade pipeline"
  key_links:
    - from: PluginProcessor::processBlock
      to: DummyDspHooks::process
      via: "wet buffer after input gain, per-sample smoothed values"
      pattern: "DummyDspHooks"
    - from: BypassCrossfade
      to: dryCopy buffer
      via: "preallocated in prepareToPlay"
      pattern: "BypassCrossfade"
---

## Phase Goal

**As a** DAW user, **I want** clickless bypass and audible dummy processing when I automate knobs, **so that** I can verify host automation before the real effect chain ships.

<objective>
Complete the audio path with 5 ms bypass crossfade and dummy DSP hooks responding to all smoothed parameters (PARM-06 + locked CONTEXT dummy hook decision).

Purpose: Delivers the phase's user-facing outcome — automation is audibly verifiable and bypass is clickless.
Output: BypassCrossfade, DummyDspHooks, full processBlock pipeline, bypass and dummy hook tests GREEN.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-parameters-state/02-CONTEXT.md
@.planning/phases/02-parameters-state/02-RESEARCH.md
@source/SmoothedParameterBank.h
@source/ParameterSnapshot.h
@source/PluginProcessor.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — bypass crossfade and dummy hook processor tests</name>
  <files>tests/BypassCrossfadeTest.cpp, tests/DummyDspHooksTest.cpp, tests/PluginBasics.cpp</files>
  <behavior>
    - BypassCrossfade: wetMix ramp from 1→0 over 5 ms at 48 kHz shows no sample discontinuity greater than 0.5 linear delta between adjacent samples when input is constant sine
    - BypassCrossfade: bypass smoother configured reset(sampleRate, 0.005)
    - DummyDspHooks: distn=1 changes buffer RMS vs distn=0 on fixed sine input
    - PluginBasics: bypass param false→true mid-session does not produce full-scale click (max adjacent delta bounded)
    - PluginBasics: raising distn from 0 to 1 changes output samples vs baseline
  </behavior>
  <action>
Add tests/BypassCrossfadeTest.cpp [parm][bypass]. Unit-test BypassCrossfade helper in isolation with synthetic dry/wet buffers and SmoothedValue wet ramp 5 ms.

Add tests/DummyDspHooksTest.cpp [parm][dummy]. Feed 512-sample sine through DummyDspHooks with distn blend 0 vs 1; assert samples differ.

Extend PluginBasics with integration tests for bypass clicklessness and distn audibility using PluginProcessor offline.

Commit RED state — tests fail before helpers exist.
  </action>
  <verify>
    <automated>cmake --build Builds --target Tests 2>&1; ! ctest --test-dir Builds -R "bypass|dummy|distn audibility" --output-on-failure</automated>
  </verify>
  <done>Bypass and dummy hook tests exist and fail RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — BypassCrossfade and DummyDspHooks helpers</name>
  <files>source/BypassCrossfade.h, source/DummyDspHooks.h</files>
  <behavior>
    - BypassCrossfade::processBlock mixes dryCopy with wet buffer per-sample: out = dry*dryG + wet*wetG, wetG from smoother, dryG = 1-wetG
    - DummyDspHooks: per-sample loop using SmoothedParameterBank getters — apply size as one-pole LPF cutoff modulation, distn as blend toward tanh(x*3), level scales wet contribution, send multiplies wet, dark_mode toggles HF rolloff target with 15 ms smoothed coefficient
    - No heap, locks, logging in process paths
  </behavior>
  <action>
Implement source/BypassCrossfade.h per 02-RESEARCH Pattern 4. Header-only static process method taking buffer, dryCopy, wet SmoothedValue reference.

Implement source/DummyDspHooks.h per 02-RESEARCH Pattern 6 and locked CONTEXT dummy hook decision: minimal gain/offset and tone indicators only — not full effect chain. Own simple one-pole state as member struct prepared in prepareToPlay (cutoff state per channel max 2). Use tanh for distn preview on wet path only.

Per locked CONTEXT: 5 ms clickless bypass crossfade — bypass wet smoother reset to 0.005 seconds in bank; target 0 when bypassed, 1 when active.

Do not call processBlockBypassed for bypass — single path in processBlock checking bypass param via snapshot.bypassed.
  </action>
  <verify>
    <automated>cmake --build Builds --target Tests && ctest --test-dir Builds -R "bypass|dummy" --output-on-failure</automated>
  </verify>
  <done>Bypass and dummy unit tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire full processBlock pipeline and integration tests GREEN</name>
  <files>source/PluginProcessor.h, source/PluginProcessor.cpp, tests/PluginBasics.cpp</files>
  <behavior>
    - processBlock: capture → setTargets → copy input to dryBuffer → apply input gain to working buffer → DummyDspHooks on wet → BypassCrossfade mix → output gain already applied or reordered per architecture diagram (input gain → dummy wet → bypass mix → output gain)
    - dryBuffer preallocated in prepareToPlay matching max block size
    - Full ctest parm suite passes
  </behavior>
  <action>
Add juce::AudioBuffer<float> dryBuffer member; size in prepareToPlay(samplesPerBlock, numChannels).

Restructure processBlock per 02-RESEARCH architecture diagram: snapshot capture, setTargets, copy dry, smoothed input gain, DummyDspHooks::process with bank, BypassCrossfade with bypass wet smoother (target inverse of bypassed: wet=0 when bypassed), smoothed output gain on result.

Ensure getBypassParameter returns bypass param; when host sets bypass, snapshot reflects it same block.

Run full test suite including Phase 1 passthrough replacement — passthrough test may need update: at default params with bypass off and unity gains, audio may differ slightly from literal passthrough; update test to use bypass on + unity params OR compare at defaults documented in SUMMARY. Prefer: set input_gain/output_gain to unity defaults and bypass=true for literal passthrough equivalence test, add separate "dummy processing responds" test.

Verify zero heap in processBlock under debugger spot-check or existing realtime pattern.
  </action>
  <verify>
    <automated>cmake --build Builds --target Tests && ctest --test-dir Builds -R "bypass|dummy|distn|curves|snapshot|smooth|layout|state" --output-on-failure</automated>
  </verify>
  <done>Full parameter audio path complete; PARM-06 satisfied; dummy hooks audibly respond to automation in tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host bypass→snapshot | Bypass bool crosses from APVTS once per block |
| dummy wet→output | Wet path only; dry copy preserved for crossfade |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09 | Tampering | Instant bypass switch | high | mitigate | 5 ms SmoothedValue crossfade per locked CONTEXT |
| T-02-10 | Denial of Service | dryBuffer alloc per block | high | mitigate | Preallocate dryBuffer in prepareToPlay |
| T-02-11 | Information Disclosure | Logging in audio thread | low | mitigate | No logging in DummyDspHooks or BypassCrossfade |
| T-02-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages |
</threat_model>

<verification>
Full parm test suite green. Bypass crossfade 5 ms verified. Dummy hooks change output when distn/size automated.
</verification>

<success_criteria>
Bypass 5 ms clickless; dummy processing hooks respond to param changes; all PARM-01–06 requirements addressed across phase plans.
</success_criteria>

<output>
Create `.planning/phases/02-parameters-state/02-04-SUMMARY.md` when done
</output>
