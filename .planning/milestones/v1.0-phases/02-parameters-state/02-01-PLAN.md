---
phase: 02-parameters-state
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - source/ParameterIDs.h
  - source/ParameterCurves.h
  - tests/ParameterCurvesTest.cpp
  - tests/ParameterIDsTest.cpp
autonomous: true
requirements:
  - PARM-01
  - PARM-05
must_haves:
  truths:
    - "All architecture-defined parameter ID strings exist as immutable constexpr in ParameterIDs.h"
    - "Curve functions size→RT60, distn→pow(2.8), level→sin equal-power, and send curves return golden values at norm 0, 0.5, 1.0"
    - "Catch2 curve and ID tests pass without loading full processor"
  artifacts:
    - path: source/ParameterIDs.h
      provides: "Immutable APVTS ID contract for 15 public parameters"
    - path: source/ParameterCurves.h
      provides: "Pure mapping functions for DSP read path"
    - path: tests/ParameterCurvesTest.cpp
      provides: "PARM-05 golden-value verification"
  key_links:
    - from: tests/ParameterCurvesTest.cpp
      to: source/ParameterCurves.h
      via: "Catch2 Approx assertions on pure functions"
      pattern: "sizeToRT60"
    - from: source/ParameterIDs.h
      to: future ParameterLayout.cpp
      via: "IDs namespace referenced by createParameterLayout"
      pattern: "ParameterID"
---

## Phase Goal

**As a** DAW user, **I want to** rely on correct parameter curve math, **so that** Size, Level, Distn, and Send automation behave identically when real DSP ships in later phases.

<objective>
Establish immutable parameter ID contract and unit-tested curve mapping functions (PARM-01, PARM-05).

Purpose: Curve math is the foundation every later phase reuses; locking IDs and formulas now prevents session-breaking drift.
Output: ParameterIDs.h, ParameterCurves.h, ParameterCurvesTest.cpp, ParameterIDsTest.cpp — all GREEN.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-parameters-state/02-CONTEXT.md
@.planning/phases/02-parameters-state/02-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<feature>
  <name>Parameter IDs and curve mappings</name>
  <files>source/ParameterIDs.h, source/ParameterCurves.h, tests/ParameterCurvesTest.cpp, tests/ParameterIDsTest.cpp</files>
  <behavior>
    - ParameterIDs: 15 constexpr string IDs — input_gain, input_threshold, size, level, distn, output_gain, dark_mode, gate_pre_post, send_connected, send_amount, send_feel, authentic_color, extended_stereo, dirt_os, bypass
    - sizeToRT60(0)=0.25, sizeToRT60(1)=6.0, midpoint matches 0.25 + 5.75×norm^2.4
    - distnBlend(n) = pow(n, 2.8); distnBlend(0)=0, distnBlend(1)=1
    - levelEqualPower(0.5): dry ≈ wet ≈ sin(halfPi×0.5)
    - inputGainDb: smoothstep maps 0→+9 dB, 1→−3 dB per RESEARCH_CORPUS R8
    - inputThresholdDb: pow skew maps 0→−52 dB, 1→−18 dB
    - sendGain: smoothstep then pow(s, 1.85) Firm or pow(s, 1.2) Soft; send_connected=false path returns 1.0 at capture time (tested via sendGain with connected semantics in snapshot plan)
  </behavior>
  <implementation>
    RED: Add failing Catch2 tests first. GREEN: Header-only implementations in namespace sendbloom::ParameterIDs and sendbloom::ParameterCurves. Use juce::MathConstants for halfPi. No APVTS or processor includes in curve header beyond juce_core math if needed.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing ParameterCurves and ParameterIDs tests</name>
  <files>tests/ParameterCurvesTest.cpp, tests/ParameterIDsTest.cpp</files>
  <behavior>
    - sizeToRT60 golden values at 0, 0.5, 1.0 fail until implemented
    - distnBlend pow(2.8) at 0, 0.25, 1.0
    - levelEqualPower equal-power at 0.5
    - sendGain Firm vs Soft at amount 0.5 differ
    - ParameterIDs test asserts 15 unique non-empty ID strings match expected literals
  </behavior>
  <action>
Create tests/ParameterCurvesTest.cpp with Catch2 tags [curves][parm]. Test pure functions that will live in ParameterCurves.h — include only a stub header or forward declarations so tests fail to link until Task 2.

Create tests/ParameterIDsTest.cpp with tag [parm][ids]. Assert count of 15 parameters and spot-check critical IDs (input_gain, bypass, size) as exact string matches per locked decision: immutable IDs in ParameterIDs.h.

Run ctest to confirm RED (tests fail or do not compile).
  </action>
  <verify>
    <automated>cmake --build Builds --target Tests 2>&1 | tee /tmp/p2-01-red.log; ! ctest --test-dir Builds -R "curves|ParameterIDs" --output-on-failure</automated>
  </verify>
  <done>Curve and ID tests exist and fail before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — ParameterIDs.h and ParameterCurves.h</name>
  <files>source/ParameterIDs.h, source/ParameterCurves.h</files>
  <behavior>
    - All RED tests pass
    - sizeToRT60 uses 0.25f + 5.75f * pow(norm, 2.4f)
    - distnBlend uses pow(norm, 2.8f)
    - levelEqualPower uses sin(halfPi*(1-norm)) dry, sin(halfPi*norm) wet
    - inputGainDb uses smoothstep then lerp +9 to -3 dB via Decibels
    - sendGain applies smoothstep then exponent per firmFeel flag
  </behavior>
  <action>
Implement source/ParameterIDs.h in namespace sendbloom with struct IDs holding constexpr const char* for all 15 parameters from 02-RESEARCH.md derived table. Use juce::StringRef-compatible literals; document that IDs are immutable after ship per locked CONTEXT decision.

Implement source/ParameterCurves.h as header-only pure functions: sizeToRT60, distnBlend, levelEqualPower, smoothstep, inputGainDb, inputThresholdDb, outputGainLinear (dB to linear via juce::Decibels), sendGain. Match formulas in 02-RESEARCH.md Pattern 5 exactly.

No APVTS, no heap, no logging. Files auto-link via SharedCode GLOB.
  </action>
  <verify>
    <automated>cmake --build Builds --target Tests && ctest --test-dir Builds -R "curves|ParameterIDs" --output-on-failure</automated>
  </verify>
  <done>All curve and ID unit tests GREEN; formulas locked for Phase 3+ reuse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test→header | Catch2 tests call pure functions; no host input |
| header→future DSP | Curve outputs consumed by audio thread; must be noexcept-safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | ParameterCurves | medium | mitigate | Golden-value tests at boundary norms; formulas match REQUIREMENTS |
| T-02-02 | Tampering | ParameterIDs | high | mitigate | constexpr IDs; test asserts literal strings; never rename after release |
| T-02-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages this plan — JUCE/Catch2 already approved Phase 1 |
</threat_model>

<verification>
ctest --test-dir Builds -R "curves|ParameterIDs" passes. No processor changes yet — passthrough still works from Phase 1.
</verification>

<success_criteria>
PARM-01 ID contract defined; PARM-05 curve mappings verified by automated golden-value tests.
</success_criteria>

<output>
Create `.planning/phases/02-parameters-state/02-01-SUMMARY.md` when done
</output>
