---
phase: 02-parameters-state
plan: 05
type: execute
wave: 5
depends_on:
  - 02-04
files_modified:
  - README.md
autonomous: false
requirements:
  - PARM-01
  - PARM-02
  - PARM-03
  - PARM-04
  - PARM-05
  - PARM-06
must_haves:
  truths:
    - "Human confirms all parameters visible in DAW automation menu"
    - "Human confirms smooth automation on Size and input_gain lanes without zipper steps"
    - "Human confirms bypass toggle is clickless at 5 ms crossfade"
    - "Human confirms distn and size automation produce audible dummy processing changes"
  artifacts:
    - path: README.md
      provides: "Phase 2 DAW parameter automation smoke procedure"
  key_links:
    - from: README.md
      to: Builds/SendBloom_artefacts
      via: "DAW load instructions"
      pattern: "DAW smoke"
---

## Phase Goal

**As a** DAW user, **I want to** confirm parameters automate smoothly in a real host, **so that** Phase 3 ugly-chain work builds on verified automation infrastructure.

<objective>
Document and human-verify DAW parameter automation smoke for Phase 2 completion gate.

Purpose: Automated tests cannot fully validate host automation UX, zipper audibility, or bypass clicklessness in a real DAW session.
Output: README smoke procedure updated; human approval recorded for phase gate.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-parameters-state/02-03-SUMMARY.md
@.planning/phases/02-parameters-state/02-04-SUMMARY.md
@.planning/phases/01-scaffold-passthrough/01-04-SUMMARY.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated preflight — full test suite and Release artifact paths</name>
  <files>README.md</files>
  <action>
Run full ctest suite and Release build of AU+VST3. Confirm all parm-tagged tests pass.

Add README section "Phase 2 — Parameter Automation Smoke" with numbered steps: load SendBloom VST3/AU from Builds artefact path; open plugin generic UI or host param list; verify 15 parameters listed including input_gain, input_threshold, size, level, distn, output_gain, bypass, send_amount; record automation on Size and input_gain for 10 s; toggle bypass rapidly 5 times; automate distn 0→1; note audible tone/grind change from dummy hooks; confirm no clicks on bypass.

Reference Assumptions Log items A1–A8 from 02-RESEARCH.md for human spot-check of defaults if behavior surprises.

Do not modify processor code unless preflight tests fail — fix blockers in prior plans first.
  </action>
  <verify>
    <automated>ctest --test-dir Builds --output-on-failure && cmake --build Builds --config Release</automated>
  </verify>
  <done>All automated gates green; README documents DAW smoke procedure.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human DAW parameter automation confirmation</name>
  <files>README.md</files>
  <action>Pause for human verification per README Phase 2 smoke section. No code changes unless issues found.</action>
  <what-built>Complete Phase 2 parameter system: APVTS layout, snapshot smoothing, 5 ms bypass crossfade, dummy DSP hooks, curve unit tests</what-built>
  <how-to-verify>
1. Build Release plugin per README (cmake --build Builds --config Release).
2. Load SendBloom in REAPER, Logic, or Cubase from documented artefact path.
3. Open host parameter/automation view — confirm 15 parameters including split input_gain and input_threshold.
4. Play guitar or sine test tone; automate Size knob lane for 10 seconds — listen for smooth decay tone change without stepped zipper.
5. Automate input_gain — confirm smooth level ride without blocky steps.
6. Toggle bypass 5 times quickly — confirm no audible clicks (5 ms crossfade).
7. Automate distn from 0 to 100% — confirm audible grind increase on wet dummy path.
8. Save project, reload — confirm parameter values restored (state round-trip in host).
  </how-to-verify>
  <verify>
    <human-check>User confirms DAW parameter automation smoke per steps above</human-check>
  </verify>
  <done>Human types "approved" or reports issues for fix-up</done>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human→DAW | User loads unsigned/local plugin binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-12 | Tampering | Local unsigned binary | low | accept | Personal/portfolio build; user controls DAW plugin path |
| T-02-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages |
</threat_model>

<verification>
Automated ctest full suite green. Human approves DAW smoke checklist.
</verification>

<success_criteria>
Phase 2 completion gate: pluginval 5 (from Phase 1 CI) still passes; parameter curve unit tests pass; DAW param automation smoke documented and human-approved.
</success_criteria>

<output>
Create `.planning/phases/02-parameters-state/02-05-SUMMARY.md` when done
</output>
