---
phase: 02-parameters-state
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - source/ParameterSnapshot.h
  - source/SmoothedParameterBank.h
  - source/PluginProcessor.h
  - source/PluginProcessor.cpp
  - tests/ParameterSnapshotTest.cpp
  - tests/SmoothedParameterBankTest.cpp
  - tests/PluginBasics.cpp
autonomous: true
requirements:
  - PARM-03
  - PARM-04
must_haves:
  truths:
    - "ParameterSnapshot::capture reads all APVTS atomics once per processBlock and applies curve mappings to derived fields"
    - "No getRawParameterValue or apvts access inside per-sample inner loops"
    - "SmoothedParameterBank ramps configured in prepareToPlay: 20 ms gains, 50 ms size/threshold, 25 ms send, 15 ms dark_mode, 5 ms bypass wet mix target"
    - "Automating input_gain produces smooth per-sample gain changes without block-constant zipper steps"
  artifacts:
    - path: source/ParameterSnapshot.h
      provides: "Block-rate capture with derived RT60, wet/dry, send gain"
    - path: source/SmoothedParameterBank.h
      provides: "SmoothedValue bank with prepare/setTargets"
    - path: source/PluginProcessor.cpp
      provides: "Snapshot at block start + smoothed input/output gain in processBlock"
  key_links:
    - from: PluginProcessor::processBlock
      to: ParameterSnapshot::capture
      via: "single call at block top before sample loop"
      pattern: "capture"
    - from: SmoothedParameterBank
      to: juce::SmoothedValue
      via: "getNextValue per sample in gain application"
      pattern: "getNextValue"
---

## Phase Goal

**As a** DAW user, **I want** parameter automation to apply smoothly without zipper noise, **so that** knob sweeps and host lanes sound professional during mixing.

<objective>
Implement ParameterSnapshot block-rate capture and per-parameter SmoothedValue smoothing; wire input/output gain into processBlock (PARM-03, PARM-04).

Purpose: This vertical slice makes automation audibly smooth for gain params before dummy effect hooks land in Plan 04.
Output: ParameterSnapshot, SmoothedParameterBank, smoothed gain processing, snapshot and smoothing tests GREEN.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-parameters-state/02-CONTEXT.md
@.planning/phases/02-parameters-state/02-RESEARCH.md
@source/ParameterCurves.h
@source/ParameterIDs.h
@source/PluginProcessor.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — snapshot derived fields and smoothing ramp tests</name>
  <files>tests/ParameterSnapshotTest.cpp, tests/SmoothedParameterBankTest.cpp, tests/PluginBasics.cpp</files>
  <behavior>
    - Snapshot test: given known APVTS values, capture() produces expected rt60Seconds, distnBlend, wet/dry gains, sendGain=1 when send_connected false
    - SmoothedParameterBank test: after prepare(48000), setTargets step change, getNextValue over 960 samples (~20ms) reaches within 1% of target
    - PluginBasics: automate input_gain mid-block; adjacent samples differ by less than block-constant step would (zipper test heuristic)
  </behavior>
  <action>
Add tests/ParameterSnapshotTest.cpp [parm][snapshot]. Build minimal APVTS or use PluginProcessor, set raw values, call ParameterSnapshot::capture, assert derived fields match ParameterCurves outputs.

Add tests/SmoothedParameterBankTest.cpp [parm][smooth]. Instantiate bank, prepare at 48000 Hz, setTargets with step, sample getNextValue until converged; assert ramp duration matches configured seconds within tolerance.

Add PluginBasics TEST_CASE "Smoothed gain automation" [parm][zipper]: process 512 samples while input_gain target steps; assert sample-to-sample delta bounded (no single-block constant gain).

Tests fail RED before implementation.
  </action>
  <verify>
    <automated>cmake --build Builds --target Tests 2>&1; ! ctest --test-dir Builds -R "snapshot|smooth|zipper" --output-on-failure</automated>
  </verify>
  <done>Snapshot and smoothing tests exist and fail RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — ParameterSnapshot and SmoothedParameterBank</name>
  <files>source/ParameterSnapshot.h, source/SmoothedParameterBank.h</files>
  <behavior>
    - capture() reads each getRawParameterValue once; applies ParameterCurves for all derived floats
    - sendGain forced to 1.0 when send_connected is false per SEND-01 semantics at read time
    - Bank owns SmoothedValue for: input gain linear, output gain linear, size norm, level wet/dry, distn, send, dark mode crossfade target, bypass wet target (bypass processing in Plan 04)
    - Ramp table: 20 ms input_gain/level/distn/output_gain; 50 ms size/input_threshold; 25 ms send_amount; 15 ms dark_mode; 5 ms bypass wet mix
  </behavior>
  <action>
Implement source/ParameterSnapshot.h struct with all fields from 02-RESEARCH Pattern 2. Static capture(const AudioProcessorValueTreeState&) reads atomics once, maps curves, resolves gate_pre_post and send_feel choice indices, sets bypassed from bypass param.

Implement source/SmoothedParameterBank.h class: prepare(double sampleRate) resets all SmoothedValue members with ramp seconds per RESEARCH table and locked CONTEXT smoothing discretion; setTargets(const ParameterSnapshot&) sets target values; provide getters for each smoother used in processBlock.

Stack-allocate snapshot in processBlock — no heap. Header-only or minimal inline implementation acceptable.

Per locked CONTEXT decision: no APVTS atomics in DSP inner loops — enforce by design: only capture() touches atomics.
  </action>
  <verify>
    <automated>cmake --build Builds --target Tests && ctest --test-dir Builds -R "snapshot|smooth" --output-on-failure</automated>
  </verify>
  <done>Snapshot and bank unit tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire smoothed input/output gain in processBlock</name>
  <files>source/PluginProcessor.h, source/PluginProcessor.cpp</files>
  <behavior>
    - processBlock: capture snapshot, setTargets, apply per-sample input then output gain via SmoothedValue getNextValue
    - ScopedNoDenormals preserved; zero heap alloc
    - Zipper test passes
    - Static analysis: grep shows no getRawParameterValue inside sample loops
  </behavior>
  <action>
Add SmoothedParameterBank member to PluginProcessor; initialize in prepareToPlay with sample rate from host; releaseResources can reset.

Rewrite processBlock: ScopedNoDenormals; auto snap = ParameterSnapshot::capture(apvts); smoothedBank.setTargets(snap); for each channel and sample apply input gain linear then output gain linear using getNextValue() — use juce::Decibels::decibelsToGain on smoothed dB targets or store linear in bank.

Do not implement bypass crossfade or dummy wet path yet — bypass target may be set in bank but wet/dry mix deferred to Plan 04.

Add prepareToPlay preallocation if needed (none for gain-only path).

Verify with grep -v comment lines that processBlock inner loops contain no getRawParameterValue.
  </action>
  <verify>
    <automated>cmake --build Builds --target Tests && ctest --test-dir Builds -R "zipper|snapshot|smooth|Passthrough" --output-on-failure; ! grep -n getRawParameterValue source/PluginProcessor.cpp | grep -v capture</automated>
  </verify>
  <done>Smoothed gain automation works; PARM-03/04 core satisfied; no atomics in inner loops.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| APVTS atomics→snapshot | Single read point per block |
| snapshot→SmoothedValue | Plain floats cross to audio thread smoothers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Denial of Service | SmoothedValue denormals | low | mitigate | ScopedNoDenormals in processBlock |
| T-02-07 | Tampering | Stale snapshot in inner loop | medium | mitigate | capture() only at block start; grep gate in verify |
| T-02-08 | Denial of Service | Heap alloc in processBlock | high | mitigate | Stack snapshot; fixed SmoothedValue members prepared in prepareToPlay |
| T-02-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages |
</threat_model>

<verification>
Snapshot and smoothing tests pass. Zipper heuristic test passes. processBlock applies smoothed gains.
</verification>

<success_criteria>
ParameterSnapshot once per block; per-parameter smoothing ramps match architecture table; automation smooth without zipper on gain params.
</success_criteria>

<output>
Create `.planning/phases/02-parameters-state/02-03-SUMMARY.md` when done
</output>
