---
phase: 08-full-integration-realtime-fallback
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - source/GatedBloomChain.h
  - source/PluginProcessor.h
  - tests/LatencyTest.cpp
  - tests/ReverbEngineSwapTest.cpp
autonomous: true
requirements:
  - CHN-04
  - VERB-06
must_haves:
  truths:
    - "GatedBloomChain uses IReverbEngine; default SchroederTank32"
    - "Chain can swap to Fdn8Reverb for tests"
    - "getLatencySamples returns 0"
    - "Phase 3-7 routing regression intact"
  artifacts:
    - path: source/GatedBloomChain.h
      provides: "IReverbEngine integration"
    - path: tests/LatencyTest.cpp
      provides: "CHN-04 zero latency proof"
---

<objective>
Wire IReverbEngine into chain; explicit zero latency; swap test.
</objective>

<context>
@source/GatedBloomChain.h
@source/IReverbEngine.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chain integration and latency audit</name>
  <files>source/GatedBloomChain.h, source/PluginProcessor.h, tests/LatencyTest.cpp, tests/ReverbEngineSwapTest.cpp</files>
  <action>
GatedBloomChain: unique_ptr IReverbEngine defaulting to SchroederTank32; setEngine() for tests only in prepare.
PluginProcessor: getLatencySamples() override returns 0.
LatencyTest: assert processor reports 0 samples.
ReverbEngineSwapTest: Fdn8 in chain produces different tail than Schroeder.
Run routing regression ctest.
  </action>
  <verify>
    <automated>ctest --test-dir Builds -R "Latency|ReverbEngine|chain|routing|GatedBloom" --output-on-failure</automated>
  </verify>
  <done>CHN-04 + VERB-06 chain integration GREEN.</done>
</task>

</tasks>

<success_criteria>
Chain uses interface; zero latency asserted; routing regression green.
</success_criteria>
