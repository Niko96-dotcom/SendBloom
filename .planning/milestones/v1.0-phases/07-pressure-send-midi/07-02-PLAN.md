---
phase: 07-pressure-send-midi
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - source/GatedBloomChain.h
  - tests/GatedBloomChainTest.cpp
autonomous: true
requirements:
  - SEND-03
must_haves:
  truths:
    - "GatedBloomChain uses PressureSend instead of StubPressureSend"
    - "Tank tail audible ≥500 ms after send release to zero"
    - "Phase 3/4/5/6 routing tests still pass"
  artifacts:
    - path: source/GatedBloomChain.h
      provides: "PressureSend swap preserving topology"
    - path: tests/GatedBloomChainTest.cpp
      provides: "SEND-03 500 ms trail proof"
---

<objective>
Swap stub in chain; extend tank trail test to ≥500 ms.
</objective>

<context>
@source/GatedBloomChain.h
@source/PressureSend.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chain swap and 500 ms trail test</name>
  <files>source/GatedBloomChain.h, tests/GatedBloomChainTest.cpp</files>
  <action>
Replace StubPressureSend with PressureSend in GatedBloomChain.
Extend "send release preserves tank energy" test: after sendGain=0, verify tail RMS > floor at 500 ms offset (24 000 samples @ 48 kHz).
Run routing regression: GatedBloomChain, DryPath, PluginBasics chain tests.
  </action>
  <verify>
    <automated>ctest --test-dir Builds -R "GatedBloomChain|DryPath|chain|routing|send|tank" --output-on-failure</automated>
  </verify>
  <done>SEND-03 passes; routing intact.</done>
</task>

</tasks>

<success_criteria>
PressureSend in chain; 500 ms trail test passes; routing regression green.
</success_criteria>
