---
phase: 07-pressure-send-midi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - source/PressureSend.h
  - tests/PressureSendTest.cpp
autonomous: true
requirements:
  - SEND-01
  - SEND-02
must_haves:
  truths:
    - "PressureSend returns 1.0 when send_connected is false"
    - "Connected send_amount uses smoothstep + Firm/Soft exponents"
    - "Firm and Soft produce different gain at 0.5 amount"
  artifacts:
    - path: source/PressureSend.h
      provides: "SEND-01/02 momentary send gain"
    - path: tests/PressureSendTest.cpp
      provides: "SEND-01/02 unit proofs"
---

<objective>
PressureSend gain module with unit tests (SEND-01, SEND-02).
</objective>

<context>
@.planning/phases/07-pressure-send-midi/07-CONTEXT.md
@.planning/phases/07-pressure-send-midi/07-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — PressureSend unit tests</name>
  <files>tests/PressureSendTest.cpp</files>
  <behavior>
    - computeGain with send_connected=false returns 1.0 regardless of amount
    - computeGain Firm at 0.5 matches pow(smoothstep(0.5), 1.85)
    - computeGain Soft at 0.5 matches pow(smoothstep(0.5), 1.2)
    - process multiplies input by gain
  </behavior>
  <action>Create failing tests tagged [send][PressureSend].</action>
  <verify>
    <automated>ctest --test-dir Builds -R "PressureSend|send" --output-on-failure 2>&1; test $? -ne 0</automated>
  </verify>
  <done>Tests exist and fail RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — PressureSend implementation</name>
  <files>source/PressureSend.h</files>
  <behavior>
    - computeGain delegates to ParameterCurves::sendGain when connected
    - process(input, sendGain) returns input * sendGain
  </behavior>
  <action>Implement header-only PressureSend per RESEARCH. Run tests GREEN.</action>
  <verify>
    <automated>ctest --test-dir Builds -R "PressureSend|send" --output-on-failure</automated>
  </verify>
  <done>SEND-01/02 unit tests GREEN.</done>
</task>

</tasks>

<success_criteria>
PressureSend implemented with SEND-01/02 unit tests passing.
</success_criteria>
