---
phase: 03-ugly-signature-chain
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - README.md
autonomous: false
requirements:
  - CHN-01
  - CHN-02
  - CHN-03
must_haves:
  truths:
    - "Full Catch2 suite passes including all chain routing tests"
    - "pluginval strictness 5 passes on Release VST3 artifact"
    - "Human confirms DAW play-test feels like gated-dirty-ambience object (tone irrelevant)"
    - "Human confirms dry guitar clean, wet blooms, chop on stop, send tail persists"
  artifacts:
    - path: README.md
      provides: "Phase 3 ugly-chain DAW smoke procedure"
  key_links:
    - from: README.md
      to: Builds/SendBloom_artefacts
      via: "DAW load instructions for ugly-chain audition"
      pattern: "ugly.chain|DAW smoke"
---

## Phase Goal

**As a** guitarist, **I want to** hear crude gated-dirty-ambience routing through the plugin, **so that** the signature product behavior is audible before real DSP ships.

<objective>
Complete Phase 3 completion gate: full automated test suite, pluginval 5, and documented human DAW smoke for ugly-chain routing feel (success criterion 5).

Purpose: Automated tests prove signal routing; only human ears confirm the product object feel in a real host.
Output: README Phase 3 smoke section; human approval for phase gate.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ugly-signature-chain/03-03-SUMMARY.md
@.planning/phases/01-scaffold-passthrough/01-03-SUMMARY.md
@.planning/ROADMAP.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated phase gate — full suite, Release build, pluginval 5</name>
  <files>README.md</files>
  <action>
Run full ctest suite: ctest --test-dir Builds --output-on-failure.

Build Release VST3/AU: cmake --build Builds --config Release.

Run pluginval strictness 5 on Release VST3 per Phase 1 CI pattern (scripts or documented command from 01-03).

Add README section "Phase 3 — Ugly Signature Chain DAW Smoke" with numbered audition steps:
1. Load SendBloom from Builds artefact path on guitar or DI track.
2. Set level ~50%, size ~50%, distn ~30%, send connected on, gate Post, defaults elsewhere.
3. Play palm-muted riff — confirm dry attack stays clean while wash blooms in parallel.
4. Raise distn to 100% — confirm grind on wash only; dry pick attack unchanged.
5. Stop playing — confirm wet chops hard within ~15 ms ("edited sample" feel) while gate Post active.
6. Hold send pad / automate send 1→0 during ring — confirm tail decays naturally, not instant mute.
7. Toggle gate Pre — confirm hum suppression character changes on wet feed (crude stub OK).
8. Note tone is intentionally ugly; routing feel is the pass criteria.

Document pluginval and ctest pass results in SUMMARY, not README body.

Fix only blocker bugs if preflight fails — no scope creep into Phase 4 DSP.
  </action>
  <verify>
    <automated>ctest --test-dir Builds --output-on-failure && cmake --build Builds --config Release</automated>
  </verify>
  <done>All automated gates green; README documents ugly-chain DAW smoke procedure.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human ugly-chain DAW routing confirmation</name>
  <files>README.md</files>
  <action>Pause for human verification per README Phase 3 smoke section. No processor changes unless issues found.</action>
  <what-built>Complete Phase 3 ugly signature chain: parallel dry tap, GatedBloomChain stubs, ParallelWetMixer, routing proof tests, PluginProcessor integration</what-built>
  <how-to-verify>
1. Build Release plugin per README.
2. Load SendBloom on guitar track in REAPER, Logic, or Cubase.
3. Play rhythmic part — dry stays clean at unity; wet ambience blooms in parallel (success criterion 1).
4. Raise distn — wet gets gritty; dry pick transients unchanged (success criterion 2).
5. Stop playing with gate Post — wet chops hard quickly; no reverb tail holding gate open (success criterion 3).
6. Send 1→0 while notes ring — tail decays, tank not cleared (success criterion 4).
7. Overall feel: gated-dirty-ambience pedal object even if tone is crude (success criterion 5).
8. Type "approved" or describe routing issues.
  </how-to-verify>
  <verify>
    <human-check>User confirms ugly-chain DAW smoke per steps above</human-check>
  </verify>
  <done>Human types "approved" or reports issues for fix-up</done>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DAW→plugin binary | Host loads signed/unsigned local build only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Tampering | Release artefact | low | accept | Local dev build; no distribution |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages |

</threat_model>

<verification>
Full ctest green; pluginval 5 green; human DAW smoke approved.
</verification>

<success_criteria>
Phase 3 completion gate satisfied: pluginval 5, routing proof unit tests, audible ugly-chain DAW smoke documented and approved.
</success_criteria>

<output>
Create `.planning/phases/03-ugly-signature-chain/03-04-SUMMARY.md` when done
</output>
