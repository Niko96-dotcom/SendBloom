---
phase: 03-ugly-signature-chain
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - source/PluginProcessor.h
  - source/PluginProcessor.cpp
  - source/DummyDspHooks.h
  - tests/DummyDspHooksTest.cpp
  - tests/PluginBasics.cpp
autonomous: true
requirements:
  - CHN-01
  - CHN-02
  - CHN-03
must_haves:
  truths:
    - "Plugin processBlock replaces DummyDspHooks with GatedBloomChain + ParallelWetMixer"
    - "Dry buffer copy stays clean at unity; wet path receives input gain, chain, level wet scale"
    - "Bypass 5 ms crossfade and output gain preserved from Phase 2"
    - "No DummyDspHooks references remain in source or tests"
    - "Integration tests confirm distn affects wet only and level does not attenuate dry"
  artifacts:
    - path: source/PluginProcessor.cpp
      provides: "Full ugly-chain audio path in processBlock"
    - path: source/PluginProcessor.h
      provides: "GatedBloomChain member replaces DummyDspState"
  key_links:
    - from: PluginProcessor::processBlock
      to: GatedBloomChain::processSample
      via: "per-sample mono gained input + input envelope"
      pattern: "processSample"
    - from: PluginProcessor::processBlock
      to: ParallelWetMixer::mix
      via: "dryBuffer read + wet return × smoothed level wet gain"
      pattern: "ParallelWetMixer"
---

## Phase Goal

**As a** guitarist, **I want to** hear crude gated-dirty-ambience routing through the plugin, **so that** the signature product behavior is audible before real DSP ships.

<objective>
Wire the ugly signature chain into PluginProcessor (CHN-01/02/03 integration), remove DummyDspHooks, and prove end-to-end routing via PluginBasics integration tests — first loadable DAW artifact with correct parallel topology.

Purpose: MVP vertical slice — user can load plugin in DAW and hear gated-dirty-ambience routing (tone ugly, topology correct).
Output: processBlock using GatedBloomChain + ParallelWetMixer; DummyDspHooks deleted; integration tests GREEN.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ugly-signature-chain/03-CONTEXT.md
@.planning/phases/03-ugly-signature-chain/03-RESEARCH.md
@source/PluginProcessor.cpp
@source/GatedBloomChain.h
@source/ParallelWetMixer.h
@source/BypassCrossfade.h
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — PluginProcessor ugly-chain integration tests</name>
  <files>tests/PluginBasics.cpp</files>
  <behavior>
    - Offline render: default params, sine input; dry channel RMS stable when distn automated 0→1 with level wet gain=0 (dry path unchanged)
    - Offline render: level wet gain 0→1 increases output RMS without reducing dry-only component (dry tap + wet scale)
    - Offline render: gate post mode, play burst then silence; output wet component drops sharply within 15 ms of silence onset
    - Plugin prepares without throw; processBlock 512 samples at 48 kHz completes
  </behavior>
  <action>
Extend tests/PluginBasics.cpp with [chain][routing][integration] cases using sendbloom::PluginProcessor offline.

Test "dry unchanged when distn max": set APVTS distn=1, level=0 (wetGain≈0), input sine; capture output; compare to distn=0 at same level — RMS match within 1e-3 (wet-only dirt per locked CONTEXT).

Test "level scales wet not dry": level=0 vs level=1 with size mid, distn=0, send=1; higher level increases RMS; with wetGain≈0 dry contribution dominates equally.

Test "post gate chop integration": gate_pre_post=Post, threshold mid; 480-sample burst then silence; assert output RMS in silence block &lt; 0.02 * burst RMS.

Tests fail RED while DummyDspHooks still wired.
  </action>
  <verify>
    <automated>cmake --build Builds --target Tests 2>&1; test $? -ne 0</automated>
  </verify>
  <done>Integration routing tests exist and fail RED against DummyDspHooks path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — wire processBlock, remove DummyDspHooks</name>
  <files>source/PluginProcessor.h, source/PluginProcessor.cpp, source/DummyDspHooks.h, tests/DummyDspHooksTest.cpp, tests/PluginBasics.cpp</files>
  <behavior>
    - prepareToPlay: chain.prepare(sampleRate, samplesPerBlock); remove dummyState.prepare
    - processBlock: copy input→dryBuffer; per-sample smoothed params; monoIn = drySample * inputGain (per-channel ugly proof per RESEARCH A5); env = chain envelope follow abs(monoIn); wet = chain.processSample(...); out = ParallelWetMixer::mix(dryTap, wet, wetGain); bypass crossfade dryMix/bypassWet; × outputGain
    - thresholdDb from ParameterCurves::inputThresholdDb(getNextInputThresholdNorm()) each sample
    - rt60 from ParameterCurves::sizeToRT60(getNextSizeNorm()) each sample
    - gatePreSoft from snapshot each block (bool stable within block)
    - Delete source/DummyDspHooks.h and tests/DummyDspHooksTest.cpp; remove includes and DummyDspState member
    - grep source for DummyDspHooks returns zero
  </behavior>
  <action>
Replace DummyDspHooks with GatedBloomChain per 03-RESEARCH Pattern 6 integration diagram and locked parallel routing (D-01).

PluginProcessor.h: replace DummyDspState dummyState with GatedBloomChain chain; remove DummyDspHooks.h include; add GatedBloomChain.h and ParallelWetMixer.h.

PluginProcessor.cpp prepareToPlay: chain.prepare(sampleRate, samplesPerBlock); keep smoothedBank and dryBuffer sizing.

processBlock inner loop per RESEARCH pseudocode: dryTap from dryBuffer read (unity, never distorted); monoIn = input * inputGain; envelope on gained input; pass snapshot gatePreSoft from snap.gatePreSoft captured at block start; wire sendGain, distnBlend, level wetGain from smoothed bank.

Preserve bypass: dryMix = 1 - bypassWet; final = (dryTap * dryMix + mixed * bypassWet) * outputGain — reorder if needed so bypass crossfades full mixed signal vs dry tap; match Phase 2 BypassCrossfade intent (5 ms wet smoother).

Delete DummyDspHooks.h and DummyDspHooksTest.cpp entirely. Update any CMake-irrelevant references (GLOB picks up automatically).

Run integration + chain unit tests GREEN. Run full ctest to catch regressions on parm/bypass tests.
  </action>
  <verify>
    <automated>cmake --build Builds --target Tests && ctest --test-dir Builds -R "chain|routing|ParallelWet|GatedBloom|integration|bypass|snapshot" --output-on-failure && ! grep -r DummyDspHooks source tests</automated>
  </verify>
  <done>Plugin loads ugly chain; CHN-01/02/03 integrated; DummyDspHooks fully removed; integration tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host buffer→processBlock | Untrusted sample count; use buffer.getNumSamples() bounds only |
| APVTS→snapshot | Parameter values once per block |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Denial of Service | processBlock inner loop | medium | mitigate | ScopedNoDenormals; no heap in processBlock; preallocated dryBuffer |
| T-03-05 | Tampering | buffer channel indexing | medium | mitigate | jmin numChannels with dryBuffer channels; clear extra outputs |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages |

</threat_model>

<verification>
ctest routing and integration tags pass; DummyDspHooks absent; Release plugin builds for DAW smoke in Plan 03-04.
</verification>

<success_criteria>
Loadable SendBloom plugin processes audio through ugly signature chain with automated routing integration proofs (success criteria 1–4 at signal level).
</success_criteria>

<output>
Create `.planning/phases/03-ugly-signature-chain/03-03-SUMMARY.md` when done
</output>
