---
phase: 03-ugly-signature-chain
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - source/StubInputEnvelope.h
  - source/StubNoiseGate.h
  - source/StubPressureSend.h
  - source/PlaceholderReverb.h
  - source/PlaceholderWetDirt.h
  - source/GatedBloomChain.h
  - tests/GatedBloomChainTest.cpp
autonomous: true
requirements:
  - CHN-01
  - CHN-03
must_haves:
  truths:
    - "GatedBloomChain runs locked wet-path order: preGate? → send multiply → feedback delay reverb → tanh dirt → postGate?"
    - "Post gate gain follows input envelope only, not wet tail amplitude"
    - "Send gain scales feed to reverb; send→0 does not reset delay tank"
    - "Placeholder tanh dirt blends on wet path only via distnBlend"
  artifacts:
    - path: source/GatedBloomChain.h
      provides: "Wet-path orchestrator owning all stub state (CHN-01)"
    - path: source/PlaceholderReverb.h
      provides: "DelayLine feedback stub, no tank clear on send release"
    - path: source/StubNoiseGate.h
      provides: "Crude pre/post gate profiles keyed from input envelope"
    - path: tests/GatedBloomChainTest.cpp
      provides: "Routing proof tests for chop and send trail"
  key_links:
    - from: GatedBloomChain::processSample
      to: StubNoiseGate::process
      via: "inputEnvelope argument from StubInputEnvelope on gained input"
      pattern: "inputEnvelope"
    - from: StubPressureSend
      to: PlaceholderReverb
      via: "multiply feed before reverb.processSample; never call reverb.reset on send drop"
      pattern: "processSample"
---

## Phase Goal

**As a** guitarist, **I want to** hear crude gated-dirty-ambience routing through the plugin, **so that** the signature product behavior is audible before real DSP ships.

<objective>
Build the complete ugly wet-path stub chain offline (CHN-01) with input-keyed post gate and send-trail behavior (CHN-03), proven by Catch2 routing tests before host integration.

Purpose: Routing topology must be correct in isolation so Phase 4–7 swap real modules without rewiring.
Output: GatedBloomChain + five stub headers, GatedBloomChainTest.cpp GREEN for dry-clean, wet-only dirt, input-keyed chop, send trail.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ugly-signature-chain/03-CONTEXT.md
@.planning/phases/03-ugly-signature-chain/03-RESEARCH.md
@tests/ChainTestHelpers.h
@source/ParameterCurves.h
@JUCE/modules/juce_dsp/processors/juce_DelayLine.h
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — GatedBloomChain routing proof tests</name>
  <files>tests/GatedBloomChainTest.cpp</files>
  <behavior>
    - Dry tap test: ParallelWetMixer::mix(dryIn, 0, 0) equals dryIn at distn=1 chain output zero wet (wet-only dirt does not touch dry)
    - Wet-only dirt: chain with distnBlend=1 on sine burst produces higher wet RMS than distnBlend=0; dry tap path in mix test stays at input level
    - Input-keyed post gate (CHN-03): burst 480 samples at 0.5 then silence 512 samples with gatePreSoft=false; post-gate wet RMS during silence below -40 dBFS equivalent (RMS &lt; 0.01)
    - Send trail: render burst with send=1 then 200+ samples with send=0 and zero input; tank energy RMS still above 1e-4 (tail not cleared)
    - Topology smoke: non-zero wet output when send=1, size mid, distn=0 (reverb stub audible)
  </behavior>
  <action>
Create tests/GatedBloomChainTest.cpp tagged [chain][routing][GatedBloomChain][postGate][send][tank].

Use ChainTestHelpers from Plan 03-01. Instantiate GatedBloomChain at 48000 Hz via prepare(48000, 512).

Test "dry tap unchanged when distn max": wet path driven but mix with wetGain=0 leaves dry unchanged (integrates ParallelWetMixer from 03-01).

Test "wet-only dirt": processSample burst with distnBlend 0 vs 1; assert wet sample magnitude differs; assert PlaceholderWetDirt never called on dry tap (test via mix boundary).

Test "post gate keyed from input" per RESEARCH burst→silence fixture: gatePreSoft=false, threshold -40 dB, input envelope from zero input during silence forces gate closed even if reverb tail has energy.

Test "send release preserves tank": after sendGain→0, continue processSample with zero input for 240 samples; assert delayed energy decays gradually (RMS after 1 block &gt; 1e-4), not instant silence (&lt;1e-6).

Tests compile-fail RED until stub headers exist.
  </action>
  <verify>
    <automated>cmake --build Builds --target Tests 2>&1; test $? -ne 0</automated>
  </verify>
  <done>GatedBloomChain routing tests exist and fail RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — wet-path stub headers and PlaceholderReverb</name>
  <files>source/StubInputEnvelope.h, source/StubNoiseGate.h, source/StubPressureSend.h, source/PlaceholderReverb.h, source/PlaceholderWetDirt.h</files>
  <behavior>
    - StubInputEnvelope: one-pole peak follower on abs(input); prepare(sampleRate) sets attack/release coeffs
    - StubNoiseGate: pre profile release ~150 ms soft floor ~0.1; post profile release ~7 ms hard floor 0; process(inputEnvelope, thresholdDb) returns gain multiplier
    - StubPressureSend: static multiply(input, sendGain) — no side effects, no tank reset
    - PlaceholderReverb: juce::dsp::DelayLine feedback loop; delay ~300 ms; feedback from rt60 via exp(-6.9078*delay/rt60) clamped 0..0.95; prepare allocates once; no reset() on send drop
    - PlaceholderWetDirt: static process(wet, distnBlend) tanh blend per RESEARCH Pattern 5
  </behavior>
  <action>
Implement stub headers in source/ per 03-RESEARCH Patterns 3–5 and locked CONTEXT placeholder DSP decisions.

StubInputEnvelope: follow(abs(input)) with ~5 ms attack, ~50 ms release for envelope sidechain [planner discretion within RESEARCH].

StubNoiseGate: two instances will be owned by chain — configure preGate with 150 ms release and soft floor 0.1; postGate with 7 ms release and floor 0.0. Threshold compare via juce::Decibels::decibelsToGain(thresholdDb).

PlaceholderReverb: use DelayLine float; prepare in prepare() only with max delay 48000 samples; setDelay ~14400 samples at 48 kHz (~300 ms). Map rt60Seconds to feedback gain with jlimit 0..0.95. popSample/pushSample feedback pattern from RESEARCH. Optional: when darkMode coeff passed later, one-pole darker feedback — skip unless trivial; dark not required for phase gate.

PlaceholderWetDirt: wet + distnBlend * (tanh(wet*3) - wet).

All prepare-time allocation; noexcept process paths; namespace sendbloom.

Do not fork BYOD Gate.cpp (GPL) — greenfield stub only.
  </action>
  <verify>
    <automated>cmake --build Builds --target Tests 2>&1 | tail -5</automated>
  </verify>
  <done>Stub headers compile; unit pieces ready for orchestrator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — GatedBloomChain orchestrator and routing tests pass</name>
  <files>source/GatedBloomChain.h, tests/GatedBloomChainTest.cpp</files>
  <behavior>
    - processSample order locked: if gatePreSoft preGate× → send× → reverb → dirt → if !gatePreSoft postGate×
    - inputEnvelope passed into gate stubs only; post gate never reads wet amplitude
    - prepare(sampleRate, maxBlockSize) prepares reverb, envelope, both gates
    - expose getEnvelope() or envelope member for processor-side following
    - All GatedBloomChainTest cases GREEN
  </behavior>
  <action>
Implement source/GatedBloomChain.h per 03-RESEARCH Pattern 1 and locked routing topology (parallel dry + gated wet: pre/post gate stubs → pressure send stub → placeholder reverb → tanh dirt → mix).

Own PlaceholderReverb reverb, StubInputEnvelope envelope, StubNoiseGate preGate and postGate as members.

processSample signature: (float input, float inputEnvelope, float rt60Seconds, float distnBlend, float sendGain, bool gatePreSoft, float thresholdDb) — sizeNorm omitted; rt60 passed from snapshot mapping.

Wire StubPressureSend as inline multiply before reverb — sendGain→0 feeds zero new energy but tank state persists (locked CONTEXT pressure-send stub).

Run ctest GatedBloomChain and routing tags GREEN alongside 03-01 ParallelWetMixer tests.
  </action>
  <verify>
    <automated>cmake --build Builds --target Tests && ctest --test-dir Builds -R "GatedBloomChain|chain|routing|postGate|send|tank" --output-on-failure</automated>
  </verify>
  <done>CHN-01 and CHN-03 routing proofs GREEN offline; chain ready for PluginProcessor wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| offline test→chain | Synthetic floats through processSample; bounded loop counts |
| DelayLine feedback | Internal tank state; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02 | Denial of Service | PlaceholderReverb feedback | medium | mitigate | jlimit feedback &lt;0.95; jlimit output samples; minimum rt60 50 ms |
| T-03-03 | Tampering | StubNoiseGate threshold | low | accept | threshold from APVTS in Plan 03; offline tests use fixed -40 dB |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | JUCE submodule only; no new packages |

</threat_model>

<verification>
ctest --test-dir Builds -R "GatedBloomChain|chain|routing|postGate|send" passes. Dry-clean, wet-only dirt, input-keyed chop, and send-trail behaviors proven without DAW.
</verification>

<success_criteria>
GatedBloomChain implements locked topology with all four routing proof tests GREEN (CHN-01, CHN-03).
</success_criteria>

<output>
Create `.planning/phases/03-ugly-signature-chain/03-02-SUMMARY.md` when done
</output>
