---
phase: 03-ugly-signature-chain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - source/ParallelWetMixer.h
  - tests/ChainTestHelpers.h
  - tests/ParallelWetMixerTest.cpp
autonomous: true
requirements:
  - CHN-02
must_haves:
  truths:
    - "Dry tap stays at unity when level knob changes wet return gain"
    - "Wet return scales by sin equal-power curve from smoothed level"
    - "Parallel mix formula is dryTap + wetSample * wetGain (not DryWetMixer dual scaling)"
  artifacts:
    - path: source/ParallelWetMixer.h
      provides: "Header-only parallel mix helper per CHN-02"
    - path: tests/ParallelWetMixerTest.cpp
      provides: "Automated dry-unity and wet-scaling proofs"
    - path: tests/ChainTestHelpers.h
      provides: "Shared offline render utilities for chain tests"
  key_links:
    - from: ParallelWetMixer::mix
      to: ParameterCurves::levelEqualPower
      via: "wetGain argument sourced from smoothed bank wet leg in processor (Plan 03)"
      pattern: "levelEqualPower"
---

## Phase Goal

**As a** guitarist, **I want to** hear crude gated-dirty-ambience routing through the plugin, **so that** the signature product behavior is audible before real DSP ships.

<objective>
Establish the parallel wet-mix contract (CHN-02) with failing tests first, then a header-only ParallelWetMixer that keeps dry at unity while level scales wet return only.

Purpose: Parallel topology is the product differentiator — wrong mixing math breaks every later phase swap-in.
Output: ParallelWetMixer.h, ChainTestHelpers.h scaffold, ParallelWetMixerTest.cpp GREEN.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ugly-signature-chain/03-CONTEXT.md
@.planning/phases/03-ugly-signature-chain/03-RESEARCH.md
@source/ParameterCurves.h
@source/SmoothedParameterBank.h
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — parallel mix and dry-unity routing tests</name>
  <files>tests/ChainTestHelpers.h, tests/ParallelWetMixerTest.cpp</files>
  <behavior>
    - ParallelWetMixer::mix(0.25, 0.0, 0.0) returns 0.25 unchanged (dry tap unity at zero wet gain)
    - ParallelWetMixer::mix(0.25, 1.0, 0.5) returns 0.75 (dry unity + wet scaled)
    - Raising wetGain from 0 to 1 does not change dryTap contribution (dry coefficient always 1.0)
    - wetGain at levelNorm=0.5 matches ParameterCurves::levelEqualPower wet leg within 1e-5
    - mix output differs from juce sin3dB dry+wet sum when wetGain>0 (proves not DryWetMixer dual scaling)
  </behavior>
  <action>
Create tests/ChainTestHelpers.h with small utilities: rms helper, constant-sine buffer filler, and renderParallelMix lambda taking dryTap/wet/wetGain vectors — reused by Plan 02 GatedBloomChainTest.

Create tests/ParallelWetMixerTest.cpp tagged [chain][routing][ParallelWet]. Include ParallelWetMixer.h (will not exist yet — compile fails RED).

Test dry integrity: dryIn=0.25, wet=0, wetGain=0 → output equals dryIn.

Test wet scaling: dryIn=0.25, wet=1.0, wetGain=0.5 → output 0.75.

Test level curve linkage: for levelNorm in {0, 0.25, 0.5, 0.75, 1}, compute wetGain via ParameterCurves::levelEqualPower and assert mix(1,1,wetGain) equals 1 + wetGain.

Add negative comparison test documenting CHN-02 intent: equal-power dry leg must NOT be applied to dryTap (contrast with juce DryWetMixer sin3dB pattern from RESEARCH — describe behaviorally, do not paste DryWetMixer identifier strings into production headers).

Commit RED — ParallelWetMixer.h missing or stub throws link/compile failure.
  </action>
  <verify>
    <automated>cmake --build Builds --target Tests 2>&1; test $? -ne 0</automated>
  </verify>
  <done>ParallelWetMixer tests exist and fail RED before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — ParallelWetMixer header-only implementation</name>
  <files>source/ParallelWetMixer.h</files>
  <behavior>
    - static mix(dryTap, wetSample, wetGain) returns dryTap + wetSample * wetGain
    - noexcept, header-only, namespace sendbloom
    - No dry attenuation at any wetGain value (per locked CONTEXT parallel topology D-01)
  </behavior>
  <action>
Implement source/ParallelWetMixer.h per 03-RESEARCH Pattern 2 and locked routing topology (parallel dry tap + gated wet path). Single static mix method; no state; no juce::dsp::DryWetMixer include.

Document in file header comment (one line): dry unity parallel pedal topology — level knob scales wet return only (CHN-02).

Run ParallelWetMixer tests GREEN. No processor wiring in this plan.
  </action>
  <verify>
    <automated>cmake --build Builds --target Tests && ctest --test-dir Builds -R "ParallelWet|routing" --output-on-failure</automated>
  </verify>
  <done>CHN-02 unit tests GREEN; ParallelWetMixer ready for PluginProcessor wiring in Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test→header | Offline test calls mix with synthetic floats; no host buffer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | ParallelWetMixer::mix | low | accept | Pure arithmetic; tests lock contract |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages this plan; N/A |

</threat_model>

<verification>
ctest --test-dir Builds -R "ParallelWet|routing" passes. Dry unity and wet scaling behaviors verified without DAW.
</verification>

<success_criteria>
ParallelWetMixer implements CHN-02 parallel topology with automated proof tests GREEN.
</success_criteria>

<output>
Create `.planning/phases/03-ugly-signature-chain/03-01-SUMMARY.md` when done
</output>
