---
phase: 05-schroedertank32-reverb
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - source/SchroederTank32.h
  - tests/SchroederTank32Test.cpp
autonomous: true
requirements:
  - VERB-01
  - VERB-03
  - VERB-04
  - VERB-05
must_haves:
  truths:
    - "SchroederTank32 runs full APF→comb→tank topology"
    - "Dark adds predelay and darker damping vs Bright"
    - "authentic_color enables 32 kHz path and 9-bit quantization"
  artifacts:
    - path: source/SchroederTank32.h
      provides: "Primary reverb engine"
---

<objective>
SchroederTank32 engine with RT60, Dark/Bright, authentic_color.
</objective>

<context>
@.planning/phases/05-schroedertank32-reverb/05-RESEARCH.md
@source/ParameterCurves.h
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — engine tests</name>
  <files>tests/SchroederTank32Test.cpp</files>
  <behavior>
    - Impulse produces audible tail (RMS &gt; 1e-4 after 100 ms)
    - Dark mode longer initial delay than Bright (predelay)
    - authentic_color path processes without NaN
  </behavior>
  <action>Create failing tests tagged [verb][SchroederTank32].</action>
  <verify>
    <automated>ctest --test-dir Builds -R "SchroederTank32" --output-on-failure; test $? -ne 0</automated>
  </verify>
  <done>Engine tests exist and fail RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — SchroederTank32</name>
  <files>source/SchroederTank32.h</files>
  <behavior>
    - prepare(hostRate), processSample(in, rt60, darkMix 0..1, authenticColor)
    - Predelay 55 ms dark, 0 bright; damping 3200/8000 Hz crossfade
    - Modulated tank AP; optional 32 kHz resampler when authentic
  </behavior>
  <action>Implement engine per RESEARCH patterns. Run engine tests GREEN.</action>
  <verify>
    <automated>ctest --test-dir Builds -R "SchroederTank32|verb" --output-on-failure</automated>
  </verify>
  <done>VERB-03/04/05 engine tests GREEN.</done>
</task>

</tasks>

<success_criteria>
SchroederTank32 engine passes topology and mode tests.
</success_criteria>
