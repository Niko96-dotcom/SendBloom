---
phase: 05-schroedertank32-reverb
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - source/GatedBloomChain.h
  - source/PluginProcessor.cpp
  - source/SmoothedParameterBank.h
autonomous: true
requirements:
  - VERB-01
must_haves:
  truths:
    - "GatedBloomChain uses SchroederTank32 instead of PlaceholderReverb"
    - "Processor passes rt60, dark mix, authentic_color to reverb"
    - "Phase 3/4 routing tests still pass"
  artifacts:
    - path: source/GatedBloomChain.h
      provides: "Tank swap preserving bloom-then-chop"
---

<objective>
Swap placeholder reverb in chain; wire processor params.
</objective>

<context>
@source/GatedBloomChain.h
@source/PluginProcessor.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chain swap and processor wiring</name>
  <files>source/GatedBloomChain.h, source/PluginProcessor.cpp, source/SmoothedParameterBank.h</files>
  <action>
Replace PlaceholderReverb with SchroederTank32 in GatedBloomChain.
Extend processSample with darkModeMix and authenticColor.
Wire PluginProcessor per-sample from smoothed bank (add authenticColor smoother if needed).
Run full routing regression: GatedBloomChain, DryNeverGated, chain tests.
  </action>
  <verify>
    <automated>ctest --test-dir Builds -R "GatedBloomChain|DryNeverGated|chain|routing" --output-on-failure</automated>
  </verify>
  <done>Routing tests pass with real tank.</done>
</task>

</tasks>

<success_criteria>
Signature routing intact with SchroederTank32 in chain.
</success_criteria>
