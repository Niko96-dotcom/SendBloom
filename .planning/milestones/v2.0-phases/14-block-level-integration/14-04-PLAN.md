---
phase: 14-block-level-integration
plan: 04
type: execute
wave: 4
depends_on:
  - 14-03
files_modified:
  - tests/Phase14IntegrabilityTest.cpp
autonomous: true
requirements:
  - INTEG-04
must_haves:
  truths:
    - "Parameter layout still exposes exactly 15 parameters with no new Authentic32Mode or diagnostics APVTS entry"
    - "Fresh plugin load defaults authentic_color off (SAFE-01 preserved)"
    - "All factory presets recall authentic_color off (SAFE-02 preserved)"
    - "No new UI source files or ParameterIDs entries added in Phase 14 integration"
  artifacts:
    - path: tests/Phase14IntegrabilityTest.cpp
      provides: "INTEG-04 traceability static and preset gates"
  key_links:
    - from: tests/Phase14IntegrabilityTest.cpp
      to: source/ParameterIDs.h
      via: static count and absence of diagnostics param IDs
    - from: tests/Phase14IntegrabilityTest.cpp
      to: source/PluginProcessor.cpp
      via: createParameterLayout default authentic_color false
---

<objective>
Verify Phase 14 block integration introduces no new UI controls and preserves existing 32k Color toggle off-by-default behavior (INTEG-04).

Purpose: Lock product surface area while wiring internal ProperSRC path.
Output: Automated INTEG-04 traceability tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-block-level-integration/14-CONTEXT.md
@.planning/phases/14-block-level-integration/14-RESEARCH.md
@.planning/phases/14-block-level-integration/14-03-SUMMARY.md
@source/ParameterIDs.h
@source/PluginProcessor.cpp
@tests/ReleaseTruthTest.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Static ParameterIDs and layout contract tests</name>
  <files>tests/Phase14IntegrabilityTest.cpp</files>
  <read_first>source/ParameterIDs.h, tests/ReleaseTruthTest.cpp, source/PluginProcessor.cpp</read_first>
  <behavior>
    - ParameterIDs.h defines exactly 15 parameter ID string constants (count via constexpr array or documented constant)
    - createParameterLayout contains authentic_color param with default false
    - No parameter ID string contains Authentic32 or diagnostics mode naming
    - PluginProcessor.cpp and GatedBloomChain.h processBlock bodies contain no new ParameterIDs entries (grep gate)
  </behavior>
  <action>
Create tests/Phase14IntegrabilityTest.cpp with Catch2 cases tagged [integrability][INTEG-04].

Case 1 — parameter count: include ParameterIDs.h; verify known 15 IDs exist (inputGain through extendedStereo per Phase 2 contract); REQUIRE layout from PluginProcessor::createParameterLayout().size() equals 15.

Case 2 — authentic_color default: fresh layout default value for authentic_color parameter is 0.0f / false.

Case 3 — no diagnostics APVTS: read source/ParameterIDs.h as string; REQUIRE no substring matching Authentic32Mode enum names or a hypothetical diagnosticsMode param.

Case 4 — integration diff guard: read source/PluginProcessor.cpp and source/GatedBloomChain.h; REQUIRE authentic_color remains the only authentic-path APVTS routing reference (no new getRawParameterValue for diagnostics).

Do not modify ParameterIDs.h, UI files, or factory preset XML in this plan.
  </action>
  <verify>
    <automated>ctest --test-dir build -R "INTEG-04" --output-on-failure</automated>
  </verify>
  <done>Static INTEG-04 contract tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Preset and fresh-load authentic_color off regression</name>
  <files>tests/Phase14IntegrabilityTest.cpp</files>
  <read_first>tests/ReleaseTruthTest.cpp</read_first>
  <action>
Add TEST_CASE tagged [integrability][INTEG-04][safe] that duplicates or invokes same assertions as ReleaseTruthTest fresh plugin load defaults authentic_color off and all factory presets recall authentic_color off — either by shared helper or by re-running identical logic with INTEG-04 tag so traceability matrix maps TEST to requirement.

Ensure no factory preset XML files are modified. Confirm AdvancedDrawer and UI sources unmodified in this phase (optional static: git diff --name-only against phase branch should not list ui/ new files — document in SUMMARY if manual).

If ReleaseTruthTest cases already cover SAFE-01/02, add thin wrapper TEST_CASE that calls same code paths with [INTEG-04] tag for requirements matrix only.
  </action>
  <verify>
    <automated>ctest --test-dir build -R "INTEG-04|fresh plugin load defaults|factory presets recall" --output-on-failure</automated>
  </verify>
  <done>Fresh load and presets keep authentic_color off after block integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test reader → source headers | Static file reads for contract verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-10 | Tampering | Accidental diagnostics APVTS exposure | medium | mitigate | Static grep tests; Authentic32Mode remains header-only diagnostics |
| T-14-11 | Information Disclosure | Experimental 32k enabled by default | high | mitigate | SAFE-01/02 regression tests in this plan |
</threat_model>

<verification>
ctest --test-dir build -R "INTEG-04" --output-on-failure
</verification>

<success_criteria>
No new UI or APVTS parameters; authentic_color off-by-default and preset behavior unchanged.
</success_criteria>

<output>
Create `.planning/phases/14-block-level-integration/14-04-SUMMARY.md` when done
</output>
