---
phase: 14-block-level-integration
plan: 05
type: execute
wave: 4
depends_on:
  - 14-03
files_modified:
  - tests/IntegrationAllocScanTest.cpp
  - tests/BlockIntegrationTest.cpp
  - tests/RealtimeStressTest.cpp
autonomous: true
requirements:
  - TEST-09
must_haves:
  truths:
    - "Static scan finds no heap allocation tokens inside GatedBloomChain and SchroederTank32 processBlock bodies"
    - "10k varying-block plugin stress with authentic_color=1 produces finite output with peak below 4.0f"
    - "Larger-than-prepared block and authentic toggling stress cases pass with authentic_color=1"
  artifacts:
    - path: tests/IntegrationAllocScanTest.cpp
      provides: "TEST-09 static alloc token scan for integrated processBlock bodies"
    - path: tests/BlockIntegrationTest.cpp
      provides: "GatedBloomChain and plugin finite-output block integration cases"
    - path: tests/RealtimeStressTest.cpp
      provides: "TEST-09 tagged 10k stress with authentic_color on"
  key_links:
    - from: tests/IntegrationAllocScanTest.cpp
      to: source/GatedBloomChain.h
      via: extract processBlock body and forbid resize/make_unique/push_back
    - from: tests/RealtimeStressTest.cpp
      to: source/PluginProcessor.cpp
      via: plugin.processBlock 10k iterations authentic_color=1
---

<objective>
Prove integrated Fixed32SRC path does not heap-allocate in processBlock across plugin stress scenarios (TEST-09).

Purpose: Close realtime safety gate for block-level ProperSRC in the live plugin path.
Output: Static alloc scan, extended RealtimeStressTest, block integration finite-output tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-block-level-integration/14-RESEARCH.md
@.planning/phases/14-block-level-integration/14-03-SUMMARY.md
@tests/FixedRateAdapterTest.cpp
@tests/RealtimeStressTest.cpp
@source/GatedBloomChain.h
@source/SchroederTank32.h
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Static alloc token scan for integrated processBlock bodies</name>
  <files>tests/IntegrationAllocScanTest.cpp</files>
  <read_first>tests/FixedRateAdapterTest.cpp, tests/ReleaseTruthTest.cpp extractProcessBlockBody helper</read_first>
  <behavior>
    - GatedBloomChain.h processBlock body contains no make_unique, .resize(, push_back, emplace_back after comment strip
    - SchroederTank32.h processBlock body passes same token scan
    - PluginProcessor.cpp processBlock body passes same token scan
  </behavior>
  <action>
Create tests/IntegrationAllocScanTest.cpp reusing readSourceFile and extractProcessBlockBody patterns from FixedRateAdapterTest.cpp / ReleaseTruthTest.cpp.

TEST_CASE tagged [realtime][TEST-09][static][integration]: for each of source/GatedBloomChain.h, source/SchroederTank32.h, source/PluginProcessor.cpp — load text, extract processBlock body between first brace after void processBlock and next access specifier or end of class; strip block and line comments; REQUIRE stripped body lacks make_unique, .resize(, push_back, emplace_back substrings.

Reuse stripComments lambda from FixedRateAdapterTest pattern. Tag [TEST-09] for requirements traceability.

Do not add forbidden token strings to production source in comments (comment-text discipline).
  </action>
  <verify>
    <automated>ctest --test-dir build -R "TEST-09.*static|allocation tokens" --output-on-failure</automated>
  </verify>
  <done>Static scan green on all integrated processBlock bodies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend RealtimeStressTest for authentic_color=1 primary stress</name>
  <files>tests/RealtimeStressTest.cpp</files>
  <read_first>tests/RealtimeStressTest.cpp, .planning/phases/14-block-level-integration/14-RESEARCH.md TEST-09 section</read_first>
  <behavior>
    - New case: 10k varying blocks at 48000 Hz with authentic_color=1 throughout; all samples finite; peak between 0 and 4.0f
    - Existing larger-than-prepared block case already sets authentic_color=1 — add [TEST-09] tag to that case
    - Authentic toggling case tagged [TEST-09] for state stress
  </behavior>
  <action>
Add TEST_CASE processBlock survives 10k varying block stress with authentic color on tagged [realtime][integration][stress][TEST-09]. Copy structure from existing 10k case but set authenticColor param to 1.0f before loop and keep level/distn/send params active so reverb path exercises FixedRateAdapter.

Add [TEST-09] tag to existing larger-than-prepared block and authentic color toggling TEST_CASE lines without changing their logic.

Use kBlockSizes rotation and 10000 iterations per RESEARCH block size pattern. REQUIRE_NOTHROW on processBlock; finite and peak bounds match existing stress case.
  </action>
  <verify>
    <automated>ctest --test-dir build -R "TEST-09|10k varying block stress" --output-on-failure</automated>
  </verify>
  <done>Plugin-level 10k authentic-color stress passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BlockIntegrationTest chain-level finite output</name>
  <files>tests/BlockIntegrationTest.cpp</files>
  <read_first>tests/GatedBloomChainTest.cpp, source/GatedBloomChain.h</read_first>
  <behavior>
    - GatedBloomChain::processBlock authenticColor=true at 44100 and 96000 Hz produces finite output
    - Optional: PluginProcessor single-block render with authentic_color=1 finite stereo output
  </behavior>
  <action>
Create tests/BlockIntegrationTest.cpp with cases tagged [integration][TEST-09][block].

Case 1 — chain block: GatedBloomChain at 44100/512; 128-sample noise; processBlock authenticColor=true; REQUIRE all wetOut finite.

Case 2 — chain block high rate: repeat at 96000 Hz.

Case 3 — plugin smoke: PluginProcessor prepare 48000/256; authentic_color=1; single processBlock; REQUIRE finite stereo samples.

These complement static scan and 10k stress; no allocation assertions here (covered by Task 1).
  </action>
  <verify>
    <automated>ctest --test-dir build -R "BlockIntegration|TEST-09" --output-on-failure</automated>
  </verify>
  <done>Block integration finite-output tests pass at multiple sample rates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audio thread → integrated processBlock | Realtime path must not touch heap |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-12 | Denial of Service | Heap alloc in processBlock | high | mitigate | Static token scan + 10k authentic stress + prepare-only buffers |
| T-14-13 | Tampering | False pass from adapter-only coverage | medium | mitigate | Scan GatedBloomChain, SchroederTank32, PluginProcessor bodies |
| T-14-14 | Denial of Service | Non-finite float blow-up | medium | mitigate | peak less than 4.0f bound in stress tests |
</threat_model>

<verification>
ctest --test-dir build -R "TEST-09|allocation tokens|10k varying block stress|BlockIntegration" --output-on-failure
ctest --test-dir build --output-on-failure
</verification>

<success_criteria>
TEST-09 three-layer gate green: static scan, 10k authentic plugin stress, block finite-output tests.
</success_criteria>

<output>
Create `.planning/phases/14-block-level-integration/14-05-SUMMARY.md` when done
</output>
