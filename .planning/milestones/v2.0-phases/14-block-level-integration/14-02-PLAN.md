---
phase: 14-block-level-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - source/GatedBloomChain.h
  - tests/GatedBloomChainTest.cpp
autonomous: true
requirements:
  - INTEG-02
  - INTEG-03
must_haves:
  truths:
    - "GatedBloomChain::processBlock batches reverb+SRC while gate, send, and OD remain per-sample at host rate when authenticColor=true"
    - "GatedBloomChain::processBlock with authenticColor=false executes identical logic to the existing processSample loop (INTEG-03 parity)"
    - "Scratch buffers wetSendScratch_ and reverbScratch_ are sized only in prepare, never in processBlock"
  artifacts:
    - path: source/GatedBloomChain.h
      provides: "processBlock two-phase API with preallocated scratch"
    - path: tests/GatedBloomChainTest.cpp
      provides: "block path and v1 parity regression cases"
  key_links:
    - from: source/GatedBloomChain.h
      to: source/IReverbEngine.h
      via: reverb->processBlock on authentic path
    - from: source/GatedBloomChain.h
      to: source/PressureSend.h
      via: per-sample wetSendScratch_ fill before reverb block
---

<objective>
Add GatedBloomChain::processBlock that splits per-sample gate/send/OD from block-level reverb+SRC (INTEG-02) while preserving exact v1.0 behavior when authenticColor=false (INTEG-03).

Purpose: Wire block reverb into the wet chain without refactoring gate, send, or overdrive semantics.
Output: GatedBloomChain::processBlock, prepare scratch buffers, parity unit tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-block-level-integration/14-CONTEXT.md
@.planning/phases/14-block-level-integration/14-RESEARCH.md
@.planning/phases/14-block-level-integration/14-01-SUMMARY.md
@source/GatedBloomChain.h
@tests/GatedBloomChainTest.cpp
@tests/ChainTestHelpers.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preallocate GatedBloomChain scratch buffers in prepare</name>
  <files>source/GatedBloomChain.h</files>
  <read_first>source/GatedBloomChain.h, source/FixedRateAdapter.h</read_first>
  <action>
Add private members: int maxBlockSize_ default 0; std::vector float wetSendScratch_; std::vector float reverbScratch_.

In prepare after reverb->prepare: store maxBlockSize_ = maxBlockSize; wetSendScratch_.assign(static_cast size_t maxBlockSize, 0.0f); reverbScratch_.assign(static_cast size_t maxBlockSize, 0.0f). All resizing confined to prepare only — never in processBlock (TEST-09 / CHN-05 contract).

Add public processBlock signature: void processBlock(const float* monoIn, const float* envelope, float* wetOut, int numSamples, float rt60Seconds, float darkMix, bool authenticColor, float distnBlend, float sendGain, bool gatePreSoft, float thresholdDb) noexcept.
  </action>
  <verify>
    <automated>cmake --build build --target Tests -j8</automated>
  </verify>
  <done>Scratch buffers sized at prepare; processBlock declared.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GatedBloomChain::processBlock two-phase split</name>
  <files>source/GatedBloomChain.h</files>
  <read_first>source/GatedBloomChain.h, .planning/phases/14-block-level-integration/14-RESEARCH.md Pattern 2</read_first>
  <behavior>
    - numSamples greater than maxBlockSize_ returns early without writing
    - authenticColor false: loop calls existing processSample verbatim for each index
    - authenticColor true: per-sample pre-gate and PressureSend into wetSendScratch_; reverb->processBlock into reverbScratch_; per-sample OD and post-gate into wetOut
    - Gate order unchanged: preGate before send when gatePreSoft; postGate after OD when not gatePreSoft
  </behavior>
  <action>
Implement processBlock per RESEARCH Pattern 2.

Branch authenticColor false (INTEG-03): for i in 0..numSamples-1 set wetOut[i] = processSample(monoIn[i], envelope[i], rt60Seconds, darkMix, false, distnBlend, sendGain, gatePreSoft, thresholdDb). Copy existing processSample body logic only via delegation — do not refactor processSample internals.

Branch authenticColor true (INTEG-02): guard numSamples against maxBlockSize_. Phase 1 per-sample loop: apply preGate if gatePreSoft; wetSendScratch_[i] = PressureSend::process(wet, sendGain). Phase 2: reverb->processBlock(wetSendScratch_.data(), reverbScratch_.data(), numSamples, rt60Seconds, darkMix, true). Phase 3 per-sample: wet = overdrive.process(reverbScratch_[i], distnBlend); apply postGate if !gatePreSoft; wetOut[i] = wet.

Use block-constant rt60Seconds and darkMix passed by caller (PluginProcessor supplies block-start values per RESEARCH assumption A2). Leave processSample public and unchanged for direct callers.

Do not move gates inside reverb block. Do not allocate in processBlock body.
  </action>
  <verify>
    <automated>cmake --build build --target Tests -j8</automated>
  </verify>
  <done>Block authentic path runs gate/send per-sample then block reverb then OD/gate per-sample.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GatedBloomChain block parity and topology tests</name>
  <files>tests/GatedBloomChainTest.cpp</files>
  <read_first>tests/GatedBloomChainTest.cpp, tests/ChainTestHelpers.h</read_first>
  <behavior>
    - processBlock(authenticColor=false) matches processSample loop output within 1e-5f for 128-sample fixture
    - processBlock(authenticColor=true) with real SchroederTank32 produces finite wet output
    - Existing send release and post-gate cases still pass unchanged with authenticColor=false
  </behavior>
  <action>
Add TEST_CASE tagged [chain][GatedBloomChain][INTEG-02][processBlock]: prepare chain at 48000/512; build monoIn and envelope arrays; compare processBlock(authenticColor=false) vs per-sample loop; REQUIRE max abs diff below 1e-5f.

Add TEST_CASE tagged [chain][GatedBloomChain][INTEG-02][authentic-block]: authenticColor=true; 64-sample noise input; REQUIRE all wetOut finite.

Re-run existing GatedBloomChain tests without modification — they must pass with authenticColor=false host path (INTEG-03). Do not change test expectations for send release or post-gate timing cases.
  </action>
  <verify>
    <automated>ctest --test-dir build -R "GatedBloomChain" --output-on-failure</automated>
  </verify>
  <done>Block API proven; v1 parity cases green with authenticColor off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller → GatedBloomChain::processBlock | Untrusted numSamples and float buffer pointers |
| GatedBloomChain → IReverbEngine | Block buffers must not exceed prepared capacity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-04 | Tampering | Scratch buffer overrun | high | mitigate | maxBlockSize_ guard; early return when numSamples too large |
| T-14-05 | Tampering | Gate order regression | medium | mitigate | INTEG-03 verbatim processSample branch; existing GatedBloomChain test suite |
| T-14-06 | Denial of Service | Heap alloc on audio path | high | mitigate | prepare-only vector sizing; static scan in plan 14-05 |
</threat_model>

<verification>
ctest --test-dir build -R "GatedBloomChain" --output-on-failure
</verification>

<success_criteria>
GatedBloomChain processes reverb at block level when authenticColor on; host-rate path bit-for-bit equivalent to pre-integration processSample loop.
</success_criteria>

<output>
Create `.planning/phases/14-block-level-integration/14-02-SUMMARY.md` when done
</output>
