---
phase: 14-block-level-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 14-02
files_modified:
  - source/PluginProcessor.h
  - source/PluginProcessor.cpp
autonomous: true
requirements:
  - INTEG-02
  - INTEG-03
must_haves:
  truths:
    - "PluginProcessor calls GatedBloomChain::processBlock once per host block instead of per-sample chain.processSample"
    - "Dry tap, ParallelWetMixer, bypass crossfade, and OutputStage remain per-sample unchanged"
    - "Member scratch buffers monoScratch_, envelopeScratch_, wetScratch_ sized in prepareToPlay only"
  artifacts:
    - path: source/PluginProcessor.cpp
      provides: "block chain integration with per-sample mix stage"
    - path: source/PluginProcessor.h
      provides: "preallocated mono/envelope/wet scratch vectors"
  key_links:
    - from: source/PluginProcessor.cpp
      to: source/GatedBloomChain.h
      via: chain.processBlock per host block after monoIn/envelope arrays built
    - from: source/PluginProcessor.cpp
      to: source/ParallelWetMixer.h
      via: unchanged per-sample wet/dry mix using wetScratch_
---

<objective>
Refactor PluginProcessor::processBlock to drive GatedBloomChain at block level while preserving per-sample dry routing and output staging (INTEG-02, INTEG-03).

Purpose: Complete end-to-end block reverb path from host processBlock through the wet chain.
Output: PluginProcessor block wiring with prepare-time scratch buffers.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-block-level-integration/14-CONTEXT.md
@.planning/phases/14-block-level-integration/14-RESEARCH.md
@.planning/phases/14-block-level-integration/14-02-SUMMARY.md
@source/PluginProcessor.cpp
@source/PluginProcessor.h
@source/ParameterSnapshot.h
@source/SmoothedParameterBank.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PluginProcessor scratch buffers in prepareToPlay</name>
  <files>source/PluginProcessor.h, source/PluginProcessor.cpp</files>
  <read_first>source/PluginProcessor.cpp, source/PluginProcessor.h</read_first>
  <action>
Add private members to PluginProcessor: int preparedMaxBlock_ default 0; std::vector float monoScratch_; std::vector float envelopeScratch_; std::vector float wetScratch_; include vector header in .h if needed.

In prepareToPlay after chain.prepare: set preparedMaxBlock_ = samplesPerBlock; monoScratch_.assign(static_cast size_t samplesPerBlock, 0.0f); same for envelopeScratch_ and wetScratch_. Keep existing dryBuffer.setSize logic. All vector resizing in prepareToPlay/releaseResources only — never in processBlock.

In releaseResources optionally clear vectors to release memory off audio thread.
  </action>
  <verify>
    <automated>cmake --build build --target SendBloom_VST3 -j8</automated>
  </verify>
  <done>Processor scratch buffers sized at prepare; no per-block heap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor processBlock to batch wet chain</name>
  <files>source/PluginProcessor.cpp</files>
  <read_first>source/PluginProcessor.cpp, source/GatedBloomChain.h, .planning/phases/14-block-level-integration/14-RESEARCH.md Pattern 3</read_first>
  <behavior>
    - Per-sample loop builds monoScratch_ and envelopeScratch_ from inputStage and chain envelope
    - Block-start rt60, darkMix, authenticColor, distnBlend, sendGain, gatePreSoft, thresholdDb captured from first sample smoothed values
    - chain.processBlock fills wetScratch_ for full numSamples
    - Second per-sample loop applies ParallelWetMixer, bypass, OutputStage unchanged from current code
    - numSamples greater than preparedMaxBlock_ skips chain call safely (dry pass-through or early return matching existing dryBuffer guard pattern)
  </behavior>
  <action>
Restructure processBlock per RESEARCH Pattern 3.

Keep unchanged: MIDI CC1 handling, ScopedNoDenormals, dryBuffer copy at block start, snap/smoothedBank setup, extendedStereo branch structure.

Replace inner wet computation: First loop sample 0..numSamples-1 — compute smoothed params per sample as today; for index 0 also store blockStartRt60, blockStartDark, blockStartAuthentic (bool from getNextAuthenticColorTarget greater than 0.5f at sample 0 — advance smoother for samples 1..n-1 normally), blockStartDistn, blockStartSend, blockStartGatePreSoft, blockStartThresholdDb. Build monoScratch_[sample] via inputStage.processSample on mono sum from dryBuffer; envelopeScratch_[sample] = chain.getEnvelope().process(abs(monoScratch_[sample])).

After first loop: if numSamples less than or equal preparedMaxBlock_, call chain.processBlock(monoScratch_.data(), envelopeScratch_.data(), wetScratch_.data(), numSamples, blockStartRt60, blockStartDark, blockStartAuthentic, blockStartDistn, blockStartSend, blockStartGatePreSoft, blockStartThresholdDb).

Second loop sample 0..numSamples-1 — read wet from wetScratch_[sample]; apply ParallelWetMixer, bypass crossfade, OutputStage exactly as current per-channel branches. Do not alter dry tap source (still dryBuffer pre-gain copy).

Remove direct chain.processSample call from processBlock. Do not change ParameterIDs, UI, or createParameterLayout (INTEG-04).
  </action>
  <verify>
    <automated>ctest --test-dir build -R "GatedBloomChain|DryPath|PostGate|ReleaseTruth" --output-on-failure</automated>
  </verify>
  <done>Plugin drives block wet chain; dry/mix path unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Integration smoke via existing stress harness</name>
  <files>source/PluginProcessor.cpp</files>
  <action>
Run full existing RealtimeStressTest cases with authenticColor at default 0 to confirm no regression from wiring change. Fix any failures caused by scratch sizing or block-start parameter capture without altering DSP semantics on host-rate path.

Document in SUMMARY: block-start authenticColor branching until Phase 16 crossfade (RESEARCH assumption A2).
  </action>
  <verify>
    <automated>ctest --test-dir build -R "10k varying block stress" --output-on-failure</automated>
  </verify>
  <done>10k host-rate stress test passes after processor refactor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host DAW → PluginProcessor::processBlock | Variable block sizes and channel counts |
| PluginProcessor → GatedBloomChain | Scratch arrays must match prepared capacity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-07 | Tampering | wetScratch_ overrun | high | mitigate | preparedMaxBlock_ guard before chain.processBlock |
| T-14-08 | Tampering | Dry path regression | medium | mitigate | Unchanged dryBuffer copy and mix loop; DryPathIntegrityTest |
| T-14-09 | Denial of Service | Per-block vector allocation | high | mitigate | prepareToPlay-only assign; TEST-09 in plan 14-05 |
</threat_model>

<verification>
ctest --test-dir build -R "10k varying block stress|DryPath|GatedBloomChain" --output-on-failure
</verification>

<success_criteria>
PluginProcessor uses block wet chain; v1 per-sample dry/mix/output behavior preserved when authenticColor off.
</success_criteria>

<output>
Create `.planning/phases/14-block-level-integration/14-03-SUMMARY.md` when done
</output>
