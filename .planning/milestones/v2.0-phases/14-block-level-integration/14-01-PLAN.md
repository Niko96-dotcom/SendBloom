---
phase: 14-block-level-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - source/IReverbEngine.h
  - source/SchroederTank32.h
  - tests/SchroederTank32BlockTest.cpp
autonomous: true
requirements:
  - INTEG-01
must_haves:
  truths:
    - "IReverbEngine exposes processBlock with a default per-sample fallback so Fdn8Reverb and HostRateReverbEngine need no block overrides"
    - "SchroederTank32::processBlock routes authenticColor=true to FixedRateAdapter with Authentic32Mode::ProperSRC in production"
    - "SchroederTank32::processBlock with authenticColor=false matches looping processSample sample-for-sample"
    - "setAuthentic32ModeForDiagnostics exposes Authentic32Mode for Phase 15 without APVTS wiring"
  artifacts:
    - path: source/IReverbEngine.h
      provides: "processBlock virtual with default loop (INTEG-01)"
    - path: source/SchroederTank32.h
      provides: "FixedRateAdapter member, processBlock override, diagnostics setter"
    - path: tests/SchroederTank32BlockTest.cpp
      provides: "processBlock routing unit tests"
  key_links:
    - from: source/SchroederTank32.h
      to: source/FixedRateAdapter.h
      via: fixedRate_.processBlock when authenticColor true
    - from: source/SchroederTank32.h
      to: source/HostRateReverbEngine.h
      via: hostEngine.processSample when authenticColor false
---

<objective>
Extend IReverbEngine with block-level API and wire SchroederTank32 to FixedRateAdapter for the ProperSRC authentic path (INTEG-01).

Purpose: Establish the polymorphic block reverb interface before GatedBloomChain and PluginProcessor integration.
Output: IReverbEngine::processBlock, SchroederTank32 block delegation, diagnostics mode setter, unit tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-block-level-integration/14-CONTEXT.md
@.planning/phases/14-block-level-integration/14-RESEARCH.md
@source/IReverbEngine.h
@source/SchroederTank32.h
@source/FixedRateAdapter.h
@source/Authentic32Mode.h
@source/HostRateReverbEngine.h
@tests/SchroederTank32Test.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add IReverbEngine::processBlock default implementation</name>
  <files>source/IReverbEngine.h</files>
  <read_first>source/IReverbEngine.h, source/HostRateReverbEngine.h, source/Fdn8Reverb.h</read_first>
  <action>
Add virtual void processBlock (const float* input, float* output, int numSamples, float rt60Seconds, float darkMix, bool authenticColor) noexcept with inline default body that loops numSamples and assigns output[i] = processSample(input[i], rt60Seconds, darkMix, authenticColor). Keep existing prepare and processSample pure virtual. Do not change processSample signatures. Fdn8Reverb and HostRateReverbEngine inherit default block behavior without edits.
  </action>
  <verify>
    <automated>cmake --build build --target Tests -j8</automated>
  </verify>
  <done>IReverbEngine compiles; existing engine implementations build without modification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SchroederTank32 FixedRateAdapter embedding and processBlock override</name>
  <files>source/SchroederTank32.h</files>
  <read_first>source/FixedRateAdapter.h, source/SchroederTank32.h, .planning/phases/14-block-level-integration/14-RESEARCH.md</read_first>
  <behavior>
    - prepare calls fixedRate_.prepare(hostRate, maxBlockSize) alongside hostEngine.prepare
    - processBlock with authenticColor=false delegates to IReverbEngine default loop (host-rate parity)
    - processBlock with authenticColor=true calls fixedRate_.processBlock with mode ProperSRC unless diagnostics override set
    - setAuthentic32ModeForDiagnostics(Authentic32Mode) stores optional override for tests only
    - processSample(authenticColor=false) unchanged host path; processSample(authenticColor=true) may delegate single-sample to fixedRate_ for API safety but production uses block path
  </behavior>
  <action>
Include FixedRateAdapter.h in SchroederTank32.h. Add FixedRateAdapter fixedRate_ member and std::optional Authentic32Mode diagnosticsMode_ member (default nullopt).

In prepare: after hostEngine.prepare, call fixedRate_.prepare(sampleRate, maxBlockSize).

Override processBlock: if !authenticColor call IReverbEngine::processBlock (host loop). If authenticColor, resolve mode as diagnosticsMode_.value_or(Authentic32Mode::ProperSRC) and call fixedRate_.processBlock(input, output, numSamples, rt60Seconds, darkMix, mode). Guard early return when numSamples exceeds prepared maxBlockSize mirroring FixedRateAdapter.

Add public void setAuthentic32ModeForDiagnostics(Authentic32Mode mode) noexcept setting diagnosticsMode_ = mode and void clearAuthentic32ModeForDiagnostics() noexcept resetting nullopt.

Update processSample: when authenticColor true, route through fixedRate_.processBlock with n=1 instead of processAuthentic accumulator (accumulator code may remain private for LegacyAccumulator diagnostics via adapter only — do not call processAuthentic from production paths). When authenticColor false, keep existing hostEngine delegation.

Do not add APVTS parameters or UI changes (INTEG-04 boundary).
  </action>
  <verify>
    <automated>cmake --build build --target Tests -j8</automated>
  </verify>
  <done>SchroederTank32 prepares and processes block authentic path via FixedRateAdapter ProperSRC.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SchroederTank32 processBlock unit tests</name>
  <files>tests/SchroederTank32BlockTest.cpp</files>
  <read_first>tests/SchroederTank32Test.cpp, tests/ReverbTestHelpers.h, source/SchroederTank32.h</read_first>
  <behavior>
    - Host path: processBlock(authenticColor=false) output matches per-sample loop within 1e-5f max abs diff for 256-sample impulse
    - Authentic path: processBlock(authenticColor=true) produces finite samples with peak below 4.0f
    - Diagnostics setter: setAuthentic32ModeForDiagnostics(LegacyAccumulator) changes block output vs ProperSRC for same input block
  </behavior>
  <action>
Create tests/SchroederTank32BlockTest.cpp with Catch2 cases tagged [verb][SchroederTank32][INTEG-01][processBlock].

Case 1 — host parity: prepare at 48000 Hz maxBlock 512; render 256-sample impulse via processBlock(authenticColor=false) vs sample loop; REQUIRE maxAbsDiff below 1e-5f using sendbloom::test::reverb helpers if available.

Case 2 — ProperSRC finite: same prepare; processBlock authenticColor=true with noise input; all outputs finite and peak less than 4.0f.

Case 3 — diagnostics mode: compare ProperSRC vs LegacyAccumulator block outputs with setAuthentic32ModeForDiagnostics; REQUIRE maxAbsDiff above 1e-4f proving distinct paths.

No heap allocation assertions in this file (deferred to plan 14-05 TEST-09).
  </action>
  <verify>
    <automated>ctest --test-dir build -R "SchroederTank32.*processBlock|INTEG-01" --output-on-failure</automated>
  </verify>
  <done>SchroederTank32 block routing proven by automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture → SchroederTank32 | Offline float buffers cross into reverb DSP |
| FixedRateAdapter → r8brain FIFO | Internal scratch buffers sized at prepare only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | SchroederTank32::processBlock buffer bounds | high | mitigate | Early return when numSamples exceeds maxBlockSize prepared |
| T-14-02 | Denial of Service | Non-finite DSP output | medium | mitigate | jlimit on host path; stress tests in plan 14-05 |
| T-14-03 | Information Disclosure | Diagnostics mode in production | low | accept | Override ignored unless test setter called; APVTS forces ProperSRC at chain level |
</threat_model>

<verification>
ctest --test-dir build -R "SchroederTank32.*processBlock|INTEG-01" --output-on-failure
</verification>

<success_criteria>
IReverbEngine::processBlock exists; SchroederTank32 routes authentic block path through FixedRateAdapter ProperSRC; host-rate block path matches sample loop.
</success_criteria>

<output>
Create `.planning/phases/14-block-level-integration/14-01-SUMMARY.md` when done
</output>
