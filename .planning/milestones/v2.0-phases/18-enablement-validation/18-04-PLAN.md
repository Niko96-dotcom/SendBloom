---
phase: 18-enablement-validation
plan: 04
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - source/SchroederTank32.h
  - source/LegacyAccumulatorPath.h
  - source/SchroederTank32DelayTable.h
autonomous: true
requirements:
  - ENAB-03
must_haves:
  truths:
    - "SchroederTank32 production facade has no dead anti-image SVF or processAuthentic accumulator path"
    - "LegacyAccumulatorPath retains SVF and processAuthentic for SRC-06 and three-path diagnostics"
    - "HF ringing and 14825 Hz imaging regression tests remain green after demotion"
  artifacts:
    - path: source/SchroederTank32.h
      provides: "Production tank facade without dead authentic accumulator code"
  key_links:
    - from: source/SchroederTank32.h
      to: source/FixedRateAdapter.h
      via: authentic_color routes through fixedRate_.processBlock only
    - from: source/LegacyAccumulatorPath.h
      to: tests/FixedRateAdapterTest.cpp
      via: LegacyAccumulator A/B regression unchanged
---

<objective>
Demote ENAB-03 dead anti-image SVF and accumulator code from SchroederTank32 production facade while preserving legacy diagnostics path.

Purpose: processAuthentic and antiImageFilter in SchroederTank32.h are never called — production uses FixedRateAdapter ProperSRC; dead code confuses VERB-05 and future maintainers.
Output: Cleaned SchroederTank32.h, annotated LegacyAccumulatorPath.h, updated delay table comment.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-enablement-validation/18-RESEARCH.md
@source/SchroederTank32.h
@source/LegacyAccumulatorPath.h
@source/FixedRateAdapter.h
@source/SchroederTank32DelayTable.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead authentic accumulator from SchroederTank32</name>
  <files>source/SchroederTank32.h</files>
  <action>
From source/SchroederTank32.h remove all dead authentic accumulator machinery per 18-RESEARCH.md demotion table row 1:

Remove members and methods: antiImageFilter, processAuthentic(), inputAccumulator, outputHold, lastInternalOut.

Remove prepare() setup for antiImageFilter and accumulator state reset lines.

Verify no remaining references to removed symbols in SchroederTank32.h.

Do NOT remove FixedRateAdapter, diagnosticsMode_, or ProperSRC routing — production path unchanged.

Do NOT touch LegacyAccumulatorPath.h in this task.
  </action>
  <verify>
    <automated>cd build && cmake --build . --config Release --target SharedCode -j8 2>&1 | tail -5</automated>
  </verify>
  <done>SchroederTank32 compiles without dead SVF/accumulator; ProperSRC path intact.</done>
</task>

<task type="auto">
  <name>Task 2: Legacy path comments and delay table annotation</name>
  <files>source/LegacyAccumulatorPath.h, source/SchroederTank32DelayTable.h</files>
  <action>
Add header comment to source/LegacyAccumulatorPath.h: legacy accumulator + anti-image SVF used only for Authentic32Mode::LegacyAccumulator diagnostics and SRC-06 A/B — not production ProperSRC path.

In source/SchroederTank32DelayTable.h update kAuthenticAntiImageLpHz comment to indicate LegacyAccumulatorPath-only usage.

Do not remove SVF from LegacyAccumulatorPath — SRC-06 and AuthenticPathDiagnostics depend on it.
  </action>
  <verify>
    <automated>grep -c 'LegacyAccumulator' source/LegacyAccumulatorPath.h | grep -qv '^0$' && grep -c 'kAuthenticAntiImageLpHz' source/SchroederTank32DelayTable.h | grep -qv '^0$'</automated>
  </verify>
  <done>Legacy path clearly scoped to diagnostics; delay constant annotated.</done>
</task>

<task type="auto">
  <name>Task 3: HF and legacy regression verification</name>
  <files>source/SchroederTank32.h</files>
  <action>
Run post-demotion regression filters per 18-RESEARCH.md:
- ctest --test-dir build -C Release -R "LegacyAccumulator|14825 Hz imaging|HF ringing.*regression|ReleaseTruth"

Rebuild Release target SendBloom_VST3 after source change.

All listed tests must pass — especially SRC-06 imaging comparison and TEST-11 HF ringing suite.
  </action>
  <verify>
    <automated>ctest --test-dir build -C Release -R "LegacyAccumulator|14825 Hz imaging|HF ringing.*regression"</automated>
  </verify>
  <done>HF gates and legacy A/B tests green after production SVF demotion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source/SchroederTank32.h | Production DSP facade |
| source/LegacyAccumulatorPath.h | Diagnostics-only legacy tier |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-06 | Tampering | SchroederTank32.h demotion | medium | mitigate | HF ringing + 14825 Hz imaging ctest gates after removal |
| T-18-07 | Denial of service | Breaking legacy diagnostics | medium | mitigate | Keep LegacyAccumulatorPath intact; run LegacyAccumulator ctest filter |
</threat_model>

<verification>
SchroederTank32 has no antiImageFilter/processAuthentic; LegacyAccumulatorPath unchanged functionally; HF regression green.
</verification>

<success_criteria>
ENAB-03 complete — dead production SVF removed; legacy diagnostics path preserved and tested.
</success_criteria>

<output>
Create `.planning/phases/18-enablement-validation/18-04-SUMMARY.md` when done
</output>
