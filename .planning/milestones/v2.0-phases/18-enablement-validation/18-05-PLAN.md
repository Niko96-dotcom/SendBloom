---
phase: 18-enablement-validation
plan: 05
type: execute
wave: 3
depends_on:
  - 18-01
  - 18-02
  - 18-03
  - 18-04
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - TEST-12
must_haves:
  truths:
    - "Full Catch2 suite passes on Release build after Phase 18 changes"
    - "pluginval strictness level 10 passes on Release VST3 artifact"
    - "Legal metadata audit passes including r8brain MIT assertions"
    - "TEST-12, TEST-13, ENAB-01, ENAB-02, ENAB-03 marked complete in REQUIREMENTS.md"
  artifacts:
    - path: build/SendBloom_artefacts/Release/VST3/SendBloom.vst3
      provides: "Release binary validated by pluginval 10"
  key_links:
    - from: pluginval CLI
      to: build/SendBloom_artefacts/Release/VST3/SendBloom.vst3
      via: --strictness-level 10 --validate
    - from: .github/workflows/build_and_test.yml
      to: pluginval v1.0.3
      via: CI matrix matches local strictness 10 invocation
---

<objective>
Close TEST-12 with pluginval strictness 10 on Release VST3 and finalize Phase 18 requirement checkboxes after full regression.

Purpose: Local Debug pluginval pass exists but TEST-12 requires Release artifact validation post v2.0 SRC/latency integration; phase closure updates REQUIREMENTS traceability.
Output: Green full ctest + pluginval 10 on Release VST3; REQUIREMENTS.md Phase 18 items checked complete.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-enablement-validation/18-RESEARCH.md
@.github/workflows/build_and_test.yml
@docs/RELEASE_CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Release build and full test suite</name>
  <files>build/SendBloom_artefacts/Release/VST3/SendBloom.vst3</files>
  <action>
Configure and build Release explicitly:
cmake -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build --config Release --target SendBloom_VST3 -j8

Run full Catch2 suite: ctest --test-dir build -C Release --output-on-failure

Run ENAB-01 composite: bash scripts/enab-acceptance-gates.sh

Run legal audit: bash scripts/check-legal-metadata.sh

All must exit 0 before pluginval step.
  </action>
  <verify>
    <automated>test -d build/SendBloom_artefacts/Release/VST3/SendBloom.vst3 && ctest --test-dir build -C Release --output-on-failure && bash scripts/enab-acceptance-gates.sh && bash scripts/check-legal-metadata.sh</automated>
  </verify>
  <done>Release VST3 artifact exists; full ctest, gates, and legal audit green.</done>
</task>

<task type="auto">
  <name>Task 2: pluginval strictness 10 on Release VST3</name>
  <files>build/SendBloom_artefacts/Release/VST3/SendBloom.vst3</files>
  <action>
Run pluginval v1.0.3 strictness 10 on Release VST3 per CI canonical invocation from 18-RESEARCH.md:

If pluginval not on PATH, download from https://github.com/Tracktion/pluginval/releases/download/v1.0.3/pluginval_macOS.zip (or platform equivalent) and extract.

Command:
pluginval --strictness-level 10 --verbose --validate "build/SendBloom_artefacts/Release/VST3/SendBloom.vst3"

Must complete without validation failures. Authentic Color parameter fuzz (param 11) is exercised at level 10 — non-zero SRC latency with Policy A is expected and must pass.

Do not use Debug artifact path for TEST-12 sign-off.
  </action>
  <verify>
    <automated>pluginval --strictness-level 10 --validate "build/SendBloom_artefacts/Release/VST3/SendBloom.vst3" 2>&1 | grep -qi 'passed\|success\|All tests passed' || pluginval --strictness-level 10 --validate "build/SendBloom_artefacts/Release/VST3/SendBloom.vst3"</automated>
  </verify>
  <done>pluginval level 10 passes on Release VST3 — TEST-12 validated locally.</done>
</task>

<task type="auto">
  <name>Task 3: Mark Phase 18 requirements complete</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
In .planning/REQUIREMENTS.md mark complete (change [ ] to [x]) for:
- TEST-12: pluginval strictness 10 passes after integration
- TEST-13: Legal metadata audit passes with r8brain MIT license cited
- ENAB-01: 32k Color may be enabled by default only after TEST-11, DIAG-04, LAT-02, and XFADE-01 pass
- ENAB-02: VERB-05 documentation updated to describe ProperSRC bandlimited bridge
- ENAB-03: Anti-image SVF demoted or removed after ProperSRC passes HF acceptance gates

Update traceability table rows for TEST-12 through ENAB-03 status from Pending to Complete.

Do NOT change authentic_color APVTS default or factory preset XML values — ENAB-01 grants permission only; default stays off per CONTEXT.md.
  </action>
  <verify>
    <automated>grep -c '\[x\] \*\*TEST-12\*\*' .planning/REQUIREMENTS.md | grep -qv '^0$' && grep -c '\[x\] \*\*ENAB-03\*\*' .planning/REQUIREMENTS.md | grep -qv '^0$'</automated>
  </verify>
  <done>All five Phase 18 requirement IDs checked complete in REQUIREMENTS.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pluginval → VST3 binary | External host compatibility validator on built artifact |
| CI matrix | Cross-platform TEST-12 confirmation on push |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-08 | Tampering | VST3 binary under test | medium | mitigate | Build from clean Release target; pluginval strictness 10 |
| T-18-09 | Spoofing | Wrong pluginval version | low | mitigate | Pin v1.0.3 matching CI workflow |
| T-18-SC | Tampering | pluginval download | high | mitigate | Official GitHub releases URL only; verify v1.0.3 tag |
</threat_model>

<verification>
Full ctest green; pluginval 10 on Release VST3; REQUIREMENTS Phase 18 items marked complete.
</verification>

<success_criteria>
TEST-12 closed with Release pluginval 10; Phase 18 ship-readiness requirements traceable as complete.
</success_criteria>

<output>
Create `.planning/phases/18-enablement-validation/18-05-SUMMARY.md` when done
</output>
