---
phase: 18-enablement-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/enab-acceptance-gates.sh
autonomous: true
requirements:
  - ENAB-01
must_haves:
  truths:
    - "Composite acceptance gate script runs TEST-11, DIAG-04, LAT-02, and XFADE-01 regression filters green in one command"
    - "ADR-003 policy file exists before any enablement claims"
    - "RC1 safety invariants remain true — authentic_color default off and all factory presets at 0"
    - "ENAB-01 permission is verified, not exercised — no default-on or preset flip"
  artifacts:
    - path: scripts/enab-acceptance-gates.sh
      provides: "Fail-fast composite gate runner for Phase 18 entry"
  key_links:
    - from: scripts/enab-acceptance-gates.sh
      to: docs/architecture/ADR-003-proper-32k-src.md
      via: test -f existence check after ctest gate filters
    - from: scripts/enab-acceptance-gates.sh
      to: build/
      via: ctest -C Release -R gate regex filters
---

<objective>
Verify ENAB-01 acceptance prerequisites (TEST-11, DIAG-04, LAT-02, XFADE-01) via an automated composite gate script and RC1 safety invariant checks.

Purpose: Phase 18 enablement claims require proof that upstream HF, invariance, PDC, and crossfade gates are green before doc truth or dead-code cleanup proceeds.
Output: scripts/enab-acceptance-gates.sh executable in CI and locally; gate run evidence for ENAB-01.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-enablement-validation/18-CONTEXT.md
@.planning/phases/18-enablement-validation/18-RESEARCH.md
@docs/architecture/ADR-003-proper-32k-src.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Composite acceptance gate script</name>
  <files>scripts/enab-acceptance-gates.sh</files>
  <read_first>.planning/phases/18-enablement-validation/18-RESEARCH.md (Acceptance Gate Checklist section)</read_first>
  <action>
Create scripts/enab-acceptance-gates.sh as an executable bash script (chmod +x).

Script behavior per 18-RESEARCH.md composite gate:
- set -euo pipefail
- ROOT resolves repo root from script location; BUILD_DIR defaults to $ROOT/build (honor BUILD_DIR env override)
- Run ctest --test-dir "$BUILD" -C Release --output-on-failure -R with regex: HF ringing.*regression|ProperSRC HF metrics invariant|EngineCrossfade|processBlock survives 1000 authentic_color|Plugin reports SRC latency|14825 Hz imaging vs LegacyAccumulator
- Assert docs/architecture/ADR-003-proper-32k-src.md exists (test -f)
- Print success line: ENAB-01 acceptance gates: PASS

Do not flip authentic_color default or factory presets — verification only per CONTEXT.md deferred default-on.
  </action>
  <verify>
    <automated>test -x scripts/enab-acceptance-gates.sh && grep -c 'ENAB-01 acceptance gates' scripts/enab-acceptance-gates.sh | grep -qv '^0$'</automated>
  </verify>
  <done>Script exists, executable, and encodes all four upstream gate test filters plus ADR-003 file check.</done>
</task>

<task type="auto">
  <name>Task 2: Run gates and RC1 safety invariants</name>
  <files>scripts/enab-acceptance-gates.sh</files>
  <action>
Ensure Release build exists: cmake -B build -DCMAKE_BUILD_TYPE=Release if build/ missing; cmake --build build --config Release.

Run bash scripts/enab-acceptance-gates.sh — must exit 0.

Run RC1 safety invariant ctest filters separately (not in composite script):
- ctest --test-dir build -C Release -R "INTEG-04.*defaults off"
- ctest --test-dir build -C Release -R "factory presets recall authentic_color off"

If any gate fails, stop and report failing test name — do not proceed to doc or demotion work in later plans.
  </action>
  <verify>
    <automated>bash scripts/enab-acceptance-gates.sh && ctest --test-dir build -C Release -R "INTEG-04.*defaults off" && ctest --test-dir build -C Release -R "factory presets recall authentic_color off"</automated>
  </verify>
  <done>Composite gates and RC1 invariants pass on local Release build; ENAB-01 prerequisites verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/ctest | Local test harness executes compiled acceptance binaries |
| scripts/ | Gate script invoked by humans and CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Tampering | scripts/enab-acceptance-gates.sh | medium | mitigate | Script uses fixed ctest regex from RESEARCH; verify task runs script before downstream plans |
| T-18-02 | Repudiation | ENAB-01 gate evidence | low | accept | Local ctest output sufficient; CI re-runs on merge |
</threat_model>

<verification>
bash scripts/enab-acceptance-gates.sh exits 0 on Release build; RC1 invariant tests pass.
</verification>

<success_criteria>
ENAB-01 acceptance gates automated and green; RC1 default-off invariants confirmed; no authentic_color default flip.
</success_criteria>

<output>
Create `.planning/phases/18-enablement-validation/18-01-SUMMARY.md` when done
</output>
