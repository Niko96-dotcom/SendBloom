---
phase: 18-enablement-validation
plan: 03
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - README.md
  - docs/RELEASE_CHECKLIST.md
  - .planning/REQUIREMENTS.md
  - tests/ReleaseTruthTest.cpp
autonomous: true
requirements:
  - ENAB-02
must_haves:
  truths:
    - "README describes ProperSRC bandlimited host ↔ 32,768 Hz bridge when 32k Color on"
    - "RELEASE_CHECKLIST no longer claims ProperSRC is not shipped or describes accumulator stepping as production"
    - "REQUIREMENTS VERB-05 text matches ProperSRC reality"
    - "ReleaseTruthTest asserts FixedRateAdapter/ProperSRC in production path and processAuthentic only in LegacyAccumulatorPath.h"
  artifacts:
    - path: tests/ReleaseTruthTest.cpp
      provides: "Static doc truth enforcement for VERB-05"
  key_links:
    - from: tests/ReleaseTruthTest.cpp
      to: source/SchroederTank32.h
      via: grep FixedRateAdapter and ProperSRC in production tank source
    - from: tests/ReleaseTruthTest.cpp
      to: source/LegacyAccumulatorPath.h
      via: grep processAuthentic in legacy diagnostics tier only
---

<objective>
Update ENAB-02 VERB-05 documentation across all user-facing surfaces to describe ProperSRC bandlimited bridge instead of accumulator stepping.

Purpose: Stale docs still describe processAuthentic accumulator + anti-image SVF as production; code routes authentic_color through FixedRateAdapter/ProperSRC since Phase 14.
Output: Honest README, RELEASE_CHECKLIST, REQUIREMENTS VERB-05, and ReleaseTruthTest assertions.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-enablement-validation/18-RESEARCH.md
@docs/architecture/ADR-003-proper-32k-src.md
@README.md
@docs/RELEASE_CHECKLIST.md
@tests/ReleaseTruthTest.cpp
@source/SchroederTank32.h
@source/LegacyAccumulatorPath.h
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ReleaseTruthTest ProperSRC assertions</name>
  <files>tests/ReleaseTruthTest.cpp</files>
  <behavior>
    - tankSource (SchroederTank32.h) contains FixedRateAdapter and ProperSRC
    - tankSource does not require processAuthentic in production facade
    - legacySource (LegacyAccumulatorPath.h) contains processAuthentic
    - Existing firmware/EEPROM bans in README unchanged
  </behavior>
  <action>
Update TEST_CASE "32k Color docs describe software model not firmware claims" in tests/ReleaseTruthTest.cpp per 18-RESEARCH.md ReleaseTruthTest migration.

Read legacySource from source/LegacyAccumulatorPath.h in addition to tankSource from SchroederTank32.h.

Replace REQUIRE tankSource.find("processAuthentic") with:
- REQUIRE tankSource.find("FixedRateAdapter") != npos
- REQUIRE tankSource.find("ProperSRC") != npos (or Authentic32Mode::ProperSRC string fragment)
- REQUIRE legacySource.find("processAuthentic") != npos

Keep existing README/checklist firmware-derived and EEPROM/bytecode bans.

Run test first if RED — then proceed to doc updates in task 2.
  </action>
  <verify>
    <automated>cd build && cmake --build . --target Tests -j8 && ./Tests "[release][verb][authentic]" -r compact</automated>
  </verify>
  <done>ReleaseTruthTest encodes production ProperSRC vs legacy accumulator split.</done>
</task>

<task type="auto">
  <name>Task 2: README and RELEASE_CHECKLIST VERB-05 truth</name>
  <files>README.md, docs/RELEASE_CHECKLIST.md</files>
  <action>
Update README.md 32k Color bullet (currently ~L14): replace accumulator stepping and "ProperSRC enablement awaits Phases 13–18" with canonical ADR-003 description:
- When 32k Color on: bandlimited hostRate ↔ 32,768 Hz via r8brain ProperSRC (FixedRateAdapter); tank at fixed delay table with per-comb calibration, damping, optional 9-bit quantization
- RC1: 32k Color remains off by default; host-rate tank is production default
- Original software, not firmware-derived
- Legacy accumulator exists only for diagnostics regression (LegacyAccumulatorPath)

Rewrite docs/RELEASE_CHECKLIST.md §32k Color Truth (VERB-05):
- ProperSRC sandwich per ADR-003 when enabled
- Remove processAuthentic accumulator and kAuthenticAntiImageLpHz as production path
- Note legacy accumulator + SVF only under Authentic32Mode::LegacyAccumulator for A/B

Update RC1 Safety Freeze subsection:
- Remove "ProperSRC is not shipped in RC1"
- Replace experimental disclaimer with: ProperSRC validated; 32k Color remains off by default until product explicitly enables default-on
- Add ENAB-01 gate command: bash scripts/enab-acceptance-gates.sh
- Retain preset/default-off invariants
  </action>
  <verify>
    <automated>grep -qi ProperSRC README.md && grep -qi ProperSRC docs/RELEASE_CHECKLIST.md && ! grep -q 'not shipped' docs/RELEASE_CHECKLIST.md</automated>
  </verify>
  <done>README and RELEASE_CHECKLIST describe ProperSRC production path; stale RC1 "not shipped" removed.</done>
</task>

<task type="auto">
  <name>Task 3: REQUIREMENTS VERB-05 text and full ReleaseTruth pass</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Update VERB-05 requirement description in .planning/REQUIREMENTS.md from accumulator stepping to:
"authentic_color (32k Color) uses bandlimited hostRate ↔ 32,768 Hz SRC via r8brain ProperSRC; SchroederTankCore runs at fixed delay-table lengths with per-comb feedback calibration, damping, and optional 9-bit param quantization — original software, not firmware-derived. Legacy accumulator path retained for diagnostics only."

Run full ReleaseTruth test suite: ctest --test-dir build -C Release -R "ReleaseTruth|32k Color docs describe"

All ReleaseTruth cases must pass including updated VERB-05 case.
  </action>
  <verify>
    <automated>grep -qi ProperSRC .planning/REQUIREMENTS.md && ctest --test-dir build -C Release -R "32k Color docs describe"</automated>
  </verify>
  <done>REQUIREMENTS VERB-05 text honest; ReleaseTruth VERB-05 test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/ + README | User-facing DSP claims |
| tests/ReleaseTruthTest.cpp | Static enforcement of doc truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-05 | Information disclosure | Stale VERB-05 marketing | medium | mitigate | ReleaseTruthTest + four-surface doc update in one plan |
</threat_model>

<verification>
ctest -R "32k Color docs describe" passes; README and RELEASE_CHECKLIST cite ProperSRC; REQUIREMENTS VERB-05 updated.
</verification>

<success_criteria>
ENAB-02 complete — all VERB-05 surfaces describe ProperSRC bandlimited bridge, not accumulator stepping.
</success_criteria>

<output>
Create `.planning/phases/18-enablement-validation/18-03-SUMMARY.md` when done
</output>
