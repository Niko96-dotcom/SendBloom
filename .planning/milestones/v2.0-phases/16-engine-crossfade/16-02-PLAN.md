---
phase: 16-engine-crossfade
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - tests/RealtimeStressTest.cpp
  - tests/IntegrationAllocScanTest.cpp
autonomous: true
requirements:
  - XFADE-02
must_haves:
  truths:
    - "1000 explicit authentic_color toggles with rotating block sizes produce finite output"
    - "Peak sample magnitude stays below 4.0f across 1000-toggle stress"
    - "Per-block max adjacent delta stays below 1.0f integration click gate"
    - "EngineCrossfade and SchroederTank32 processBlock bodies pass static heap-allocation scan"
    - "Blocks larger than prepared maxBlockSize do not overrun scratch during crossfade"
  artifacts:
    - path: tests/RealtimeStressTest.cpp
      provides: "processBlock survives 1000 authentic_color toggles [XFADE-02] case"
    - path: tests/IntegrationAllocScanTest.cpp
      provides: "EngineCrossfade.h in alloc scan source list"
  key_links:
    - from: tests/RealtimeStressTest.cpp
      to: source/PluginProcessor.cpp
      via: full plugin processBlock under worst-case toggle cadence
    - from: tests/IntegrationAllocScanTest.cpp
      to: source/EngineCrossfade.h
      via: static token scan of mixWetBlock / processBlock bodies
---

<objective>
Gate engine crossfade with 1000-toggle realtime stress and allocation regression coverage (XFADE-02).

Purpose: Prove rapid authentic_color toggling is safe under variable host block sizes — no NaN/Inf, heap alloc, or buffer overrun.
Output: Extended RealtimeStressTest case, IntegrationAllocScanTest path update.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-engine-crossfade/16-CONTEXT.md
@.planning/phases/16-engine-crossfade/16-RESEARCH.md
@tests/RealtimeStressTest.cpp
@tests/IntegrationAllocScanTest.cpp
@tests/FixedRateAdapterTest.cpp
@.planning/phases/16-engine-crossfade/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend IntegrationAllocScanTest for crossfade sources</name>
  <files>tests/IntegrationAllocScanTest.cpp</files>
  <read_first>tests/IntegrationAllocScanTest.cpp, source/EngineCrossfade.h, source/SchroederTank32.h</read_first>
  <action>
Update kSources array in TEST_CASE "integrated processBlock bodies have no heap allocation tokens" to include source/EngineCrossfade.h.

Keep existing entries (GatedBloomChain.h, SchroederTank32.h, PluginProcessor.cpp). Final array length 4.

For EngineCrossfade.h use existing extractHeaderProcessBlockBody path — ensure mixWetBlock body is discoverable (if helper uses different function name, add extract logic or rename to processBlock only if consistent with scan utility; prefer extending extractHeaderProcessBlockBody to also match mixWetBlock symbol per FixedRateAdapterTest precedent).

Verify scan covers new SchroederTank32 dual-engine crossfade branch added in 16-01 (already listed — no removal).

Do not weaken requireNoAllocTokens checks.
  </action>
  <verify>
    <automated>cd build && ./Tests "integrated processBlock bodies have no heap allocation tokens" -r compact</automated>
  </verify>
  <done>Static alloc scan passes for EngineCrossfade.h and updated SchroederTank32 crossfade branch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 1000-toggle RealtimeStressTest case</name>
  <files>tests/RealtimeStressTest.cpp</files>
  <read_first>tests/RealtimeStressTest.cpp, .planning/phases/16-engine-crossfade/16-RESEARCH.md</read_first>
  <behavior>
    - Exactly 1000 authentic_color toggles (not 2000 blocks with sparse toggles)
    - Toggle every host block (worst case) alternating 0.0f and 1.0f on APVTS raw authenticColor
    - Rotate kBlockSizes each block: {32,64,128,256,512,1024,256,128}
    - Every output sample is finite; global peak below 4.0f
    - Per-block maxAdjacentDelta below 1.0f on mono sum or channel 0
    - REQUIRE_NOTHROW on every processBlock call
  </behavior>
  <action>
Add new TEST_CASE "processBlock survives 1000 authentic_color toggles" with tags [realtime][integration][stress][XFADE-02].

Setup mirrors existing stress cases: PluginProcessor prepared 48000 Hz maxBlock 1024; inputGain 1, outputGain 0, bypass 0, level 0.5, size 0.75; initial authenticColor 0.

Loop for toggleIndex 0..999 inclusive (1000 toggles):

Set authenticColor raw to toggleIndex % 2 == 0 ? 1.0f : 0.0f BEFORE each processBlock (toggle every block).

blockSize = kBlockSizes[toggleIndex % kBlockSizes.size()].

Allocate stereo buffer blockSize, fillNoise with toggleIndex seed.

REQUIRE_NOTHROW plugin.processBlock.

For all channels/samples: REQUIRE std::isfinite; track global peak.

Compute per-block maxAdjacentDelta on channel 0 (or mono sum); REQUIRE below 1.0f.

After loop REQUIRE toggle count executed == 1000 (loop bound enforcement).

Do not remove existing "processBlock stress with authentic color toggling" TEST-09 case — XFADE-02 adds stricter coverage alongside it.

Optional tail: one larger-than-prepared block case (1024) with crossfade active is covered by rotation including 1024 at 48k/1024 prepare — no separate case required unless flaky.
  </action>
  <verify>
    <automated>cd build && ./Tests "1000 authentic_color toggles" -r compact</automated>
  </verify>
  <done>1000-toggle stress case passes finiteness, peak, and click gates.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 16 regression sweep</name>
  <files>tests/RealtimeStressTest.cpp</files>
  <read_first>tests/RealtimeStressTest.cpp</read_first>
  <action>
Run combined phase gate locally and fix any failures in stress or alloc tests introduced by crossfade wiring.

Command scope: EngineCrossfade, XFADE, authentic color toggling, integrated processBlock bodies have no heap allocation tokens, SchroederTank32 block tests.

If SchroederTank32BlockTest lacks finite-output coverage during crossfade, add optional CASE "SchroederTank32 crossfade dual path finite output" tagged [SchroederTank32][XFADE-01]: prepare tank, requestEngineCrossfade(true), processBlock 256-sample noise, REQUIRE all finite peak below 4 — only if 16-01 did not add equivalent coverage and combined sweep shows gap.

Do not modify production DSP in this task — test/fix test expectations only.
  </action>
  <verify>
    <automated>cd build && ctest -R "EngineCrossfade|XFADE|authentic color toggling|integrated processBlock bodies" --output-on-failure</automated>
  </verify>
  <done>All Phase 16 automated gates green in CI-equivalent ctest slice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stress test → PluginProcessor | Untrusted rapid parameter toggling simulates worst-case performer |
| Static scan → source headers | Build-time enforcement of realtime safety |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-04 | Denial of Service | 1000-toggle stress loop | medium | mitigate | Finiteness + peak bounds; REQUIRE_NOTHROW per block |
| T-16-05 | Tampering | numSamples > maxBlockSize_ | high | mitigate | Stress rotates sizes up to 1024 matching prepare; early-return guards in DSP |
| T-16-06 | Information Disclosure | Test output logging | low | accept | Noise buffers only; no user audio |
| T-16-SC | Tampering | npm/pip/cargo installs | high | accept | No packages installed this plan |
</threat_model>

<verification>
cd build && ctest -R "XFADE|1000 authentic_color|integrated processBlock bodies" --output-on-failure
Full suite: cd build && ctest --output-on-failure before /gsd-verify-work
</verification>

<success_criteria>
XFADE-02 satisfied: 1000-toggle stress passes with finiteness, peak less than 4.0f, click less than 1.0f, alloc scan clean.
</success_criteria>

<output>
Create .planning/phases/16-engine-crossfade/16-02-SUMMARY.md when done
</output>
