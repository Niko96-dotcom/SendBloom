---
phase: 11-rc1-safety-freeze
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - source/ParameterLayout.cpp
  - resources/presets/Sparkle_Verb.xml
  - resources/presets/Cut_Sample_Gate.xml
  - resources/presets/Spacerock_Burn.xml
  - resources/presets/Dry_Dub_Sends.xml
  - resources/presets/Dark_Bloom.xml
  - resources/presets/Firm_Pressure.xml
  - resources/presets/Gated_Room.xml
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
must_haves:
  truths:
    - "Fresh PluginProcessor with no saved state reads authentic_color APVTS raw value as 0"
    - "All 8 factory preset XML files contain authentic_color value 0"
    - "Hot_Clip.xml remains authentic_color=0; seven other presets flipped from 1 to 0"
  artifacts:
    - path: source/ParameterLayout.cpp
      provides: "APVTS authentic_color default false per D-03"
    - path: resources/presets/*.xml
      provides: "Factory presets SAFE-02 compliant"
  key_links:
    - from: source/ParameterLayout.cpp
      to: PluginProcessor APVTS construction
      via: createParameterLayout default for authenticColor
    - from: resources/presets/*.xml
      to: BinaryData embedded presets
      via: CMake rebuild regenerates preset blobs consumed by FactoryPresets
  prohibitions:
    - statement: "Must not modify SchroederTank32.h or processAuthentic accumulator path"
      status: resolved
      verification: judgment
    - statement: "Must not modify GatedBloomChain, PluginProcessor routing, WetOverdrive, or r8brain/SRC code"
      status: resolved
      verification: judgment
    - statement: "Must not touch UI files except AdvancedDrawer.cpp tooltip in plan 11-03"
      status: resolved
      verification: judgment
---

<objective>
Flip APVTS and factory preset defaults so 32k Color is off for fresh loads and every factory preset recall (SAFE-01, SAFE-02).

Purpose: RC1 users must not hit accumulator HF imaging on default or preset load paths per D-02/D-03.
Output: ParameterLayout default false; all 8 preset XML files at authentic_color=0.
Scope: D-01 allowed touch list — source/ParameterLayout.cpp and resources/presets/*.xml only. D-07: no SchroederTank32, GatedBloomChain, WetOverdrive, or SRC edits.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-rc1-safety-freeze/11-CONTEXT.md
@source/ParameterLayout.cpp
@source/ParameterIDs.h
@resources/presets/Sparkle_Verb.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: APVTS authentic_color default off (D-03, SAFE-01)</name>
  <files>source/ParameterLayout.cpp</files>
  <read_first>source/ParameterLayout.cpp, source/ParameterIDs.h, .planning/phases/11-rc1-safety-freeze/11-CONTEXT.md</read_first>
  <action>
In createParameterLayout(), locate the juce::AudioParameterBool registration for ParameterIDs::authenticColor (display name "Authentic Color"). Change the third constructor argument (default value) from true to false. Do not rename the parameter ID or display string. Do not modify any other parameter defaults or ranges.
  </action>
  <verify>
    <automated>grep -n 'authenticColor' source/ParameterLayout.cpp | grep -v '^#' | grep -c 'false' | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - ParameterLayout.cpp registers authenticColor with default false (third AudioParameterBool ctor arg)
    - No other lines in ParameterLayout.cpp changed beyond the authenticColor default
    - Display name remains "Authentic Color"
  </acceptance_criteria>
  <done>SAFE-01 layout default: fresh APVTS construction yields authentic_color off.</done>
</task>

<task type="auto">
  <name>Task 2: Factory preset XML authentic_color=0 for all 8 (D-02, SAFE-02)</name>
  <files>resources/presets/Sparkle_Verb.xml, resources/presets/Cut_Sample_Gate.xml, resources/presets/Spacerock_Burn.xml, resources/presets/Dry_Dub_Sends.xml, resources/presets/Dark_Bloom.xml, resources/presets/Firm_Pressure.xml, resources/presets/Gated_Room.xml, resources/presets/Hot_Clip.xml</files>
  <read_first>resources/presets/Sparkle_Verb.xml, resources/presets/Hot_Clip.xml, .planning/phases/11-rc1-safety-freeze/11-CONTEXT.md</read_first>
  <action>
For each of the 8 factory preset XML files under resources/presets/, set the PARAM element with id="authentic_color" to value="0". Seven files currently have value="1" (all except Hot_Clip.xml); flip only that attribute. Leave every other PARAM id and value unchanged. Do not edit FactoryPresets.cpp or BinaryData manually — embedded blobs regenerate on next CMake build.
  </action>
  <verify>
    <automated>grep -r 'id="authentic_color"' resources/presets/*.xml | grep -v 'value="0"' | wc -l | awk '{exit ($1==0)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - All 8 preset XML files contain exactly one authentic_color PARAM with value="0"
    - Zero preset XML files contain authentic_color value="1"
    - Non-authentic_color PARAM values unchanged from pre-edit state
  </acceptance_criteria>
  <done>SAFE-02: every factory preset source XML ships authentic_color off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Preset XML → BinaryData → host recall | User-facing preset values cross from source XML into embedded plugin state |
| APVTS default → fresh instance | Host instantiates plugin without saved state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | resources/presets/*.xml | high | mitigate | Flip all 8 to value="0"; verified by grep gate and SAFE-02 test in plan 11-02 |
| T-11-02 | Tampering | ParameterLayout.cpp default | high | mitigate | Single-line default false per D-03; SAFE-01 test in plan 11-02 |
| T-11-03 | Information Disclosure | N/A (no docs in this plan) | low | accept | Docs handled in plan 11-03 |
</threat_model>

<verification>
- grep confirms authentic_color default false in ParameterLayout.cpp
- grep confirms zero preset XML files with authentic_color value other than 0
- Rebuild not required for plan 11-01 completion; plan 11-02 rebuilds before ctest
</verification>

<success_criteria>
- SAFE-01 layout default implemented in ParameterLayout.cpp
- SAFE-02 all 8 preset XML sources at authentic_color=0
- D-07 touch list respected (only allowed files modified)
</success_criteria>

## Artifacts this phase produces

| Artifact | Location | Description |
|----------|----------|-------------|
| APVTS default off | `createParameterLayout()` in `source/ParameterLayout.cpp` | `authenticColor` bool param default `false` |
| Preset auth=0 | `resources/presets/*.xml` (8 files) | `<PARAM id="authentic_color" value="0"/>` on every factory preset |

<output>
Create `.planning/phases/11-rc1-safety-freeze/11-01-SUMMARY.md` when done
</output>
