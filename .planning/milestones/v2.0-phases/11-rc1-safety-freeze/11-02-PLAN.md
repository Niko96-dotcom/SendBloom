---
phase: 11-rc1-safety-freeze
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - tests/ReleaseTruthTest.cpp
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - SAFE-03
must_haves:
  truths:
    - "ReleaseTruthTest has [release][safe] case proving fresh load authentic_color off"
    - "ReleaseTruthTest has [release][safe] case proving all 8 presets recall authentic_color off"
    - "ReleaseTruthTest has [release][safe] case proving host-rate default path has no HF imaging whistle at 48 kHz"
    - "Full Catch2 ctest suite passes with 32k off as default"
    - "Existing authentic_color distinct-response test remains and passes"
  artifacts:
    - path: tests/ReleaseTruthTest.cpp
      provides: "SAFE-01/02/03 automated release assertions per D-06"
  key_links:
    - from: tests/ReleaseTruthTest.cpp SAFE-03
      to: GatedBloomChain host-rate render path
      via: guitar pluck fixture at 48 kHz with authenticColor=false
    - from: tests/ReleaseTruthTest.cpp SAFE-02
      to: FactoryPresets setCurrentProgram
      via: PluginProcessor program recall 0..7
  prohibitions:
    - statement: "Must not modify HighFrequencyRingingDiagnosticsTest.cpp thresholds or cases"
      status: resolved
      verification: judgment
    - statement: "Must not remove or weaken existing authentic_color produces distinct response test"
      status: resolved
      verification: test
    - statement: "Must not add new CMake dependencies or test targets beyond ReleaseTruthTest.cpp edits"
      status: resolved
      verification: judgment
---

<objective>
Add automated [release][safe] assertions for SAFE-01/02/03 and confirm full regression suite green with RC1 defaults (D-06).

Purpose: Lock RC1 safety freeze with executable proofs auditors and CI can run.
Output: Three new Catch2 cases in ReleaseTruthTest.cpp; ctest Release pass.
Scope: D-01 allowed touch list — edits limited to tests/ReleaseTruthTest.cpp only. D-07: no DSP/routing file edits.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-rc1-safety-freeze/11-CONTEXT.md
@tests/ReleaseTruthTest.cpp
@tests/HighFrequencyRingingDiagnosticsTest.cpp
@tests/ChainTestHelpers.h
@source/GatedBloomChain.h
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SAFE-01 and SAFE-02 release truth cases (D-06)</name>
  <files>tests/ReleaseTruthTest.cpp</files>
  <read_first>tests/ReleaseTruthTest.cpp, .planning/phases/11-rc1-safety-freeze/11-CONTEXT.md, source/ParameterIDs.h</read_first>
  <behavior>
    - SAFE-01: Default-constructed PluginProcessor (no setStateInformation) has authentic_color APVTS raw value approximately 0
    - SAFE-02: For preset indices 0 through FactoryPresets::kNumPresets-1, setCurrentProgram(index) yields authentic_color approximately 0
  </behavior>
  <action>
Add two TEST_CASE blocks tagged [release][safe] to ReleaseTruthTest.cpp.

SAFE-01 case name suggestion: "fresh plugin load defaults authentic_color off". Construct sendbloom::PluginProcessor with no setStateInformation call. Read ParameterIDs::authenticColor via getAPVTS().getRawParameterValue and REQUIRE Catch::Approx(0.0f) within margin 1e-4f.

SAFE-02 case name suggestion: "all factory presets recall authentic_color off". Loop preset from 0 to sendbloom::FactoryPresets::kNumPresets - 1 inclusive. For each iteration construct a fresh PluginProcessor, call setCurrentProgram(preset), assert authentic_color raw value approximately 0. Use INFO with preset index on failure. Do not duplicate the full XML parity loop from the existing setCurrentProgram loads embedded XML test — this case is the explicit SAFE-02 auth=0 assertion only.

Place new cases after existing release truth tests; keep existing preset XML parity test unchanged.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target Tests && ctest --test-dir Builds -C Release -R 'release.*safe' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - ReleaseTruthTest.cpp contains at least two TEST_CASE entries with tag [release][safe]
    - SAFE-01 case constructs PluginProcessor without setStateInformation
    - SAFE-02 case loops all FactoryPresets::kNumPresets programs asserting authentic_color off
    - ctest -R 'release.*safe' passes after Release build
  </acceptance_criteria>
  <done>SAFE-01 and SAFE-02 have dedicated automated proofs in ReleaseTruthTest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SAFE-03 host-rate HF clean proof and full ctest (D-06, SAFE-03)</name>
  <files>tests/ReleaseTruthTest.cpp</files>
  <read_first>tests/HighFrequencyRingingDiagnosticsTest.cpp, tests/ChainTestHelpers.h, tests/ReleaseTruthTest.cpp, source/GatedBloomChain.h, source/ParameterCurves.h</read_first>
  <behavior>
    - Render GatedBloomChain at 48 kHz with guitar-pluck input, distn=0, authenticColor=false, darkMix=0 (config A_rev_bright semantics)
    - Tail imaging band (Goertzel at 14825 Hz on last 2400 samples of 24000-sample render) RMS stays below 0.0022
    - Narrowband dominance ratio on tail peak frequency stays below 10.0
    - All wet samples finite
  </behavior>
  <action>
Add a third TEST_CASE tagged [release][safe] — name suggestion: "host-rate default path no HF imaging at 48 kHz".

Include ChainTestHelpers.h and GatedBloomChain.h. Reuse constants aligned with HighFrequencyRingingDiagnosticsTest config A: sample rate 48000, render length 24000, tail start 19200, tail count 2400, imaging Goertzel frequency 14825 Hz, imaging RMS ceiling 0.0022f, narrowband dominance max 10.0f. Copy makeGuitarPluck burst logic inline or as file-local helper matching HF test (240-sample burst, 330/660/990 Hz partials, exp decay).

Render via GatedBloomChain::processSample with sizeToRT60(0.5f), darkMix 0, authenticColor false, distn 0, send connected true, send amount 1, gate threshold -40 dB — same as HF test renderChain for config A.

Measure tail slice metrics: (1) goertzelPower at 14825 Hz converted to RMS must be below 0.0022f; (2) narrowband dominance ratio around measured peak frequency below 10.0f (reuse HF test scan approach or simplified peak+neighbor ratio); (3) all samples isfinite.

Do not edit HighFrequencyRingingDiagnosticsTest.cpp. Do not remove or alter the existing "authentic_color produces distinct response" case.

After adding SAFE-03, rebuild Tests target and run full ctest Release to confirm no regressions including RealtimeStressTest and HF ringing suite. Chain-level GatedBloomChain proof is intentional per CONTEXT D-06 (matches HF test config A semantics); full PluginProcessor wet path is not required for SAFE-03.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target Tests && ctest --test-dir Builds -C Release --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - ReleaseTruthTest.cpp contains third [release][safe] TEST_CASE for host-rate HF cleanliness
    - SAFE-03 uses authenticColor=false and 48 kHz render
    - ctest --test-dir Builds -C Release exits 0 (full suite green)
    - Existing "authentic_color produces distinct response" test still present and passing
  </acceptance_criteria>
  <done>SAFE-03 proves production default path clean at 48 kHz; full regression suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture → DSP chain | Synthetic input drives GatedBloomChain without UI/host wrapper |
| Preset recall → APVTS | setCurrentProgram loads embedded BinaryData from plan 11-01 XML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04 | Tampering | ReleaseTruthTest.cpp | medium | mitigate | Three explicit SAFE cases with numeric thresholds |
| T-11-05 | Repudiation | Missing SAFE proof | high | mitigate | [release][safe] tags grep-able; ctest gate in verify |
| T-11-06 | Denial of Service | Full ctest runtime | low | accept | Existing suite runtime; no new stress beyond HF render |
</threat_model>

<verification>
- ctest -R 'release.*safe' passes (3 cases minimum)
- Full ctest --test-dir Builds -C Release passes
- grep confirms [release][safe] tag count >= 3 in ReleaseTruthTest.cpp
</verification>

<success_criteria>
- SAFE-01, SAFE-02, SAFE-03 each have dedicated [release][safe] test
- Full ctest --test-dir Builds -C Release exits 0
- No modifications outside tests/ReleaseTruthTest.cpp
</success_criteria>

## Artifacts this phase produces

| Artifact | Location | Description |
|----------|----------|-------------|
| SAFE-01 test | `tests/ReleaseTruthTest.cpp` | `[release][safe]` fresh load authentic_color off |
| SAFE-02 test | `tests/ReleaseTruthTest.cpp` | `[release][safe]` all presets authentic_color off |
| SAFE-03 test | `tests/ReleaseTruthTest.cpp` | `[release][safe]` host-rate HF imaging below ceiling at 48 kHz |
| Local HF helpers | `tests/ReleaseTruthTest.cpp` (anonymous namespace) | Guitar pluck fixture + tail metric helpers for SAFE-03 |

<output>
Create `.planning/phases/11-rc1-safety-freeze/11-02-SUMMARY.md` when done
</output>
