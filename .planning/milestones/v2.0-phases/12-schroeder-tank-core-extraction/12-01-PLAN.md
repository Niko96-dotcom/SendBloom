---
phase: 12-schroeder-tank-core-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - source/SchroederTankCore.h
  - tests/ReverbTestHelpers.h
  - tests/SchroederTankCoreTest.cpp
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-04
must_haves:
  truths:
    - "SchroederTankCore exposes prepare(processingRate) and processSample(input, rt60, darkMix) with no authenticColor parameter"
    - "SchroederTankCore contains no hostRate member, no useAuthenticPath member, and no authentic-mode branches in scaleDelay or updateCoeffs"
    - "prepare(32768.0) yields comb/APF delays matching raw SchroederTank32DelayTable integers (scale factor 1.0)"
    - "Fixed-rate core RT60 within ±15% at size 0.25, 0.5, and 1.0 measured at 32768 Hz"
  artifacts:
    - path: source/SchroederTankCore.h
      provides: "Single-rate Schroeder tank DSP (CORE-01)"
    - path: tests/ReverbTestHelpers.h
      provides: "Shared measureRT60, renderImpulseResponse, maxAbsDiff helpers"
    - path: tests/SchroederTankCoreTest.cpp
      provides: "Fixed-rate RT60 and branch-free static assertions (CORE-02, CORE-04)"
  key_links:
    - from: source/SchroederTankCore.h
      to: source/SchroederTank32DelayTable.h
      via: scaleDelay uses table integers times (processingRate / kInternalRate)
    - from: source/SchroederTankCore.h
      to: source/DampedComb.h
      via: setFeedbackForRT60 and setDampingCutoff per comb
    - from: tests/SchroederTankCoreTest.cpp
      to: source/ParameterCurves.h
      via: sizeToRT60 for target RT60 seconds
---

<objective>
Extract `SchroederTankCore` — a single-rate Schroeder tank with host-path DSP only — plus shared test helpers and fixed 32,768 Hz RT60 impulse tests (CORE-01, CORE-02, CORE-04 fixed-rate portion).

Purpose: Deliver the rate-agnostic core Phase 13's FixedRateAdapter will wrap at 32768 Hz without double-scaling risk.
Output: `source/SchroederTankCore.h`, `tests/ReverbTestHelpers.h`, `tests/SchroederTankCoreTest.cpp`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-schroeder-tank-core-extraction/12-CONTEXT.md
@.planning/phases/12-schroeder-tank-core-extraction/12-RESEARCH.md
@source/SchroederTank32.h
@source/SchroederTank32DelayTable.h
@source/DampedComb.h
@source/SchroederAllpass.h
@tests/SchroederTank32Test.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared reverb test helpers</name>
  <files>tests/ReverbTestHelpers.h</files>
  <read_first>tests/SchroederTank32Test.cpp, tests/ChainTestHelpers.h, tests/Fdn8ReverbTest.cpp</read_first>
  <behavior>
    - measureRT60(samples, sampleRate) returns Schroeder backward-integration RT60 in seconds matching SchroederTank32Test logic
    - maxAbsDiff(a, b) returns maximum absolute sample difference across equal-length vectors
    - renderCoreImpulse(core, rt60, darkMix, numSamples) returns IR from SchroederTankCore at its prepared rate
  </behavior>
  <action>
Create header-only tests/ReverbTestHelpers.h in namespace sendbloom::test::reverb (or anonymous namespace with sendbloom::test prefix matching ChainTestHelpers style). Move measureRT60 implementation from SchroederTank32Test.cpp unchanged (20 ms start skip, 75% end window, -60 dB extrapolation). Add maxAbsDiff for two std::vector<float> of equal size. Add template or overload renderCoreImpulse for SchroederTankCore: prepare must already be called; loop numSamples with impulse at index 0; call core.processSample(in, rt60, darkMix). Do not modify SchroederTank32Test.cpp in this task — duplication removal is optional in plan 12-03.
  </action>
  <verify>
    <automated>test -f tests/ReverbTestHelpers.h && grep -c 'measureRT60' tests/ReverbTestHelpers.h | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - ReverbTestHelpers.h defines measureRT60 with same algorithm as SchroederTank32Test.cpp
    - ReverbTestHelpers.h defines maxAbsDiff for float vectors
    - ReverbTestHelpers.h defines renderCoreImpulse for SchroederTankCore
  </acceptance_criteria>
  <done>Shared RT60 and IR render utilities exist for core and wrapper tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SchroederTankCore single-rate extraction (CORE-01, CORE-02)</name>
  <files>source/SchroederTankCore.h</files>
  <read_first>source/SchroederTank32.h, source/SchroederTank32DelayTable.h, source/DampedComb.h, source/SchroederAllpass.h, .planning/phases/12-schroeder-tank-core-extraction/12-RESEARCH.md</read_first>
  <behavior>
    - prepare(32768.0, 512) sets series APF delays to 167, 253, 328, 413 samples
    - prepare(48000.0, 512) scales delays by 48000/32768
    - processSample returns finite output clamped implicitly by jlimit only if added at wrapper layer; core returns raw tank output times 0.85f
    - No quantize9bit, no kAuthenticBrightDampingHz, no accumulator state in core
  </behavior>
  <action>
Create source/SchroederTankCore.h in namespace sendbloom. Public API: void prepare(double processingRate, int maxBlockSize) noexcept; float processSample(float input, float rt60Seconds, float darkMix) noexcept. Private member processingRate_ defaulting to SchroederTank32DelayTable::kInternalRate.

Extract from SchroederTank32.h host-path only (lines corresponding to processHostRate/processTank and host branch of updateCoeffs/resetDelayLengths):
- scaleDelay(int delayAt32k) returns delayAt32k * (processingRate_ / kInternalRate) with no authentic boolean overload
- resetDelayLengths sets all APF/comb/tank delays via scaleDelay only
- updateCoeffs uses kBrightDampingHz (not kAuthenticBrightDampingHz), predelay at processingRate_, no quantize9bit block
- parallelCombs[i].setProcessingSampleRate(processingRate_) in prepare and after resetDelayLengths
- processTank: predelay push/pop, series APFs, parallel combs average 0.25, LFO mod on tank AP at processingRate_, output * 0.85f
- maxDelay = ceil(processingRate * 1.2) in prepare

Do NOT include: hostRate, useAuthenticPath, authenticColor, processAuthentic, antiImageFilter, inputAccumulator, quantize9bit. Do NOT nest under source/reverb/ — place directly in source/ per CMake glob constraint.

Header must compile standalone when included from tests (include DampedComb.h, SchroederAllpass.h, SchroederTank32DelayTable.h, juce_dsp).
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*//' source/SchroederTankCore.h | grep -v '^[[:space:]]*/\*' | grep -c 'hostRate' | awk '{exit ($1==0)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - SchroederTankCore.h defines class SchroederTankCore with prepare and processSample(float, float, float)
    - No hostRate or useAuthenticPath identifiers in SchroederTankCore.h (comments excluded from grep)
    - scaleDelay uses single processingRate_ factor only
    - prepare accepts arbitrary processingRate and allocates delay lines up to ceil(rate * 1.2)
  </acceptance_criteria>
  <done>CORE-01 and CORE-02: rate-parameterized core with branch-free host-path tank math.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fixed-rate RT60 tests and CORE-01 static gate (CORE-04)</name>
  <files>tests/SchroederTankCoreTest.cpp</files>
  <read_first>tests/ReverbTestHelpers.h, tests/SchroederTank32Test.cpp, source/ParameterCurves.h, source/SchroederTankCore.h</read_first>
  <behavior>
    - RT60 at 32768 Hz for sizeNorm 0.25, 0.5, 1.0 within ±15% of ParameterCurves::sizeToRT60
    - measureRT60 called with sampleRate 32768.0 (not 48000)
    - IR length static_cast<int>(32768.0 * target * 3.0) for 0.25/0.5; use target * 2.5 for size 1.0 matching SchroederTank32Test pattern
    - Static test reads SchroederTankCore.h source and fails if host-path mode identifiers present
  </behavior>
  <action>
Create tests/SchroederTankCoreTest.cpp including ReverbTestHelpers.h, SchroederTankCore.h, ParameterCurves.h, Catch2.

Add TEST_CASE blocks tagged [verb][SchroederTankCore][rt60] for sizeNorm 0.25f, 0.5f, 1.0f: constexpr double kCoreRate = 32768.0; core.prepare(kCoreRate, 512); target = ParameterCurves::sizeToRT60(sizeNorm); ir = renderCoreImpulse(...); measured = measureRT60(ir, kCoreRate); REQUIRE Catch::Approx(target).margin(target * 0.15f).

Add TEST_CASE tagged [verb][SchroederTankCore][CORE-01]: read source/SchroederTankCore.h into std::string; REQUIRE string find for hostRate and useAuthenticPath both return npos; REQUIRE find for authenticColor returns npos.

Add TEST_CASE tagged [verb][SchroederTankCore][CORE-02]: after prepare(32768.0), verify first series APF delay equals SchroederTank32DelayTable::kSeriesApfDelays[0] (167) by rendering short impulse and checking delay-line behavior OR document delay via friend/accessor if needed — prefer testing scale factor: prepare at 32768 and assert scaleDelay equivalence by checking comb feedback setup produces expected RT60 at unscaled table (RT60 tests already validate CORE-02 behaviorally).

Configure build if missing: cmake -B build -DCMAKE_BUILD_TYPE=Debug && cmake --build build before ctest.
  </action>
  <verify>
    <automated>ctest --test-dir build -C Debug -R "SchroederTankCore" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - Three RT60 test cases pass at 32768 Hz for sizes 0.25, 0.5, 1.0
    - CORE-01 static source scan test passes
    - ctest -R SchroederTankCore exits 0
  </acceptance_criteria>
  <done>CORE-04 fixed-rate portion verified; core tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture → SchroederTankCore | Offline impulse vectors cross into DSP render path |
| Host input → processSample | Per-sample float audio input (bounded by downstream jlimit in wrapper) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering | SchroederTankCore::processSample | medium | mitigate | jmax(rt60, 0.05f); jlimit darkMix 0..1; DampedComb damping clamps inherited |
| T-12-02 | Denial of Service | prepare maxDelay | low | accept | maxDelay = ceil(rate * 1.2) caps delay allocation per existing SchroederTank32 pattern |
| T-12-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages in Phase 12 — N/A |

</threat_model>

<verification>
cmake --build build && ctest --test-dir build -C Debug -R "SchroederTankCore" --output-on-failure
grep confirms SchroederTankCore.h free of mode-branch identifiers (comments stripped).
</verification>

<success_criteria>
SchroederTankCore compiles; fixed-rate RT60 ±15% at 0.25/0.5/1.0; no hostRate/authentic branches in core source.
</success_criteria>

<artifacts_this_phase_produces>
- class `sendbloom::SchroederTankCore` in `source/SchroederTankCore.h`
- namespace helpers in `tests/ReverbTestHelpers.h`: `measureRT60`, `maxAbsDiff`, `renderCoreImpulse`
- Catch2 tests in `tests/SchroederTankCoreTest.cpp` tagged `[SchroederTankCore]`, `[rt60]`, `[CORE-01]`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/12-schroeder-tank-core-extraction/12-01-SUMMARY.md` when done
</output>
