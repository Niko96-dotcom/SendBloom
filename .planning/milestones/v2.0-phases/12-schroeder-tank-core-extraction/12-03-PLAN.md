---
phase: 12-schroeder-tank-core-extraction
plan: 03
type: execute
wave: 3
depends_on:
  - 12-02
files_modified:
  - source/SchroederTank32.h
  - tests/SchroederTank32Test.cpp
autonomous: true
requirements:
  - CORE-03
  - CORE-04
must_haves:
  truths:
    - "SchroederTank32 host path (authenticColor=false) delegates to HostRateReverbEngine without behavior change"
    - "SchroederTank32 authentic path (processAuthentic) remains inline with separate tank atoms — not rewritten"
    - "Host and authentic paths use separate delay-line state (no shared SchroederTankCore instance toggled between modes)"
    - "All existing SchroederTank32Test cases pass unchanged after facade refactor"
    - "Full Catch2 regression suite passes (135+ tests baseline)"
  artifacts:
    - path: source/SchroederTank32.h
      provides: "Facade: host delegates to HostRateReverbEngine; authentic inline (CORE-03 integration)"
  key_links:
    - from: source/SchroederTank32.h
      to: source/HostRateReverbEngine.h
      via: member hostEngine; processSample authentic=false calls hostEngine.processSample
    - from: source/SchroederTank32.h authentic inline
      to: source/SchroederTank32DelayTable.h
      via: scaleDelay(..., true) unscaled table for authentic-only atoms
    - from: tests/SchroederTank32Test.cpp
      to: source/SchroederTank32.h
      via: regression RT60 and dark/authentic cases
  prohibitions:
    - statement: "Must not rewrite processAuthentic accumulator logic or enable 32k Color by default"
      status: resolved
      verification: test
    - statement: "Must not modify GatedBloomChain.h wiring in Phase 12"
      status: resolved
      verification: judgment
    - statement: "Must not add r8brain/SRC or processBlock API"
      status: resolved
      verification: judgment
---

<objective>
Refactor `SchroederTank32` into a facade: host path delegates to `HostRateReverbEngine`, authentic path keeps inline tank with separate state; confirm full regression suite green (CORE-03 integration, CORE-04 completion).

Purpose: Complete extraction without breaking RC1 host path or authentic diagnostic path deferred to Phase 13.
Output: Refactored `source/SchroederTank32.h`; optional SchroederTank32Test helper dedup; full ctest pass.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-schroeder-tank-core-extraction/12-CONTEXT.md
@.planning/phases/12-schroeder-tank-core-extraction/12-RESEARCH.md
@source/SchroederTank32.h
@source/HostRateReverbEngine.h
@source/SchroederTankCore.h
@tests/SchroederTank32Test.cpp
@tests/ReverbTestHelpers.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: SchroederTank32 facade — host delegates, authentic inline</name>
  <files>source/SchroederTank32.h</files>
  <read_first>source/SchroederTank32.h, source/HostRateReverbEngine.h, .planning/phases/12-schroeder-tank-core-extraction/12-RESEARCH.md Pattern 3</read_first>
  <action>
Refactor source/SchroederTank32.h:

Add member HostRateReverbEngine hostEngine.

prepare(double sampleRate, int maxBlockSize): call hostEngine.prepare(sampleRate, maxBlockSize). Keep authentic-only atom preparation for inline authentic tank: seriesApfs, parallelCombs, tankAp, predelayLine, antiImageFilter at hostRate for anti-image filter only; authentic comb processing rate = kInternalRate.

Retain for authentic path only: hostRate member, useAuthenticPath, inputAccumulator, outputHold, lastInternalOut, antiImageFilter, authentic inline seriesApfs/parallelCombs/tankAp/predelayLine/lfoPhase, quantize9bit, scaleDelay(int, bool authentic) with authentic branch, resetDelayLengths(bool authentic) affecting authentic atoms only, syncCombProcessingRate for authentic combs, updateCoeffs authentic branches, processTank for authentic atoms, processAuthentic unchanged in behavior.

processSample: on authenticColor edge, set useAuthenticPath and resetDelayLengths(true) on authentic atoms only (do not reset hostEngine core). Call updateCoeffs for authentic path when useAuthenticPath true. If useAuthenticPath return processAuthentic(input); else return hostEngine.processSample(input, rt60Seconds, darkMix, false).

Remove duplicated host-path tank atoms from SchroederTank32 — host path must not call processTank on facade-owned atoms. Host DSP lives entirely in hostEngine.

Critical: hostEngine and authentic inline tank are separate instances — toggling authenticColor must not corrupt hostEngine delay state.

Do not modify IReverbEngine.h or GatedBloomChain.h.
  </action>
  <verify>
    <automated>grep -c 'hostEngine' source/SchroederTank32.h | awk '{exit ($1>=2)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - SchroederTank32 includes HostRateReverbEngine.h and owns hostEngine member
    - processSample routes authenticColor=false to hostEngine.processSample
    - processAuthentic and accumulator logic preserved for authentic path
    - No host-path processTank call on facade when authenticColor is false
  </acceptance_criteria>
  <done>SchroederTank32 facade delegates host path; authentic path isolated.</done>
</task>

<task type="auto">
  <name>Task 2: SchroederTank32Test regression and helper dedup</name>
  <files>tests/SchroederTank32Test.cpp</files>
  <read_first>tests/SchroederTank32Test.cpp, tests/ReverbTestHelpers.h, source/SchroederTank32.h</read_first>
  <action>
Run existing SchroederTank32Test suite — all cases must pass without changing assertions.

Optional cleanup: replace local measureRT60 in SchroederTank32Test.cpp with include of tests/ReverbTestHelpers.h if signatures match; keep renderImpulseResponse for SchroederTank32 local to this file (authentic flag parameter).

Do not weaken RT60 margins or remove authentic_color finite test.

If parity test from plan 12-02 now shows zero diff between HostRateReverbEngine and SchroederTank32 host path, no test change required.
  </action>
  <verify>
    <automated>ctest --test-dir build -C Debug -R "SchroederTank32" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - All SchroederTank32 tagged tests pass
    - RT60 cases at 0.25/0.5/1.0 still within ±15%
    - authentic_color path stays finite test passes
  </acceptance_criteria>
  <done>CORE-04 host path confirmed via existing SchroederTank32Test after facade refactor.</done>
</task>

<task type="auto">
  <name>Task 3: Full regression suite phase gate</name>
  <files>tests/SchroederTank32Test.cpp</files>
  <read_first>.planning/phases/12-schroeder-tank-core-extraction/12-VALIDATION.md, .planning/ROADMAP.md Phase 12 success criteria</read_first>
  <action>
Run full Catch2 suite via ctest. Fix any failures caused by SchroederTank32 refactor (do not modify unrelated production code unless test proves regression).

Confirm combined coverage: SchroederTankCore fixed-rate RT60 (plan 01), HostRate wrapper RT60+parity (plan 02), SchroederTank32 host+authentic (this plan) satisfies ROADMAP Phase 12 success criteria 1-4.

Document in SUMMARY: both cores verified at 0.25/0.5/1.0 with ±15% tolerance.
  </action>
  <verify>
    <automated>ctest --test-dir build -C Debug --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - Full ctest exits 0
    - No test count regression below Phase 11 baseline
    - Release-tagged tests still pass if present in suite
  </acceptance_criteria>
  <done>Phase 12 extraction complete; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticColor toggle | Mode switch must not cross-contaminate host vs authentic delay state |
| GatedBloomChain → SchroederTank32 | Unchanged wiring; facade must preserve IReverbEngine contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-05 | Tampering | authenticColor edge toggle | high | mitigate | Separate hostEngine and authentic inline atoms; reset authentic delays only on edge |
| T-12-06 | Tampering | Host path behavior drift | high | mitigate | Plan 12-02 parity test; SchroederTank32Test RT60 regression |
| T-12-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages — N/A |

</threat_model>

<verification>
ctest --test-dir build -C Debug --output-on-failure
ctest -R "SchroederTankCore|SchroederTank32|HostRate" for phase-scoped quick check.
</verification>

<success_criteria>
SchroederTank32 delegates host to HostRateReverbEngine; authentic path unchanged; all tests green; CORE-01 through CORE-04 satisfied end-to-end.
</success_criteria>

<artifacts_this_phase_produces>
- refactored `sendbloom::SchroederTank32` facade in `source/SchroederTank32.h` with `hostEngine` member
- authentic-only inline tank state retained in SchroederTank32 (not in SchroederTankCore)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/12-schroeder-tank-core-extraction/12-03-SUMMARY.md` when done
</output>
