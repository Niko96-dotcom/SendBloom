---
phase: 12-schroeder-tank-core-extraction
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - source/HostRateReverbEngine.h
  - tests/SchroederTankCoreTest.cpp
autonomous: true
requirements:
  - CORE-03
  - CORE-04
must_haves:
  truths:
    - "HostRateReverbEngine implements IReverbEngine and owns SchroederTankCore prepared at host sample rate"
    - "HostRateReverbEngine::processSample applies juce::jlimit(-4.0f, 4.0f, ...) matching legacy processHostRate"
    - "Host wrapper impulse IR matches SchroederTank32 host path (authentic=false) with maxAbsDiff below 1e-5f at 48 kHz"
    - "HostRateReverbEngine RT60 within ±15% at size 0.25, 0.5, and 1.0 at 48 kHz"
  artifacts:
    - path: source/HostRateReverbEngine.h
      provides: "IReverbEngine wrapper delegating to core@hostRate (CORE-03)"
    - path: tests/SchroederTankCoreTest.cpp
      provides: "Host parity and host-rate RT60 cases (CORE-03, CORE-04 host portion)"
  key_links:
    - from: source/HostRateReverbEngine.h
      to: source/SchroederTankCore.h
      via: member SchroederTankCore core; prepare forwards sampleRate
    - from: source/HostRateReverbEngine.h
      to: source/IReverbEngine.h
      via: public inheritance and override prepare/processSample
    - from: tests/SchroederTankCoreTest.cpp parity case
      to: source/SchroederTank32.h
      via: renderImpulseResponse with authentic=false baseline before facade refactor in plan 12-03
---

<objective>
Implement `HostRateReverbEngine` — thin `IReverbEngine` wrapper around `SchroederTankCore` at host rate — and prove RC1 host-path parity plus host-rate RT60 tolerance (CORE-03, CORE-04 host portion).

Purpose: RC1 primary path (authentic_color off) must remain behavior-identical through the wrapper before SchroederTank32 delegates to it.
Output: `source/HostRateReverbEngine.h`; host parity and RT60 tests appended to `tests/SchroederTankCoreTest.cpp`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-schroeder-tank-core-extraction/12-CONTEXT.md
@.planning/phases/12-schroeder-tank-core-extraction/12-RESEARCH.md
@source/SchroederTankCore.h
@source/IReverbEngine.h
@source/SchroederTank32.h
@tests/ReverbTestHelpers.h
@tests/SchroederTank32Test.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: HostRateReverbEngine IReverbEngine wrapper (CORE-03)</name>
  <files>source/HostRateReverbEngine.h</files>
  <read_first>source/IReverbEngine.h, source/SchroederTankCore.h, source/SchroederTank32.h</read_first>
  <action>
Create source/HostRateReverbEngine.h in namespace sendbloom. Class HostRateReverbEngine : public IReverbEngine.

prepare(double sampleRate, int maxBlockSize) noexcept override: call core.prepare(sampleRate, maxBlockSize).

processSample(float input, float rt60Seconds, float darkMix, bool authenticColor) noexcept override: ignore authenticColor parameter (juce::ignoreUnused or comment that host wrapper never uses authentic path); compute out = core.processSample(input, rt60Seconds, darkMix); return juce::jlimit(-4.0f, 4.0f, out) matching SchroederTank32::processHostRate clamp at lines 195-198.

Private member: SchroederTankCore core.

Place header in source/ root (not source/reverb/). Include IReverbEngine.h and SchroederTankCore.h.
  </action>
  <verify>
    <automated>test -f source/HostRateReverbEngine.h && grep -c 'class HostRateReverbEngine' source/HostRateReverbEngine.h | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - HostRateReverbEngine publicly inherits IReverbEngine
    - prepare forwards to SchroederTankCore::prepare
    - processSample returns jlimit(-4.0f, 4.0f, core.processSample(...))
    - Header compiles when included from test translation unit
  </acceptance_criteria>
  <done>CORE-03 wrapper class exists and matches legacy host-rate output clamping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Host impulse parity vs legacy SchroederTank32 host path</name>
  <files>tests/SchroederTankCoreTest.cpp</files>
  <read_first>tests/ReverbTestHelpers.h, source/HostRateReverbEngine.h, source/SchroederTank32.h, tests/SchroederTank32Test.cpp</read_first>
  <behavior>
    - At 48 kHz, rt60 = ParameterCurves::sizeToRT60(0.5f), darkMix=0, authentic=false
    - IR length 48000 samples (1 second)
    - maxAbsDiff(engineIr, tankIr) less than 1.0e-5f
  </behavior>
  <action>
Append to tests/SchroederTankCoreTest.cpp a renderEngineImpulse helper for IReverbEngine implementations (loop numSamples, processSample with authenticColor false).

Add TEST_CASE "HostRateReverbEngine matches SchroederTank32 host path impulse" tagged [verb][HostRate][parity].

constexpr double kHostRate = 48000.0; rt60 = ParameterCurves::sizeToRT60(0.5f); numSamples = 48000.

Prepare HostRateReverbEngine and SchroederTank32 both at kHostRate, 512. Render IRs with darkMix 0.0f. Tank uses authentic=false. REQUIRE maxAbsDiff less than 1.0e-5f.

If diff exceeds threshold after refactor-ready core, tighten by ensuring core extraction matches host-path coeff order exactly — do not loosen threshold without documenting in SUMMARY.
  </action>
  <verify>
    <automated>ctest --test-dir build -C Debug -R "HostRate.*parity" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - Parity test tagged [HostRate][parity] exists and passes
    - maxAbsDiff threshold 1.0e-5f or tighter
    - Both engines prepared at 48000 Hz
  </acceptance_criteria>
  <done>CORE-03: wrapper impulse response matches monolithic host path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Host-rate RT60 tests via wrapper (CORE-04 host portion)</name>
  <files>tests/SchroederTankCoreTest.cpp</files>
  <read_first>tests/SchroederTank32Test.cpp, source/HostRateReverbEngine.h, tests/ReverbTestHelpers.h</read_first>
  <behavior>
    - HostRateReverbEngine RT60 at 48 kHz for sizeNorm 0.25, 0.5, 1.0 within ±15%
    - Same IR lengths and margins as SchroederTank32Test host cases
  </behavior>
  <action>
Append three TEST_CASE blocks tagged [verb][HostRate][rt60] for sizeNorm 0.25f, 0.5f, 1.0f.

Use HostRateReverbEngine, prepare(48000.0, 512), renderEngineImpulse with authenticColor false, measureRT60(ir, 48000.0), REQUIRE Catch::Approx(target).margin(target * 0.15f).

Mirror buffer lengths from SchroederTank32Test: target * 3.0 for 0.25/0.5, target * 2.5 for 1.0.
  </action>
  <verify>
    <automated>ctest --test-dir build -C Debug -R "HostRate.*rt60" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - Three HostRate RT60 tests pass at 48 kHz
    - Tags include [HostRate] and [rt60]
    - ctest -R "HostRate" exits 0 combining parity and RT60 cases
  </acceptance_criteria>
  <done>CORE-04 host-rate portion verified through wrapper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IReverbEngine::processSample | Host audio thread per-sample float input |
| authenticColor parameter | Ignored in wrapper — must not route authentic logic here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-03 | Tampering | HostRateReverbEngine output | medium | mitigate | juce::jlimit(-4.0f, 4.0f) on every processSample return |
| T-12-04 | Tampering | authenticColor ignored | low | accept | Wrapper documents ignore; SchroederTank32 facade routes authentic separately in plan 12-03 |
| T-12-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages — N/A |

</threat_model>

<verification>
ctest --test-dir build -C Debug -R "HostRate" --output-on-failure
All host parity and RT60 cases green before proceeding to plan 12-03.
</verification>

<success_criteria>
HostRateReverbEngine passes parity vs SchroederTank32 host IR and RT60 ±15% at 0.25/0.5/1.0 at 48 kHz.
</success_criteria>

<artifacts_this_phase_produces>
- class `sendbloom::HostRateReverbEngine` in `source/HostRateReverbEngine.h`
- Catch2 tests tagged `[HostRate][parity]` and `[HostRate][rt60]` in `tests/SchroederTankCoreTest.cpp`
- helper `renderEngineImpulse` for `IReverbEngine` in test file or ReverbTestHelpers.h
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/12-schroeder-tank-core-extraction/12-02-SUMMARY.md` when done
</output>
