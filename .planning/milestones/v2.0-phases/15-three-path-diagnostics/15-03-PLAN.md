---
phase: 15-three-path-diagnostics
plan: 03
type: execute
wave: 3
depends_on:
  - 15-02
files_modified:
  - tests/AuthenticPathDiagnosticsTest.cpp
autonomous: true
requirements:
  - DIAG-04
  - TEST-08
must_haves:
  truths:
    - "ProperSRC HF metrics at 44.1, 48, and 96 kHz stay within documented tolerance vs 48 kHz reference"
    - "ProperSRC produces finite output for all five fixtures at 44.1, 48, 88.2, and 96 kHz"
    - "Variable block sizes 1, 64, 512 pass finiteness spot-check on guitar pluck"
  artifacts:
    - path: tests/AuthenticPathDiagnosticsTest.cpp
      provides: "DIAG-04 invariance and TEST-08 finiteness tagged cases"
  key_links:
    - from: tests/AuthenticPathDiagnosticsTest.cpp
      to: tests/HfDiagnosticsHelpers.h
      via: renderTankPath at multiple host rates
---

<objective>
Gate ProperSRC multi-rate HF consistency (DIAG-04) and cross-fixture/rate finiteness (TEST-08) on the three-path harness.

Purpose: Prove ProperSRC behaves broadly similar across host rates before Phase 18 enablement; catch rate-dependent blowups and non-finite SRC output.
Output: [DIAG-04] and [TEST-08] tagged cases in AuthenticPathDiagnosticsTest.cpp.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-three-path-diagnostics/15-RESEARCH.md
@.planning/phases/15-three-path-diagnostics/15-02-SUMMARY.md
@tests/HfDiagnosticsHelpers.h
@tests/FixedRateAdapterTest.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProperSRC multi-rate HF invariance gates</name>
  <files>tests/AuthenticPathDiagnosticsTest.cpp</files>
  <behavior>
    - At 44.1, 48, 96 kHz on guitar_pluck ProperSRC: imaging14825Rms &lt; 0.0022 at each rate
    - rmsAbove10k and rmsAbove14k ratios to 48k reference fall in [0.65, 1.55]
    - peakFrequency delta vs 48k ≤ 1500 Hz; spectralCentroid delta ≤ 3000 Hz
    - narrowbandDominance &lt; 10.0 at each rate
    - Cross-rate max/min rmsAbove10k ratio across {44.1, 48, 96} ≤ 1.6
  </behavior>
  <action>
TEST_CASE tagged [diagnostics][DIAG-04]: define named constexpr tolerances from RESEARCH Multi-Rate Tolerance Strategy as file-level constants with comment linking to DIAG-04.

Render guitar_pluck at 44100, 48000, 96000 Hz via ReverbPath::ProperSRC. Capture HfMetrics at each rate; use 48000 as reference for ratio gates.

Apply absolute ceilings (imaging14825Rms, narrowbandDominance) at every rate. Apply ratio tolerances for rmsAbove10k, rmsAbove14k, rmsTotal vs 48k reference.

Add cross-rate consistency: max(rmsAbove10k)/min(rmsAbove10k) across three rates ≤ 1.6f on guitar_pluck.

If baseline run fails a ratio tolerance, calibrate per RESEARCH workflow: run once, document measured spread in comment, adjust only ratio bands (never loosen imaging or dominance absolute ceilings without DSP fix).

88.2 kHz excluded from DIAG-04 invariance per RESEARCH A4 (finiteness only in TEST-08).
  </action>
  <verify>
    <automated>cd build && ./Tests "[DIAG-04]" -r compact</automated>
  </verify>
  <done>DIAG-04 ProperSRC host-rate invariance gates pass with documented constexpr tolerances.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fixture × rate finiteness matrix</name>
  <files>tests/AuthenticPathDiagnosticsTest.cpp</files>
  <behavior>
    - All five fixtures × four rates {44100, 48000, 88200, 96000} produce finite ProperSRC output
    - Default block size 512
  </behavior>
  <action>
TEST_CASE tagged [diagnostics][TEST-08]: constexpr std::array rates {44100.0, 48000.0, 88200.0, 96000.0}. Nested loop allFixtures() × rates. renderTankPath ProperSRC; REQUIRE std::isfinite every sample.

Extend RESEARCH Pattern: scale kRenderSamples proportionally to rate if needed so tail window remains ~50 ms equivalent, or keep fixed sample count and document — prefer matching tail duration across rates for fair comparison.

Tag [TEST-08] for requirements traceability.
  </action>
  <verify>
    <automated>cd build && ./Tests "[TEST-08]" -r compact</automated>
  </verify>
  <done>TEST-08 finiteness green for 5 fixtures × 4 host rates on ProperSRC path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Variable block-size finiteness spot check</name>
  <files>tests/AuthenticPathDiagnosticsTest.cpp</files>
  <behavior>
    - guitar_pluck at 48 kHz with block sizes 1, 64, 512 all produce finite ProperSRC output
  </behavior>
  <action>
TEST_CASE tagged [diagnostics][TEST-08][blockSize]: render guitar_pluck at 48000 Hz with blockSize in {1, 64, 512} via renderTankPath ProperSRC. REQUIRE finite output each size (RESEARCH Pitfall 4 — block boundary stress).

Keep in same file; share TEST-08 tag for traceability.
  </action>
  <verify>
    <automated>cd build && ./Tests "[TEST-08]" -r compact</automated>
  </verify>
  <done>Variable block-size finiteness spot check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| offline render → metrics | No runtime user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-04 | Denial of Service | CI test duration | low | accept | Matrix bounded: 5×4 finiteness + 3-rate invariance |

</threat_model>

<verification>
cd build && ./Tests "[DIAG-04]" -r compact && cd build && ./Tests "[TEST-08]" -r compact
</verification>

<success_criteria>
DIAG-04 multi-rate invariance and TEST-08 finiteness matrix green.
</success_criteria>

<output>
Create .planning/phases/15-three-path-diagnostics/15-03-SUMMARY.md when done
</output>
