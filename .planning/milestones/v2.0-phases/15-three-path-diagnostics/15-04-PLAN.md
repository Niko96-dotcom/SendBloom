---
phase: 15-three-path-diagnostics
plan: 04
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - tests/HighFrequencyRingingDiagnosticsTest.cpp
  - tests/FixedRateAdapterTest.cpp
autonomous: true
requirements:
  - TEST-11
must_haves:
  truths:
    - "All [hf][ringing][regression] cases pass at 48 kHz with ProperSRC production path"
    - "SRC-05 legacy parity compares LegacyAccumulatorPath to tank with diagnostics mode set, not default authentic output"
    - "HF regression suite imports shared helpers — no duplicated fixture/metric implementations"
  artifacts:
    - path: tests/HighFrequencyRingingDiagnosticsTest.cpp
      provides: "TEST-11 tagged full-chain HF regression via GatedBloomChain"
    - path: tests/FixedRateAdapterTest.cpp
      provides: "SRC-05 repaired legacy accumulator parity"
  key_links:
    - from: tests/HighFrequencyRingingDiagnosticsTest.cpp
      to: tests/HfDiagnosticsHelpers.h
      via: shared fixtures and measureTail
    - from: tests/FixedRateAdapterTest.cpp
      to: source/SchroederTank32.h
      via: setAuthentic32ModeForDiagnostics(LegacyAccumulator) before legacy comparison
---

<objective>
Restore SRC-05 legacy parity tests and make TEST-11 HF ringing regression suite green at 48 kHz by refactoring to shared helpers and resolving the authentic-vs-host 10k RMS ratio threshold.

Purpose: Full-chain regression proves production GatedBloomChain wet path passes HF gates; SRC-05 repair unblocks accurate legacy A/B baseline.
Output: Green [hf][ringing][regression] and [TEST-11] cases; repaired SRC-05 in FixedRateAdapterTest.cpp.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-three-path-diagnostics/15-RESEARCH.md
@.planning/phases/15-three-path-diagnostics/15-01-SUMMARY.md
@tests/HighFrequencyRingingDiagnosticsTest.cpp
@tests/FixedRateAdapterTest.cpp
@tests/HfDiagnosticsHelpers.h
@source/GatedBloomChain.h
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repair SRC-05 legacy accumulator parity</name>
  <files>tests/FixedRateAdapterTest.cpp</files>
  <behavior>
    - LegacyAccumulatorPath output matches SchroederTank32 with setAuthentic32ModeForDiagnostics(LegacyAccumulator) and processBlock
    - Impulse and burst SRC-05 cases pass with margin 1e-5 per sample
  </behavior>
  <action>
Fix both SRC-05 TEST_CASE blocks in FixedRateAdapterTest.cpp per RESEARCH Pitfall 1:

Before tank render in legacy comparison, call tank.setAuthentic32ModeForDiagnostics(Authentic32Mode::LegacyAccumulator).

Migrate comparison from processSample to processBlock loop matching SchroederTank32BlockTest pattern (block size 512, per-sample block of 1 acceptable only if block test already covers — prefer 512 for consistency).

LegacyAccumulatorPath side continues using legacy.processBlock.

Do NOT compare legacy path to default authenticColor=true output (that is ProperSRC post–Phase 14).

Clear diagnostics mode after test if tank reused. Re-run existing SRC-04/SRC-06 cases to ensure no regression.
  </action>
  <verify>
    <automated>cd build && ./Tests "[SRC-05]" -r compact</automated>
  </verify>
  <done>SRC-05 legacy parity green using diagnostics mode API.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor HF test to import HfDiagnosticsHelpers</name>
  <files>tests/HighFrequencyRingingDiagnosticsTest.cpp</files>
  <action>
Refactor HighFrequencyRingingDiagnosticsTest.cpp to #include tests/HfDiagnosticsHelpers.h.

Remove duplicated makeGuitarPluck, makeDecayingSine, makeImpulseBurst, makeSweptSine, SpectralScan, measureTail, narrowbandDominanceRatio, bandRmsAbove, and related helpers now in header.

Keep renderChain using GatedBloomChain::processSample for full-chain TEST-11 scope (RESEARCH Pattern 4 — production wet path with gates/OD). Do not switch TEST-11 to tank-only harness.

Retain kMatrixConfigs and diagnostic matrix TEST_CASE; update to use shared measureTail and fixtures.

Add [TEST-11] tag alongside existing [hf][ringing][regression] on the four regression TEST_CASE lines for requirements traceability.

Preserve existing threshold constants unless Task 3 adjusts them.
  </action>
  <verify>
    <automated>cd build && ./Tests "[hf][ringing]" -r compact</automated>
  </verify>
  <done>HF test compiles with shared helpers; diagnostic matrix still prints CSV.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Resolve TEST-11 authentic vs host 10k RMS threshold</name>
  <files>tests/HighFrequencyRingingDiagnosticsTest.cpp</files>
  <behavior>
    - All [hf][ringing][regression] cases pass including guitar 10k RMS ratio case
    - Imaging band (14825 Hz) and narrowband dominance cases remain passing without loosening
    - Threshold change documented with measured ratio if adjusted
  </behavior>
  <action>
Diagnose failing case HF ringing authentic bright guitar 10k RMS near host-rate level (measured ratio ~1.456 vs threshold 1.4 per RESEARCH).

Step 1: Using HfDiagnosticsHelpers, measure reverb-only tank ProperSRC vs HostRate ratio on guitar_pluck. Record in INFO or comment.

Step 2: If tank ratio &lt; 1.4 but full-chain ratio ~1.456, the chain (gates/OD) adds HF — document and set kAuthenticVsHostRmsAbove10kMaxRatio to measured P99 + margin (e.g. 1.5f) with comment citing ProperSRC authentic coloration vs accumulator-era threshold (RESEARCH Pitfall 2 / A1). Prefer minimal adjustment over DSP tuning unless ratio indicates defect.

Step 3: If tank ratio also exceeds 1.4, investigate DSP before loosening — only adjust threshold with explicit comment linking measured distribution.

REQUIRE all four [hf][ringing][regression] cases pass. Do not loosen kImagingBandPeakRmsMax or kNarrowbandDominanceMaxRatio.

Run full hf suite: cd build && ./Tests "[hf][ringing][regression]" -r compact
  </action>
  <verify>
    <automated>cd build && ./Tests "[TEST-11]" -r compact</automated>
  </verify>
  <done>TEST-11 regression suite green at 48 kHz with documented threshold rationale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GatedBloomChain full-chain test | Exercises production wet routing; no diagnostics API on chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-05 | Tampering | Threshold relaxation | medium | mitigate | Document measured ratio; never loosen imaging or dominance ceilings; only 10k host comparison ratio per discretion |
| T-15-06 | Spoofing | Wrong legacy baseline | high | mitigate | SRC-05 must use setAuthentic32ModeForDiagnostics(LegacyAccumulator) |

</threat_model>

<verification>
cd build && ./Tests "[SRC-05]" -r compact && cd build && ./Tests "[TEST-11]" -r compact && ctest --test-dir build --output-on-failure -R "hf|SRC-05" 2>&1 | tail -20
</verification>

<success_criteria>
SRC-05 repaired; all TEST-11 HF ringing regression cases pass at 48 kHz.
</success_criteria>

<output>
Create .planning/phases/15-three-path-diagnostics/15-04-SUMMARY.md when done
</output>
