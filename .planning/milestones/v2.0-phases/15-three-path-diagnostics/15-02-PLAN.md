---
phase: 15-three-path-diagnostics
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - tests/AuthenticPathDiagnosticsTest.cpp
autonomous: true
requirements:
  - DIAG-01
  - DIAG-02
  - DIAG-03
must_haves:
  truths:
    - "Three-path side-by-side render compares host-rate, LegacyAccumulator, and ProperSRC on identical input"
    - "All five fixtures participate in the diagnostics matrix"
    - "HF metrics (RMS >10k, RMS >14k, peak frequency, narrowband dominance, spectral centroid) are asserted on rendered tails"
    - "Legacy path shows measurable imaging that ProperSRC suppresses on guitar pluck"
  artifacts:
    - path: tests/AuthenticPathDiagnosticsTest.cpp
      provides: "DIAG-01/02/03 tagged Catch2 cases and CSV matrix output"
  key_links:
    - from: tests/AuthenticPathDiagnosticsTest.cpp
      to: tests/HfDiagnosticsHelpers.h
      via: renderTankPath + measureTail per path/fixture
    - from: tests/AuthenticPathDiagnosticsTest.cpp
      to: source/SchroederTank32.h
      via: processBlock three-path render at 48 kHz
---

<objective>
Deliver the three-path diagnostics matrix test file proving host-rate, LegacyAccumulator, and ProperSRC paths side-by-side with full HF metric gates (DIAG-01, DIAG-02, DIAG-03).

Purpose: Formalize the engineering harness that demonstrates ProperSRC architecturally fixes accumulator HF imaging.
Output: tests/AuthenticPathDiagnosticsTest.cpp with [DIAG-01], [DIAG-02], [DIAG-03] tagged cases.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-three-path-diagnostics/15-RESEARCH.md
@.planning/phases/15-three-path-diagnostics/15-01-SUMMARY.md
@tests/HfDiagnosticsHelpers.h
@tests/FixedRateAdapterTest.cpp
@source/Authentic32Mode.h
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Three-path render matrix with CSV output</name>
  <files>tests/AuthenticPathDiagnosticsTest.cpp</files>
  <behavior>
    - For each fixture × each ReverbPath, render produces finite output
    - CSV header prints path, fixture, and all metric columns including rms_above_14k
    - Three paths produce distinct HF metrics on guitar_pluck (legacy imaging higher than proper)
  </behavior>
  <action>
Create tests/AuthenticPathDiagnosticsTest.cpp including HfDiagnosticsHelpers.h and SchroederTank32.h.

TEST_CASE tagged [diagnostics][DIAG-01]: at 48 kHz / block 512, loop allFixtures() × {HostRate, LegacyAccumulator, ProperSRC}. For each cell: renderTankPath, measureTail, printThreePathCsvRow. REQUIRE all samples finite. SUCCEED after matrix print.

Add differential gate on guitar_pluck: legacy imaging14825Rms > proper imaging14825Rms (proves paths differ architecturally). ProperSRC imaging14825Rms &lt; 0.0022f ceiling from existing HF contract.

Use fresh SchroederTank32 per path render to avoid state bleed between paths.
  </action>
  <verify>
    <automated>cd build && ./Tests "[DIAG-01]" -r compact</automated>
  </verify>
  <done>DIAG-01 three-path side-by-side matrix runs and prints CSV; legacy vs proper differential holds on guitar pluck.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fixture coverage gate</name>
  <files>tests/AuthenticPathDiagnosticsTest.cpp</files>
  <behavior>
    - Exactly five fixtures named guitar_pluck, sine220_decay, sine880_decay, impulse, swept_sine
    - Each fixture renders all three paths without NaN/Inf
  </behavior>
  <action>
TEST_CASE tagged [diagnostics][DIAG-02]: iterate allFixtures(), REQUIRE size == 5 and names match expected set. For each fixture and ReverbPath::ProperSRC, render and REQUIRE std::isfinite on every sample.

Spot-check HostRate and LegacyAccumulator on impulse for finiteness only (impulse masks imaging — do not use impulse for legacy-vs-proper imaging gates per RESEARCH Pitfall 3).
  </action>
  <verify>
    <automated>cd build && ./Tests "[DIAG-02]" -r compact</automated>
  </verify>
  <done>All five DIAG-02 fixtures verified across three-path harness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: HF metric suite assertions on ProperSRC path</name>
  <files>tests/AuthenticPathDiagnosticsTest.cpp</files>
  <behavior>
    - guitar_pluck ProperSRC: rmsAbove10k > 0, rmsAbove14k >= 0, peakFrequency in 4–16 kHz, spectralCentroid > 0
    - guitar_pluck ProperSRC: narrowbandDominanceRatio &lt; 10.0
    - guitar_pluck ProperSRC: imaging14825Rms &lt; 0.0022
    - LegacyAccumulator guitar_pluck: imaging14825Rms strictly greater than ProperSRC (differential gate)
  </behavior>
  <action>
TEST_CASE tagged [diagnostics][DIAG-03]: render guitar_pluck on all three paths at 48 kHz. On ProperSRC metrics, REQUIRE all DIAG-03 fields populated and gates above pass.

On LegacyAccumulator, REQUIRE imaging14825Rms > ProperSRC imaging14825Rms (≥70% reduction style gate aligned with SRC-06 spirit).

Document metric field meanings in a brief file comment linking to DIAG-03 requirement. Use narrowbandDominanceRatio from helpers with measured peakFrequency.

Do not embed literal acceptance strings that would trip negative grep gates in executor commit hooks.
  </action>
  <verify>
    <automated>cd build && ./Tests "[DIAG-03]" -r compact</automated>
  </verify>
  <done>DIAG-03 metrics quantified with ProperSRC passing absolute ceilings and legacy failing differential imaging gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test stdout → engineer | CSV diagnostics engineering-only; no file I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-02 | Tampering | CSV output | low | accept | Stdout only; no persistent artifact write |
| T-15-03 | Information Disclosure | LegacyAccumulator path | low | accept | Test-only diagnostics mode; not user-facing |

</threat_model>

<verification>
cd build && ./Tests "[diagnostics]" -r compact passes DIAG-01 through DIAG-03.
</verification>

<success_criteria>
DIAG-01 three-path harness, DIAG-02 five fixtures, and DIAG-03 full metric suite green at 48 kHz.
</success_criteria>

<output>
Create .planning/phases/15-three-path-diagnostics/15-02-SUMMARY.md when done
</output>
