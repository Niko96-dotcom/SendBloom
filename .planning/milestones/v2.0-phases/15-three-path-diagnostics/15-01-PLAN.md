---
phase: 15-three-path-diagnostics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/HfDiagnosticsHelpers.h
autonomous: true
requirements:
  - DIAG-02
  - DIAG-03
must_haves:
  truths:
    - "Five standard HF fixtures (impulse, guitar pluck, 220 Hz decay, 880 Hz decay, swept sine) are available from a single shared header"
    - "HfMetrics includes rmsAbove14k plus existing tail spectral fields used by downstream diagnostics"
    - "SchroederTank32 three-path render uses processBlock with diagnostics mode API per path"
  artifacts:
    - path: tests/HfDiagnosticsHelpers.h
      provides: "Shared fixtures, ReverbPath enum, renderTankPath, measureTail, CSV helpers"
  key_links:
    - from: tests/HfDiagnosticsHelpers.h
      to: source/SchroederTank32.h
      via: setAuthentic32ModeForDiagnostics / clearAuthentic32ModeForDiagnostics before processBlock
    - from: tests/HfDiagnosticsHelpers.h
      to: tests/ChainTestHelpers.h
      via: goertzelPower for tail-band metrics
---

<objective>
Extract shared HF diagnostics primitives from HighFrequencyRingingDiagnosticsTest into HfDiagnosticsHelpers.h — fixtures (DIAG-02), full metric suite including RMS >14 kHz (DIAG-03), and three-path tank render primitives supporting DIAG-01.

Purpose: Single source of truth prevents metric drift between diagnostics matrix and TEST-11 regression suite.
Output: tests/HfDiagnosticsHelpers.h (header-only helpers in sendbloom::test namespace).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-three-path-diagnostics/15-RESEARCH.md
@tests/HighFrequencyRingingDiagnosticsTest.cpp
@tests/SchroederTank32BlockTest.cpp
@source/SchroederTank32.h
@tests/ChainTestHelpers.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HfDiagnosticsHelpers.h with fixtures and render constants</name>
  <files>tests/HfDiagnosticsHelpers.h</files>
  <action>
Create tests/HfDiagnosticsHelpers.h in namespace sendbloom::test. Extract from HighFrequencyRingingDiagnosticsTest.cpp without changing numeric behavior:

Constants: kSampleRate (48000 default), kBlockSize (512), kRenderSamples (24000), kTailStart, kTailCount.

Fixture generators (DIAG-02): makeGuitarPluck, makeDecayingSine (220 and 880 Hz via parameter), makeImpulseBurst, makeSweptSine (200–8000 Hz sweep). Match existing formulas and amplitudes exactly.

Provide allFixtures() returning std::vector of {name, input} pairs with the five required fixture names: guitar_pluck, sine220_decay, sine880_decay, impulse, swept_sine.

Do not duplicate forbidden production tokens in comments (comment-text discipline).
  </action>
  <verify>
    <automated>cmake --build build --target Tests 2>&1 | tail -5</automated>
  </verify>
  <done>Header compiles when included; five named fixtures exposed.</done>
</task>

<task type="auto">
  <name>Task 2: Add ReverbPath enum and processBlock render harness</name>
  <files>tests/HfDiagnosticsHelpers.h</files>
  <action>
Add enum class ReverbPath { HostRate, LegacyAccumulator, ProperSRC } and applyPathDiagnostics(SchroederTank32&, ReverbPath) per RESEARCH Pattern 1 (DIAG-01 foundation):

HostRate: clearAuthentic32ModeForDiagnostics, authenticColor false at render.
LegacyAccumulator: setAuthentic32ModeForDiagnostics(Authentic32Mode::LegacyAccumulator), authenticColor true.
ProperSRC: clearAuthentic32ModeForDiagnostics, authenticColor true.

Implement renderTankPath(tank, input, path, sampleRate, blockSize) using processBlock in a block loop (not per-sample). Use ParameterCurves::sizeToRT60(0.5f), darkMix 0.0f. Fresh tank per call or document caller must reset between paths.

This establishes the three-path render contract for downstream AuthenticPathDiagnosticsTest.
  </action>
  <verify>
    <automated>cmake --build build --target Tests 2>&1 | tail -5</automated>
  </verify>
  <done>Three-path render API exists and routes through SchroederTank32::processBlock with correct diagnostics mode per path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: HfMetrics suite with rmsAbove14k and tail analysis</name>
  <files>tests/HfDiagnosticsHelpers.h</files>
  <behavior>
    - measureTail returns rmsAbove10k, rmsAbove14k, peakFrequency, spectralCentroid, maxNarrowbandPeak, rmsTotal
    - narrowbandDominanceRatio matches existing HF test neighbor-band logic
    - imaging14825Rms uses goertzelPower at 14825 Hz on tail window
    - printMetricsCsv includes rms_above_14k column
  </behavior>
  <action>
Port SpectralScan, scanSpectrum, bandRmsAbove, rmsFromPower, measureTail, narrowbandDominanceRatio, printMetricsCsv from HighFrequencyRingingDiagnosticsTest.cpp into the header. Extend HfMetrics with float rmsAbove14k computed via bandRmsAbove(scanFull, 14000.0) (DIAG-03).

Add imaging14825Rms(wet, sampleRate) helper returning tail RMS at 14825 Hz probe.

Add printThreePathCsvHeader() and printThreePathCsvRow(pathLabel, fixture, metrics) for DIAG-01 matrix output to stdout.

Keep Goertzel step sizes and tail window identical to source HF test to avoid metric drift (RESEARCH Pitfall 5).
  </action>
  <verify>
    <automated>cmake --build build --target Tests 2>&1 | tail -5</automated>
  </verify>
  <done>HfMetrics includes all DIAG-03 fields; CSV helpers ready for three-path matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → SchroederTank32 | Offline renders only; diagnostics API must not leak to APVTS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Information Disclosure | setAuthentic32ModeForDiagnostics | low | accept | Test-only API; Phase 14 integrability forbids APVTS exposure |
| T-15-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages this phase — N/A |

</threat_model>

<verification>
cmake --build build --target Tests succeeds with HfDiagnosticsHelpers.h present.
</verification>

<success_criteria>
HfDiagnosticsHelpers.h provides fixtures (DIAG-02), full metrics including rmsAbove14k (DIAG-03), and three-path tank render primitives for DIAG-01.
</success_criteria>

<output>
Create .planning/phases/15-three-path-diagnostics/15-01-SUMMARY.md when done
</output>
