---
phase: 17-latency-pdc-adr-003
plan: 03
type: execute
wave: 3
depends_on:
  - 17-02
files_modified:
  - source/SchroederTank32.h
  - source/GatedBloomChain.h
  - source/PluginProcessor.h
  - source/PluginProcessor.cpp
  - tests/LatencyTest.cpp
autonomous: true
requirements:
  - LAT-03
must_haves:
  truths:
    - "Fresh plugin with authentic_color off reports getLatencySamples() == 0"
    - "After prepareToPlay at 48 kHz with authentic_color on, getLatencySamples() equals SrcLatencyTable 5160"
    - "Reported latency at 44.1, 88.2, and 96 kHz with authentic on matches SrcLatencyTable per rate"
    - "Toggling authentic_color target on prepare edge updates reported latency per ADR Policy A target-path rule"
    - "RC1 default state remains zero reported latency without user enabling authentic_color"
  artifacts:
    - path: source/PluginProcessor.cpp
      provides: "updateReportedLatency() Policy A wiring in prepareToPlay and toggle edge"
    - path: source/GatedBloomChain.h
      provides: "getSrcRoundTripLatencySamples() delegate to tank"
    - path: source/SchroederTank32.h
      provides: "getSrcRoundTripLatencySamples() from FixedRateAdapter"
    - path: tests/LatencyTest.cpp
      provides: "mode-aware latency tests [chain][latency][LAT-03]"
  key_links:
    - from: source/PluginProcessor.cpp
      to: source/GatedBloomChain.h
      via: chain.getSrcRoundTripLatencySamples() when authentic on
    - from: source/GatedBloomChain.h
      to: source/SchroederTank32.h
      via: fixedRate_.getRoundTripLatencySamples() delegate
    - from: source/PluginProcessor.cpp
      to: docs/architecture/ADR-003-proper-32k-src.md
      via: Policy A conditional setLatencySamples implementation
---

<objective>
Implement ADR-003 Policy A host PDC reporting in PluginProcessor and prove mode-aware latency with tests (LAT-03).

Purpose: Honest DAW alignment when ProperSRC active while preserving RC1 zero-latency default when 32k Color off.
Output: Latency query delegates, updateReportedLatency helper, toggle-edge updates, expanded LatencyTest.cpp.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-latency-pdc-adr-003/17-CONTEXT.md
@.planning/phases/17-latency-pdc-adr-003/17-RESEARCH.md
@docs/architecture/ADR-003-proper-32k-src.md
@source/SrcLatencyTable.h
@source/PluginProcessor.cpp
@source/PluginProcessor.h
@source/GatedBloomChain.h
@source/SchroederTank32.h
@source/FixedRateAdapter.h
@tests/LatencyTest.cpp
@.planning/phases/16-engine-crossfade/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Latency query delegates on tank and chain</name>
  <files>source/SchroederTank32.h, source/GatedBloomChain.h</files>
  <action>
Add int getSrcRoundTripLatencySamples() const noexcept to SchroederTank32 returning fixedRate_.getRoundTripLatencySamples() (per 17-RESEARCH open question recommendation).

Add int getSrcRoundTripLatencySamples() const noexcept to GatedBloomChain: if reverb is SchroederTank32, delegate to tank; else return 0 (test engines like Fdn8Reverb).

Use dynamic_cast on reverb.get() or std::visit pattern consistent with codebase — minimal change, no IReverbEngine interface churn unless cast is ugly.

Do not call setLatencySamples here — query only per Architectural Responsibility Map (DSP adapter exposes count; processor reports).
  </action>
  <verify>
    <automated>cd build && cmake --build . --target SendBloom -j8 2>&1 | tail -5</automated>
  </verify>
  <done>Chain can return prepared SRC round-trip sample count from FixedRateAdapter path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PluginProcessor Policy A latency wiring</name>
  <files>source/PluginProcessor.h, source/PluginProcessor.cpp</files>
  <behavior>
    - updateReportedLatency(bool targetAuthenticOn): setLatencySamples(0) when false; setLatencySamples(chain.getSrcRoundTripLatencySamples()) when true
    - prepareToPlay calls updateReportedLatency based on current authentic_color APVTS after chain.prepare
    - On authentic_color smoothed target 0.5 crossing in processBlock (same block as requestEngineCrossfade), call updateReportedLatency(authenticColorTarget > 0.5f) using **target** path per ADR-003
    - Constructor may keep setLatencySamples(0) until first prepare
  </behavior>
  <action>
Add private helper void updateReportedLatency(bool targetAuthenticOn) noexcept to PluginProcessor (declare in .h).

Implementation per ADR-003 Policy A (LAT-03):
- If !targetAuthenticOn: setLatencySamples(0)
- Else: setLatencySamples(chain.getSrcRoundTripLatencySamples())

Call updateReportedLatency from prepareToPlay after chain.prepare using apvts authentic_color raw value > 0.5f.

In processBlock per-sample loop where crossfade edge is detected (alongside chain.requestEngineCrossfade), add updateReportedLatency(authenticColorTarget > 0.5f) — target-path reporting during crossfade per ADR.

Do not add internal dry delay line (Policy B deferred).

Do not leave setLatencySamples(0) as only path when authentic on after prepare.

Try synchronous setLatencySamples on toggle edge first; do not add AsyncUpdater unless tests fail (per research discretion).

Remove or narrow any comment saying PDC deferred to Phase 17 if present near latency calls.
  </action>
  <verify>
    <automated>cd build && cmake --build . --target SendBloom -j8 2>&1 | tail -5</automated>
  </verify>
  <done>Processor reports 0 when authentic off and measured SRC latency when authentic on after prepare and on toggle edge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mode-aware LatencyTest.cpp LAT-03 gates</name>
  <files>tests/LatencyTest.cpp</files>
  <read_first>source/SrcLatencyTable.h, tests/LatencyTest.cpp, docs/architecture/ADR-003-proper-32k-src.md</read_first>
  <behavior>
    - Default/fresh plugin: getLatencySamples() == 0 (RC1)
    - authentic_color off + prepare 48 kHz: latency remains 0
    - authentic_color on + prepare at each table rate: latency equals SrcLatencyTable row
    - Setting authentic on before prepare then prepareToPlay updates latency non-zero
  </behavior>
  <action>
Update tests/LatencyTest.cpp:

Include SrcLatencyTable.h and ParameterIDs.h (already has PluginProcessor).

Retag or clarify existing cases "Plugin reports zero latency samples" and "Plugin latency unchanged after prepare" to assert RC1 path — authentic_color remains default off; keep [chain][latency] tags.

Add TEST_CASE "Plugin reports SRC latency when authentic_color on" with tags [chain][latency][LAT-03]:
- Set apvts authentic_color to 1.0f before prepareToPlay(48000, 512)
- REQUIRE getLatencySamples() == lookupRoundTripLatencySamples(48000.0) (5160)

Add TEST_CASE "Plugin SRC latency tracks host rate with authentic on" [chain][latency][LAT-03]:
- Loop kMeasuredLatencyTable rows; set authentic on; prepareToPlay(row.hostRateHz, 512); REQUIRE getLatencySamples() == row.roundTripSamples

Add TEST_CASE "Plugin latency returns to zero when authentic off after prepare" [chain][latency][LAT-03]:
- prepare 48k with authentic on (non-zero); set authentic off; call update path via prepareToPlay again or re-invoke prepare after param change; REQUIRE getLatencySamples() == 0

Use apvts.getRawParameterValue(ParameterIDs::authenticColor) for param writes matching other tests.

Do not break tail-length tests in same file.
  </action>
  <verify>
    <automated>cd build && cmake --build . --target Tests -j8 && ./Tests "[LAT-03]" -r compact && ./Tests "[chain][latency]" -r compact</automated>
  </verify>
  <done>LAT-03 automated: zero off, measured SRC on at four rates, returns to zero when off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host ↔ plugin | setLatencySamples informs host PDC; incorrect values break session alignment |
| APVTS → processor | authentic_color param drives latency mode |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-05 | — | Wrong latency at 88.2/96 kHz | high | mitigate | Per-rate query from prepared converter; four-rate LAT-03 tests |
| T-17-06 | — | Stale zero latency after toggle on | high | mitigate | Toggle-edge updateReportedLatency alongside crossfade trigger |
| T-17-07 | — | processBlockBypassed mismatch | low | accept | ADR flags bypass delay for Phase 18 pluginval if needed |
| T-17-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages this plan |
</threat_model>

<verification>
ctest -R "LAT-03|LAT-01" --test-dir build; full ctest --test-dir build green.
</verification>

<success_criteria>
LAT-03 satisfied: reported latency matches ADR-003 Policy A; RC1 zero when 32k off.
</success_criteria>

<output>
Create `.planning/phases/17-latency-pdc-adr-003/17-03-SUMMARY.md` when done
</output>
