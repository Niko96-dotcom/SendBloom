---
phase: 17-latency-pdc-adr-003
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - source/SrcLatencyTable.h
  - tests/FixedRateAdapterTest.cpp
autonomous: true
requirements:
  - LAT-01
must_haves:
  truths:
    - "SRC round-trip latency at 44.1, 48, 88.2, and 96 kHz is measured via r8brain getInLenBeforeOutPos(0), not estimated"
    - "Measured sample counts match a single shared constants table used by tests and downstream ADR"
    - "Latency values are stable across reset() and re-prepare() at each host rate"
    - "Cross-check against r8brain legacy getInLenBeforeOutStart(0) matches getRoundTripLatencySamples() with zero delta"
  artifacts:
    - path: source/SrcLatencyTable.h
      provides: "constexpr measured round-trip latency per supported host rate"
    - path: tests/FixedRateAdapterTest.cpp
      provides: "[verb][LAT-01] four-rate table test and API cross-check"
  key_links:
    - from: tests/FixedRateAdapterTest.cpp
      to: source/RateConverterPair.h
      via: prepare(hostRate, 512) then getRoundTripLatencySamples()
    - from: source/SrcLatencyTable.h
      to: source/RateConverterPair.h
      via: expected sample counts derived from same prepare path as production
---

<objective>
Establish authoritative four-rate SRC latency measurements (LAT-01) as tested constants before ADR and PDC wiring.

Purpose: r8brain CDSPResampler::getLatency() always returns 0; PDC requires getInLenBeforeOutPos priming delay. Measured values must not be guessed or hard-coded only in prose.
Output: SrcLatencyTable.h shared constants, LAT-01 Catch2 gates, legacy API cross-check test.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-latency-pdc-adr-003/17-CONTEXT.md
@.planning/phases/17-latency-pdc-adr-003/17-RESEARCH.md
@source/RateConverterPair.h
@source/FixedRateAdapter.h
@tests/FixedRateAdapterTest.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SrcLatencyTable.h measured constants</name>
  <files>source/SrcLatencyTable.h</files>
  <read_first>source/RateConverterPair.h, .planning/phases/17-latency-pdc-adr-003/17-RESEARCH.md</read_first>
  <behavior>
    - Table lists exactly four supported host rates: 44100, 48000, 88200, 96000
    - Round-trip sample counts match offline r8brain measurement (5208, 5160, 8915, 8670)
    - lookupRoundTripLatencySamples(double hostRate) returns matching count or 0 for unsupported rates
    - kMaxHostBlock constant is 512 matching existing prepare() tests
  </behavior>
  <action>
Create source/SrcLatencyTable.h in namespace sendbloom as a header-only measured-latency contract.

Define struct LatencyRow { double hostRateHz; int roundTripSamples; } and constexpr std::array kMeasuredLatencyTable with rows:
44100 → 5208, 48000 → 5160, 88200 → 8915, 96000 → 8670 (per 17-RESEARCH.md verified probe 2026-07-08).

Add static constexpr int kMaxHostBlock = 512.

Implement lookupRoundTripLatencySamples(double hostRateHz) noexcept: iterate table, return roundTripSamples on exact rate match (use integer Hz doubles), else return 0.

Add brief header comment stating values come from sum of upsampler and downsampler getInLenBeforeOutPos(0) after prepare(rate, 512) — not from getLatency().

Do not add runtime measurement or impulse-peak logic here; constants only.
  </action>
  <verify>
    <automated>test -f source/SrcLatencyTable.h && grep -c '5208' source/SrcLatencyTable.h | grep -qv '^0$'</automated>
  </verify>
  <done>Header exists with four-rate constexpr table and lookup helper; values match research measurements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LAT-01 four-rate unit test</name>
  <files>tests/FixedRateAdapterTest.cpp</files>
  <behavior>
    - Fresh RateConverterPair per row; prepare(row.rate, 512); getRoundTripLatencySamples() equals SrcLatencyTable expected
    - Test tagged [verb][LAT-01] for ctest -R LAT-01
    - All four host rates pass in one TEST_CASE loop
  </behavior>
  <action>
Add #include "SrcLatencyTable.h" to tests/FixedRateAdapterTest.cpp.

Add TEST_CASE named "RateConverterPair round-trip latency at four host rates" with tags [verb][LAT-01].

Loop sendbloom::kMeasuredLatencyTable rows: construct fresh sendbloom::RateConverterPair, call prepare(row.hostRateHz, sendbloom::kMaxHostBlock), REQUIRE getRoundTripLatencySamples() == row.roundTripSamples.

Do not assert a single constant for all rates (88.2/96 kHz differ from 48 kHz by thousands of samples).

Keep existing "[verb][FixedRateAdapter][latency]" stability test unchanged.
  </action>
  <verify>
    <automated>cd build && cmake --build . --target Tests -j8 && ./Tests "[LAT-01]" -r compact</automated>
  </verify>
  <done>ctest -R LAT-01 passes; four rates assert measured not estimated values from SrcLatencyTable.</done>
</task>

<task type="auto">
  <name>Task 3: r8brain API cross-check test</name>
  <files>tests/FixedRateAdapterTest.cpp</files>
  <action>
Add TEST_CASE "RateConverterPair latency matches getInLenBeforeOutStart cross-check" with tag [verb][LAT-01].

For each row in kMeasuredLatencyTable: fresh RateConverterPair, prepare(rate, 512).

Independently query upsampler and downsampler priming via getInLenBeforeOutPos(0) if exposed, or add package-visible test hook on RateConverterPair only if resamplers are private — prefer minimal friend test accessor or public debug accessor getUpsamplerPrimingSamples()/getDownsamplerPrimingSamples() const noexcept on RateConverterPair returning getInLenBeforeOutPos(0) per stage (smallest surface change).

Sum priming samples; REQUIRE equals getRoundTripLatencySamples().

If RateConverterPair already exposes enough for cross-check without new accessors, use existing API only.

Also REQUIRE getRoundTripLatencySamples() equals lookupRoundTripLatencySamples(rate).

Tag [verb][LAT-01] so ctest -R LAT-01 includes cross-check.
  </action>
  <verify>
    <automated>cd build && cmake --build . --target Tests -j8 && ./Tests "[LAT-01]" -r compact</automated>
  </verify>
  <done>Priming-sum cross-check passes at all four rates with zero delta vs getRoundTripLatencySamples().</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → DSP adapter | Tests construct RateConverterPair with production prepare params; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Tampering | r8brain pin | low | accept | No new packages; existing CPM pin from Phase 13 unchanged |
| T-17-02 | — | Mis-measured latency constants | medium | mitigate | LAT-01 automated four-rate tests gate constants in SrcLatencyTable.h |
| T-17-SC | Tampering | npm/pip/cargo installs | high | mitigate | Phase installs no packages; no supply-chain surface this plan |
</threat_model>

<verification>
After plan: ctest -R "LAT-01" --test-dir build passes; SrcLatencyTable.h is single source for sample counts.
</verification>

<success_criteria>
LAT-01 satisfied: measured four-rate table exists, tested, cross-checked against r8brain priming API.
</success_criteria>

<output>
Create `.planning/phases/17-latency-pdc-adr-003/17-01-SUMMARY.md` when done
</output>
