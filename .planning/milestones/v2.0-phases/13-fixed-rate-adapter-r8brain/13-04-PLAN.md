---
phase: 13-fixed-rate-adapter-r8brain
plan: 04
type: execute
wave: 3
depends_on:
  - 13-02
  - 13-03
files_modified:
  - source/FixedRateAdapter.h
  - tests/FixedRateAdapterTest.cpp
autonomous: true
requirements:
  - SRC-01
  - SRC-02
  - SRC-03
  - SRC-06
  - TEST-10
must_haves:
  truths:
    - "ProperSRC mode upsamples host block to 32768 Hz, processes SchroederTankCore per internal sample, downsamples to host with FIFO — bandlimited SRC sandwich complete"
    - "processBlock performs zero heap allocation after prepare across 10k varying blocks"
    - "ProperSRC reduces 14825 Hz Goertzel tail power at least 70% versus LegacyAccumulator on impulse at 48 kHz"
    - "reset() clears resampler FIFO, core delay lines, and legacy accumulator; impulse-after-reset matches fresh impulse"
    - "Adapter supports host rates 44.1, 48, 88.2, 96 kHz end-to-end through ProperSRC with variable blocks"
  artifacts:
    - path: source/FixedRateAdapter.h
      provides: "Complete ProperSRC sandwich and reset (SRC-01, SRC-02, SRC-03, TEST-10)"
    - path: tests/FixedRateAdapterTest.cpp
      provides: "SRC-06 imaging gate, reset parity, realtime stress (SRC-06, TEST-10)"
  key_links:
    - from: source/FixedRateAdapter.h
      to: source/RateConverterPair.h
      via: upsample → core loop → downsample in ProperSRC branch
    - from: source/FixedRateAdapter.h
      to: source/SchroederTankCore.h
      via: core.prepare(32768.0) only — never pass hostRate to core (no double scaling)
    - from: tests/FixedRateAdapterTest.cpp
      to: tests/ChainTestHelpers.h
      via: goertzelPower at 14825 Hz for SRC-06 acceptance
---

<objective>
Complete FixedRateAdapter ProperSRC sandwich, enforce realtime-safe processBlock, and deliver SRC-06 imaging reduction plus TEST-10 reset acceptance tests.

Purpose: Prove bandlimited hostRate ↔ 32,768 Hz conversion fixes accumulator HF imaging before Phase 14 GatedBloomChain integration.
Output: Production-ready FixedRateAdapter ProperSRC path and full FixedRateAdapterTest suite.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-fixed-rate-adapter-r8brain/13-RESEARCH.md
@source/FixedRateAdapter.h
@source/RateConverterPair.h
@source/SchroederTankCore.h
@tests/HighFrequencyRingingDiagnosticsTest.cpp
@tests/ChainTestHelpers.h
@tests/FixedRateAdapterTest.cpp
@.planning/phases/13-fixed-rate-adapter-r8brain/13-02-SUMMARY.md
@.planning/phases/13-fixed-rate-adapter-r8brain/13-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProperSRC sandwich and full reset semantics (SRC-01, SRC-02, SRC-03, TEST-10)</name>
  <files>source/FixedRateAdapter.h</files>
  <read_first>source/FixedRateAdapter.h, source/RateConverterPair.h, source/SchroederTankCore.h, .planning/phases/13-fixed-rate-adapter-r8brain/13-RESEARCH.md</read_first>
  <behavior>
    - ProperSRC: float in → upsample to double internal → core.processSample per internal sample with host-path coeffs at 32768 → downsample to float out matching n host samples
    - core always prepared at 32768.0; rt60 and darkMix passed to processSample each internal sample
    - reset() then impulse matches fresh adapter impulse within maxAbsDiff 1e-4f
    - Four host rates 44.1/48/88.2/96 kHz ProperSRC impulse produces finite output
    - Variable blocks 1..512 within prepared max process without throw
  </behavior>
  <action>
Replace ProperSRC stub in FixedRateAdapter::processBlock. Preallocate at prepare: std::vector double internalScratch sized to converters upsampler getMaxOutLen(maxBlockSize); std::vector double internalProcessBuf same size.

ProperSRC branch per block: call converters.upsample(in, n, internalScratch.data()) → nInternal; loop i in 0..nInternal-1: internalProcessBuf[i] = core.processSample(static_cast&lt;float&gt;(internalScratch[i]), rt60, darkMix); call converters.downsample(internalProcessBuf.data(), nInternal, out, n) filling exactly n host samples using leftover FIFO.

Use host-path SchroederTankCore coeffs (bright/dark jmap) — not authentic quantization — for ProperSRC tank at 32768 per RESEARCH authentic coefficient note for SRC-06 impulse bright case.

Ensure no make_unique, resize, or new in processBlock. Clamp n &lt;= maxBlockSize_.

reset() already calls converters.reset(), core.reset(), legacy.reset() — verify all three invoked; add TEST-10 cases in task 2.

Extend tests/FixedRateAdapterTest.cpp: TEST_CASE [verb][FixedRateAdapter][ProperSRC][48] impulse at 48000 bright; TEST_CASE [verb][FixedRateAdapter][ProperSRC][multiRate] four rates; TEST_CASE [verb][FixedRateAdapter][ProperSRC][variableBlock] block rotation at 48000.
  </action>
  <verify>
    <automated>ctest --test-dir build -C Debug -R "FixedRateAdapter.*ProperSRC" --output-on-failure</automated>
  </verify>
  <done>ProperSRC sandwich operational at all four host rates with variable blocks; no heap in processBlock.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SRC-06 imaging gate, reset parity, realtime stress (SRC-06, TEST-10)</name>
  <files>tests/FixedRateAdapterTest.cpp, source/FixedRateAdapter.h</files>
  <read_first>tests/HighFrequencyRingingDiagnosticsTest.cpp, tests/ChainTestHelpers.h, tests/ReverbTestHelpers.h, tests/RealtimeStressTest.cpp</read_first>
  <behavior>
    - At 48000 Hz impulse bright darkMix=0: Goertzel power at 14825 Hz on tail slice shows properPower &lt;= legacyPower * 0.30 (70% reduction)
    - Legacy path produces measurable imaging power above proper path
    - After reset(): second impulse IR matches first within maxAbsDiff 1e-4f for both LegacyAccumulator and ProperSRC modes
    - 10000 processBlock calls with random block sizes 1..512 at 48000 ProperSRC: REQUIRE_NOTHROW, all outputs finite
    - Static scan: FixedRateAdapter.h processBlock body has no make_unique or vector resize tokens (comments excluded)
  </behavior>
  <action>
Add renderAdapterImpulse helper in tests/FixedRateAdapterTest.cpp: FixedRateAdapter adapter; prepare(48000, 512); loop blocks with impulse at 0; processBlock with given Authentic32Mode, rt60 from ParameterCurves::sizeToRT60(0.5f), darkMix=0; return accumulated IR length 24000 matching HighFrequencyRingingDiagnosticsTest kRenderSamples pattern.

TEST_CASE tagged [verb][FixedRateAdapter][SRC-06]: constexpr tailStart = 19200, tailCount = 2400, imagingHz = 14825.0; render legacy and proper IRs; legacyPower = goertzelPower(legacy, 48000, imagingHz, tailStart, tailCount); properPower = goertzelPower(proper, 48000, imagingHz, tailStart, tailCount); REQUIRE properPower &lt;= legacyPower * 0.30; INFO legacy and proper RMS values.

TEST_CASE tagged [verb][FixedRateAdapter][reset][TEST-10]: for modes LegacyAccumulator and ProperSRC: render impulse IR1; reset(); render IR2; REQUIRE maxAbsDiff(IR1, IR2) &lt; 1e-4f using ReverbTestHelpers maxAbsDiff.

TEST_CASE tagged [verb][FixedRateAdapter][realtime][SRC-02]: prepare 48000 max 512; seed RNG; 10000 iterations random block 1..512; feed silence or noise; REQUIRE_NOTHROW processBlock ProperSRC; REQUIRE all outputs finite.

Add TEST_CASE [verb][FixedRateAdapter][SRC-02][static]: read source/FixedRateAdapter.h; locate processBlock function body substring; grep excluding comments for make_unique and .resize( — REQUIRE count zero in processBlock region (use line-range between processBlock opening brace and next public method).

Do not wire adapter to GatedBloomChain or SchroederTank32 — Phase 14 scope.
  </action>
  <verify>
    <automated>ctest --test-dir build -C Debug -R "FixedRateAdapter" --output-on-failure</automated>
  </verify>
  <done>SRC-06 70% imaging reduction passes; TEST-10 reset parity passes; full FixedRateAdapter suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host audio block → FixedRateAdapter::processBlock | Block length and float samples are untrusted; must respect maxBlockSize_ |
| ProperSRC internal → SchroederTankCore | Double internal samples cross into float processSample |
| Test metrics → acceptance | Offline Goertzel power gates production SRC quality |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-09 | Tampering | processBlock block length | medium | mitigate | Reject n &gt; maxBlockSize_; buffers sized at prepare via getMaxOutLen |
| T-13-10 | Denial of Service | Realtime heap alloc | high | mitigate | SRC-02 static scan + 10k block stress test; prepare-only allocation |
| T-13-11 | Tampering | Incomplete reset | medium | mitigate | TEST-10 impulse parity for legacy and ProperSRC; converters.clear + core.reset |
| T-13-12 | Spoofing | SRC-06 false pass | medium | mitigate | Compare against LegacyAccumulator on same fixture; use established 14825 Hz Goertzel contract |

</threat_model>

<verification>
cmake --build build --target Tests
ctest --test-dir build -C Debug -R "FixedRateAdapter" --output-on-failure
Full suite: ctest --test-dir build -C Debug --output-on-failure (regression check)
</verification>

<success_criteria>
All SRC-01–SRC-06 and TEST-10 requirements satisfied; ProperSRC reduces 14825 Hz imaging ≥70% vs legacy; reset clears state; four host rates and variable blocks pass.
</success_criteria>

<output>
Create `.planning/phases/13-fixed-rate-adapter-r8brain/13-04-SUMMARY.md` when done
</output>
