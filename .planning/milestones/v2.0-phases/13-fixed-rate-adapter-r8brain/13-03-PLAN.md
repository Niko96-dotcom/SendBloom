---
phase: 13-fixed-rate-adapter-r8brain
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - source/LegacyAccumulatorPath.h
  - source/FixedRateAdapter.h
autonomous: true
requirements:
  - SRC-04
  - SRC-05
must_haves:
  truths:
    - "LegacyAccumulatorPath reproduces SchroederTank32 processAuthentic accumulator+hold+anti-image SVF behavior for A/B regression"
    - "FixedRateAdapter routes Off and LegacyAccumulator modes without invoking r8brain ProperSRC path"
    - "FixedRateAdapter prepare stores hostRate and maxBlockSize; default mode is Off"
    - "Legacy path renders finite impulse response at 48 kHz distinguishable from silence"
  artifacts:
    - path: source/LegacyAccumulatorPath.h
      provides: "Ported processAuthentic reference path (SRC-05)"
    - path: source/FixedRateAdapter.h
      provides: "Mode switch shell with legacy branch and prepare/reset API (SRC-04)"
  key_links:
    - from: source/LegacyAccumulatorPath.h
      to: source/SchroederTank32.h
      via: behavioral port of processAuthentic, updateCoeffs authentic branch, antiImageFilter
    - from: source/FixedRateAdapter.h
      to: source/Authentic32Mode.h
      via: processBlock mode parameter selects legacy vs future ProperSRC
    - from: source/FixedRateAdapter.h
      to: source/SchroederTankCore.h
      via: core member prepared at kInternalRate for ProperSRC stub (not wired yet)
---

<objective>
Extract LegacyAccumulatorPath from SchroederTank32::processAuthentic and wire FixedRateAdapter shell with Authentic32Mode routing for Off and LegacyAccumulator (SRC-04, SRC-05).

Purpose: Preserve broken accumulator path for SRC-06 A/B comparison before ProperSRC sandwich lands in plan 13-04.
Output: source/LegacyAccumulatorPath.h, source/FixedRateAdapter.h with legacy modes operational.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-fixed-rate-adapter-r8brain/13-RESEARCH.md
@source/SchroederTank32.h
@source/SchroederTankCore.h
@source/Authentic32Mode.h
@tests/FixedRateAdapterTest.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LegacyAccumulatorPath port from processAuthentic (SRC-05)</name>
  <files>source/LegacyAccumulatorPath.h</files>
  <read_first>source/SchroederTank32.h, source/SchroederTank32DelayTable.h, source/DampedComb.h, source/SchroederAllpass.h</read_first>
  <behavior>
    - prepare(48000.0, 512) configures tank at kInternalRate with authentic coefficient quantization
    - processBlock with impulse input produces non-zero tail energy above noise floor
    - reset() clears inputAccumulator, outputHold, lastInternalOut, and delay lines
    - Behavior matches SchroederTank32 processAuthentic stepping ratio kInternalRate/hostRate
  </behavior>
  <action>
Create source/LegacyAccumulatorPath.h in namespace sendbloom. Port authentic-path tank from SchroederTank32 private members: seriesApfs, parallelCombs, tankAp, predelayLine, antiImageFilter, accumulator state (inputAccumulator, outputHold, lastInternalOut, lfoPhase). Copy quantize9bit, scaleDelay (unscaled table integers), syncCombProcessingRate (combs at kInternalRate), updateCoeffs authentic branch (kAuthenticBrightDampingHz, 9-bit quantize), processTank, and processAuthentic loop logic from SchroederTank32.h lines 91–194.

Public API: void prepare(double hostRate, int maxBlockSize) noexcept; void processBlock(const float* in, float* out, int n, float rt60, float darkMix) noexcept; void reset() noexcept.

processBlock: per sample call updateCoeffs once per block start or per sample matching SchroederTank32; run processAuthentic equivalent; write to out[i] with jlimit -4..4 on output.

Do NOT modify SchroederTank32.h public behavior in this task — duplication is intentional until Phase 14 facade wiring.

Add TEST_CASE tagged [verb][FixedRateAdapter][LegacyAccumulator][SRC-05] in tests/FixedRateAdapterTest.cpp: LegacyAccumulatorPath at 48000 Hz; impulse render 24000 samples bright darkMix=0; REQUIRE max absolute sample above 1e-4f in tail region.
  </action>
  <verify>
    <automated>ctest --test-dir build -C Debug -R "LegacyAccumulator" --output-on-failure</automated>
  </verify>
  <done>LegacyAccumulatorPath renders impulse tail; SRC-05 regression path exists for SRC-06 comparison.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FixedRateAdapter shell with mode routing (SRC-04)</name>
  <files>source/FixedRateAdapter.h, tests/FixedRateAdapterTest.cpp</files>
  <read_first>source/Authentic32Mode.h, source/LegacyAccumulatorPath.h, source/RateConverterPair.h, .planning/phases/13-fixed-rate-adapter-r8brain/13-RESEARCH.md</read_first>
  <behavior>
    - Authentic32Mode::Off leaves output buffer silent (zeros) for diagnostics isolation
    - Authentic32Mode::LegacyAccumulator delegates to LegacyAccumulatorPath
    - Authentic32Mode::ProperSRC in this plan returns zeros or copies input unchanged as stub — plan 13-04 replaces stub
    - prepare calls core.prepare(32768.0, maxBlockSize), legacy.prepare(hostRate, maxBlockSize), converters.prepare(hostRate, maxBlockSize)
    - reset() calls core.reset(), legacy.reset(), converters.reset()
  </behavior>
  <action>
Create source/FixedRateAdapter.h including Authentic32Mode.h, LegacyAccumulatorPath.h, RateConverterPair.h, SchroederTankCore.h.

Members: SchroederTankCore core; RateConverterPair converters; LegacyAccumulatorPath legacy_; std::vector float hostScratch; double hostRate_; int maxBlockSize_.

Public API: void prepare(double hostRate, int maxBlockSize) noexcept; void processBlock(const float* in, float* out, int n, float rt60, float darkMix, Authentic32Mode mode) noexcept; void reset() noexcept; int getRoundTripLatencySamples() const noexcept delegating to converters.

prepare: store hostRate_ and maxBlockSize_; core.prepare(SchroederTank32DelayTable::kInternalRate, maxBlockSize); converters.prepare(hostRate, maxBlockSize); legacy.prepare(hostRate, maxBlockSize); hostScratch.assign(maxBlockSize, 0.0f).

processBlock: assert or early-return if n &gt; maxBlockSize_; switch(mode): Off → std::fill(out, out+n, 0.0f); LegacyAccumulator → legacy.processBlock(in, out, n, rt60, darkMix); ProperSRC → stub std::fill(out, out+n, 0.0f) with comment that plan 13-04 implements SRC sandwich. No APVTS wiring.

reset: converters.reset(); core.reset(); legacy.reset().

Append TEST_CASE tagged [verb][Authentic32Mode][SRC-04]: verify three enum values via FixedRateAdapter prepare at 48000 and processBlock with each mode produces expected Off silence vs Legacy non-silence on impulse.
  </action>
  <verify>
    <automated>ctest --test-dir build -C Debug -R "Authentic32Mode|LegacyAccumulator" --output-on-failure</automated>
  </verify>
  <done>FixedRateAdapter routes Off/LegacyAccumulator; ProperSRC stub present; SRC-04 enum integration verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authentic32Mode → FixedRateAdapter | Internal enum only; must not reach APVTS in Phase 13 |
| LegacyAccumulatorPath → host output | Authentic path applies jlimit -4..4 on output samples |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-06 | Tampering | LegacyAccumulatorPath::processBlock | medium | mitigate | jlimit output; jlimit darkMix 0..1; jmax rt60 0.05f inherited from SchroederTank32 |
| T-13-07 | Information Disclosure | Authentic32Mode in headers | low | accept | Enum is compile-time diagnostics only; no user-facing exposure until Phase 15 |
| T-13-08 | Tampering | FixedRateAdapter mode stub | low | mitigate | ProperSRC stub replaced in plan 13-04; tests gate legacy path only in this plan |

</threat_model>

<verification>
cmake --build build --target Tests
ctest --test-dir build -C Debug -R "LegacyAccumulator|Authentic32Mode" --output-on-failure
</verification>

<success_criteria>
Legacy accumulator path operational for A/B; FixedRateAdapter mode switch works for Off and LegacyAccumulator; SchroederTank32 unchanged.
</success_criteria>

<output>
Create `.planning/phases/13-fixed-rate-adapter-r8brain/13-03-SUMMARY.md` when done
</output>
