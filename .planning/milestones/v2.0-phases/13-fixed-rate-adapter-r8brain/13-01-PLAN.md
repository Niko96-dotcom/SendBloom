---
phase: 13-fixed-rate-adapter-r8brain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmake/R8brain.cmake
  - CMakeLists.txt
  - source/Authentic32Mode.h
  - source/SchroederTankCore.h
autonomous: true
requirements:
  - SRC-02
  - SRC-04
  - TEST-10
must_haves:
  truths:
    - "r8brain-free-src 7.1 is fetched via CPM at pin e71c31bf and linked to SharedCode as an INTERFACE target"
    - "Authentic32Mode enum exposes Off, LegacyAccumulator, and ProperSRC with default Off"
    - "SchroederTankCore::reset() clears predelay, comb/APF state, and LFO phase without reallocation"
  artifacts:
    - path: cmake/R8brain.cmake
      provides: "CPM fetch + r8brain INTERFACE include target (SRC-02 foundation)"
    - path: source/Authentic32Mode.h
      provides: "Internal diagnostics mode enum (SRC-04)"
    - path: source/SchroederTankCore.h
      provides: "public reset() for adapter and TEST-10 core state clear"
  key_links:
    - from: CMakeLists.txt
      to: cmake/R8brain.cmake
      via: include(R8brain) after SharedCode target definition
    - from: SharedCode
      to: r8brain INTERFACE target
      via: target_link_libraries(SharedCode INTERFACE r8brain)
    - from: source/SchroederTankCore.h
      to: source/DampedComb.h
      via: reset() calls predelayLine.reset and comb/APF delay-line clears
---

<objective>
Bootstrap r8brain-free-src CMake integration, internal Authentic32Mode enum, and SchroederTankCore reset semantics required by FixedRateAdapter and TEST-10.

Purpose: Establish dependency pin and core reset before RateConverterPair or adapter work begins (per RESEARCH.md Wave 0 gaps).
Output: cmake/R8brain.cmake, CMakeLists.txt wiring, source/Authentic32Mode.h, SchroederTankCore::reset().
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-fixed-rate-adapter-r8brain/13-CONTEXT.md
@.planning/phases/13-fixed-rate-adapter-r8brain/13-RESEARCH.md
@cmake/CPM.cmake
@CMakeLists.txt
@source/SchroederTankCore.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: r8brain CPM fetch and CMake INTERFACE target (SRC-02)</name>
  <files>cmake/R8brain.cmake, CMakeLists.txt</files>
  <read_first>cmake/CPM.cmake, .planning/phases/13-fixed-rate-adapter-r8brain/13-RESEARCH.md</read_first>
  <action>
Create cmake/R8brain.cmake following RESEARCH.md Standard Stack installation block: include(CPM) at top; CPMAddPackage for r8brain-free-src from GITHUB_REPOSITORY avaneev/r8brain-free-src with GIT_TAG e71c31bf320f84210bb4bdcb57e296c39ce940f9 and DOWNLOAD_ONLY YES; add_library(r8brain INTERFACE); target_include_directories(r8brain INTERFACE "${r8brain-free-src_SOURCE_DIR}").

In root CMakeLists.txt after add_library(SharedCode INTERFACE) and before target_sources, add include(R8brain). After existing target_link_libraries(SharedCode INTERFACE ...) block (before juce_add_binary_data or immediately after SharedCode link libs), add target_link_libraries(SharedCode INTERFACE r8brain). Do not add new APVTS parameters or modify plugin formats.

Package legitimacy is pre-approved in RESEARCH.md Package Legitimacy Audit — pin exact commit e71c31bf; no alternate registry packages.
  </action>
  <verify>
    <automated>cmake -B build -DCMAKE_BUILD_TYPE=Debug 2>&1 | tail -5; test -f build/_deps/r8brain-free-src-src/CDSPResampler.h || test -f build/_deps/r8brain-free-src-src/CDSPResampler.h</automated>
  </verify>
  <done>CMake configures successfully; r8brain headers available to SharedCode and Tests via INTERFACE include path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Authentic32Mode enum and SchroederTankCore::reset() (SRC-04, TEST-10)</name>
  <files>source/Authentic32Mode.h, source/SchroederTankCore.h, tests/SchroederTankCoreTest.cpp</files>
  <read_first>source/SchroederTankCore.h, source/SchroederTank32.h, tests/SchroederTankCoreTest.cpp</read_first>
  <behavior>
    - Authentic32Mode has exactly three enumerators: Off, LegacyAccumulator, ProperSRC
    - Default-constructed mode value is Off
    - SchroederTankCore::reset() zeroes LFO phase and clears all delay-line state without changing processingRate_ or delay lengths
    - After reset, second impulse response matches first within maxAbsDiff tolerance
  </behavior>
  <action>
Create source/Authentic32Mode.h in namespace sendbloom with enum class Authentic32Mode : uint8_t { Off, LegacyAccumulator, ProperSRC }. Add brief comment that enum is diagnostics-only — not APVTS or user-facing (SRC-04). No ParameterIDs references.

Add public void reset() noexcept to SchroederTankCore: call predelayLine.reset(); for each seriesApfs and parallelCombs call reset if available or re-push zero samples to clear state per existing JUCE delay atom patterns; call tankAp.reset() or equivalent; set lfoPhase = 0.0f. Do NOT re-run prepare or resize buffers. Do NOT change processingRate_ or call resetDelayLengths unless required to restore delay tap positions — prefer atom reset APIs.

Append TEST_CASE tagged [verb][SchroederTankCore][reset] in tests/SchroederTankCoreTest.cpp: prepare at 32768 Hz; render impulse IR twice with reset() between passes; REQUIRE sendbloom::test::reverb::maxAbsDiff(ir1, ir2) below 1e-5f. Add TEST_CASE tagged [verb][Authentic32Mode] verifying three enum values exist via static_cast round-trip.
  </action>
  <verify>
    <automated>ctest --test-dir build -C Debug -R "SchroederTankCore.*reset|Authentic32Mode" --output-on-failure</automated>
  </verify>
  <done>Authentic32Mode.h compiles; SchroederTankCore reset produces repeatable IR; enum test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CPM fetch → build tree | Third-party header source crosses network at configure time |
| Test fixture → SchroederTankCore | Offline impulse vectors exercise reset path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | CPM r8brain fetch | high | mitigate | Pin git commit e71c31bf; official avaneev/r8brain-free-src repo only per Package Legitimacy Audit |
| T-13-02 | Tampering | SchroederTankCore::reset | low | mitigate | reset() clears state without realloc; verified by impulse parity test |
| T-13-SC | Tampering | CPM package install | high | mitigate | Approved pin only; no npm registry packages; slopcheck on cmake/R8brain.cmake |

</threat_model>

<verification>
cmake -B build -DCMAKE_BUILD_TYPE=Debug && cmake --build build
ctest --test-dir build -C Debug -R "SchroederTankCore.*reset|Authentic32Mode" --output-on-failure
grep confirms Authentic32Mode.h defines Off, LegacyAccumulator, ProperSRC
</verification>

<success_criteria>
r8brain linked to SharedCode; Authentic32Mode enum present; SchroederTankCore::reset() clears state and passes impulse parity test.
</success_criteria>

<output>
Create `.planning/phases/13-fixed-rate-adapter-r8brain/13-01-SUMMARY.md` when done
</output>
