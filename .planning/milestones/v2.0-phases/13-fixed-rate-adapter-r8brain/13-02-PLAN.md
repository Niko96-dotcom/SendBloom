---
phase: 13-fixed-rate-adapter-r8brain
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - source/RateConverterPair.h
  - tests/FixedRateAdapterTest.cpp
autonomous: true
requirements:
  - SRC-01
  - SRC-02
  - SRC-03
must_haves:
  truths:
    - "RateConverterPair upsamples host float blocks to double at 32768 Hz and downsamples back with leftover FIFO between blocks"
    - "CDSPResampler instances and all double scratch buffers are created only in prepare(); process paths use preallocated storage"
    - "Round-trip latency is queryable via getRoundTripLatencySamples() for future Phase 17 PDC work"
    - "Multi-rate prepare smoke at 44.1, 48, 88.2, and 96 kHz produces finite output on impulse without throwing"
  artifacts:
    - path: source/RateConverterPair.h
      provides: "r8brain up/down wrapper with FIFO and reset (SRC-01, SRC-02)"
    - path: tests/FixedRateAdapterTest.cpp
      provides: "RateConverterPair compile link and multi-rate smoke scaffold (SRC-03 partial)"
  key_links:
    - from: source/RateConverterPair.h
      to: CDSPResampler.h
      via: unique_ptr resamplers constructed in prepare with aMaxInLen from getMaxOutLen
    - from: source/RateConverterPair.h
      to: SchroederTank32DelayTable.h
      via: constexpr kInternalRate = 32768.0 for resampler ratio
    - from: tests/FixedRateAdapterTest.cpp
      to: source/RateConverterPair.h
      via: direct unit tests before FixedRateAdapter exists
---

<objective>
Implement RateConverterPair — the r8brain hostRate ↔ 32,768 Hz conversion layer with prepare-time allocation, downsampler FIFO, and multi-rate smoke tests.

Purpose: Isolate r8brain lifecycle and buffer sizing from FixedRateAdapter tank sandwich (RESEARCH Pattern 1).
Output: source/RateConverterPair.h, tests/FixedRateAdapterTest.cpp scaffold with converter-only cases.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-fixed-rate-adapter-r8brain/13-RESEARCH.md
@source/SchroederTank32DelayTable.h
@tests/ChainTestHelpers.h
@.planning/phases/13-fixed-rate-adapter-r8brain/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RateConverterPair r8brain wrapper (SRC-01, SRC-02)</name>
  <files>source/RateConverterPair.h</files>
  <read_first>.planning/phases/13-fixed-rate-adapter-r8brain/13-RESEARCH.md, source/SchroederTank32DelayTable.h</read_first>
  <behavior>
    - prepare(48000.0, 512) constructs upsampler hostRate→32768 and downsampler 32768→hostRate
    - downsampler aMaxInLen equals upsampler getMaxOutLen(maxHostBlock)
    - upsample and downsample process paths return sample counts without heap allocation
    - reset() calls clear() on both resamplers and zeroes leftover FIFO
    - getRoundTripLatencySamples returns upsampler getLatency plus downsampler getLatency
  </behavior>
  <action>
Create header-only source/RateConverterPair.h in namespace sendbloom. Include CDSPResampler.h from r8brain. Use SchroederTank32DelayTable::kInternalRate (32768.0) as fixed internal rate.

Public API: void prepare(double hostRate, int maxHostBlock) noexcept; int upsample(const float* hostIn, int nHost, double* internalOut) noexcept; int downsample(const double* internalIn, int nInternal, float* hostOut, int nHostWanted) noexcept; void reset() noexcept; int getRoundTripLatencySamples() const noexcept.

In prepare: construct std::unique_ptr r8b::CDSPResampler for up (hostRate, 32768, maxHostBlock) and down (32768, hostRate, upsampler->getMaxOutLen(maxHostBlock)). Preallocate std::vector double scratchIn, upOut, downOut sized via getMaxOutLen worst cases. Store hostToInternalRatio = 32768/hostRate for diagnostics only.

upsample: convert float hostIn to double scratchIn; call upsampler process pull loop copying output pointers into internalOut until nHost consumed; return total internal samples produced.

downsample: feed internalIn through downsampler process; maintain leftoverDown_ FIFO for samples exceeding nHostWanted this block; pull loop until hostOut filled or input exhausted. Never call make_unique, resize, or new in upsample/downsample/reset.

reset: upsampler->clear(), downsampler->clear(), leftoverDown_ = 0.
  </action>
  <verify>
    <automated>cmake --build build -C Debug --target Tests 2>&1 | tail -3; test -f source/RateConverterPair.h</automated>
  </verify>
  <done>RateConverterPair.h compiles against r8brain; prepare allocates; process paths are noexcept without heap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Converter multi-rate smoke and variable block tests (SRC-03 partial)</name>
  <files>tests/FixedRateAdapterTest.cpp</files>
  <read_first>tests/RealtimeStressTest.cpp, tests/ReverbTestHelpers.h, source/RateConverterPair.h</read_first>
  <behavior>
    - For host rates 44100, 48000, 88200, 96000: passthrough up→down impulse yields finite samples
    - Variable block sizes 1, 7, 64, 512 within maxBlock 512 all process without throw
    - Round-trip latency is non-negative and stable across two prepare calls at same rate
  </behavior>
  <action>
Create tests/FixedRateAdapterTest.cpp with Catch2. Include RateConverterPair.h and ChainTestHelpers.h.

Add helper renderConverterPassthrough(converter, hostRate, blockSize, totalSamples): prepare converter; loop blocks feeding impulse at sample 0; upsample block to internal buffer preallocated to getMaxOutLen; immediately downsample back to host; accumulate host output until totalSamples reached.

TEST_CASE tagged [verb][FixedRateAdapter][multiRate][SRC-03]: for each hostRate in {44100.0, 48000.0, 88200.0, 96000.0}, render 4096 samples; REQUIRE all std::isfinite.

TEST_CASE tagged [verb][FixedRateAdapter][variableBlock][SRC-03]: at 48000 Hz prepare max 512; process total 2000 samples using block sizes {1, 7, 64, 512} in rotation; REQUIRE_NOTHROW and finite output.

TEST_CASE tagged [verb][FixedRateAdapter][latency]: at 48000 verify getRoundTripLatencySamples greater than 0 and unchanged after reset() without re-prepare.

Do not reference FixedRateAdapter or SchroederTankCore yet — converter-only scope.
  </action>
  <verify>
    <automated>ctest --test-dir build -C Debug -R "FixedRateAdapter.*multiRate|FixedRateAdapter.*variableBlock|FixedRateAdapter.*latency" --output-on-failure</automated>
  </verify>
  <done>Multi-rate and variable-block converter smoke tests pass at four host rates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host float blocks → RateConverterPair | Untrusted block length n must be bounded by maxHostBlock from prepare |
| r8brain double buffers → host float out | SRC conversion crosses float/double boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-03 | Tampering | RateConverterPair::upsample/downsample | medium | mitigate | Reject or assert nHost &lt;= maxHostBlock_; size internal buffers via getMaxOutLen not manual ratio |
| T-13-04 | Denial of Service | prepare-time allocation | medium | mitigate | All CDSPResampler construction in prepare only; no resize in process paths (SRC-02) |
| T-13-05 | Tampering | downsampler FIFO | medium | mitigate | leftoverDown_ carried across blocks per RESEARCH Pitfall 6; covered by variable-block test |

</threat_model>

<verification>
cmake --build build --target Tests
ctest --test-dir build -C Debug -R "FixedRateAdapter" --output-on-failure
</verification>

<success_criteria>
RateConverterPair handles 44.1/48/88.2/96 kHz with variable blocks; zero heap in hot path; tests green.
</success_criteria>

<output>
Create `.planning/phases/13-fixed-rate-adapter-r8brain/13-02-SUMMARY.md` when done
</output>
