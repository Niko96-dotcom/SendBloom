---
phase: 27
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - source/SafeXml.h
  - source/PluginProcessor.cpp
  - source/FactoryPresets.cpp
  - tests/ReleaseTruthTest.cpp
  - tests/XmlDocumentEntityExpansionTest.cpp
  - tests/ZipDecompressionBoundsTest.cpp
requirements: [REL-03, REL-04, REL-20]
---

# Plan 27-01: Close canonical automated blockers honestly

## Objective

Resolve the five known suite failures by following the real production call path and testing SendBloom's actual untrusted XML boundary, without pretending unsupported JUCE generic APIs provide hardening.

## Tasks

1. Add a bounded, DTD-free XML ingestion helper and use it for host state and embedded presets.
2. Update ReleaseTruth to recognize `processBlock -> processSpan -> chain.processBlock`.
3. Replace mismatched generic JUCE XML/GZIP expectations with product-boundary security assertions and accurate JUCE stream semantics.
4. Build Tests and require the complete discovered CTest suite to pass with no failed tests.

## Verification

- Release Tests build succeeds.
- `ctest --test-dir Builds -C Release --output-on-failure` exits zero.
- Security tests prove DTD/entity payloads are rejected before JUCE parsing at product boundaries.
