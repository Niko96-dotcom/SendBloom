---
phase: 25-presets-ui-branding-release-truth
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - source/ui/AdvancedDrawer.cpp
  - 25-SUMMARY.md
autonomous: true
requirements:
  - UX-06
  - UX-11
must_haves:
  truths:
    - "Pressure Mode tooltip in AdvancedDrawer.cpp is verbatim canonical copy with no third-party controller name (UX-06)"
    - "The word 'controller' is intentionally absent from all user-facing strings; no third-party controller product name appears anywhere user-facing"
    - "32k Color 'HF artifacts' warning is either trimmed (if Phase 24 ProperSRC evidence supports removal) or retained with cited evidence (copy-truth decision, not branding)"
    - "Path B satisfies UX-11 this release (procedural faceplate ships); Path A (Niko-approved sendbloom-faceplate.png) is documented as deferred post-RC0 with human_needed asset approval"
    - "Final release-truth confirmation: [v1][contract][shipping-policy] is green and [release][safe] is preserved across Plans 01+02+03"
    - "No human gate is silently passed (BASE-08): UX-11 asset approval and visual sign-off are marked human_needed"
  artifacts:
    - path: "source/ui/AdvancedDrawer.cpp"
      provides: "Canonical Pressure Mode tooltip + verified 32k Color tooltip"
      contains: "Pressure Mode"
    - path: "25-SUMMARY.md"
      provides: "Path A deferral + UX-11 human_needed documentation + release-truth confirmation"
      contains: "human_needed"
  key_links:
    - from: "AdvancedDrawer.cpp pressureModeToggle tooltip"
      to: "UI-SPEC §6 canonical copy"
      via: "verbatim string; single source of truth for Pressure Mode explanation"
      pattern: "Pressure Mode"
    - from: "25-SUMMARY.md"
      to: "Path A deferral + UX-11 human_needed"
      via: "documents that procedural ships now, Niko-approved asset post-RC0"
      pattern: "human_needed"
---

<objective>
Verify the Pressure Mode tooltip is canonical and clean (UX-06), verify or trim the 32k Color "HF artifacts" warning against Phase 24 ProperSRC evidence, document the Path A faceplate-asset deferral as human_needed (UX-11), and produce the final release-truth confirmation that `[v1][contract][shipping-policy]` is green and `[release][safe]` is preserved across all three plans.

Purpose: Close the copy-truth and human-gate items; confirm the phase greens the shipping-policy contract without weakening any release-safe guarantee.
Output: Verified/trimmed AdvancedDrawer tooltips, 25-SUMMARY.md documenting Path A deferral + UX-11 human_needed + release-truth confirmation. UX-06/11 satisfied.
</objective>

<execution_context>
@$HOME/.agents/skills/gsd-execute-phase/SKILL.md
@$HOME/.agents/skills/gsd-capture/SKILL.md
</execution_context>

<context>
@.planning/phases/SENDBLOOM-25-presets-ui-branding-release-truth/25-CONTEXT.md
@.planning/phases/SENDBLOOM-25-presets-ui-branding-release-truth/25-RESEARCH.md
@.planning/phases/SENDBLOOM-25-presets-ui-branding-release-truth/25-UI-SPEC.md
@source/ui/AdvancedDrawer.cpp
@source/ui/AdvancedDrawer.h
@source/ui/PedalFaceplatePaint.cpp
@source/PluginEditor.cpp
@tests/ReleaseTruthTest.cpp
@.planning/MILESTONE-SPEC-v1.0-interaction-truth.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Pressure Mode tooltip is canonical and clean (UX-06 / D-05)</name>
  <files>source/ui/AdvancedDrawer.cpp</files>
  <behavior>
    - pressureModeToggle tooltip is verbatim: "Pressure Mode: when on, wet feed follows pressure; when off, reverb stays always-on."
    - No third-party controller product name appears in the tooltip
    - The word "controller" is absent (intentionally — UI-SPEC §6 rule 2)
    - This string is the single source of truth for the Pressure Mode explanation in the UI
    - No other user-facing string in AdvancedDrawer.cpp / PedalFaceplatePaint.cpp / PluginEditor.cpp contains a third-party controller name
    - If the spec §14.5 longer form is preferred, only ONE form ships (do not ship both)
  </behavior>
  <read_first>
    - 25-CONTEXT.md D-05 (Pressure Mode copy stays verbatim — locked)
    - 25-UI-SPEC.md §6 (Pressure Mode Copy — canonical, already clean; rules 1-4)
    - 25-RESEARCH.md §"Current State" locus 5 (AdvancedDrawer.cpp lines 34-35 already clean) and §"Required Changes & Approach" Area 5 (no change required)
    - .planning/MILESTONE-SPEC-v1.0-interaction-truth.md §14.5 (Pressure Mode tooltip acceptable alternative form)
    - source/ui/AdvancedDrawer.cpp lines 34-35 (current tooltip)
  </read_first>
  <action>
    1. Confirm the pressureModeToggle tooltip string matches the canonical copy exactly. If it does, make no edit (this is primarily a verification task).
    2. Grep all user-facing string literals across AdvancedDrawer.cpp, PedalFaceplatePaint.cpp, PluginEditor.cpp for any third-party controller product name (normalize and compare against banned tokens). The expected user-facing labels are SendBloom-original: ADVANCED, GATE SENS, SEND FEEL, PRESSURE MODE, 32K COLOR, EXTENDED STEREO, DIRT OS, DARK MODE, GATE, PRE, POST, OVERLOAD / CLIP, SAVE, NEW, DELETE, Niko, FX, SENDBLOOM, INITIAL PATCH.
    3. Do NOT reintroduce the word "controller" or any third-party controller name. UI-SPEC §6 rule 2 is binding.
    4. If the spec §14.5 longer form is preferred for clarity, replace the tooltip with exactly that form — but do not ship both forms. Default: keep the existing canonical short form (no edit).
  </action>
  <verify>
    <automated>rg -in "pressure mode" source/ui/AdvancedDrawer.cpp; rg -in "controller" source/ui/ source/PluginEditor.cpp || true</automated>
    <manual>Read every user-facing string in the three UI source files and confirm none references a third-party controller. human_needed for final copy sign-off.</manual>
  </verify>
  <acceptance_criteria>
    - pressureModeToggle tooltip matches canonical copy verbatim
    - rg -in "controller" source/ui/ source/PluginEditor.cpp returns no matches (the word is intentionally absent)
    - No user-facing string contains a banned token (normalized scan across the three UI files)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify/trim 32k Color "HF artifacts" warning against Phase 24 evidence (UX-06 copy-truth / D-05)</name>
  <files>source/ui/AdvancedDrawer.cpp</files>
  <behavior>
    - The "May exhibit HF artifacts at some host rates" line is either removed (if Phase 24 ProperSRC evidence shows no HF artifacts at supported rates) or retained with a cited evidence reference (if artifacts persist at some edge rate)
    - The "Original software — not firmware-derived." line stays (clean, ReleaseTruthTest-pinned via README)
    - The "Experimental — off by default until validated." framing stays accurate
    - The tooltip makes no claim that exceeds current DSP evidence (copy-truth in the spirit of UX-16)
    - ReleaseTruthTest docs assertions (which pin README strings, not tooltip strings) stay green
  </behavior>
  <read_first>
    - 25-CONTEXT.md D-05 (32k Color warning must be verified against Phase 24 ProperSRC evidence; trim if unsupported)
    - 25-UI-SPEC.md §6 rule 4 (32k Color tooltip clean; "HF artifacts" warning potentially stale post-ProperSRC; verify and trim if unsupported)
    - 25-RESEARCH.md §"Current State" locus 5 (AdvancedDrawer.cpp lines 38-43 tooltip) and Pitfall 7 (stale warning is a copy-truth violation)
    - .planning/MILESTONE-SPEC-v1.0-interaction-truth.md §14.5 32k Color (ProperSRC validated against current automated gates; optional/off by default, not "may exhibit HF artifacts" unless current evidence supports)
    - Phase 24 DSP-06 (ProperSRC output pre-cleared), DSP-07 (unwritten samples remain zero), DSP-08 (imaging/HF gates green)
    - source/ui/AdvancedDrawer.cpp lines 38-43 (current 32k Color tooltip)
    - Phase 24 V1ContractSrcUnderfillTest and HF diagnostic test outcomes (grep tests/ for the ProperSRC underfill + HF imaging gates)
  </read_first>
  <action>
    1. Read the Phase 24 ProperSRC evidence: DSP-06 (output pre-clear via std::fill before downsample), DSP-07 (unwritten samples remain zero), DSP-08 (existing ProperSRC imaging/HF gates green). Confirm whether the 32k Color path still exhibits HF artifacts at supported host rates.
    2. Decision:
       - If ProperSRC is clean at all supported rates (44.1/48/88.2/96/176.4/192 kHz per the standard host-rate set): trim the "May exhibit HF artifacts at some host rates; host-rate reverb is the production default." line. Keep the "host-rate reverb is the production default." sentence only if it still adds useful context; otherwise trim the whole clause.
       - If artifacts persist at some edge rate (e.g. non-standard rates outside the supported set): keep the warning but ensure it is evidence-backed — cite the rate(s) and the Phase 24 test that demonstrates the residual.
    3. Default to trimming unless evidence shows residual artifacts (spec §14.5 says ProperSRC has been validated; the warning is stale unless current evidence supports it).
    4. Do NOT touch the "Original software — not firmware-derived." line (clean and pinned).
    5. Do NOT change the off-by-default behaviour or the authentic_color parameter (DSP-14/15).
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target Tests &amp;&amp; Builds/Tests "[release][verb][authentic]" &amp;&amp; Builds/Tests "[v1][contract][shipping-policy]"</automated>
  </verify>
  <acceptance_criteria>
    - 32k Color tooltip either omits the HF-artifacts warning OR retains it with a cited Phase 24 evidence reference
    - "Original software — not firmware-derived." line preserved
    - Builds/Tests "[release][verb][authentic]" exits 0 (README pinned strings unaffected)
    - Builds/Tests "[v1][contract][shipping-policy]" exits 0 (no banned material introduced)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Document Path A deferral + UX-11 human_needed in 25-SUMMARY.md (UX-11 / D-01)</name>
  <files>25-SUMMARY.md</files>
  <behavior>
    - 25-SUMMARY.md explicitly states Path B (procedural chassis) is the production faceplate this release
    - Path A (Niko-approved resources/ui/sendbloom-faceplate.png) is documented as deferred post-RC0
    - UX-11 asset approval is marked human_needed — never silently passed (BASE-08)
    - The deferral is documented so it is not mistaken for a permanent decision (Path A may replace procedural post-RC0, reusing the identical canvas/hotspot grid/overlay coords from UI-SPEC §5)
    - Visual sign-off for the procedural faceplate is marked human_needed (EditorSnapshot coordinates are provable from source, but final pixel sign-off is human)
    - The summary records what flipped green this phase and what remains human_needed
  </behavior>
  <read_first>
    - 25-CONTEXT.md D-01 (Path B now; Path A later — deferred post-RC0, human_needed) and <deferred> section
    - 25-UI-SPEC.md §2 (Path A deferred note — must reuse identical canvas/hotspot/overlay coords) and §7 (Path A deferral documented verification row)
    - 25-RESEARCH.md §"Deferred Ideas" (Path A post-RC0, human_needed)
    - .planning/MILESTONE-SPEC-v1.0-interaction-truth.md §14.3 (Path A preferred when approved; Path B safe fallback)
    - REQUIREMENTS.md UX-11 (Niko-approved original ships, or procedural ships — human_needed for asset approval)
    - BASE-08 (human-only gates marked human_needed, never silently passed)
  </read_first>
  <action>
    1. Create 25-SUMMARY.md at the phase directory (.planning/phases/SENDBLOOM-25-presets-ui-branding-release-truth/25-SUMMARY.md) — this is the phase summary, written as the final plan's output.
    2. Document:
       - Path B is production this release (procedural chassis = sole paint path; SENDBLOOM wordmark; original central art).
       - Path A (Niko-approved sendbloom-faceplate.png) is deferred post-RC0. Asset approval is human_needed (UX-11, BASE-08). Path A must reuse the identical canvas (420x780), hotspot grid, and overlay coords locked in UI-SPEC §5.
       - Visual sign-off for the procedural faceplate is human_needed (coordinates provable from source; final pixel confirmation human).
       - UX-06/07/08/09/10/11/12/13/14/15/16 status: which are green-by-automation vs green-with-human-sign-off.
       - The [v1][contract][shipping-policy] tag is green (flipped by material removal in Plan 01, not scanner weakening).
       - [release][safe] is preserved across all three plans.
    3. Make the human_needed markings explicit and unambiguous — no human gate is represented as automated success (BASE-08 / REL-20 spirit).
  </action>
  <verify>
    <automated>rg -n "human_needed" 25-SUMMARY.md; rg -n "Path A" 25-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - 25-SUMMARY.md exists and documents Path B as production this release
    - 25-SUMMARY.md documents Path A deferral post-RC0 with human_needed asset approval
    - 25-SUMMARY.md marks UX-11 and visual sign-off as human_needed
    - 25-SUMMARY.md records the shipping-policy green flip and release-safe preservation
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Final release-truth confirmation across Plans 01+02+03 (UX-06/11, phase acceptance)</name>
  <files></files>
  <behavior>
    - [v1][contract][shipping-policy] tag exits 0 (UX-07/08/09/10/12 + RT-01 all green)
    - [release][legal] exits 0 (scanner rewrite clean; no banned material in product-facing surface)
    - [release][safe] exits 0 (authentic_color=0 across presets; no preset param regression)
    - [release][preset][xml] exits 0 (preset classification root attribute did not break parity)
    - [release][verb][authentic] exits 0 (README pinned strings + 32k Color docs truth preserved)
    - Phase 19-24 contract regression bundle exits 0 (no regression from Plans 01+02+03)
    - No human gate silently passed (UX-11 / visual sign-off marked human_needed in 25-SUMMARY.md)
  </behavior>
  <read_first>
    - .planning/MILESTONE-SPEC-v1.0-interaction-truth.md §14.9 (Phase 25 acceptance) and lines beyond (no banned product-facing strings/filenames; scanner normalizes; design-qa portable; pre-v1 classification; README/clean-room verified behavior)
    - 25-RESEARCH.md §"Test Posture" (full green/stay-green matrix)
    - 25-UI-SPEC.md §7 (verification matrix)
    - .planning/ROADMAP.md Phase 25 success criteria (4 criteria)
  </read_first>
  <action>
    1. Run the full shipping-policy tag: Builds/Tests "[v1][contract][shipping-policy]" — expect exit 0.
    2. Run the release-truth bundle: [release][legal] [release][safe] [release][preset][xml] [release][verb][authentic].
    3. Run the Phase 19-24 regression bundle: [pressure-release] [oversized-block] [true-bypass] [midi-apvts] [input-anchors] [posthard] [per-sample] [realtime] [authentic] [wet-dirt].
    4. Confirm the four ROADMAP Phase 25 success criteria are met:
       (1) no product-facing third-party string/filename; procedural says SENDBLOOM; reference faceplate removed;
       (2) procedural faceplate ships (UX-11 human_needed for Path A asset approval); hotspots/overlays aligned;
       (3) scanner normalizes + scans filenames; design-qa.md repo-relative with current evidence;
       (4) Pressure Mode explained without third-party controller naming; pre-v1 sessions classified with no migration promise; README/clean-room describe verified behavior.
    5. Record the final green/flipped status in 25-SUMMARY.md (Task 3's artifact).
  </action>
  <verify>
    <automated>Builds/Tests "[v1][contract][shipping-policy]" &amp;&amp; Builds/Tests "[release][legal]" &amp;&amp; Builds/Tests "[release][safe]" &amp;&amp; Builds/Tests "[release][preset][xml]" &amp;&amp; Builds/Tests "[release][verb][authentic]" &amp;&amp; Builds/Tests "[pressure-release]" &amp;&amp; Builds/Tests "[oversized-block]" &amp;&amp; Builds/Tests "[true-bypass]" &amp;&amp; Builds/Tests "[midi-apvts]" &amp;&amp; Builds/Tests "[input-anchors]" &amp;&amp; Builds/Tests "[posthard]" &amp;&amp; Builds/Tests "[per-sample]" &amp;&amp; Builds/Tests "[realtime]" &amp;&amp; Builds/Tests "[authentic]" &amp;&amp; Builds/Tests "[wet-dirt]"</automated>
  </verify>
  <acceptance_criteria>
    - [v1][contract][shipping-policy] exits 0 (the phase's primary green target)
    - All release-truth filters exit 0
    - All Phase 19-24 regression filters exit 0
    - All four ROADMAP Phase 25 success criteria met
    - 25-SUMMARY.md records the final status with human_needed gates explicitly marked
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-facing copy → clean-room policy | No third-party controller name in any tooltip/label |
| Copy → DSP evidence | Tooltip claims (HF artifacts) must not exceed current ProperSRC evidence |
| Human gates → release truth | UX-11 asset approval and visual sign-off must never be silently automated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-03-S | Spoofing | Third-party controller name reintroduced in tooltip | medium | mitigate | UI-SPEC §6 canonical copy locked; grep for "controller" and banned tokens across UI sources (Task 1) |
| T-25-03-I | Information Disclosure | Stale HF-artifacts warning misleads users (UI lies) | medium | mitigate | Verify against Phase 24 DSP-06/07/08; trim if unsupported (Task 2, Pitfall 7) |
| T-25-03-E | Elevation | UX-11 Path A asset approval silently passed as automated | high | mitigate | Mark human_needed explicitly in 25-SUMMARY.md (BASE-08); procedural Path B satisfies UX-11 without asset approval |
| T-25-03-R | Repudiation | Phase 25 declares shipping-policy green without evidence | medium | mitigate | Task 4 runs the full green/stay-green matrix and records results in 25-SUMMARY.md |
| T-25-03-T | Tampering | Path A deferral mistaken for permanent decision | low | mitigate | 25-SUMMARY.md documents Path A may replace procedural post-RC0 reusing locked coords (UI-SPEC §5) |

</threat_model>

<verification>
Builds/Tests "[v1][contract][shipping-policy]" && Builds/Tests "[release][legal]" && Builds/Tests "[release][safe]" && Builds/Tests "[release][preset][xml]" && Builds/Tests "[release][verb][authentic]" && Phase 19-24 regression bundle. 25-SUMMARY.md documents Path A deferral + UX-11 human_needed + final release-truth status.
</verification>

<success_criteria>
- UX-06/11 satisfied: Pressure Mode copy canonical and clean; 32k Color warning evidence-aligned; Path B satisfies UX-11 with Path A deferred human_needed post-RC0
- [v1][contract][shipping-policy] green across all three plans (flipped by material removal, scanner strengthened, not weakened)
- [release][safe] preserved (authentic_color=0; no preset param regression)
- 25-SUMMARY.md records final status with all human_needed gates explicitly marked (BASE-08)
- All four ROADMAP Phase 25 success criteria met
</success_criteria>

## Artifacts this plan produces

| Symbol / File | Plan | Role |
|---------------|------|------|
| Verified canonical Pressure Mode tooltip | 03 | UX-06 single source of truth |
| Evidence-aligned 32k Color tooltip | 03 | UX-06/16 copy-truth |
| `25-SUMMARY.md` | 03 | Path A deferral + UX-11 human_needed + release-truth confirmation |
| Green `[shipping-policy]` + preserved `[release][safe]` | 03 | Phase 25 primary acceptance |

<output>
After completion, create `25-03-SUMMARY.md` (or fold into the phase-level `25-SUMMARY.md` produced by Task 3 — they are the same artifact for this final plan).
</output>
