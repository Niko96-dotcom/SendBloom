---
phase: 25-presets-ui-branding-release-truth
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - resources/presets/Sparkle_Verb.xml
  - resources/presets/Cut_Sample_Gate.xml
  - resources/presets/Spacerock_Burn.xml
  - resources/presets/Dry_Dub_Sends.xml
  - resources/presets/Dark_Bloom.xml
  - resources/presets/Firm_Pressure.xml
  - resources/presets/Gated_Room.xml
  - resources/presets/Hot_Clip.xml
  - design-qa.md
  - docs/CLEAN_ROOM.md
autonomous: true
requirements:
  - UX-13
  - UX-15
  - UX-16
must_haves:
  truths:
    - "All 8 factory preset XMLs carry a root attribute declaring preset_class=\"pre-v1-dev\""
    - "Classification is declared in XML, not enforced by loader code — applyEmbeddedXml stays pure replaceState"
    - "No hidden migration promise: no version check, no state-version wrapper, no conditional rewrite in FactoryPresets.cpp"
    - "All presets keep authentic_color=\"0\" (DSP-14/15 preserved — no param value changes)"
    - "design-qa.md uses only repo-relative paths; no /Users/... absolute paths; no RAINGER FX references"
    - "design-qa.md drops the stale 135/135 count in favour of runtime test-count discovery (RELEASE_CHECKLIST pattern)"
    - "design-qa.md references the procedural faceplate as the source of truth (Path B), not an external PNG"
    - "docs/CLEAN_ROOM.md scanner description reflects normalized matching + filename coverage"
    - "README.md verified compliant (no trim of firmware-derived / 32,768 Hz strings); ReleaseTruthTest docs assertions stay green"
    - "[release][preset][xml] and [release][safe] stay green after classification edit"
    - "[shipping-policy] stays green (Plan 01's flip preserved; no new banned material introduced)"
  artifacts:
    - path: "resources/presets/Sparkle_Verb.xml"
      provides: "pre-v1-dev classification root attribute"
      contains: "preset_class"
    - path: "design-qa.md"
      provides: "Repo-relative design QA with runtime test discovery"
      contains: "PedalFaceplatePaint.cpp"
    - path: "docs/CLEAN_ROOM.md"
      provides: "Updated scanner description (normalized + filename)"
      contains: "normalize"
  key_links:
    - from: "SendBloomParams root element"
      to: "preset_class attribute"
      via: "XML root attribute; applyEmbeddedXml hasTagName check ignores attributes"
      pattern: "preset_class"
    - from: "design-qa.md"
      to: "source/ui/PedalFaceplatePaint.cpp (procedural chassis)"
      via: "repo-relative source-of-truth path"
      pattern: "PedalFaceplatePaint"
---

<objective>
Classify all 8 factory presets as explicit pre-v1 development state (root XML attribute, no loader migration) and rewrite the design-qa and clean-room docs to use repo-relative paths, runtime test-count discovery, and verified-behavior-only claims — so UX-13/15/16 are satisfied while preserving every preset parity and release-safe contract.

Purpose: Make the preset classification honest (pre-v1, no compatibility guarantee) and make the docs portable and current.
Output: 8 XMLs with preset_class="pre-v1-dev", rewritten design-qa.md, updated CLEAN_ROOM.md scanner description. UX-13/15/16 green; [release][safe]/[preset][xml] preserved.
</objective>

<execution_context>
@$HOME/.agents/skills/gsd-execute-phase/SKILL.md
@$HOME/.agents/skills/gsd-capture/SKILL.md
</execution_context>

<context>
@.planning/phases/SENDBLOOM-25-presets-ui-branding-release-truth/25-CONTEXT.md
@.planning/phases/SENDBLOOM-25-presets-ui-branding-release-truth/25-RESEARCH.md
@.planning/phases/SENDBLOOM-25-presets-ui-branding-release-truth/25-UI-SPEC.md
@source/FactoryPresets.cpp
@resources/presets/Sparkle_Verb.xml
@design-qa.md
@docs/CLEAN_ROOM.md
@docs/RELEASE_CHECKLIST.md
@README.md
@tests/ReleaseTruthTest.cpp
@.planning/MILESTONE-SPEC-v1.0-interaction-truth.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add preset_class="pre-v1-dev" root attribute to all 8 factory XMLs (UX-15 / D-03)</name>
  <files>resources/presets/Sparkle_Verb.xml, resources/presets/Cut_Sample_Gate.xml, resources/presets/Spacerock_Burn.xml, resources/presets/Dry_Dub_Sends.xml, resources/presets/Dark_Bloom.xml, resources/presets/Firm_Pressure.xml, resources/presets/Gated_Room.xml, resources/presets/Hot_Clip.xml</files>
  <behavior>
    - Each XML root element becomes <SendBloomParams preset_class="pre-v1-dev">
    - No <PARAM> entry is added, removed, or modified (classification is metadata, not a parameter)
    - All existing PARAM id/value pairs are byte-identical (authentic_color="0" preserved — DSP-14/15)
    - The XML declaration <?xml ...?> and tag name SendBloomParams are unchanged
    - applyEmbeddedXml in FactoryPresets.cpp is NOT modified — it stays a pure replaceState
    - ReleaseTruthTest [release][preset][xml] parity test stays green (it iterates known param IDs only; root attribute is invisible to it)
    - ReleaseTruthTest [release][safe] authentic_color=0 stays green (no param value changed)
  </behavior>
  <read_first>
    - 25-CONTEXT.md D-03 (root attribute or metadata element; no migration promise — locked)
    - 25-RESEARCH.md §"Required Changes & Approach" Area 3 (root attribute rationale: hasTagName ignores attributes; parity test iterates known IDs only)
    - 25-RESEARCH.md Pitfall 6 (root attribute does not break parseXML / replaceState / setStateInformation)
    - .planning/MILESTONE-SPEC-v1.0-interaction-truth.md §14.7 (pre-v1, no hidden state-version migration, factory presets explicitly migrated/classified)
    - source/FactoryPresets.cpp lines 29-38 (applyEmbeddedXml — pure replaceState, must stay unchanged)
    - tests/ReleaseTruthTest.cpp lines 441-488 (preset parity — iterates known param IDs) and 490-516 (authentic_color safe)
    - resources/presets/Sparkle_Verb.xml (canonical structure to mirror across all 8)
  </read_first>
  <action>
    1. For each of the 8 XML files, change the opening root tag from `<SendBloomParams>` to `<SendBloomParams preset_class="pre-v1-dev">`. Leave the closing `</SendBloomParams>` and every <PARAM .../> entry untouched.
    2. Do NOT add any <PARAM id="preset_class"> entry — that would pollute the APVTS ValueTree as an unregistered parameter (misleading). A root attribute is clearly metadata.
    3. Do NOT edit source/FactoryPresets.cpp — applyEmbeddedXml must remain a pure replaceState with no version check, no migration, no conditional. The classification is declared in the file, not enforced at load.
    4. After editing all 8, reconfigure CMake (the XMLs are embedded via juce_add_binary_data) so BinaryData regenerates with the new content.
  </action>
  <verify>
    <automated>cmake -B Builds -S . &amp;&amp; cmake --build Builds --config Release --target Tests &amp;&amp; Builds/Tests "[release][preset][xml]" &amp;&amp; Builds/Tests "[release][safe]"</automated>
  </verify>
  <acceptance_criteria>
    - rg -n 'preset_class="pre-v1-dev"' resources/presets/ returns 8 matches
    - rg -n 'authentic_color" value="0"' resources/presets/ returns 8 matches (unchanged)
    - Builds/Tests "[release][preset][xml]" exits 0 (parity preserved)
    - Builds/Tests "[release][safe]" exits 0 (authentic_color=0 preserved)
    - source/FactoryPresets.cpp applyEmbeddedXml body is unchanged (pure replaceState)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rewrite design-qa.md to repo-relative paths + runtime test discovery (UX-13 / D-04)</name>
  <files>design-qa.md</files>
  <behavior>
    - No /Users/... absolute paths remain anywhere in the file
    - No "RAINGER FX" references remain anywhere in the file
    - Source-of-truth path is repo-relative: source/ui/PedalFaceplatePaint.cpp (procedural chassis is now the truth, per Path B)
    - Artifact/screenshot paths are repo-relative (e.g. artifacts/...) or replaced with the generation command (screenshots are generated, not committed)
    - The stale "135/135" count is removed; replaced with the RELEASE_CHECKLIST runtime-discovery pattern: "Run `ctest --test-dir Builds -N` and record the discovered count + date" (BASE-06: do not hard-code totals)
    - The stale "final result: passed" line is updated to reflect current truth (shipping-policy gates flip green after Plan 01; state this)
    - The stale follow-up about "future production packaging can remove the local development faceplate fallback" is corrected — Path B removes the fallback entirely this phase
    - References the Path B decision (procedural chassis is production) and the Path A deferral (post-RC0, human_needed)
    - screenshot QA references updated to the procedural faceplate (not reverbx-faceplate.png)
  </behavior>
  <read_first>
    - 25-CONTEXT.md D-04 (repo-relative paths, runtime discovery, verified behavior only — locked)
    - 25-RESEARCH.md §"Current State" locus 4 (absolute paths lines 8-19, stale 135/135 line 26, stale follow-up line 30, stale final result line 32)
    - 25-RESEARCH.md §"Required Changes & Approach" Area 4 (full design-qa.md rewrite spec)
    - .planning/MILESTONE-SPEC-v1.0-interaction-truth.md §14.8 (Design QA: no absolute paths, repo-relative, generated screenshot paths, original-branding sign-off, test/build evidence)
    - docs/RELEASE_CHECKLIST.md lines 7, 11, 15 (runtime-discovery pattern to mirror: "Discovers ctest suite size at runtime", "do not claim a fixed N/N total", "Run ctest --test-dir Builds -N ... and record the discovered count")
    - design-qa.md (current full content — to rewrite)
  </read_first>
  <action>
    1. Replace the "Source visual truth path" line: point to `source/ui/PedalFaceplatePaint.cpp` (`paintProceduralChassis`) as the source of truth. Remove the external PNG path.
    2. Replace each `/Users/niko/Documents/RAINGER FX/artifacts/...` path with a repo-relative `artifacts/<name>.png` reference, OR replace with the generation command (e.g. "Generated via the EditorSnapshot target; run `cmake --build Builds --target EditorSnapshot` and capture from the editor viewport"). Prefer the generation-command form since screenshots are not committed.
    3. Remove the `135/135` literal. Replace with the runtime-discovery instruction: "Test gate: run `ctest --test-dir Builds -N` to discover the current suite size; run `ctest --test-dir Builds --output-on-failure` for the pass result. Record the discovered count and date at QA time (do not hard-code a fixed total — BASE-06)."
    4. Update the "Follow-up Polish" section: remove the stale "future production packaging can remove the local development faceplate fallback" line. State that Path B removed the fallback this phase (procedural chassis is the sole production paint path), and Path A (Niko-approved sendbloom-faceplate.png) is deferred post-RC0 (human_needed).
    5. Update "final result" to reflect truth: shipping-policy gates are green after Phase 25 Plan 01; design QA reflects the procedural SENDBLOOM faceplate.
    6. Ensure no line in the rewritten file normalizes to a banned token (reverbx/rainger/igor) — the file is not in the product-facing scanner surface today, but keeping it clean avoids future scanner-surface decisions.
    7. Keep the "Findings" / "Open Questions" / "Implementation Checklist" structure or refactor cleanly — executor discretion, but every locked §14.8 item must be addressable.
  </action>
  <verify>
    <automated>rg -n "/Users/" design-qa.md; rg -in "rainger" design-qa.md; rg -n "135/135" design-qa.md</automated>
    <manual>Read the rewritten design-qa.md end-to-end and confirm every path is repo-relative, the test-count is runtime-discovered (not fixed), and the Path B / Path A deferral is documented.</manual>
  </verify>
  <acceptance_criteria>
    - rg -n "/Users/" design-qa.md returns no matches
    - rg -in "rainger" design-qa.md returns no matches
    - rg -n "135/135" design-qa.md returns no matches
    - design-qa.md references source/ui/PedalFaceplatePaint.cpp as the source of truth
    - design-qa.md documents the runtime-discovery command (ctest --test-dir Builds -N)
    - design-qa.md documents Path B as production and Path A as deferred post-RC0 (human_needed)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update docs/CLEAN_ROOM.md scanner description (UX-16 / D-04)</name>
  <files>docs/CLEAN_ROOM.md</files>
  <behavior>
    - The scanner description reflects the Plan 01 rewrite: normalized matching (lowercase + strip non-alphanumeric) and filename coverage
    - The scan surface is described accurately (source, resources, tests, README, CMake, cmake modules, CI)
    - The allowlist for internal citation docs (.planning/, docs clean-room/release-tracking) is noted per spec §14.4
    - No absolute paths and no new third-party product-facing naming introduced
    - docs/CLEAN_ROOM.md remains OUT of the product-facing scanner surface (Plan 01 excludes docs/ internal docs)
  </behavior>
  <read_first>
    - 25-CONTEXT.md D-04 (CLEAN_ROOM scanner description update)
    - 25-RESEARCH.md §"Current State" locus 4 (CLEAN_ROOM.md line 36 stale description) and §"Required Changes & Approach" Area 4 (CLEAN_ROOM update spec)
    - .planning/MILESTONE-SPEC-v1.0-interaction-truth.md §14.4 (allowlist for cited research)
    - docs/CLEAN_ROOM.md lines 30-45 (current "Legal Metadata Controls" section)
    - scripts/check-legal-metadata.sh (post-Plan-01 rewrite — to describe accurately)
  </read_first>
  <action>
    1. Update the "Legal Metadata Controls" section (around line 36): replace the stale "scans sources, tests, CI, README, and resources/presets/*.xml for banned third-party identifiers" with a description of normalized matching + filename coverage.
    2. Describe the normalization (lowercase, strip spaces/hyphens/underscores/punctuation, match one token) so a reader understands how `Reverb-X`, `REVERB X`, and `reverbx` all collapse to one banned token.
    3. Note the filename scan coverage (asset names, BinaryData symbols, CMake references).
    4. Add a note that internal citation docs (.planning/, docs clean-room and release-tracking) are allowlisted per spec §14.4 because they cite third parties for the clean-room record.
    5. Keep CI-runs-audit-on-every-build-matrix-leg line (still true).
    6. Do NOT add docs/CLEAN_ROOM.md to the scanner surface (it cites names to describe the position — Pitfall 3).
  </action>
  <verify>
    <automated>rg -n "/Users/" docs/CLEAN_ROOM.md; rg -n "normalize" docs/CLEAN_ROOM.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/CLEAN_ROOM.md scanner description mentions normalized matching (or normalization)
    - docs/CLEAN_ROOM.md scanner description mentions filename coverage
    - No absolute paths introduced
    - Builds/Tests "[release][legal]" still exits 0 (CLEAN_ROOM.md not in scanner surface)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Verify README compliance + no over-claim (UX-16 / D-04)</name>
  <files>README.md</files>
  <behavior>
    - README retains the four ReleaseTruthTest-pinned strings: "firmware-derived", "32,768 Hz", and the absence of "EEPROM" and "bytecode"
    - No absolute paths in README
    - No third-party product names in README
    - No circuit-emulation / exact-fidelity claims (those depend on Phase 26 CLAIM_STATUS.md / ADR-V1-17)
    - No trim of load-bearing lines
  </behavior>
  <read_first>
    - 25-CONTEXT.md D-04 (README verify only; describe verified behavior)
    - 25-RESEARCH.md §"Current State" locus 4 (README verified mostly compliant; ReleaseTruthTest 32k Color docs assertions pin 4 strings)
    - tests/ReleaseTruthTest.cpp lines 302-321 (the 4 pinned README assertions)
    - README.md (full current content)
  </read_first>
  <action>
    1. Read README.md end-to-end.
    2. Confirm "firmware-derived" and "32,768 Hz" are present (do NOT trim — they are pinned by ReleaseTruthTest).
    3. Confirm "EEPROM" and "bytecode" are absent (do NOT add).
    4. Trim any line that claims circuit emulation or exact hardware fidelity without evidence (Phase 26 owns claim wording). If none exist, make no edit — this is a verify-only task by default.
    5. If a trim is made, re-run the ReleaseTruthTest docs assertions to confirm the 4 pinned strings still pass.
  </action>
  <verify>
    <automated>Builds/Tests "[release][verb][authentic]"</automated>
  </verify>
  <acceptance_criteria>
    - Builds/Tests "[release][verb][authentic]" exits 0 (the 4 pinned README strings preserved)
    - README contains "firmware-derived" and "32,768 Hz"
    - README contains no absolute paths and no third-party product names
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Regression — preset parity, release-safe, shipping-policy, legal (UX-13/15/16)</name>
  <files></files>
  <behavior>
    - [release][preset][xml] green — root attribute did not break parity
    - [release][safe] green — authentic_color=0 across all 8 presets
    - [v1][contract][shipping-policy] green — Plan 01 flip preserved; no new banned material in docs
    - [release][legal] green — design-qa.md and CLEAN_ROOM.md edits did not introduce banned tokens into the scanner surface
    - design-qa.md and CLEAN_ROOM.md are not in the scanner surface (or, if they were added, they are clean) — confirm no false positive
  </behavior>
  <read_first>
    - 25-RESEARCH.md §"Test Posture" (what must stay green)
    - tests/ReleaseTruthTest.cpp lines 271-278 (legal), 302-321 (docs), 441-488 (parity), 490-516 (safe)
    - scripts/check-legal-metadata.sh (scan surface — confirm design-qa.md / CLEAN_ROOM.md not falsely included)
  </read_first>
  <action>
    1. Run [release][preset][xml] and [release][safe] to confirm preset edits are clean.
    2. Run [v1][contract][shipping-policy] to confirm Plan 01's flip is preserved.
    3. Run [release][legal] to confirm docs edits did not introduce banned tokens into the scanner surface.
    4. Run [release][verb][authentic] to confirm README pinned strings preserved.
    5. If [release][legal] fails, identify whether design-qa.md or CLEAN_ROOM.md was added to the scanner surface and either remove it from the surface (preferred — they are citation docs) or clean the offending line.
  </action>
  <verify>
    <automated>Builds/Tests "[release][preset][xml]" &amp;&amp; Builds/Tests "[release][safe]" &amp;&amp; Builds/Tests "[v1][contract][shipping-policy]" &amp;&amp; Builds/Tests "[release][legal]" &amp;&amp; Builds/Tests "[release][verb][authentic]"</automated>
  </verify>
  <acceptance_criteria>
    - All five filters exit 0
    - rg 'preset_class="pre-v1-dev"' resources/presets/ returns 8 matches
    - design-qa.md and docs/CLEAN_ROOM.md are clean of absolute paths and stale counts
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Preset XML → APVTS state restore | Root attribute must not break replaceState / setStateInformation / parity iteration |
| Docs → scanner surface | design-qa.md / CLEAN_ROOM.md edits must not introduce banned tokens into product-facing scan |
| Docs → release truth | design-qa.md must not claim a fixed test total or stale pass result (BASE-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-02-T | Tampering | Root attribute breaks preset load | medium | mitigate | hasTagName ignores attributes; parity test iterates known IDs; run [release][preset][xml] after edit (Pitfall 6) |
| T-25-02-T2 | Tampering | Hidden migration sneaks into FactoryPresets.cpp | high | mitigate | applyEmbeddedXml stays pure replaceState — no version check, no conditional (D-03) |
| T-25-02-S | Spoofing | design-qa.md re-introduces reverbx path under new spelling | low | mitigate | rg for normalize-collisions; design-qa.md not in scanner surface but kept clean |
| T-25-02-I | Information Disclosure | docs/CLEAN_ROOM.md added to scanner surface, flags its own citation | medium | mitigate | Keep docs/ internal docs out of scan surface (Pitfall 3); scanner description documents the allowlist |
| T-25-02-E | Elevation | README trimmed of pinned strings, breaks ReleaseTruthTest | medium | mitigate | Do not trim "firmware-derived" / "32,768 Hz"; run [release][verb][authentic] after any edit |
| T-25-02-D | Denial of Service | Stale 135/135 count misleads release gate | low | mitigate | Drop fixed count; use runtime discovery (BASE-06 / RELEASE_CHECKLIST pattern) |

</threat_model>

<verification>
Builds/Tests "[release][preset][xml]" && Builds/Tests "[release][safe]" && Builds/Tests "[v1][contract][shipping-policy]" && Builds/Tests "[release][legal]" && Builds/Tests "[release][verb][authentic]". rg confirms 8 preset_class attributes, no absolute paths in design-qa.md, no stale 135/135 count.
</verification>

<success_criteria>
- UX-13/15/16 satisfied: presets classified pre-v1-dev with no migration promise; design-qa.md repo-relative with runtime discovery; README/CLEAN_ROOM describe verified behavior
- [release][preset][xml] and [release][safe] green (preset edit clean)
- [v1][contract][shipping-policy] and [release][legal] green (Plan 01 flip preserved; no new banned material)
- applyEmbeddedXml unchanged (pure replaceState — no hidden migration)
</success_criteria>

## Artifacts this plan produces

| Symbol / File | Plan | Role |
|---------------|------|------|
| `preset_class="pre-v1-dev"` on 8 XMLs | 02 | UX-15 explicit pre-v1 classification |
| Preserved `applyEmbeddedXml` pure replaceState | 02 | UX-15 no migration promise |
| Rewritten `design-qa.md` | 02 | UX-13 repo-relative + runtime discovery |
| Updated `docs/CLEAN_ROOM.md` scanner description | 02 | UX-16 verified-behavior docs |
| Verified `README.md` | 02 | UX-16 pinned strings preserved |

<output>
After completion, create `25-02-SUMMARY.md`
</output>
