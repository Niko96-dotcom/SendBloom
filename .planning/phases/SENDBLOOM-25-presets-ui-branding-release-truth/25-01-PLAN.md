---
phase: 25-presets-ui-branding-release-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - source/ui/PedalFaceplatePaint.cpp
  - source/PluginEditor.cpp
  - CMakeLists.txt
  - scripts/check-legal-metadata.sh
files_deleted:
  - resources/ui/reverbx-faceplate.png
autonomous: true
requirements:
  - UX-07
  - UX-08
  - UX-09
  - UX-10
  - UX-12
  - UX-14
must_haves:
  truths:
    - "Procedural chassis is the sole production paint path; no PNG loadFrom block"
    - "Title wordmark is SENDBLOOM at the unchanged rect (104, 90, 218, 42)"
    - "No third-party-derived central art (vertical letters, giant X, concentric ellipses removed)"
    - "resources/ui/reverbx-faceplate.png is absent from disk, CMake BinaryData, and all source references"
    - "INITIAL PATCH default preset name is visible under Path B (chassis paints it)"
    - "All editor hotspots and overlays remain on their painted targets (zero coordinate changes)"
    - "Scanner normalizes both sides to alnum-only; one token reverbx catches Reverb-X / REVERB X / reverbx"
    - "Scanner walks filenames as well as file contents across the product-facing surface"
    - "[v1][contract][shipping-policy][UX-07] and [UX-08] assertions flip green by material removal"
    - "[release][legal] scanner audit stays exit 0 (Pamplejuce include allowlist preserved)"
  artifacts:
    - path: "source/ui/PedalFaceplatePaint.cpp"
      provides: "SENDBLOOM wordmark + original central art + Path B paint-only body"
      contains: "SENDBLOOM"
    - path: "source/PluginEditor.cpp"
      provides: "INITIAL PATCH gap fix under procedural chassis"
      contains: "INITIAL PATCH"
    - path: "CMakeLists.txt"
      provides: "BinaryData SOURCES without reverbx-faceplate.png"
      contains: "knob.png"
    - path: "scripts/check-legal-metadata.sh"
      provides: "Normalized content + filename scanner"
      contains: "normalize"
  key_links:
    - from: "paintPedalFaceplate"
      to: "paintProceduralChassis + drawStateOverlays"
      via: "direct call; no ImageFileFormat::loadFrom"
      pattern: "paintProceduralChassis"
    - from: "PluginEditor::paint"
      to: "procedural INITIAL PATCH default"
      via: "chassis paints default; editor overrides for selectedId != 1"
      pattern: "INITIAL PATCH"
    - from: "check-legal-metadata.sh normalize"
      to: "BANNED_TOKENS content + filename scan"
      via: "tr alnum-only normalization on both sides"
      pattern: "reverbx"
---

<objective>
Rebrand the procedural pedal chassis as the production SendBloom faceplate (Path B), delete the third-party reference PNG from the shipping binary, and rewrite the legal scanner around normalized matching with filename coverage so the two intentionally-red `[v1][contract][shipping-policy]` assertions (UX-07 faceplate string ban, UX-08 filename ban) flip green by material removal — never by weakening the scanner.

Purpose: Make the shipping binary clean-room truthful (no third-party faceplate art/name/filename) and make the scanner actually catch all spelling variants.
Output: SENDBLOOM wordmark + original central art in PedalFaceplatePaint.cpp, gutted paintPedalFaceplate, removed CMake entry, deleted PNG, fixed INITIAL PATCH gap, normalized scanner. UX-07/08/09/10/12/14 green.
</objective>

<execution_context>
@$HOME/.agents/skills/gsd-execute-phase/SKILL.md
@$HOME/.agents/skills/gsd-capture/SKILL.md
</execution_context>

<context>
@.planning/phases/SENDBLOOM-25-presets-ui-branding-release-truth/25-CONTEXT.md
@.planning/phases/SENDBLOOM-25-presets-ui-branding-release-truth/25-RESEARCH.md
@.planning/phases/SENDBLOOM-25-presets-ui-branding-release-truth/25-UI-SPEC.md
@source/ui/PedalFaceplatePaint.cpp
@source/ui/PedalFaceplatePaint.h
@source/PluginEditor.cpp
@scripts/check-legal-metadata.sh
@tests/V1ContractShippingPolicyTest.cpp
@tests/ReleaseTruthTest.cpp
@CMakeLists.txt
@.planning/MILESTONE-SPEC-v1.0-interaction-truth.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Title wordmark swap REVERB X → SENDBLOOM (UX-09 / D-01)</name>
  <files>source/ui/PedalFaceplatePaint.cpp, tests/V1ContractShippingPolicyTest.cpp</files>
  <behavior>
    - paintProceduralChassis draws "SENDBLOOM" (uppercase, one word) at the unchanged rect (104, 90, 218, 42)
    - Font stays juce::FontOptions(34.0f, juce::Font::bold), colour black, centred, max scale 0.86f
    - Cyan underline segments (48,113)→(112,113) and (312,113)→(372,113), stroke 5.0f — unchanged
    - The literal "REVERB X" no longer appears anywhere in PedalFaceplatePaint.cpp
    - V1ContractShippingPolicyTest [UX-07] assertion (faceplate.find("REVERB X") == npos) flips to pass
  </behavior>
  <read_first>
    - 25-UI-SPEC.md §1.1 (wordmark contract — locked rect, font, colour, underline)
    - 25-RESEARCH.md §"Required Changes & Approach" Area 1 Step 1 (1:1 swap)
    - source/ui/PedalFaceplatePaint.cpp lines 300-306 (current title + underline)
    - tests/V1ContractShippingPolicyTest.cpp lines 91-101 (UX-07 assertion)
  </read_first>
  <action>
    1. In paintProceduralChassis, replace the single drawFittedText literal "REVERB X" with "SENDBLOOM". Leave the rect (104, 90, 218, 42), font, colour, justification, and max scale 0.86f untouched.
    2. Do NOT touch the cyan underline drawLine calls — UI-SPEC §1.1 locks them.
    3. Do NOT touch drawLogo (Niko/FX plate is SendBloom-original).
    4. Re-run the [UX-07] test to confirm it now passes (the assertion already exists; no test edit needed beyond verifying).
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target Tests &amp;&amp; Builds/Tests "[v1][contract][shipping-policy][UX-07]"</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "SENDBLOOM" source/ui/PedalFaceplatePaint.cpp matches the wordmark line
    - rg -n "REVERB X" source/ui/PedalFaceplatePaint.cpp returns no matches
    - Builds/Tests "[v1][contract][shipping-policy][UX-07]" exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Remove third-party central art, replace with original SendBloom geometry (UX-07 / D-01)</name>
  <files>source/ui/PedalFaceplatePaint.cpp</files>
  <behavior>
    - Vertical "REVERB" letter loop (lines 320-323) removed
    - Standalone giant "X" glyph (lines 325-326) removed
    - Four concentric ellipses around the X (lines 327-332) removed
    - Replacement art is confined to the existing leftPanel rect (58, 195, 154, 219) — no panel coord change (UX-14)
    - Replacement uses ONLY the locked palette (cyan, black, white gradient, optional accent orange) — no new colours
    - "OVERLOAD / CLIP" header label (line 318) retained verbatim
    - No letterform in the replacement spells or abbreviates any product/brand name
    - No hotspot, knob, or overlay lives in the reworked region (verified: the clip lamp at (174,191) is in the panel header band, not the art region)
  </behavior>
  <read_first>
    - 25-UI-SPEC.md §1.3 (central art replacement contract — permitted motifs, palette, region)
    - 25-UI-SPEC.md §3 (locked palette — no new colours)
    - 25-UI-SPEC.md §5.3 (overlay coords — clip lamp (174,191,19,19) is the only overlay near the panel; sits in header band)
    - source/ui/PedalFaceplatePaint.cpp lines 313-332 (current panel + third-party art)
    - source/ui/PedalFaceplatePaint.cpp drawChevronRail (lines 56-72) — existing original motif to reuse/extend
  </read_first>
  <action>
    1. Delete lines 320-323 (vertical REVERB letter loop) and the `constexpr const char* reverb = "REVERB";` declaration.
    2. Delete lines 325-326 (giant "X" drawFittedText).
    3. Delete lines 327-332 (concentric ellipses loop).
    4. Within the leftPanel rect (58, 195, 154, 219), paint original SendBloom abstract geometry. Permitted motifs (pick one or combine, executor discretion per UI-SPEC §1.3): extend drawChevronRail into the panel, paint concentric rounded rects, or render an abstract geometry motif (e.g. a chevron stack or rounded-rect nested frame). Use only cyan / black / white-with-alpha from the locked palette.
    5. Keep the "OVERLOAD / CLIP" header drawText (line 318) and the leftPanel drawRect (lines 314-315) exactly as-is.
    6. Do NOT introduce any string literal that could normalize to a banned token. Abstract geometry only — no text in the replacement.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target SendBloom_All &amp;&amp; rg -n "REVERB|\"X\"" source/ui/PedalFaceplatePaint.cpp || true</automated>
    <manual>Run tools/EditorSnapshot (or build EditorSnapshot target) and visually confirm the central panel shows original geometry, the wordmark reads SENDBLOOM, and no third-party letterform remains. human_needed for final visual sign-off.</manual>
  </verify>
  <acceptance_criteria>
    - rg -n "reverb\[|drawEllipse|\"X\"" source/ui/PedalFaceplatePaint.cpp returns no matches in the central-art region
    - Build succeeds; EditorSnapshot renders the panel with original art only
    - No new juce::Colour literal introduced outside the locked palette in UI-SPEC §3
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Gut PNG load path — procedural chassis becomes sole paint path (UX-10 / D-01)</name>
  <files>source/ui/PedalFaceplatePaint.cpp</files>
  <behavior>
    - paintPedalFaceplate body is exactly: paintProceduralChassis(...); drawStateOverlays(...);
    - No juce::ImageFileFormat::loadFrom call remains
    - No BinaryData::reverbxfaceplate_png / reverbxfaceplate_pngSize reference remains
    - No File::getCurrentWorkingDirectory().getChildFile("resources/ui/reverbx-faceplate.png") reference remains
    - #include <BinaryData.h> may stay (BinaryData still serves preset XMLs + knob.png) — leaving it is safe
    - Signature of paintPedalFaceplate is unchanged (callers in PluginEditor.cpp unaffected)
  </behavior>
  <read_first>
    - 25-UI-SPEC.md §2 (Asset Removal Contract — locked body)
    - 25-RESEARCH.md §"Required Changes & Approach" Area 1 Step 3 (gut order before CMake edit)
    - source/ui/PedalFaceplatePaint.cpp lines 413-436 (current paintPedalFaceplate)
    - source/ui/PedalFaceplatePaint.h (signature contract)
  </read_first>
  <action>
    1. Replace the entire body of paintPedalFaceplate (lines 422-436) with the two-line Path B body:
       paintProceduralChassis(g, bounds, cyan, advancedExpanded);
       drawStateOverlays(g, apvts, clipActive, advancedExpanded, padPressed, padDisplayAmount);
    2. Remove the now-dead `auto faceplate = juce::ImageFileFormat::loadFrom(...)` block entirely.
    3. CRITICAL ORDERING: this Task 3 must land in the same commit (or before) Task 4's CMake edit. Removing the CMake entry first would leave a dangling BinaryData::reverbxfaceplate_png symbol reference and break compilation (Pitfall 1).
    4. Leave #include <BinaryData.h> in place — other code paths in this TU do not use it, but it is harmless and BinaryData still exists for presets/knob.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target SendBloom_All &amp;&amp; rg -n "reverbx|ImageFileFormat::loadFrom" source/ui/PedalFaceplatePaint.cpp || true</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "reverbxfaceplate_png|ImageFileFormat::loadFrom" source/ui/PedalFaceplatePaint.cpp returns no matches
    - Build succeeds (compiles clean before CMake edit in Task 4)
    - paintPedalFaceplate body contains exactly paintProceduralChassis + drawStateOverlays calls
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Remove reverbx-faceplate.png from CMake BinaryData (UX-08 / D-01)</name>
  <files>CMakeLists.txt</files>
  <behavior>
    - juce_add_binary_data(SendBloomPresets SOURCES ...) contains exactly 9 entries: 8 preset XMLs + knob.png
    - BinaryData::reverbxfaceplate_png and BinaryData::reverbxfaceplate_pngSize symbols are no longer generated
    - V1ContractShippingPolicyTest [UX-08] assertion (containsIgnoreCase(cmake, "reverbx") == false) flips to pass
    - Build still succeeds (Task 3 already removed all source references)
  </behavior>
  <read_first>
    - 25-UI-SPEC.md §2 (CMake removal row)
    - 25-RESEARCH.md Pitfall 1 (source → CMake → delete ordering)
    - CMakeLists.txt lines 72-83 (current BinaryData block)
    - tests/V1ContractShippingPolicyTest.cpp lines 103-112 (UX-08 assertion)
  </read_first>
  <action>
    1. Delete line 82 (`resources/ui/reverbx-faceplate.png`) from the juce_add_binary_data SOURCES list.
    2. Ensure the list still ends with `resources/ui/knob.png)` — 9 entries total.
    3. This edit MUST come after Task 3 (source gut) in commit order to avoid an undefined-symbol compile break.
    4. Reconfigure CMake (cmake -B Builds ...) so BinaryData regenerates without the PNG symbol.
  </action>
  <verify>
    <automated>cmake -B Builds -S . &amp;&amp; cmake --build Builds --config Release --target SendBloom_All &amp;&amp; Builds/Tests "[v1][contract][shipping-policy][UX-08]"</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "reverbx" CMakeLists.txt returns no matches
    - Builds/Tests "[v1][contract][shipping-policy][UX-08]" exits 0
    - Build succeeds with regenerated BinaryData (no undefined reverbxfaceplate_png symbol)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Delete reverbx-faceplate.png from disk (UX-10 / D-01)</name>
  <files>resources/ui/reverbx-faceplate.png</files>
  <behavior>
    - resources/ui/reverbx-faceplate.png no longer exists in the working tree
    - resources/ui/knob.png is untouched (orthogonal knob asset, out of scope)
    - No CMake or source reference to the deleted file remains (Tasks 3-4 already cleared them)
    - The deletion is a clean-room policy gain, not a loss
  </behavior>
  <read_first>
    - 25-CONTEXT.md D-01 (delete asset — clean-room policy prohibits shipping)
    - 25-RESEARCH.md §"Current State" locus 1 (file is 379944 bytes on disk)
    - 25-UI-SPEC.md §2 (Delete file row + Keep knob.png row)
  </read_first>
  <action>
    1. Run: git rm resources/ui/reverbx-faceplate.png
    2. Verify resources/ui/ now contains only knob.png (and any other non-reverbx assets).
    3. This Task 5 MUST come after Tasks 3 and 4 in commit order — deleting the file before removing the CMake entry would break the build (Pitfall 1).
  </action>
  <verify>
    <automated>test ! -e resources/ui/reverbx-faceplate.png &amp;&amp; test -e resources/ui/knob.png &amp;&amp; echo "asset deletion verified"</automated>
  </verify>
  <acceptance_criteria>
    - resources/ui/reverbx-faceplate.png does not exist
    - resources/ui/knob.png still exists
    - git status shows the PNG as deleted
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Fix INITIAL PATCH default-name gap under Path B (UX-14 / D-01)</name>
  <files>source/ui/PedalFaceplatePaint.cpp, source/PluginEditor.cpp</files>
  <behavior>
    - When presetBox.getSelectedId() == 1 (default), the preset name field shows "INITIAL PATCH"
    - When getSelectedId() != 1, the editor paints the selected preset name on a white field (existing behaviour preserved)
    - No double-render: chassis paints the default, editor paints overrides — exactly one option applied
    - The (64, 148, 210, 16) hotspot is preserved and the visual style matches the editor's redraw
    - "INITIAL PATCH" is original SendBloom copy (not third-party) — safe to bake
  </behavior>
  <read_first>
    - 25-UI-SPEC.md §5.2 (KNOWN GAP — default preset name field, Option A vs B)
    - 25-RESEARCH.md §"Current State" locus 1 (PluginEditor.cpp lines 170-181 gap) and Pitfall 5 (do not mix options)
    - source/PluginEditor.cpp lines 159-182 (paint + INITIAL PATCH conditional)
    - source/ui/PedalFaceplatePaint.cpp paintProceduralChassis (where to add the default paint)
  </read_first>
  <action>
    1. Pick ONE option (UI-SPEC expresses preference for Option A; both acceptable):
       - Option A (preferred): in paintProceduralChassis, after the title/underline block (after line 306) and before drawCyanFrame, add:
         g.setColour(juce::Colours::black);
         g.setFont(juce::FontOptions(12.0f, juce::Font::bold));
         g.drawText("INITIAL PATCH", 70, 148, 200, 14, juce::Justification::centredLeft);
         Leave the PluginEditor.cpp `if (presetBox.getSelectedId() != 1)` conditional unchanged.
       - Option B: remove the `if (presetBox.getSelectedId() != 1)` conditional in PluginEditor::paint so the editor always paints the preset name (white background 70,148,200,14 + black text). The chassis paints nothing there.
    2. Do NOT implement both — Pitfall 5 warns this double-renders or blanks the field.
    3. Update the stale comment at PluginEditor.cpp line 170 ("Default INITIAL PATCH is baked into the faceplate") to match the chosen option's reality.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target SendBloom_All EditorSnapshot</automated>
    <manual>Run EditorSnapshot for the default preset and confirm "INITIAL PATCH" is visible in the preset name field. human_needed for visual sign-off.</manual>
  </verify>
  <acceptance_criteria>
    - Default-preset EditorSnapshot shows "INITIAL PATCH" in the name field
    - rg -n "INITIAL PATCH" source/ matches in chassis (Option A) or editor-only (Option B), not both painting the default
    - Build succeeds; no double-render artefact
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 7: Rewrite scanner with normalized matching + filename coverage (UX-07/08/12 / D-02)</name>
  <files>scripts/check-legal-metadata.sh</files>
  <behavior>
    - normalize() function: lowercase, then strip all non-alphanumeric chars (tr '[:upper:]' '[:lower:]' | tr -cd '[:alnum:]')
    - One normalized banned token "reverbx" catches Reverb-X, REVERB X, reverbx (all collapse to reverbx)
    - Banned tokens include the normalized forms of: company name, product name, proprietary controller name, and the existing Pamplejuce/Pamp/P001/Igor concepts where product-facing
    - Content scan: normalize each product-facing file's full content, match banned tokens as substrings
    - Filename scan: normalize each scanned file's repo-relative path, match banned tokens as substrings
    - Required terms (SendBloom, NkMo, SbLm, Niko Audio Labs) normalized on BOTH sides — required token "nikoaudiolabs" matches normalized CMake content
    - Scan surface: source, resources, tests, README.md, CMakeLists.txt, cmake, cmake-local, .github/workflows (product-facing only)
    - Explicitly EXCLUDE .planning/ and docs/ internal citation docs (CLEAN_ROOM.md, RELEASE_CHECKLIST.md, THIRD_PARTY_LICENSES.md) per spec §14.4
    - Pamplejuce allowlist preserved: CMake include(Pamplejuce*) directives are skipped (build-template dependency, not a product reference)
    - r8brain / MIT citation check preserved (orthogonal dependency-license audit)
    - Exit-code semantics identical: 0 = clean, non-zero = violation (ReleaseTruthTest [release][legal] and verify-v1.sh gate [1/6] depend on this)
  </behavior>
  <read_first>
    - 25-CONTEXT.md D-02 (normalized matching + filename coverage — locked)
    - 25-UI-SPEC.md §1.4 (copywriting rule — binding on filenames and BinaryData symbols)
    - 25-RESEARCH.md §"Current State" locus 2 (three concrete scanner failures) and §"Required Changes & Approach" Area 2 (full rewrite pseudocode)
    - 25-RESEARCH.md Pitfalls 2 (Pamplejuce allowlist), 3 (docs citation exclusion), 4 (normalize both sides for required terms)
    - .planning/MILESTONE-SPEC-v1.0-interaction-truth.md §14.4 (normalize both sides, filename scan, allowlist for cited research)
    - scripts/check-legal-metadata.sh (current literal-grep implementation)
    - tests/ReleaseTruthTest.cpp lines 271-278 (std::system invocation, exit 0 = pass)
  </read_first>
  <action>
    1. Define normalize() at top of script: `normalize() { tr '[:upper:]' '[:lower:]' | tr -cd '[:alnum:]'; }`
    2. Define BANNED_TOKENS as normalized alnum-only tokens: "reverbx" "rainger" "igor". (Pamplejuce/Pamp handled via allowlist below; keep them as tokens but allowlist the include() directive.)
    3. Define REQUIRED_TOKENS as normalized: "sendbloom" "nkmo" "sblm" "nikoaudiolabs".
    4. Required-term check: `cmake_norm=$(normalize < CMakeLists.txt)`; assert each REQUIRED_TOKEN is a substring of cmake_norm.
    5. scan_content(file): `content_norm=$(normalize < "$file")`; for each banned token, fail if substring match.
    6. scan_filename(relpath): `path_norm=$(echo "$relpath" | normalize)`; for each banned token, fail if substring match.
    7. For CMakeLists.txt specifically, preserve the Pamplejuce include() allowlist: skip lines matching `include(pamplejuce` (normalized) before content-scanning the rest. Do NOT blanket-allowlist Pamplejuce everywhere — only the include() directive.
    8. Define scan surface: source tests README.md CMakeLists.txt cmake cmake-local .github/workflows resources/presets resources/ui. For each file: run scan_content AND scan_filename (relative path from repo root). Use find with a broadened extension list (add *.png *.cmake) so binary/asset filenames are covered by the filename scan.
    9. Keep docs/THIRD_PARTY_LICENSES.md and README.md r8brain+MIT citation check unchanged (orthogonal).
    10. Do NOT add docs/RELEASE_CHECKLIST.md or docs/CLEAN_ROOM.md to the scan surface — they cite third-party names for clean-room record (spec §14.4 allowlist; Pitfall 3).
    11. Echo "Legal metadata audit passed." and exit 0 on clean.
  </action>
  <verify>
    <automated>bash scripts/check-legal-metadata.sh &amp;&amp; Builds/Tests "[release][legal]"</automated>
    <manual>Negative test (do not commit): temporarily re-add a "reverbx" token to a product-facing file and confirm the scanner exits non-zero with a clear error; then revert. Also confirm the three raw variants (Reverb-X, REVERB X, reverbx) each trigger detection when placed in a scratch fixture.</manual>
  </verify>
  <acceptance_criteria>
    - bash scripts/check-legal-metadata.sh exits 0 on the clean repo (after Tasks 1-5)
    - Builds/Tests "[release][legal]" exits 0
    - Scratch negative test: each of Reverb-X, REVERB X, reverbx in a scanned file triggers exit non-zero
    - Scratch negative test: a file named with a banned token in its path triggers exit non-zero
    - Pamplejuce include() lines in CMakeLists.txt do NOT trigger a failure
    - docs/RELEASE_CHECKLIST.md line 36 (cites names literally) does NOT trigger a failure (excluded from surface)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 8: Verify hotspot/overlay alignment + shipping-policy flip + regression (UX-14 / D-02)</name>
  <files></files>
  <behavior>
    - Every setBounds in PluginEditor::resized (lines 184-216) maps to a procedural-chassis painted target — zero coordinate changes (UI-SPEC §5.1 table)
    - Every overlay in drawStateOverlays aligns to procedural coords — zero changes (UI-SPEC §5.3 table)
    - [v1][contract][shipping-policy] tag as a whole now exits 0 (UX-07 + UX-08 + RT-01 all green)
    - [release][legal] scanner audit exits 0
    - Phase 19-24 contract tags stay green (no regression from faceplate/scanner edits)
    - [release][safe] authentic_color=0 preserved (no preset param touched in this plan)
  </behavior>
  <read_first>
    - 25-UI-SPEC.md §5.1 (hotspot → paint alignment table) and §5.3 (overlay coords table)
    - 25-RESEARCH.md §"Test Posture" (what flips green, what must stay green)
    - tests/V1ContractShippingPolicyTest.cpp lines 114-127 (RT-01 — must stay green)
    - tests/ReleaseTruthTest.cpp lines 271-278 (legal), 441-488 (preset parity), 490-516 (authentic_color safe)
  </read_first>
  <action>
    1. Diff PluginEditor::resized against the UI-SPEC §5.1 table — confirm no coordinate drifted (this plan made zero edits to resized()).
    2. Run the full shipping-policy tag: Builds/Tests "[v1][contract][shipping-policy]" — expect exit 0 now.
    3. Run the legal audit: Builds/Tests "[release][legal]" — expect exit 0.
    4. Run the Phase 19-24 regression bundle: [pressure-release] [oversized-block] [true-bypass] [midi-apvts] [input-anchors] [posthard] [per-sample] [realtime] [authentic] [wet-dirt].
    5. Run [release][safe] to confirm authentic_color=0 across presets (no preset edited this plan).
    6. Run the preset XML/program parity test ([release][preset][xml]) to confirm no preset regression.
  </action>
  <verify>
    <automated>Builds/Tests "[v1][contract][shipping-policy]" &amp;&amp; Builds/Tests "[release][legal]" &amp;&amp; Builds/Tests "[release][safe]" &amp;&amp; Builds/Tests "[release][preset][xml]" &amp;&amp; Builds/Tests "[pressure-release]" &amp;&amp; Builds/Tests "[oversized-block]" &amp;&amp; Builds/Tests "[true-bypass]" &amp;&amp; Builds/Tests "[midi-apvts]" &amp;&amp; Builds/Tests "[input-anchors]" &amp;&amp; Builds/Tests "[posthard]" &amp;&amp; Builds/Tests "[per-sample]" &amp;&amp; Builds/Tests "[realtime]" &amp;&amp; Builds/Tests "[authentic]" &amp;&amp; Builds/Tests "[wet-dirt]"</automated>
  </verify>
  <acceptance_criteria>
    - Builds/Tests "[v1][contract][shipping-policy]" exits 0 (UX-07/08/09/10/12 satisfied, RT-01 preserved)
    - Builds/Tests "[release][legal]" exits 0 (scanner rewrite clean on green repo)
    - All Phase 19-24 regression filters exit 0
    - PluginEditor::resized coords match UI-SPEC §5.1 table (zero drift)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shipping binary → clean-room policy | No third-party faceplate art/name/filename may ship in the binary or resources |
| Scanner → CI / verify-v1.sh / ReleaseTruthTest | Scanner exit code is a release gate; false positives or false negatives both break the chain |
| Procedural paint → editor hotspots | Path B must not move any painted region that a setBounds target depends on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01-S | Spoofing | Third-party faceplate re-shipped under a new name | high | mitigate | Normalized filename + content scan catches any reverbx variant in any spelling (Task 7) |
| T-25-01-T | Tampering | Scanner weakened to pass (allowlisting product-facing paths) | high | mitigate | Green only by material removal (Tasks 1-5); scanner normalized on both sides; product-facing paths never allowlisted (CONTEXT D-02) |
| T-25-01-D | Denial of Service | Build breaks from dangling BinaryData symbol | high | mitigate | Source gut (Task 3) before CMake edit (Task 4) before file delete (Task 5) — Pitfall 1 ordering |
| T-25-01-E | Elevation | INITIAL PATCH double-render or blank field | medium | mitigate | Pick exactly one of Option A/B; do not mix (Pitfall 5) |
| T-25-01-R | Repudiation | Scanner flags its own build template (Pamplejuce) | medium | mitigate | Preserve include(Pamplejuce*) allowlist; do not blanket-allowlist (Pitfall 2) |
| T-25-01-I | Information Disclosure | Scanner flags internal citation docs citing third parties | medium | mitigate | Exclude docs/ internal docs and .planning/ from scan surface per spec §14.4 (Pitfall 3) |
| T-25-01-DoS | Denial of Service | Scanner false-positive on required term with spaces | low | mitigate | Normalize both sides — "Niko Audio Labs" → "nikoaudiolabs" matches normalized content (Pitfall 4) |

</threat_model>

<verification>
Builds/Tests "[v1][contract][shipping-policy]" && Builds/Tests "[release][legal]" && Builds/Tests "[release][safe]" && Phase 19-24 regression bundle from Task 8. EditorSnapshot visual confirmation is human_needed for final sign-off but coordinate parity is provable from source.
</verification>

<success_criteria>
- UX-07/08/09/10/12/14 satisfied: no third-party faceplate string/filename in product-facing surface; title is SENDBLOOM; PNG removed; scanner normalizes and scans filenames; hotspots aligned
- [v1][contract][shipping-policy] tag fully green (flipped by material removal, not scanner weakening)
- [release][legal] green on the clean repo; scanner catches all three raw variants in a scratch negative test
- All Phase 19-24 contracts preserved (no regression)
- INITIAL PATCH default visible under Path B
</success_criteria>

## Artifacts this plan produces

| Symbol / File | Plan | Role |
|---------------|------|------|
| `SENDBLOOM` wordmark in paintProceduralChassis | 01 | UX-09 production title |
| Original central art (replaces vertical letters + giant X + ellipses) | 01 | UX-07 clean-room art |
| `paintPedalFaceplate` Path B body | 01 | UX-10 sole paint path |
| Removed `reverbx-faceplate.png` | 01 | UX-10 clean-room deletion |
| INITIAL PATCH chassis paint (Option A) or editor-always paint (Option B) | 01 | UX-14 default-name gap fix |
| `normalize()` + filename scan in check-legal-metadata.sh | 01 | UX-12 normalized scanner |
| Green `[shipping-policy][UX-07]` / `[UX-08]` | 01 | Flipped by material removal |

<output>
After completion, create `25-01-SUMMARY.md`
</output>
