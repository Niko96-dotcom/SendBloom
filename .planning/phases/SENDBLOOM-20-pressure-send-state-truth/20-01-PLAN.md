---
phase: 20-pressure-send-state-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - source/PressureController.h
  - source/ParameterLayout.cpp
  - source/ParameterSnapshot.h
  - source/SmoothedParameterBank.h
  - source/PluginProcessor.h
  - source/PluginProcessor.cpp
  - source/GatedBloomChain.h
  - tests/PressureSendTest.cpp
  - tests/PressureControllerTest.cpp
  - tests/ParameterLayoutTest.cpp
autonomous: true
requirements:
  - SEND-01
  - SEND-02
  - SEND-03
  - SEND-04
  - SEND-09
  - SEND-10
  - SEND-12
  - SEND-13
  - UX-01
  - UX-02
must_haves:
  truths:
    - "Disconnected mode yields unity send gain (always-on wet feed)"
    - "Connected with amount 0 yields send gain 0 (no new wet); Firm and Soft curves remain distinct"
    - "Asymmetric pressure smoothing is ~3 ms attack / ~25 ms release on raw pressure before the Firm/Soft curve"
    - "Default send_amount is 0; ParameterIDs::sendAmount / send_connected strings unchanged"
    - "PluginProcessor drives wet send from PressureController (MIDI target stubbed 0); bank sendGain smoother is not dual-smoothing pressure"
  artifacts:
    - path: "source/PressureController.h"
      provides: "prepare/reset, setConnected/Host/Midi/FirmFeel, processSample → send gain"
      contains: "processSample"
    - path: "source/ParameterLayout.cpp"
      provides: "send_amount default 0.0f"
    - path: "tests/PressureControllerTest.cpp"
      provides: "SEND-01/02/09/10/12 timing and gain unit coverage"
    - path: "tests/ParameterLayoutTest.cpp"
      provides: "UX-02 default send_amount == 0 assert"
  key_links:
    - from: "PluginProcessor processBlock"
      to: "PressureController::processSample"
      via: "per-sample send gain into GatedBloomChain / PressureSend"
      pattern: "pressureController|PressureController"
    - from: "PressureController"
      to: "ParameterCurves::sendGain"
      via: "smooth raw pressure then curve when connected"
      pattern: "sendGain"
    - from: "ParameterLayout default"
      to: "APVTS send_amount"
      via: "layout construction default 0"
---

<objective>
Add PressureController (asymmetric 3/25 ms pressure smooth + Firm/Soft curve), set default send_amount to 0, and wire the processor so connected-at-rest is dry and disconnected is always-on — without fixing pad UI, presets, or Phase 21/22 scope.

Purpose: DSP truth for SEND-01…04/09/10/12/13 and UX-01/02 so later UI/preset plans flip contracts on honest gain semantics.
Output: PressureController.h, processor/bank/snapshot/layout wiring, Catch2 controller + layout tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/SENDBLOOM-20-pressure-send-state-truth/20-CONTEXT.md
@.planning/phases/SENDBLOOM-20-pressure-send-state-truth/20-RESEARCH.md
@.planning/phases/SENDBLOOM-20-pressure-send-state-truth/20-UI-SPEC.md
@source/PressureSend.h
@source/ParameterCurves.h
@source/ParameterSnapshot.h
@source/SmoothedParameterBank.h
@source/ParameterLayout.cpp
@source/ParameterIDs.h
@source/PluginProcessor.cpp
@source/PluginProcessor.h
@source/GatedBloomChain.h
@tests/PressureSendTest.cpp
@tests/ParameterLayoutTest.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PressureController + unit tests (SEND-01/02/09/10/12)</name>
  <files>source/PressureController.h, tests/PressureControllerTest.cpp, tests/PressureSendTest.cpp</files>
  <behavior>
    - Disconnected: processSample returns 1.0 regardless of host pressure (SEND-01/12)
    - Connected + host 0: gain settles at 0 (SEND-02)
    - Connected + host &gt; 0: gain equals ParameterCurves::sendGain after smooth settle; Firm ≠ Soft at mid amount (SEND-03/09)
    - Attack: step 0→1 at 48 kHz reaches ~63% of settled Firm gain within ~3 ms ±25% (SEND-10)
    - Release: step 1→0 reaches ~37% residual within ~25 ms ±20% (SEND-10)
    - Existing PressureSend::computeGain static cases stay green
  </behavior>
  <read_first>
    - 20-RESEARCH.md Pattern 2 + PressureController shape (§ Code Examples)
    - source/PressureSend.h computeGain
    - source/ParameterCurves.h sendGain (Firm 1.85 / Soft 1.2)
    - tests/PressureSendTest.cpp tags and Approx style
    - ADR-V1-04 / milestone pressure controller contract in 20-CONTEXT.md
  </read_first>
  <action>
    Per ADR-V1-04 / SEND-10 and research Open Question 5 (RESOLVED: add controller now; MIDI stub 0):

    1. Create header-only `source/PressureController.h` in namespace sendbloom with: prepare(sampleRate), reset(), setConnected(bool), setHostPressureTarget(float), setMidiPressureTarget(float), setFirmFeel(bool), processSample() → float send gain. No heap after prepare; clamp host/midi norms to [0,1].
    2. Connected path: raw = max(host, midi); asymmetric one-pole smooth with separate attack (~3 ms) and release (~25 ms) coefficients from sampleRate; then ParameterCurves::sendGain(smoothed, firm). Disconnected path: return 1.0f and do not require curve.
    3. Phase 20: callers leave midi target at 0 (Phase 22 wires realtime MIDI). Do not invent Soft Pressure factory preset.
    4. Add `tests/PressureControllerTest.cpp` tagged `[send][PressureController]` covering the behavior block. Keep `tests/PressureSendTest.cpp` Firm/Soft static cases; extend only if needed to assert computeGain still matches curves.
    5. CMake GLOBs `source/*.h` and `tests/*.cpp` — no CMakeLists edit required for the new header/test.

    Forbidden: pad/UI edits; preset XML; removing Phase 19 contract files; renaming ParameterIDs; fixing MIDI APVTS store; implementing oversized span engine.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target Tests && Builds/Tests "[send][PressureController]" && Builds/Tests "[send][PressureSend]"</automated>
  </verify>
  <done>PressureController unit tests green for disconnect unity, connected-at-rest zero, Firm≠Soft, and attack/release timing within SEND-10 tolerances.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Layout default 0 + processor/bank handoff (SEND-04/13, UX-01/02)</name>
  <files>source/ParameterLayout.cpp, source/ParameterSnapshot.h, source/SmoothedParameterBank.h, source/PluginProcessor.h, source/PluginProcessor.cpp, source/GatedBloomChain.h, tests/ParameterLayoutTest.cpp</files>
  <behavior>
    - Fresh PluginProcessor: raw send_amount default is 0.0 (UX-02); IDs remain send_amount / send_connected (UX-01 / SEND-13)
    - Connected amount 0 through processBlock: energy into test reverb stays ~0 while dry output nonzero (SEND-02/04 semantics at DSP)
    - Disconnected: wet feed present (SEND-01/12) via controller unity gain
    - No dual smoothing: bank must not also ramp sendGain after controller owns pressure gain
  </behavior>
  <read_first>
    - source/ParameterLayout.cpp sendAmount default (currently 1.0f)
    - source/ParameterSnapshot.h sendGain capture
    - source/SmoothedParameterBank.h sendGain SmoothedValue
    - source/PluginProcessor.cpp processBlock sample loop + chain.processBlock sendGain arg
    - source/GatedBloomChain.h processBlock / processSample sendGain
    - 20-RESEARCH.md pitfall 2 (smooth after curve) and pitfall 4 (oversized dry fallback — leave alone)
  </read_first>
  <action>
    Per SEND-13 / UX-02 and Contract Flip Strategy (DSP half only):

    1. Change ParameterLayout send_amount default from 1.0f to 0.0f. Do not rename any ParameterIDs string (UX-01).
    2. Own a PressureController member on PluginProcessor; prepare/reset with prepareToPlay/releaseResources.
    3. Each processBlock (prepared-size path): from ParameterSnapshot raw fields call setConnected, setHostPressureTarget(sendAmountNorm), setFirmFeel, setMidiPressureTarget(0). Advance processSample() per sample into a send-gain scratch buffer. Feed per-sample gains into the wet path so attack/release are real inside a host block (extend GatedBloomChain::processBlock to accept const float* sendGains, or equivalent per-sample PressureSend multiply before reverb — pick one approach and apply consistently on both authentic and host-rate paths).
    4. Stop dual-smoothing: remove or bypass SmoothedParameterBank sendGain target/next usage once the controller owns gain (do not leave bank sendGain.reset(..., 0.025) on the pressure path). Snapshot may keep raw sendAmountNorm/sendConnected/sendFirmFeel; stop treating snapshot.sendGain as the live pressure truth for the audio thread.
    5. Oversized dry-fallback branch (numSamples &gt; preparedMaxBlock_): leave wet=0 behavior unchanged (Phase 21). Still advance or ignore controller consistently without claiming SEND-14 for oversized.
    6. Do not clear/reset the reverb tank when amount→0 (SEND-04 tails). Do not edit MidiSendAmountTest or CC1 store path (Phase 22).
    7. Extend ParameterLayoutTest with assert default send_amount == 0 tagged `[parm][layout]`. Optional light integration assert under `[send]` for connected-at-rest energy if easy via existing EnergyTrackingReverb pattern — otherwise defer energy proof to 20-02 pressure-release contract.

    Forbidden: PressureSendPad / overlay / AdvancedDrawer; preset XML; deleting V1Contract* files; Phase 21 span; Phase 22 MIDI purity.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target Tests && Builds/Tests "[parm][layout]" && Builds/Tests "[send]" && Builds/Tests "[release]" && Builds/Tests "[DryPath]"</automated>
  </verify>
  <done>Default amount 0; controller owns send gain in processBlock; [send] and layout tests green; BASE-04 [release]/[DryPath] still green; no pad/preset fixes yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host/UI → APVTS | Normalized send_amount / send_connected written on message thread |
| APVTS → audio thread | Snapshot + PressureController consume params; must stay allocation-free after prepare |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Tampering | PressureController targets | medium | mitigate | Clamp host/midi norms to [0,1] before smooth |
| T-20-02 | Denial of Service | processBlock / prepare | high | mitigate | No heap in processSample after prepare; reuse scratch buffers |
| T-20-03 | Tampering | MIDI→APVTS store (existing) | medium | accept | Deferred Phase 22; midiPressureTarget forced 0 in Phase 20 |
| T-20-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages this plan |
</threat_model>

<verification>
Builds/Tests "[send]" and "[parm][layout]" pass; PressureController timing cases present; [release] and [DryPath] remain green; source/ui and resources/presets untouched.
</verification>

<success_criteria>
- Connected-at-rest DSP gain is 0; disconnected gain is 1
- Attack/release measured on PressureController within SEND-10 tolerances
- send_amount default 0; IDs unchanged
- Bank does not double-smooth pressure gain
</success_criteria>

<output>
Create `.planning/phases/SENDBLOOM-20-pressure-send-state-truth/20-01-SUMMARY.md` when done
</output>
