---
phase: 20-pressure-send-state-truth
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - source/ui/PressureSendPad.h
  - source/ui/PressureSendPad.cpp
  - source/ui/PedalFaceplatePaint.cpp
  - source/ui/PedalFaceplatePaint.h
  - source/ui/AdvancedDrawer.h
  - source/ui/AdvancedDrawer.cpp
  - source/PluginEditor.cpp
  - source/PluginEditor.h
  - tests/V1ContractPressureReleaseTest.cpp
autonomous: true
requirements:
  - SEND-05
  - SEND-06
  - SEND-07
  - SEND-08
must_haves:
  truths:
    - "Pad mouseUp keeps send_connected true and sets send_amount to 0 (release-to-zero, not disconnect)"
    - "Pad mouseDown may auto-connect; release never disconnects"
    - "Pressed overlay follows pressed state and/or amount, not connection alone"
    - "Advanced drawer exposes persistent PRESSURE MODE toggle bound to send_connected"
    - "[v1][contract][pressure-release] is green; other Phase 19 [v1][contract] filters stay red"
  artifacts:
    - path: "source/ui/PressureSendPad.cpp"
      provides: "release zeros amount with gesture bracketing; no disconnect on up"
    - path: "source/ui/PedalFaceplatePaint.cpp"
      provides: "overlay predicate from amount/pressed"
    - path: "source/ui/AdvancedDrawer.cpp"
      provides: "PRESSURE MODE toggle → send_connected"
    - path: "tests/V1ContractPressureReleaseTest.cpp"
      provides: "existing contract flipped green by production fix"
  key_links:
    - from: "PressureSendPad::mouseUp"
      to: "APVTS send_amount / send_connected"
      via: "beginChangeGesture + setValueNotifyingHost(0) + endChangeGesture; never write connected false"
    - from: "AdvancedDrawer PRESSURE MODE"
      to: "ParameterIDs::sendConnected"
      via: "ButtonAttachment"
    - from: "drawStateOverlays"
      to: "pad pressed or send_amount"
      via: "overlay predicate (not connection alone)"
---

<objective>
Fix pad release, pressed overlay, and Advanced Pressure Mode connection so SEND-05/06/07/08 hold and Phase 19 [pressure-release] flips green — without touching presets, MIDI, or other contract tags.

Purpose: Close the UI half of Pressure Mode honesty on top of 20-01 DSP truth.
Output: Correct pad/overlay/Advanced wiring; green Builds/Tests "[pressure-release]".
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/SENDBLOOM-20-pressure-send-state-truth/20-CONTEXT.md
@.planning/phases/SENDBLOOM-20-pressure-send-state-truth/20-RESEARCH.md
@.planning/phases/SENDBLOOM-20-pressure-send-state-truth/20-UI-SPEC.md
@.planning/phases/SENDBLOOM-20-pressure-send-state-truth/20-01-PLAN.md
@source/ui/PressureSendPad.cpp
@source/ui/PressureSendPad.h
@source/ui/PedalFaceplatePaint.cpp
@source/ui/AdvancedDrawer.cpp
@source/ui/AdvancedDrawer.h
@source/PluginEditor.cpp
@source/PluginEditor.h
@tests/V1ContractPressureReleaseTest.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pad release-to-zero + auto-connect (SEND-05/08)</name>
  <files>source/ui/PressureSendPad.h, source/ui/PressureSendPad.cpp</files>
  <behavior>
    - mouseDown: may set connected true and drive amount from Y (existing auto-connect kept) (SEND-08)
    - mouseUp: amount → 0 with begin/endChangeGesture; connected stays true; isPressed false; bloom fade remains display-only (SEND-05)
    - Public isPressed() and getDisplayAmount() (or equivalent) available for overlay consumers
    - Builds/Tests "[pressure-release]" passes after this fix (with 20-01 DSP already present)
  </behavior>
  <read_first>
    - source/ui/PressureSendPad.cpp mouseUp / setConnected / setAmountFromY
    - 20-RESEARCH.md Exact Bug Locus 1 + Pattern 1 (pad release-to-zero)
    - tests/V1ContractPressureReleaseTest.cpp REQUIRE connectedAfter / amountAfter / energy
    - 20-UI-SPEC.md Pressure pad surface
  </read_first>
  <action>
    Per SEND-05 / SEND-08 / ADR-V1-02 and Contract Flip Strategy:

    1. Change mouseUp so it does not write send_connected to false. Zero send_amount via beginChangeGesture / setValueNotifyingHost(0) / endChangeGesture on the amount parameter. Keep isPressed=false and startBloomFade() for the 200 ms visual-only fade (not DSP release).
    2. Keep mouseDown auto-connect (setConnected(true)) and amount-from-Y. Bracket amount gestures consistently on down/drag/up so hosts see automation endpoints (research pitfall 1).
    3. Expose public const getters for pressed state and display amount for overlay wiring.
    4. Do not delete or weaken V1ContractPressureReleaseTest — flip green by production behavior only. Do not change unrelated V1Contract* files.

    Forbidden: preset XML; ParameterLayout; PressureController redesign; MIDI store removal; branding string overhaul (Phase 25); claiming other [v1][contract] tags green.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target Tests && Builds/Tests "[pressure-release]"</automated>
  </verify>
  <done>[pressure-release] green; mouseUp leaves connected true and amount 0; bloom fade still display-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Overlay + Advanced PRESSURE MODE (SEND-06/07)</name>
  <files>source/ui/PedalFaceplatePaint.cpp, source/ui/PedalFaceplatePaint.h, source/ui/AdvancedDrawer.h, source/ui/AdvancedDrawer.cpp, source/PluginEditor.cpp, source/PluginEditor.h</files>
  <behavior>
    - Footswitch pressed overlay draws when amount &gt; ~0.001 or pad isPressed — not merely because send_connected is true (SEND-06)
    - Advanced drawer shows a persistent PRESSURE MODE toggle bound to send_connected; disconnect restores always-on feed via existing DSP (SEND-07 / SEND-12 already in controller)
    - Minimal UI surgery only — no faceplate redesign, no new brand assets (20-UI-SPEC)
  </behavior>
  <read_first>
    - source/ui/PedalFaceplatePaint.cpp drawStateOverlays (connection-keyed overlay)
    - source/ui/AdvancedDrawer.h/.cpp constructor + resized layout
    - source/PluginEditor.cpp AdvancedDrawer construction and paintPedalFaceplate call
    - 20-RESEARCH.md Open Question 6 (RESOLVED: prefer pad isPressed/displayAmount; amount&gt;epsilon acceptable interim)
    - 20-UI-SPEC.md Advanced / connection control
  </read_first>
  <action>
    Per SEND-06 / SEND-07 and UI-SPEC minimal state-truth:

    1. Overlay: stop keying drawFootswitchPressedOverlay solely on send_connected. Prefer threading pad isPressed / displayAmount from PluginEditor into paintPedalFaceplate / drawStateOverlays; if signature churn is awkward, amount &gt; 0.001f from APVTS is an acceptable SEND-06 interim once release zeros amount (research Q6). Connection must not alone light the pressed overlay.
    2. AdvancedDrawer: add a PRESSURE MODE ToggleButton (or equivalent existing control style) with ButtonAttachment to ParameterIDs::sendConnected. Extend constructor to accept the connected param id; update PluginEditor initializer list. Show/hide with other advanced controls in setExpanded/resized; keep preferred height usable (bump slightly if needed — no full redesign).
    3. Copy: label PRESSURE MODE without third-party controller product names (full branding Phase 25).
    4. Optional small Catch2 helper under `[send]` asserting overlay predicate logic if extracted as a pure function; otherwise rely on pressure-release + manual UI-SPEC acceptance later.

    Forbidden: Soft Pressure factory preset; visual redesign beyond state truth; Phase 25 branding replacement; editing non-pressure V1 contracts.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target Tests && Builds/Tests "[pressure-release]" && Builds/Tests "[send]" && (Builds/Tests "[v1][contract][oversized-block]"; test $? -ne 0) && (Builds/Tests "[v1][contract][midi-apvts]"; test $? -ne 0)</automated>
  </verify>
  <done>Overlay follows press/amount; Advanced exposes PRESSURE MODE; pressure-release green; oversized-block and midi-apvts still intentionally red.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mouse/UI gestures → APVTS | Pad and Advanced write connection/amount on message thread only |
| Paint path → APVTS/pad state | Overlay reads params/pressed; must not allocate on audio thread |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-04 | Tampering | PressureSendPad Y→amount | medium | mitigate | Keep jlimit on pad Y norm; gesture bracketing for host automation |
| T-20-05 | Elevation of Privilege | N/A | low | accept | Local plugin UI; no remote auth surface |
| T-20-06 | Information Disclosure | Advanced tooltips/labels | low | accept | No secrets; avoid third-party brand names in new copy |
| T-20-SC | Tampering | package installs | high | accept | No new packages |
</threat_model>

<verification>
Builds/Tests "[pressure-release]" passes; "[send]" passes; at least one unrelated [v1][contract] filter still fails; no preset matrix work in this plan.
</verification>

<success_criteria>
- SEND-05/08: release-to-zero with auto-connect on press
- SEND-06: overlay not stuck on while connected-at-rest
- SEND-07: Advanced PRESSURE MODE toggles send_connected
- Only pressure-release among Phase 19 contracts is green from this work
</success_criteria>

<output>
Create `.planning/phases/SENDBLOOM-20-pressure-send-state-truth/20-02-SUMMARY.md` when done
</output>
