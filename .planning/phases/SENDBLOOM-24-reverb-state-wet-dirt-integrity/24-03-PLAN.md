---
phase: 24-reverb-state-wet-dirt-integrity
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - source/WetOverdrive.h
  - tests/V1ContractWetDirtTest.cpp
autonomous: true
requirements:
  - DSP-09
  - DSP-10
  - DSP-11
  - DSP-12
  - DSP-13
  - DSP-14
  - DSP-15
must_haves:
  truths:
    - "Dirty branch: HP 100 Hz → LP 6.5 kHz → clip → LP 7.5 kHz → DC blocker 20 Hz"
    - "30 Hz strongly attenuated vs 1 kHz on filtered branch; distn=0 returns original wet within tolerance"
    - "Long-run DC mean abs output &lt; 1e-4 after asymmetric clip"
    - "Dry path extract unchanged at max Distn; dirt_os stays disabled in DSP and UI"
    - "authentic_color off by default and in all factory presets ([release][safe] green)"
    - "[v1][contract][wet-dirt] green; [shipping-policy] stays red"
  artifacts:
    - path: "source/WetOverdrive.h"
      provides: "ADR-V1-15 OnePoleHighpass + full dirty chain"
      contains: "OnePoleHighpass"
    - path: "tests/V1ContractWetDirtTest.cpp"
      provides: "DSP-09…13 contracts"
      contains: "[v1][contract][wet-dirt]"
  key_links:
    - from: "WetOverdriveState::processFilteredBranch"
      to: "preClipHp → preClipLp → clip → postClipLp → postClipDcBlock"
      via: "dirty branch only; blend unchanged"
      pattern: "preClipHp"
    - from: "GatedBloomChain"
      to: "WetOverdriveState::process"
      via: "wet path after reverb; dry tap upstream"
      pattern: "processFilteredBranch|WetOverdriveState"
---

<objective>
Wire ADR-V1-15 wet-dirt HP/DC filter chain and lock safety defaults (dirt_os disabled, authentic_color off) so DSP-09…15 are proven without enabling oversampling or changing dry path integrity.

Purpose: Complete dirty-branch filtering and DC containment; preserve existing release-truth safe defaults.
Output: OnePoleHighpass stages in WetOverdriveState, V1ContractWetDirtTest green, [release][safe] preserved.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/SENDBLOOM-24-reverb-state-wet-dirt-integrity/24-CONTEXT.md
@.planning/phases/SENDBLOOM-24-reverb-state-wet-dirt-integrity/24-RESEARCH.md
@.planning/phases/SENDBLOOM-24-reverb-state-wet-dirt-integrity/24-VALIDATION.md
@source/WetOverdrive.h
@tests/WetOverdriveTest.cpp
@tests/WetOverdriveDiagnosticsTest.cpp
@tests/DryPathIntegrityTest.cpp
@tests/ReleaseTruthTest.cpp
@tests/V1ContractShippingPolicyTest.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OnePoleHighpass + dirty chain (DSP-09…13 / D-04)</name>
  <files>source/WetOverdrive.h, tests/V1ContractWetDirtTest.cpp</files>
  <behavior>
    - OnePoleHighpass mirrors OnePoleLowpass pattern (prepare/reset/process; allocation-free)
    - preClipHp @ kPreClipHpHz (100 Hz); postClipDcBlock @ 20 Hz
    - processFilteredBranch: HP → preClipLp → clipSample → postClipLp → DC block
    - distn=0: process() returns wet unchanged within float tolerance
    - 30 Hz sine on filtered branch attenuated vs 1 kHz reference
    - Long symmetric sine + DC offset: mean abs output &lt; 1e-4
    - Asymmetry preserved: |clip(+x)| &gt; |clip_sym(+x)| for test level
    - dirt_os not referenced in GatedBloomChain processor path; UI remains disabled
  </behavior>
  <read_first>
    - 24-CONTEXT.md D-04 wet dirt chain
    - source/WetOverdrive.h OnePoleLowpass + WetOverdriveState (current gap)
    - MILESTONE-SPEC §13.7 wet dirt chain
    - tests/WetOverdriveTest.cpp / WetOverdriveDiagnosticsTest.cpp (220 Hz swell &lt; 1.15)
  </read_first>
  <action>
    1. Add OnePoleHighpass class adjacent to OnePoleLowpass (ω = 2πfc/fs; y = α(x - x₁) + α·y₁ or equivalent one-pole HP).
    2. Add preClipHp and postClipDcBlock members to WetOverdriveState; prepare/reset at 100 Hz and 20 Hz.
    3. Rewrite processFilteredBranch per D-04 chain order; keep process(wet, distnBlend) blend semantics.
    4. Create tests/V1ContractWetDirtTest.cpp with [v1][contract][wet-dirt][DSP-09]…[DSP-13] (red first).
    5. DSP-13: grep/source scan that dirtOs is not consumed in GatedBloomChain audio path; UI setEnabled(false) unchanged.
    6. Do NOT enable dirt_os or oversampling anywhere.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target Tests &amp;&amp; Builds/Tests "[v1][contract][wet-dirt]" &amp;&amp; Builds/Tests "[chain][od]"</automated>
  </verify>
  <acceptance_criteria>
    - rg "OnePoleHighpass" source/WetOverdrive.h matches
    - rg "preClipHp" source/WetOverdrive.h matches processFilteredBranch
    - Builds/Tests "[v1][contract][wet-dirt]" exits 0
    - Builds/Tests "[chain][od][thd][DryPath]" exits 0 (dry unaffected)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Safety defaults + phase regression (DSP-14/15 / D-04)</name>
  <files>source/WetOverdrive.h</files>
  <behavior>
    - Fresh plugin load: authentic_color default false
    - All factory presets recall authentic_color=0
    - WetOverdriveDiagnostics 220 Hz swell &lt; 1.15; finite output
    - Phase 20–23 contract tags green; [shipping-policy] still fails
  </behavior>
  <read_first>
    - tests/ReleaseTruthTest.cpp [release][safe] cases
    - tests/V1ContractShippingPolicyTest.cpp (expect red)
    - 24-VALIDATION.md regression gate list
    - ParameterLayout authentic_color default (grep if needed)
  </read_first>
  <action>
    1. Run Builds/Tests "[release][safe]" — must pass without preset XML edits unless a preset wrongly has authentic_color=1 (fix only if failing).
    2. Confirm ParameterLayout default authentic_color=false and dirt_os=false unchanged.
    3. Run WetOverdriveDiagnosticsTest swell/finite guards.
    4. Run Phase 20–23 regression bundle from 24-VALIDATION.md: pressure-release, oversized-block, true-bypass, midi-apvts, input-anchors, posthard, per-sample, realtime, authentic.
    5. Confirm Builds/Tests "[v1][contract][shipping-policy]" still fails (Phase 25 scope).
  </action>
  <verify>
    <automated>Builds/Tests "[release][safe]" &amp;&amp; Builds/Tests "[wet-dirt]" &amp;&amp; Builds/Tests "[pressure-release]" &amp;&amp; Builds/Tests "[oversized-block]" &amp;&amp; Builds/Tests "[true-bypass]" &amp;&amp; Builds/Tests "[midi-apvts]" &amp;&amp; Builds/Tests "[input-anchors]" &amp;&amp; Builds/Tests "[posthard]" &amp;&amp; Builds/Tests "[per-sample]" &amp;&amp; Builds/Tests "[realtime]" &amp;&amp; Builds/Tests "[authentic]" &amp;&amp; (Builds/Tests "[v1][contract][shipping-policy]" &gt;/dev/null 2&gt;&amp;1; test $? -ne 0)</automated>
  </verify>
  <acceptance_criteria>
    - Builds/Tests "[release][safe]" exits 0 (DSP-14/15)
    - All Phase 20–23 listed filters exit 0
    - Builds/Tests "[v1][contract][shipping-policy]" exits non-zero (still red)
    - No dirt_os enablement introduced in source
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wet path → WetOverdriveState | Distn blend; filters must not mutate dry tap |
| Parameter defaults → preset recall | authentic_color must stay off unless user enables |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-03-T | Tampering | Dry path via wrong filter placement | high | mitigate | Filters only in processFilteredBranch; DSP-12 + DryPathIntegrityTest |
| T-24-03-D | Denial of Service | DC accumulation on dirty branch | high | mitigate | 20 Hz DC blocker + DSP-11 mean &lt; 1e-4 contract |
| T-24-03-RT | Tampering | Audio-thread allocation | high | mitigate | OnePoleHighpass stack-only state; prepare sets coefficients only |
| T-24-03-E | Elevation | dirt_os accidental enable | medium | mitigate | DSP-13 source scan; no GatedBloomChain read; UI disabled |
| T-24-03-I | Information Disclosure | N/A | low | accept | No new network or secrets |

</threat_model>

<verification>
Builds/Tests "[v1][contract][wet-dirt]" && Builds/Tests "[release][safe]" && Phase 20–23 regression bundle from Task 2
</verification>

<success_criteria>
- DSP-09…15 satisfied; wet-dirt contracts green; release safe defaults preserved
- dirt_os stays disabled; shipping-policy remains red
- WetOverdrive diagnostics within existing bounds
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Plan | Role |
|---------------|------|------|
| `OnePoleHighpass` | 03 | Allocation-free HP / DC blocker |
| `WetOverdriveState::preClipHp`, `postClipDcBlock` | 03 | ADR-V1-15 chain |
| `tests/V1ContractWetDirtTest.cpp` | 03 | DSP-09…13 contracts |
| Preserved `[release][safe]` | 03 | DSP-14/15 (no change unless preset bug) |

<output>
After completion, create `24-03-SUMMARY.md`
</output>
