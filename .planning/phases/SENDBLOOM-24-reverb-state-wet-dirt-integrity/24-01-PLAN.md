---
phase: 24-reverb-state-wet-dirt-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - source/SchroederTankCore.h
  - source/LegacyAccumulatorPath.h
  - tests/V1ContractPredelayTest.cpp
autonomous: true
requirements:
  - DSP-01
  - DSP-02
  - DSP-03
  - DSP-04
must_haves:
  truths:
    - "Predelay line clocks every sample in bright and dark modes (no conditional freeze)"
    - "Dark tap is fixed 55 ms at processing rate; darkMix blends input↔delayed only"
    - "Re-enabling Dark after bright silence emits no pre-55 ms stale burst"
    - "darkMix automation on tone stays finite with bounded adjacent-sample deltas"
    - "Host-rate and ProperSRC dark onset agree in wall-clock time within tolerance"
    - "[v1][contract][predelay] green"
  artifacts:
    - path: "source/SchroederTankCore.h"
      provides: "ADR-V1-12 continuous fixed 55 ms predelay tap"
      contains: "kDarkPredelaySeconds"
    - path: "source/LegacyAccumulatorPath.h"
      provides: "SRC-05 legacy predelay parity"
      contains: "darkMix"
    - path: "tests/V1ContractPredelayTest.cpp"
      provides: "DSP-01…04 contract proofs"
      contains: "[v1][contract][predelay]"
  key_links:
    - from: "SchroederTankCore::prepare"
      to: "predelayLine.setDelay"
      via: "kDarkPredelaySeconds * processingRate_ (once per rate)"
      pattern: "setDelay"
    - from: "SchroederTankCore::processTank"
      to: "predelayLine push/pop"
      via: "unconditional every sample + lerp by darkMix"
      pattern: "pushSample"
    - from: "FixedRateAdapter::core"
      to: "SchroederTankCore predelay"
      via: "shared SchroederTankCore at 32768 Hz"
      pattern: "processSample"
---

<objective>
Implement ADR-V1-12 continuous fixed 55 ms dark predelay tap in production tank core and legacy accumulator parity path so DSP-01…04 flip green without retuning reverb character.

Purpose: Eliminate frozen predelay line and variable delay-length bug that causes stale bursts on dark re-enable.
Output: Fixed-delay continuous-clock predelay in SchroederTankCore + LegacyAccumulatorPath; V1ContractPredelayTest green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/SENDBLOOM-24-reverb-state-wet-dirt-integrity/24-CONTEXT.md
@.planning/phases/SENDBLOOM-24-reverb-state-wet-dirt-integrity/24-RESEARCH.md
@.planning/phases/SENDBLOOM-24-reverb-state-wet-dirt-integrity/24-VALIDATION.md
@.planning/MILESTONE-SPEC-v1.0-interaction-truth.md
@source/SchroederTankCore.h
@source/LegacyAccumulatorPath.h
@source/SchroederTank32DelayTable.h
@tests/FixedRateAdapterTest.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 predelay contracts (DSP-01…04)</name>
  <files>tests/V1ContractPredelayTest.cpp</files>
  <behavior>
    - DSP-01: bright mode (darkMix≈0) still advances predelay line — impulse timing differs from frozen-line baseline
    - DSP-02: full dark (darkMix=1) IR onset ≈ 55 ms ± tolerance vs bright immediate onset
    - DSP-03: bright → silence &gt; 55 ms → dark: no energy burst before 55 ms wall-clock
    - DSP-04: ramp darkMix 0→1 on steady tone: all samples finite; max adjacent |Δ| bounded (e.g. ≤ 0.05)
    - Host @ 48 kHz vs ProperSRC @ 48 kHz dark onset agree in seconds within tolerance
  </behavior>
  <read_first>
    - 24-CONTEXT.md D-01 (locked predelay topology)
    - 24-RESEARCH.md § Current State §1–2 + Recommended Plan 01 tests
    - 24-VALIDATION.md DSP-01…04 command map
    - tests/V1ContractInputAnchorsTest.cpp (tag/style exemplar)
  </read_first>
  <action>
    1. Create tests/V1ContractPredelayTest.cpp with Catch2 cases tagged [v1][contract][predelay][DSP-0N] per requirement.
    2. Drive SchroederTankCore directly at 44100/48000 and via FixedRateAdapter ProperSRC at same host rate for wall-clock parity case.
    3. Use impulse or tone + envelope metrics; document tolerances in test comments (55 ms ± 2 ms typical).
    4. Run filter — tests MUST fail against current variable-delay / conditional-clock implementation.
    5. No cmake/Tests.cmake edit needed (GLOB_RECURSE picks up new file).
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target Tests &amp;&amp; Builds/Tests "[v1][contract][predelay]" 2&gt;&amp;1 | tee /tmp/predelay-red.txt; test $(Builds/Tests "[v1][contract][predelay]" &gt;/dev/null 2&gt;&amp;1; echo $?) -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - tests/V1ContractPredelayTest.cpp exists with tags [v1][contract][predelay][DSP-01] through [DSP-04]
    - Builds/Tests "[v1][contract][predelay]" fails (red) before production fix
    - No heap allocation in test hot loops (prepare-only setup)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SchroederTankCore fixed tap (D-01)</name>
  <files>source/SchroederTankCore.h</files>
  <behavior>
    - prepare sets predelayLine delay to kDarkPredelaySeconds * processingRate_ once
    - updateCoeffs stores darkMix member; does NOT mutate predelay delay length
    - processTank: every sample push(input), pop delayed, x = input + darkMix * (delayed - input)
    - No predelaySamples &gt; 0.5f gate; no mix * 55ms delay shrink
  </behavior>
  <read_first>
    - source/SchroederTankCore.h updateCoeffs + processTank (defect loci)
    - 24-RESEARCH.md REQUIRED predelay snippet (ADR-V1-12)
    - MILESTONE-SPEC ADR-V1-12 / §13.3
  </read_first>
  <action>
    1. In prepare(): after predelayLine.prepare, call setDelay(static_cast&lt;float&gt;(kDarkPredelaySeconds * processingRate_)).
    2. Add darkMix_ member; set in updateCoeffs from jlimited darkMix; remove predelaySamples delay mutation from updateCoeffs.
    3. Rewrite processTank per D-01 lerp (equivalent to push/pop + blend); drop conditional clock gate.
    4. Remove unused predelaySamples member if nothing else references it.
    5. Do NOT change comb delays, damping maps, tank gain 0.85f, or kTankLfoHz.
    6. Confirm [v1][contract][predelay] passes; SchroederTankCoreTest / SchroederTank32Test dark cases still pass.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target Tests &amp;&amp; Builds/Tests "[v1][contract][predelay]" &amp;&amp; Builds/Tests "[verb][SchroederTank]"</automated>
  </verify>
  <acceptance_criteria>
    - rg "predelaySamples > 0\\.5" source/SchroederTankCore.h returns no matches
    - rg "mix \\* SchroederTank32DelayTable::kDarkPredelaySeconds" source/SchroederTankCore.h returns no matches
    - Builds/Tests "[v1][contract][predelay]" exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: LegacyAccumulator parity + predelay regression (D-01 / SRC-05)</name>
  <files>source/LegacyAccumulatorPath.h</files>
  <behavior>
    - Legacy path uses same fixed 55 ms delay at kInternalRate and continuous clock + darkMix lerp
    - [verb][FixedRateAdapter][LegacyAccumulator][SRC-05] stays green
    - LatencyTest tail +0.055 s characterization unchanged within tolerance
  </behavior>
  <read_first>
    - source/LegacyAccumulatorPath.h predelay + updateCoeffs + processTank mirror
    - tests/FixedRateAdapterTest.cpp SRC-05 cases
    - 24-CONTEXT.md D-01 legacy parity note
  </read_first>
  <action>
    1. Mirror SchroederTankCore predelay fix in LegacyAccumulatorPath: fixed delay in prepare, darkMix lerp, unconditional push/pop.
    2. Remove variable predelaySec = mix * kDarkPredelaySeconds and conditional clock gate.
    3. Run FixedRateAdapter LegacyAccumulator + LatencyTest + RT60 suites — no comb/LFO retune.
    4. Skip Fdn8Reverb.h unless a failing diagnostic explicitly reaches it (Claude discretion per CONTEXT).
  </action>
  <verify>
    <automated>Builds/Tests "[v1][contract][predelay]" &amp;&amp; Builds/Tests "[verb][FixedRateAdapter][LegacyAccumulator]" &amp;&amp; Builds/Tests "[latency]"</automated>
  </verify>
  <acceptance_criteria>
    - LegacyAccumulatorPath matches continuous-clock fixed-tap semantics
    - Builds/Tests "[verb][FixedRateAdapter][LegacyAccumulator]" exits 0
    - Phase 20–23 contract tags still pass when spot-checked: Builds/Tests "[pressure-release]" &amp;&amp; Builds/Tests "[input-anchors]"
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host audio → SchroederTankCore | Untrusted block sizes and automation; predelay state must stay finite |
| prepare → processBlock | Delay length set only at prepare; no audio-thread realloc |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01-T | Tampering | SchroederTankCore predelayLine | high | mitigate | Fixed delay at prepare; unconditional push/pop; contract tests DSP-01…03 |
| T-24-01-I | Information Disclosure | N/A (DSP-only) | low | accept | No secrets in predelay path |
| T-24-01-D | Denial of Service | darkMix automation | medium | mitigate | DSP-04 bounded adjacent-delta contract; jlimit darkMix 0…1 |
| T-24-01-E | Elevation | N/A | low | accept | No privilege boundary |
| T-24-01-RT | Tampering | processBlock RT safety | high | mitigate | No setMaximumDelayInSamples or vector growth in processTank; prepare-only delay resize |

</threat_model>

<verification>
Builds/Tests "[v1][contract][predelay]" && Builds/Tests "[verb][FixedRateAdapter][LegacyAccumulator]" && Builds/Tests "[verb][SchroederTank]"
</verification>

<success_criteria>
- DSP-01…04 satisfied; [predelay] contracts green
- No reverb character retune (RT60/tail suites green)
- Legacy SRC-05 parity preserved
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Plan | Role |
|---------------|------|------|
| `tests/V1ContractPredelayTest.cpp` | 01 | DSP-01…04 [v1][contract][predelay] |
| `SchroederTankCore` fixed predelay + `darkMix_` lerp | 01 | ADR-V1-12 production path |
| `LegacyAccumulatorPath` predelay parity | 01 | SRC-05 alignment |

<output>
After completion, create `24-01-SUMMARY.md`
</output>
