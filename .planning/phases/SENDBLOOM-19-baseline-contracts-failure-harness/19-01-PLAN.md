---
phase: 19-baseline-contracts-failure-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmake
  - .gitmodules
  - .planning/phases/SENDBLOOM-19-baseline-contracts-failure-harness/19-BASELINE.md
  - .planning/phases/SENDBLOOM-19-baseline-contracts-failure-harness/19-BASELINE-METRICS.md
  - .planning/REQUIREMENTS.md
  - tests/RequirementsTraceabilityTest.cpp
  - tests/BaselinePresetMetricsTest.cpp
autonomous: true
requirements:
  - BASE-01
  - BASE-02
  - BASE-03
  - BASE-07
must_haves:
  truths:
    - "cmake/Tests.cmake is present and Builds discovers a non-zero Catch2/ctest suite"
    - "19-BASELINE.md records commit SHA, branch, VERSION, build config, discovered test count, CI workflow names, and known manual gaps"
    - "Factory-preset baseline peak/RMS metrics are committed with a reproducible method before any production DSP/UI change"
    - "Every milestone requirement ID maps to exactly one phase and at least one verification artifact (tag or doc path)"
    - "No production DSP/UI source under source/ is modified in this plan"
  artifacts:
    - path: "cmake/Tests.cmake"
      provides: "Catch2 test target include path restored"
    - path: ".planning/phases/SENDBLOOM-19-baseline-contracts-failure-harness/19-BASELINE.md"
      provides: "Frozen baseline report (BASE-01 + metrics summary)"
    - path: ".planning/phases/SENDBLOOM-19-baseline-contracts-failure-harness/19-BASELINE-METRICS.md"
      provides: "Per-preset peak/RMS tables (BASE-07)"
    - path: "tests/BaselinePresetMetricsTest.cpp"
      provides: "Reproducible offline metrics capture tagged [baseline][metrics]"
    - path: "tests/RequirementsTraceabilityTest.cpp"
      provides: "Extended BASE-03 artifact anchors"
  key_links:
    - from: "CMakeLists.txt include(Tests)"
      to: "cmake/Tests.cmake GLOB tests/*.cpp"
      via: "submodule restore + reconfigure"
    - from: "19-BASELINE.md discovered count"
      to: "ctest --test-dir Builds -N"
      via: "runtime discovery only (no hard-coded totals)"
    - from: "REQUIREMENTS.md Traceability table"
      to: "RequirementsTraceabilityTest + artifact column"
      via: "BASE-02/03 verification"
---

<objective>
Restore a healthy cmake submodule, freeze current truth in 19-BASELINE.md with factory-preset metrics, and complete requirement→phase→artifact traceability (BASE-01/02/03/07) without any production DSP/UI fixes.

Purpose: Phase 19 cannot measure or contract anything until Tests.cmake exists and baseline truth is committed.
Output: Restored cmake/, 19-BASELINE.md, 19-BASELINE-METRICS.md, BaselinePresetMetricsTest.cpp, extended RequirementsTraceabilityTest + REQUIREMENTS artifact mapping.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/SENDBLOOM-19-baseline-contracts-failure-harness/19-CONTEXT.md
@.planning/phases/SENDBLOOM-19-baseline-contracts-failure-harness/19-RESEARCH.md
@CMakeLists.txt
@.github/workflows/build_and_test.yml
@tests/RequirementsTraceabilityTest.cpp
@tests/ReleaseTruthTest.cpp
@source/FactoryPresets.h
@docs/RELEASE_CHECKLIST.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Restore cmake submodule and rediscover tests</name>
  <files>cmake, .gitmodules (only if submodule URL/branch needs touch — prefer pointer-only)</files>
  <read_first>
    - .gitmodules (cmake → https://github.com/sudara/cmake-includes.git)
    - Parent gitlink: currently records unreachable SHA 38bc0f5… while working tree is at d5cb9b3 with all cmake files staged deleted
    - CMakeLists.txt line with include(Tests)
    - git -C cmake show HEAD:Tests.cmake (Catch2@3.8.1 CPM pin + GLOB tests/*.cpp + catch_discover_tests)
  </read_first>
  <action>
    Blocking Wave-0 restore per D-04 / research pitfall 4 — do this before any baseline metrics or contract work.

    1. In the cmake submodule: discard the staged deletions and restore files at the checked-out commit (d5cb9b387d38208b3250398666c5c4b88361a9e9 or newer main tip that still contains Tests.cmake). Prefer `git -C cmake reset --hard HEAD` after confirming HEAD has Tests.cmake. Do not install Catch2 from npm. Do not add new packages.
    2. Align the parent repo gitlink so `git ls-tree HEAD cmake` (after commit) records the same reachable SHA that contains Tests.cmake — not the missing 38bc0f5 pointer. Stage the updated submodule pointer with `git add cmake`.
    3. Reconfigure and build the Tests target using CI conventions: `BUILD_DIR=Builds`, Release config (`cmake -B Builds -DCMAKE_BUILD_TYPE=Release .` then `cmake --build Builds --config Release --target Tests`).
    4. Prove discovery: `ctest --test-dir Builds -N` must report Total Tests greater than 0. Record the discovered total for Task 2 (runtime value only — do not bake a fixed total into scripts).
    5. Confirm `test -f cmake/Tests.cmake` and that `include(Tests)` configure no longer fails.

    Forbidden: any edits under source/ for DSP/UI behavior; any new npm/pip packages; leaving cmake/ empty.
  </action>
  <verify>
    <automated>test -f cmake/Tests.cmake &amp;&amp; ctest --test-dir Builds -N 2>/dev/null | tee /tmp/ctest-n.txt | grep -E 'Total Tests: [1-9][0-9]*' &amp;&amp; test -x Builds/Tests -o -x Builds/Release/Tests -o -f Builds/Tests</automated>
  </verify>
  <done>cmake/Tests.cmake exists on disk, parent submodule pointer is a reachable SHA with Tests.cmake, Builds configure/build succeeds for Tests, and ctest -N discovers a non-zero suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author baseline report and factory-preset metrics</name>
  <files>.planning/phases/SENDBLOOM-19-baseline-contracts-failure-harness/19-BASELINE.md, .planning/phases/SENDBLOOM-19-baseline-contracts-failure-harness/19-BASELINE-METRICS.md, tests/BaselinePresetMetricsTest.cpp</files>
  <behavior>
    - BaselinePresetMetricsTest loads each of FactoryPresets::kNumPresets (8) via PluginProcessor::setCurrentProgram, renders a fixed number of 512-sample blocks at 48 kHz with a deterministic sine (or equivalent), and records finite per-preset peak and RMS for L/R (or mono-equivalent).
    - Test is tagged [baseline][metrics], remains green, and does not assert v1-correct DSP contracts.
    - Printed or written metrics match the tables committed in 19-BASELINE-METRICS.md.
  </behavior>
  <read_first>
    - source/FactoryPresets.h / FactoryPresets.cpp (kNumPresets, applyPreset, names)
    - tests/ReleaseTruthTest.cpp (PluginProcessor prepare/process, bufferRms helpers, repo-root finder patterns)
    - resources/presets/*.xml (Firm_Pressure, Hot_Clip pressure defaults — document as current truth, do not fix)
    - VERSION file (expect 0.0.1)
    - .github/workflows/build_and_test.yml (workflow name build_and_test, BUILD_DIR=Builds, matrix OS)
  </read_first>
  <action>
    Per D-01, D-02, D-12 (BASE-01 / BASE-07):

    1. Create 19-BASELINE.md at the locked phase path with at least: commit SHA (`git rev-parse HEAD`), branch, VERSION contents, build config (generator + CMAKE_BUILD_TYPE=Release + BUILD_DIR=Builds), discovered Catch2/ctest count from `ctest --test-dir Builds -N` (write the number observed at capture time as a snapshot field labeled “discovered at capture”, not as a forever-expected constant for scripts), CI workflow file/name(s) from `.github/workflows/`, and known manual gaps (AU pluginval, Win/Linux when not run locally, DAW smoke, signing/notarization, license decision — list as gaps, not passes).
    2. Note current factory pressure-preset truth: Firm_Pressure / Hot_Clip load send_connected=1 with non-zero send_amount (document only).
    3. Add tests/BaselinePresetMetricsTest.cpp (discretion: Catch2 offline render). For each factory preset index 0..kNumPresets-1: setCurrentProgram, prepare 48 kHz / 512, render N blocks of deterministic excitation, compute peak and RMS. Tag `[baseline][metrics]`. Keep assertions limited to finite/non-NaN so the test stays green under current DSP.
    4. Commit 19-BASELINE-METRICS.md with the captured per-preset peak/RMS table plus the exact method (test filter command: Builds/Tests "[baseline][metrics]" or ctest -R BaselinePreset). Summarize metrics location in 19-BASELINE.md.
    5. Explicitly state in 19-BASELINE.md that Phase 19 commits harness/docs/failing-contracts only — no production DSP/UI fixes yet (D-04).

    Do not hard-code expected suite totals into scripts (BASE-06 is Plan 03; baseline may record the discovered snapshot once). Do not edit ParameterCurves, PluginProcessor behavior, NoiseGate, PressureSendPad, or faceplate sources.
  </action>
  <verify>
    <automated>test -f .planning/phases/SENDBLOOM-19-baseline-contracts-failure-harness/19-BASELINE.md &amp;&amp; test -f .planning/phases/SENDBLOOM-19-baseline-contracts-failure-harness/19-BASELINE-METRICS.md &amp;&amp; test -f tests/BaselinePresetMetricsTest.cpp &amp;&amp; grep -E 'commit|branch|VERSION|manual|ctest|workflow' -i .planning/phases/SENDBLOOM-19-baseline-contracts-failure-harness/19-BASELINE.md | head -5 &amp;&amp; cmake --build Builds --config Release --target Tests &amp;&amp; Builds/Tests "[baseline][metrics]"</automated>
  </verify>
  <done>19-BASELINE.md and 19-BASELINE-METRICS.md exist with required fields and committed preset metrics; BaselinePresetMetricsTest is green under [baseline][metrics]; no production DSP/UI diffs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify REQUIREMENTS completeness and artifact mapping</name>
  <files>.planning/REQUIREMENTS.md, tests/RequirementsTraceabilityTest.cpp</files>
  <behavior>
    - Every milestone requirement ID present in REQUIREMENTS.md Active/v1 list also appears exactly once in the Traceability table with a phase.
    - Extended Catch2 coverage adds [traceability] cases (or a data-driven case) that require each ID to have a non-empty verification artifact string (Catch2 tag and/or doc path).
    - Phase 19 BASE IDs map to artifacts under this phase dir / [baseline] / future [v1][contract] tags as appropriate.
  </behavior>
  <read_first>
    - .planning/REQUIREMENTS.md (BASE-01…REL-20 checklist + Traceability table claiming 128/128)
    - .planning/MILESTONE-SPEC-v1.0-interaction-truth.md or imported master spec ID list if present under .planning/
    - tests/RequirementsTraceabilityTest.cpp (existing TEST-01..TEST-05 [traceability] anchors — extend, do not delete)
  </read_first>
  <action>
    Per D-03 (BASE-02 / BASE-03):

    1. BASE-02 verification (catalog already authored): confirm REQUIREMENTS.md enumerates all milestone IDs (BASE 8 + SEND 14 + MIDI 10 + RT 15 + CORE 18 + DSP 15 + UX 16 + REF 12 + REL 20 = 128) and FUT deferred separately. If any ID from the milestone spec is missing from REQUIREMENTS.md, add it. Do not invent new product requirements.
    2. Extend the Traceability section: add a Verification artifact column (or adjacent mapping table) so each requirement has ≥1 artifact — Catch2 tag, test file path, script path, or planning doc path. For Phase 19 BASE rows use concrete artifacts (19-BASELINE.md, BaselinePresetMetricsTest, RequirementsTraceabilityTest, and placeholder paths/tags for contracts/verifier that Plan 02/03 will create — e.g. tests/V1Contract*.cpp and scripts/verify-v1.sh).
    3. Extend RequirementsTraceabilityTest.cpp with a [traceability][BASE-03] (and/or per-family) case that fails if any mapped ID lacks a non-empty artifact string. Prefer an embedded constexpr table mirroring the planning map for the BASE family fully, plus a representative check that the planning file still lists all 128 IDs (read REQUIREMENTS.md from repo root like ReleaseTruthTest does) OR keep a compile-time ID list in the test. Keep preexisting TEST-01..05 cases intact and green.
    4. Do not weaken MidiSendAmountTest, ParameterCurvesTest, ReleaseTruthTest, PostGateTimingTest, DryPathIntegrityTest, or HF/ProperSRC tests.

    Discretion: exact table shape and whether BASE-only or all-128 IDs are embedded in Catch2 — must satisfy “each ID → phase + ≥1 artifact” observably.
  </action>
  <verify>
    <automated>grep -c 'BASE-0[1-8]' .planning/REQUIREMENTS.md | awk '{exit !($1&gt;=8)}' &amp;&amp; cmake --build Builds --config Release --target Tests &amp;&amp; Builds/Tests "[traceability]"</automated>
  </verify>
  <done>REQUIREMENTS.md remains a complete 128-ID catalog with phase + verification artifact for each row; RequirementsTraceabilityTest [traceability] suite passes; green legacy suites untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer workstation → cmake submodule / CPM | Untrusted network fetch of cmake-includes + Catch2 pin |
| planning docs → CI/humans | Baseline fields must not falsely claim gates passed |
| test harness → PluginProcessor | Offline renders use deterministic fixtures only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | cmake submodule / CPM Catch2 | high | mitigate | Restore only known sudara/cmake-includes SHA with Tests.cmake; keep existing Catch2@3.8.1 pin; no npm Catch2 |
| T-19-02 | Repudiation | 19-BASELINE.md manual gaps | medium | mitigate | List AU/DAW/signing/license as gaps/not verified — never as pass |
| T-19-03 | Tampering | BaselinePresetMetricsTest preset XML | low | accept | Uses in-repo FactoryPresets only; XmlDocumentEntityExpansionTest remains elsewhere |
| T-19-SC | Tampering | package installs | high | mitigate | No new packages in this plan; legitimacy already OK for existing Catch2 pin |
</threat_model>

<verification>
- ctest -N discovers tests after cmake restore
- 19-BASELINE.md + metrics artifacts committed
- Builds/Tests "[baseline][metrics]" and "[traceability]" pass
- `git diff --name-only` shows no production fix diffs under source/ui DSP cores intended for Phases 20–25 (allowlist only harness/docs/tests listed in files_modified)
</verification>

<success_criteria>
BASE-01/02/03/07 satisfied: frozen baseline + metrics + complete requirement→phase→artifact map on a working Catch2 build, with zero production DSP/UI behavior changes.
</success_criteria>

<output>
Create `.planning/phases/SENDBLOOM-19-baseline-contracts-failure-harness/19-01-SUMMARY.md` when done
</output>
