---
phase: 19-baseline-contracts-failure-harness
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - tests/V1ContractPressureReleaseTest.cpp
  - tests/V1ContractOversizedBlockTest.cpp
  - tests/V1ContractTrueBypassTest.cpp
  - tests/V1ContractPostHardRampTest.cpp
  - tests/V1ContractInputAnchorsTest.cpp
  - tests/V1ContractMidiApvtsPurityTest.cpp
  - tests/V1ContractShippingPolicyTest.cpp
autonomous: true
requirements:
  - BASE-04
must_haves:
  truths:
    - "Seven dedicated [v1][contract] suites exist and fail for the intended defect reasons on current production code"
    - "Preexisting ProperSRC, HF, dry-integrity, and release-truth suites still pass when [v1] contracts are excluded"
    - "No preexisting green test file is edited to encode failures (MidiSendAmountTest, ParameterCurvesTest, ReleaseTruthTest MIDI case, PostGateTimingTest stay as-is)"
    - "No production DSP/UI fixes land in this plan"
  artifacts:
    - path: "tests/V1ContractPressureReleaseTest.cpp"
      provides: "Pressure release contract (SEND-05 / ADR-V1-02)"
    - path: "tests/V1ContractOversizedBlockTest.cpp"
      provides: "Oversized-block wet continuity (RT-02/RT-03)"
    - path: "tests/V1ContractTrueBypassTest.cpp"
      provides: "Settled true bypass unity (CORE-14..16 / ADR-V1-10)"
    - path: "tests/V1ContractPostHardRampTest.cpp"
      provides: "PostHard ramp not snap (CORE-10/11 / ADR-V1-11)"
    - path: "tests/V1ContractInputAnchorsTest.cpp"
      provides: "Input −9/0/+9 anchors (CORE-01 / ADR-V1-08)"
    - path: "tests/V1ContractMidiApvtsPurityTest.cpp"
      provides: "MIDI must not mutate APVTS send_amount (MIDI-03)"
    - path: "tests/V1ContractShippingPolicyTest.cpp"
      provides: "Banned shipping strings/filenames + processBlock policy scans (UX-07/08, RT-01 hints)"
  key_links:
    - from: "V1Contract* REQUIRE assertions"
      to: "current broken loci in source/"
      via: "observe-only; no production edits"
    - from: "BASE-04 green proof"
      to: "Builds/Tests filter excluding [v1]"
      via: "tag filter ~[v1] plus ENAB script"
---

<objective>
Add seven dedicated Catch2 contract translation units tagged for the milestone harness that assert v1-CORRECT behavior so they FAIL now for the right reason, while proving BASE-04 green suites remain green.

Purpose: Lock defect detection before Phases 20–25 fix production code.
Output: Seven V1Contract*.cpp files that fail intentionally; green-suite proof command results documented in the plan SUMMARY.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/SENDBLOOM-19-baseline-contracts-failure-harness/19-CONTEXT.md
@.planning/phases/SENDBLOOM-19-baseline-contracts-failure-harness/19-RESEARCH.md
@.planning/phases/SENDBLOOM-19-baseline-contracts-failure-harness/19-BASELINE.md
@tests/MidiSendAmountTest.cpp
@tests/ParameterCurvesTest.cpp
@tests/ReleaseTruthTest.cpp
@tests/PostGateTimingTest.cpp
@tests/PressureSendTest.cpp
@source/PluginProcessor.cpp
@source/NoiseGate.h
@source/ParameterCurves.h
@source/ui/PressureSendPad.cpp
@source/PressureSend.h
@source/GatedBloomChain.h
@scripts/enab-acceptance-gates.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pressure release, oversized-block, and true-bypass contracts</name>
  <files>tests/V1ContractPressureReleaseTest.cpp, tests/V1ContractOversizedBlockTest.cpp, tests/V1ContractTrueBypassTest.cpp</files>
  <behavior>
    - Pressure: after a UI press→release sequence equivalent to PressureSendPad::mouseUp, v1 requires send_connected stays true and send_amount→0 with no new wet energy into a mock reverb (connected-at-rest dry feed). Current mouseUp disconnects — contract must FAIL on disconnected=true and/or always-on wet feed after release.
    - Oversized: prepare 48 kHz / 512 then process one 2048-sample block vs four×512 reference; max abs diff &lt; 2e-5 and wet energy nonzero. Current dry-only oversized fallback must FAIL this.
    - True bypass: settle bypass (&gt;5 ms), stereo anti-phase or distinct L/R input; each output channel matches its input within 1e-6 and ignores output gain. Current mono-sum / output-stage path must FAIL.
  </behavior>
  <read_first>
    - 19-RESEARCH.md defect→contract map rows for pressure, oversized, bypass
    - source/ui/PressureSendPad.cpp mouseUp (setConnected(false))
    - source/PressureSend.h computeGain (disconnected → 1.0)
    - source/PluginProcessor.cpp oversized-block branch and bypass mix loop
    - source/GatedBloomChain.h setReverbEngineForTests
    - tests/helpers / ReleaseTruthTest for PluginProcessor harness patterns
  </read_first>
  <action>
    Per D-05, D-06, D-07 — new files only under tests/, tags must include both harness tags used in CONTEXT (`[v1]` and `[contract]`) plus a stable defect id tag:

    1. V1ContractPressureReleaseTest.cpp — drive PressureSendPad (or the exact APVTS mutations mouseUp performs via the pad API) through press then release; REQUIRE connected remains true, amount is 0, and injected IReverbEngine mock accepts no new energy after release while dry path stays nonzero (milestone §8.4 / ADR-V1-02 / SEND-05). Use setReverbEngineForTests when asserting wet-energy stop. Failure must be about connection/amount/wet-feed — not prepare mismatch.
    2. V1ContractOversizedBlockTest.cpp — PluginProcessor prepare(48000, 512); compare 2048 one-shot vs 4×512; REQUIRE max abs sample diff &lt; 2e-5 and wet path nonzero (RT-02/RT-03). Failure must be wet-zero / large diff from dry fallback — not unrelated NaNs from forgotten prepare.
    3. V1ContractTrueBypassTest.cpp — settle bypass longer than crossfade; distinct stereo channels; REQUIRE per-channel unity within 1e-6 ignoring Output/Input/Distn/Gate/Level (CORE-14..16 / ADR-V1-10). Failure must show channel collapse and/or output gain applied.

    Forbidden: edits to source/PluginProcessor.cpp, PressureSendPad.cpp, or any production fix; edits to existing green tests; marking cases skipped / disabled.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target Tests &amp;&amp; (Builds/Tests "[v1][contract][pressure-release]" ; test $? -ne 0) &amp;&amp; (Builds/Tests "[v1][contract][oversized-block]" ; test $? -ne 0) &amp;&amp; (Builds/Tests "[v1][contract][true-bypass]" ; test $? -ne 0)</automated>
  </verify>
  <done>Three contract files exist, are discovered by Catch2, and each filtered run exits non-zero for the intended assertion failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PostHard, Input anchors, and MIDI APVTS purity contracts</name>
  <files>tests/V1ContractPostHardRampTest.cpp, tests/V1ContractInputAnchorsTest.cpp, tests/V1ContractMidiApvtsPurityTest.cpp</files>
  <behavior>
    - PostHard: after open, first close sample gain in (0,1), |Δgain| ≤ 0.05, and gain ≤ 1e-4 within 1 ms (CORE-10/11). Current one-sample snap to 0 must FAIL on first-sample requirements.
    - Input: inputGainDb(0/0.5/1) == −9 / 0 / +9 within 1e-4 (CORE-01 / ADR-V1-08). Current +9…−3 mapping must FAIL. Do not change ParameterCurvesTest.
    - MIDI: with send_connected true and send_amount at a known value, processBlock a CC1 event; REQUIRE APVTS send_amount unchanged (MIDI-03). Current store into APVTS must FAIL. Do not edit MidiSendAmountTest or ReleaseTruth MIDI cases. Positive “DSP pressure still changes” half may be documented as Phase 22 companion if no RT lane exists (research A1) — purity + optional source scan is required now.
  </behavior>
  <read_first>
    - source/NoiseGate.h PostHard floorGain snap branch
    - source/ParameterCurves.h inputGainDb
    - tests/ParameterCurvesTest.cpp (documents current +9/−3 — leave untouched)
    - tests/MidiSendAmountTest.cpp (documents current APVTS mutation — leave untouched)
    - source/PluginProcessor.cpp MIDI CC1 → sendParam->store path
  </read_first>
  <action>
    Per D-05, D-06, D-07 and research A1:

    1. V1ContractPostHardRampTest.cpp — NoiseGate prepare PostHard @ 48 kHz; open then close; assert first closed sample ramp properties and ≤1 ms to near-zero (ADR-V1-11).
    2. V1ContractInputAnchorsTest.cpp — REQUIRE ParameterCurves::inputGainDb anchors −9/0/+9. Optionally note display must share the same curve (CORE-02) as a secondary REQUIRE if a pure function/formatter exists; do not rewrite ParameterLayout production code.
    3. V1ContractMidiApvtsPurityTest.cpp — REQUIRE APVTS send_amount unchanged across CC1 processBlock when connected. Optionally add a ReleaseTruth-style source scan that the processBlock body must not contain raw sendParam store — as an additional contract case. Do not assert host notify if not needed for MIDI-03. Document in test comment that DSP-effect half awaits Phase 22 if no separate RT observable exists.

    Forbidden: weakening ParameterCurvesTest / MidiSendAmountTest / ReleaseTruthTest / PostGateTimingTest; production DSP edits.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target Tests &amp;&amp; (Builds/Tests "[v1][contract][posthard]" ; test $? -ne 0) &amp;&amp; (Builds/Tests "[v1][contract][input-anchors]" ; test $? -ne 0) &amp;&amp; (Builds/Tests "[v1][contract][midi-apvts]" ; test $? -ne 0)</automated>
  </verify>
  <done>PostHard, Input, and MIDI purity contracts fail for the intended reasons; legacy green tests that document current buggy behavior are unmodified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Shipping-policy contract and BASE-04 green-suite proof</name>
  <files>tests/V1ContractShippingPolicyTest.cpp</files>
  <behavior>
    - Shipping policy contract fails while product-facing "REVERB X" string and/or shipping resource basename containing reverbx remains, and/or while processBlock still performs dryBuffer.setSize / banned alloc patterns detectable by source scan (UX-07/08, RT-01 evidence).
    - Builds/Tests "~[v1]" (or equivalent exclusion) passes for ProperSRC/HF/DryPath/release subsets.
    - scripts/enab-acceptance-gates.sh still passes with BUILD_DIR=Builds.
  </behavior>
  <read_first>
    - source/ui/PedalFaceplatePaint.cpp (product-facing string)
    - CMakeLists.txt binary resource names
    - scripts/check-legal-metadata.sh (banned list gaps — contract must cover strings the script misses)
    - tests/ReleaseTruthTest.cpp source-scan helpers (extractProcessBlockBody pattern)
    - scripts/enab-acceptance-gates.sh
  </read_first>
  <action>
    Per D-05, D-06, D-07, D-08 (BASE-04):

    1. Add V1ContractShippingPolicyTest.cpp with harness tags plus `[shipping-policy]`. Scan product-facing sources/resources the same way ReleaseTruth scans processBlock: REQUIRE absence of product-facing third-party faceplate naming and absence of shipping resource filenames containing the banned reverbx token; REQUIRE processBlock body has no dryBuffer.setSize and no sendParam->store (or equivalent raw APVTS write). These REQUIREs must FAIL on current tree for the right strings/patterns.
    2. Rebuild Tests (GLOB auto-picks new cpp — no CMakeLists edit if Tests.cmake GLOB intact).
    3. BASE-04 proof: run green filters excluding the harness tag family — `Builds/Tests "~[v1]"` must pass. Also run ENAB with `BUILD_DIR=Builds bash scripts/enab-acceptance-gates.sh`. Run targeted green tags if full ~[v1] is too long for the agent window: at minimum `[release]`, `[DryPath]`, and the ENAB script filter must pass.
    4. Record in SUMMARY the expected failing contract filters and the green proof commands/results. Optionally append a short “expected failure reasons” subsection to 19-BASELINE.md (docs-only).

    Forbidden: production branding/DSP fixes; editing check-legal-metadata.sh to silence failures instead of contracting them; disabling contracts with `[.]`.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target Tests &amp;&amp; (Builds/Tests "[v1][contract][shipping-policy]" ; test $? -ne 0) &amp;&amp; Builds/Tests "~[v1]" &amp;&amp; BUILD_DIR=Builds bash scripts/enab-acceptance-gates.sh</automated>
  </verify>
  <done>Shipping-policy contract fails for intended strings/patterns; green suite excluding harness tags still passes; ENAB gates pass; no production source fixes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract tests → production DSP/UI | Tests observe defects; must not patch production to go green |
| Catch2 filters → CI interpreters | Failed contracts must remain visible (not skipped) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-04 | Tampering | V1Contract*.cpp vs green tests | high | mitigate | New files only; forbid edits to MidiSendAmountTest/ParameterCurvesTest/ReleaseTruth/PostGateTiming; BASE-04 green proof task |
| T-19-05 | Repudiation | Intentionally failing tests | medium | mitigate | Tags + SUMMARY document expected reds; never mark `[.]` skipped |
| T-19-06 | Denial of service | Flaky contract setup | medium | mitigate | Follow §8.4 settle/prepare recipes; assert intended predicates |
| T-19-SC | Tampering | package installs | high | mitigate | No new packages |
</threat_model>

<verification>
- Seven contract filters each exit non-zero
- `Builds/Tests "~[v1]"` green
- ENAB script green with BUILD_DIR=Builds
- `git diff source/` empty of intentional behavior fixes
</verification>

<success_criteria>
BASE-04 held: every confirmed Phase-19 defect has a failing harness contract for the right reason, and preexisting ProperSRC/HF/dry-integrity/release-truth coverage remains green when contracts are excluded.
</success_criteria>

<output>
Create `.planning/phases/SENDBLOOM-19-baseline-contracts-failure-harness/19-02-SUMMARY.md` when done
</output>
