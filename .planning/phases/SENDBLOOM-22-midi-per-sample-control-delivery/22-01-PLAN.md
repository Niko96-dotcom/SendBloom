---
phase: 22-midi-per-sample-control-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - source/PluginProcessor.cpp
  - tests/MidiSendAmountTest.cpp
  - tests/V1ContractMidiApvtsPurityTest.cpp
autonomous: true
requirements:
  - MIDI-01
  - MIDI-02
  - MIDI-03
  - MIDI-07
  - MIDI-08
must_haves:
  truths:
    - "processBlock never stores into send_amount APVTS and never calls setValueNotifyingHost for MIDI"
    - "When connected, CC1 updates PressureController MIDI target; host/UI + MIDI combine via max"
    - "When disconnected, CC1 does not change audible send (unity always-on)"
    - "MIDI does not dirty saved plugin state (send_amount unchanged after CC1)"
    - "[midi-apvts] green; [pressure-release] still green"
  artifacts:
    - path: "source/PluginProcessor.cpp"
      provides: "MIDI → setMidiPressureTarget; no sendParam->store; no forced midi zero each block"
      contains: "setMidiPressureTarget"
    - path: "tests/V1ContractMidiApvtsPurityTest.cpp"
      provides: "MIDI-03 purity green"
    - path: "tests/MidiSendAmountTest.cpp"
      provides: "ADR-V1-03 contract (APVTS unchanged; wet/pressure responds)"
  key_links:
    - from: "MidiBuffer CC1"
      to: "PressureController::setMidiPressureTarget"
      via: "connected-only; no APVTS write"
      pattern: "setMidiPressureTarget"
    - from: "PressureController"
      to: "rawPressure"
      via: "max(hostTarget_, midiTarget_)"
      pattern: "std::max \\(hostTarget_|max \\(host"
---

<objective>
Remove APVTS mutation for MIDI CC1 and wire CC1 into PressureController realtime MIDI target so purity contracts flip green — without yet requiring sample-accurate span cuts (Plan 02) or distn/threshold arrays (Plan 03).

Purpose: Flip `[midi-apvts]` green; update MidiSendAmountTest to ADR-V1-03.
Output: Pure MIDI modulation path + updated MIDI tests; Phase 20/21 regressions green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/SENDBLOOM-22-midi-per-sample-control-delivery/22-CONTEXT.md
@.planning/phases/SENDBLOOM-22-midi-per-sample-control-delivery/22-RESEARCH.md
@source/PluginProcessor.cpp
@source/PressureController.h
@tests/V1ContractMidiApvtsPurityTest.cpp
@tests/MidiSendAmountTest.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove APVTS MIDI store; wire midi target (MIDI-02/03/08)</name>
  <files>source/PluginProcessor.cpp, tests/V1ContractMidiApvtsPurityTest.cpp</files>
  <behavior>
    - processBlock body has no sendParam->store and no setValueNotifyingHost
    - CC1 while connected leaves APVTS send_amount unchanged
    - Remove pressureController.setMidiPressureTarget(0.0f) forced each block
    - Apply CC1 → setMidiPressureTarget(value/127) when connected (block-level OK for Plan 01; Plan 02 adds sample accuracy)
  </behavior>
  <read_first>
    - 22-RESEARCH.md Exact Bug Loci §1
    - source/PluginProcessor.cpp processBlock MIDI loop + pressureController wiring
    - tests/V1ContractMidiApvtsPurityTest.cpp
  </read_first>
  <action>
    1. Delete the `sendParam->store` MIDI mutation loop.
    2. Remove `setMidiPressureTarget(0.0f)` at block start.
    3. For each CC1 when connected, call `pressureController.setMidiPressureTarget(norm)`.
    4. Keep host target from APVTS snapshot via `setHostPressureTarget`.
    5. Confirm `[midi-apvts]` source-scan and runtime cases pass.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target Tests &amp;&amp; Builds/Tests "[midi-apvts]"</automated>
  </verify>
  <acceptance_criteria>
    - `rg "sendParam-&gt;store" source/PluginProcessor.cpp` returns no matches in processBlock
    - Builds/Tests "[midi-apvts]" exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update MidiSendAmountTest + connected/disconnected/max (MIDI-01/07)</name>
  <files>tests/MidiSendAmountTest.cpp</files>
  <behavior>
    - Connected CC1 does NOT change APVTS send_amount; wet energy / effective pressure increases vs CC1=0
    - Disconnected CC1 leaves send_amount 0 and does not create pressure-mode dry-at-rest→wet via MIDI
    - Host amount and MIDI combine via max (PressureController already)
    - [pressure-release] still green
  </behavior>
  <read_first>
    - tests/MidiSendAmountTest.cpp
    - ADR-V1-03 in milestone spec
  </read_first>
  <action>
    Rewrite MidiSendAmountTest cases that expect APVTS mutation to assert purity + audible/sendGain-via-controller behavior. Keep acceptsMidi case. Run pressure-release regression.
  </action>
  <verify>
    <automated>Builds/Tests "[midi]" &amp;&amp; Builds/Tests "[pressure-release]"</automated>
  </verify>
  <acceptance_criteria>
    - Builds/Tests "[midi]" exits 0
    - Builds/Tests "[pressure-release]" exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
Plan 01 done when `[midi-apvts]` green, MidiSendAmountTest matches ADR-V1-03, pressure-release green. Sample-accurate spans deferred to 22-02.
</verification>

<success_criteria>
- No APVTS/host notify from MIDI in processBlock
- MIDI drives PressureController midi target when connected
- Tests updated and green for purity
</success_criteria>
