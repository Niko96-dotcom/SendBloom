---
phase: 22-midi-per-sample-control-delivery
plan: 03
type: execute
wave: 3
depends_on:
  - 22-02
files_modified:
  - source/GatedBloomChain.h
  - source/PluginProcessor.h
  - source/PluginProcessor.cpp
  - tests/V1ContractPerSampleControlsTest.cpp
  - tests/GatedBloomChainTest.cpp
autonomous: true
requirements:
  - RT-06
  - RT-07
must_haves:
  truths:
    - "send/distn/threshold controls consumed per sample in the wet chain"
    - "input gain, level, output gain, and bypass remain per-sample in processSpan"
    - "Distn automation no longer collapses to first-sample-of-span only"
    - "MIDI purity + Phase 20/21 greens preserved"
  artifacts:
    - path: "source/GatedBloomChain.h"
      provides: "per-sample distn + threshold array consumption"
    - path: "source/PluginProcessor.cpp"
      provides: "distnScratch_ / thresholdDbScratch_ filled every sample"
    - path: "tests/V1ContractPerSampleControlsTest.cpp"
      provides: "RT-06/07 contracts"
  key_links:
    - from: "processSpan"
      to: "GatedBloomChain::processBlock"
      via: "array pointers for send/distn/threshold"
      pattern: "distnScratch_|thresholdDbScratch_"
---

<objective>
Deliver ADR-V1-06 dynamic control arrays so send/distn/threshold (and existing input/level/output/bypass) are truly consumed per sample.

Purpose: Close RT-06/RT-07; remove Distn span-constant zipper.
Output: Chain array API + processor scratch fill + contract tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/SENDBLOOM-22-midi-per-sample-control-delivery/22-CONTEXT.md
@.planning/phases/SENDBLOOM-22-midi-per-sample-control-delivery/22-RESEARCH.md
@source/GatedBloomChain.h
@source/PluginProcessor.cpp
@tests/GatedBloomChainTest.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-sample distn/threshold arrays in chain + processor (RT-06)</name>
  <files>source/GatedBloomChain.h, source/PluginProcessor.h, source/PluginProcessor.cpp, tests/GatedBloomChainTest.cpp</files>
  <behavior>
    - Add prepare-sized distnScratch_ and thresholdDbScratch_
    - Fill every sample in processSpan (alongside sendGainScratch_)
    - Chain processBlock consumes per-sample distn and threshold (keep scalar overloads for existing tests)
    - Existing GatedBloomChain processBlock==processSample tests stay green
  </behavior>
  <read_first>
    - ADR-V1-06 signature in milestone
    - source/GatedBloomChain.h private processBlock
    - source/PluginProcessor.cpp processSpan control loop
  </read_first>
  <action>
    Extend chain to accept optional distn/threshold arrays; wire processor scratches; update call site. Do not change PostHard timing.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target Tests &amp;&amp; Builds/Tests "[chain][GatedBloomChain]"</automated>
  </verify>
  <acceptance_criteria>
    - Production call passes array pointers for send/distn/threshold
    - Builds/Tests "[chain][GatedBloomChain]" exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RT-06/07 contract + full Phase 22 regression gate</name>
  <files>tests/V1ContractPerSampleControlsTest.cpp</files>
  <behavior>
    - Prove Distn mid-block change affects later samples differently than span-constant collapse (or block-size Distn parity within tolerance)
    - Document/assert input/level/output/bypass scratches advanced per sample in processSpan (source-scan or behavioral)
    - Full MIDI + pressure-release + oversized + true-bypass + midi-apvts green
  </behavior>
  <read_first>
    - Milestone §11.9 Distortion zipper test
    - 22-VALIDATION.md regression gates
  </read_first>
  <action>
    Add `[v1][contract][per-sample]` tests for RT-06/07. Run full Phase 22 gate set.
  </action>
  <verify>
    <automated>Builds/Tests "[v1][contract][per-sample]" &amp;&amp; Builds/Tests "[v1][contract][midi]" &amp;&amp; Builds/Tests "[midi-apvts]" &amp;&amp; Builds/Tests "[pressure-release]" &amp;&amp; Builds/Tests "[oversized-block]" &amp;&amp; Builds/Tests "[true-bypass]"</automated>
  </verify>
  <acceptance_criteria>
    - All listed filters exit 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
Plan 03 done when RT-06/07 green and Phase 22 MIDI gates + Phase 20/21 regressions green.
</verification>

<success_criteria>
- Dynamic controls consumed per sample
- No regressions on pressure/oversized/bypass/MIDI purity
</success_criteria>
