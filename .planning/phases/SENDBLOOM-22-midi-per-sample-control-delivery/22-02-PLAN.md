---
phase: 22-midi-per-sample-control-delivery
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - source/PluginProcessor.cpp
  - source/PluginProcessor.h
  - tests/V1ContractMidiSampleAccurateTest.cpp
autonomous: true
requirements:
  - MIDI-04
  - MIDI-05
  - MIDI-06
  - MIDI-09
  - MIDI-10
  - RT-04
must_haves:
  truths:
    - "Span length includes distance to next CC1 event (RT-04)"
    - "CC1 at sample N does not affect samples before N"
    - "Multiple CC1 events in one block apply in order; last at same sample wins"
    - "CC1 value 0 releases MIDI pressure; non-CC1 ignored"
    - "Behavior finite/deterministic across host block sizes 64/512/2048"
  artifacts:
    - path: "source/PluginProcessor.cpp"
      provides: "MIDI cursor + span cut at next CC1"
      contains: "kControlQuantum"
    - path: "tests/V1ContractMidiSampleAccurateTest.cpp"
      provides: "MIDI-04/05/06/09/10 + RT-04 contracts"
  key_links:
    - from: "span loop"
      to: "next CC1 sample"
      via: "span = min(remaining, preparedMaxBlock_, 128, nextCc1 - offset)"
      pattern: "nextCc1|controllerEvent|samplePosition"
---

<objective>
Make CC1 sample-position-aware and cut processing spans at MIDI event boundaries so RT-04 and MIDI-04…06/09/10 hold.

Purpose: Audible pressure window matches MIDI sample positions without APVTS writes.
Output: Cursor-driven span loop + V1Contract sample-accurate MIDI suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/SENDBLOOM-22-midi-per-sample-control-delivery/22-CONTEXT.md
@.planning/phases/SENDBLOOM-22-midi-per-sample-control-delivery/22-RESEARCH.md
@source/PluginProcessor.cpp
@tests/V1ContractMidiApvtsPurityTest.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sample-accurate MIDI cursor + span cuts (RT-04, MIDI-04/05)</name>
  <files>source/PluginProcessor.cpp, source/PluginProcessor.h</files>
  <behavior>
    - At span start offset, apply all connected CC1 events at that samplePosition
    - span = min(remaining, preparedMaxBlock_, kControlQuantum, distanceToNextCc1)
    - Events at sample 0 affect sample 0; event at 256 does not affect samples &lt; 256
    - No APVTS writes; no alloc
  </behavior>
  <read_first>
    - Milestone §11.3 MIDI cursor behavior
    - 22-RESEARCH Recommended Implementation Shape
    - source/PluginProcessor.cpp processBlock span loop
  </read_first>
  <action>
    Replace block-front MIDI apply with per-span cursor. Helper may live as private methods on PluginProcessor. Keep midi target persistent across blocks.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target Tests &amp;&amp; Builds/Tests "[midi-apvts]"</automated>
  </verify>
  <acceptance_criteria>
    - processBlock span uses distance to next CC1
    - No sendParam-&gt;store
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: V1Contract sample-accurate MIDI suite (MIDI-04…06/09/10)</name>
  <files>tests/V1ContractMidiSampleAccurateTest.cpp</files>
  <behavior>
    - CC1 @256: no pressure feed (wet send energy) before 256 when connected+resting host amount 0
    - CC1 127 @128 then CC1 0 @384 → bounded send window
    - Non-CC1 (e.g. CC2 / noteOn) ignored
    - Finite across 64/512/2048
    - Regressions: pressure-release, oversized, true-bypass still green
  </behavior>
  <read_first>
    - Milestone §11.8 required tests
    - tests/V1ContractPressureReleaseTest.cpp helpers pattern
  </read_first>
  <action>
    Add Catch2 suite tagged `[v1][contract][midi]` covering sample accuracy, release-to-zero, non-CC1, block-size finite. Measure wet path via buffer energy with dry-silent / known input as appropriate.
  </action>
  <verify>
    <automated>Builds/Tests "[v1][contract][midi]" &amp;&amp; Builds/Tests "[pressure-release]" &amp;&amp; Builds/Tests "[oversized-block]" &amp;&amp; Builds/Tests "[true-bypass]"</automated>
  </verify>
  <acceptance_criteria>
    - Builds/Tests "[v1][contract][midi]" exits 0
    - Regression tags exit 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
Plan 02 done when sample-accurate MIDI contracts green and Phase 20/21 regressions green.
</verification>

<success_criteria>
- RT-04 span cuts at CC1
- MIDI-04/05/06/09/10 green
</success_criteria>
