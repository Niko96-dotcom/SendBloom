---
phase: 23-input-level-gate-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - source/ParameterCurves.h
  - source/PluginEditor.cpp
  - tests/ParameterCurvesTest.cpp
  - tests/V1ContractInputAnchorsTest.cpp
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-04
  - CORE-05
must_haves:
  truths:
    - "inputGainDb(0/0.5/1) == −9/0/+9 within 1e-4"
    - "Input knob formatter calls ParameterCurves::inputGainDb (no duplicated arithmetic)"
    - "Clockwise Input increases wet drive; dry tap remains pre-Input gain"
    - "[input-anchors] green"
  artifacts:
    - path: "source/ParameterCurves.h"
      provides: "ADR-V1-08 canonical inputGainDb"
      contains: "-9.0f + 18.0f"
    - path: "source/PluginEditor.cpp"
      provides: "Display uses canonical curve"
      contains: "inputGainDb"
    - path: "tests/V1ContractInputAnchorsTest.cpp"
      provides: "CORE-01 contract green"
  key_links:
    - from: "UI Input formatter"
      to: "ParameterCurves::inputGainDb"
      via: "PedalKnob setValueFormatter"
      pattern: "inputGainDb"
    - from: "ParameterSnapshot"
      to: "SmoothedParameterBank inputGainLinear"
      via: "inputGainDb → decibelsToGain"
      pattern: "inputGainDb"
---

<objective>
Make Input mapping and display share one canonical −9/0/+9 curve so clockwise Input drives wet harder and CORE-01/02 flip green.

Purpose: Flip `[input-anchors]`; fix display/DSP agreement.
Output: ADR-V1-08 curve + UI formatter; legacy ParameterCurvesTest updated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/SENDBLOOM-23-input-level-gate-truth/23-CONTEXT.md
@.planning/phases/SENDBLOOM-23-input-level-gate-truth/23-RESEARCH.md
@.planning/phases/SENDBLOOM-23-input-level-gate-truth/23-UI-SPEC.md
@source/ParameterCurves.h
@source/PluginEditor.cpp
@tests/ParameterCurvesTest.cpp
@tests/V1ContractInputAnchorsTest.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Canonical inputGainDb anchors (CORE-01)</name>
  <files>source/ParameterCurves.h, tests/ParameterCurvesTest.cpp, tests/V1ContractInputAnchorsTest.cpp</files>
  <behavior>
    - inputGainDb(0)==−9, (0.5)==0, (1)==+9 within 1e-4
    - Formula: -9 + 18*smoothstep(norm)
    - Update ParameterCurvesTest away from old +9/−3 expects
  </behavior>
  <read_first>
    - 23-RESEARCH.md Exact Bug Loci §1
    - ADR-V1-08 in MILESTONE-SPEC
    - tests/V1ContractInputAnchorsTest.cpp
  </read_first>
  <action>
    1. Replace `inputGainDb` body with `-9.0f + 18.0f * smoothstep(norm)`.
    2. Update `ParameterCurvesTest` "inputGainDb smoothstep mapping" to ADR anchors.
    3. Confirm `[input-anchors]` passes.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target Tests &amp;&amp; Builds/Tests "[input-anchors]" &amp;&amp; Builds/Tests "[curves][parm]"</automated>
  </verify>
  <acceptance_criteria>
    - `rg "-9.0f \\+ 18.0f" source/ParameterCurves.h` matches
    - Builds/Tests "[input-anchors]" exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Input display calls canonical curve (CORE-02…05)</name>
  <files>source/PluginEditor.cpp</files>
  <behavior>
    - Remove formatSignedDbFromNorm duplicated arithmetic
    - inKnob formatter uses ParameterCurves::inputGainDb
    - Preserve dry-tap-before-Input and ReleaseTruth input_gain contracts
  </behavior>
  <read_first>
    - 23-UI-SPEC.md Display Contract
    - source/PluginEditor.cpp formatSignedDbFromNorm + inKnob formatter
    - tests/ReleaseTruthTest.cpp input_gain case
  </read_first>
  <action>
    1. Include ParameterCurves.h if needed.
    2. Replace formatter with call to `inputGainDb((float)value)` + signed dB string (keep mid near-zero display style).
    3. Run ReleaseTruth input_gain + DryPath smoke.
  </action>
  <verify>
    <automated>Builds/Tests "[release][io][input_gain]" &amp;&amp; Builds/Tests "[chain][od][thd][DryPath]"</automated>
  </verify>
  <acceptance_criteria>
    - `rg "formatSignedDbFromNorm|\\(0\\.5 - value\\) \\* 18" source/PluginEditor.cpp` returns no matches
    - `rg "inputGainDb" source/PluginEditor.cpp` matches
    - Builds/Tests "[release][io][input_gain]" exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
Builds/Tests "[input-anchors]" && Builds/Tests "[curves][parm]" && Builds/Tests "[release][io][input_gain]"
</verification>

<success_criteria>
- CORE-01/02 satisfied; Input anchors green; display uses ParameterCurves
- No regression on dry-tap / input_gain ReleaseTruth
</success_criteria>

<output>
After completion, create `23-01-SUMMARY.md`
</output>
