---
phase: 23-input-level-gate-truth
plan: 03
type: execute
wave: 2
depends_on:
  - 23-01
  - 23-02
files_modified:
  - source/NoiseGate.h
  - tests/V1ContractPostHardRampTest.cpp
  - tests/NoiseGateTest.cpp
autonomous: true
requirements:
  - CORE-10
  - CORE-11
  - CORE-12
  - CORE-13
must_haves:
  truths:
    - "PostHard close uses 0.75 ms ramp — first closed sample gain in (0,1) with |Δ|≤0.05"
    - "PostHard gain ≤ 1e-4 within 1 ms after close command"
    - "Post gate chops wet within 15 ms after silence"
    - "PreSoft long close character unchanged"
    - "[posthard] green; Phase 20–22 greens preserved; shipping-policy still red"
  artifacts:
    - path: "source/NoiseGate.h"
      provides: "ADR-V1-11 PostHard de-click ramp"
      contains: "0.75"
    - path: "tests/V1ContractPostHardRampTest.cpp"
      provides: "CORE-10/11 green"
  key_links:
    - from: "PostHard close"
      to: "gain ramp → 0"
      via: "0.75 ms deterministic ramp (no one-sample snap)"
      pattern: "floorGain|release|0\\.75"
---

<objective>
Replace PostHard one-sample snap with a 0.75 ms deterministic close ramp that still meets the 15 ms chop budget and leaves PreSoft untouched.

Purpose: Flip `[posthard]`; satisfy CORE-10…13 / ADR-V1-11.
Output: De-clicked PostHard + updated NoiseGate tests; regression gates green; shipping still red.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/SENDBLOOM-23-input-level-gate-truth/23-CONTEXT.md
@.planning/phases/SENDBLOOM-23-input-level-gate-truth/23-RESEARCH.md
@source/NoiseGate.h
@tests/V1ContractPostHardRampTest.cpp
@tests/NoiseGateTest.cpp
@tests/PostGateTimingTest.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PostHard 0.75 ms ramp (CORE-10/11)</name>
  <files>source/NoiseGate.h, tests/V1ContractPostHardRampTest.cpp, tests/NoiseGateTest.cpp</files>
  <behavior>
    - Remove `if (floorGain<=0 && !isOpen) gain=0` snap
    - PostHard close ramp = 0.75 ms deterministic (prefer linear step to guarantee ≤1 ms zero)
    - Attack remains 0.5 ms
    - Update NoiseGateTest cases that assumed instant zero on first closed sample
  </behavior>
  <read_first>
    - ADR-V1-11 in MILESTONE-SPEC
    - 23-RESEARCH.md Exact Bug Loci §5 + ramp recommendation
    - tests/V1ContractPostHardRampTest.cpp
  </read_first>
  <action>
    1. Set PostHard release to 0.75 ms ramp semantics; keep attack 0.5 ms.
    2. Implement close ramp that reaches 0 by 0.75 ms (clamp).
    3. Adjust NoiseGateTest "yields zero when closed" / release-budget cases to allow ramp then assert near-zero within 1 ms / 15 ms.
    4. Confirm `[posthard]` passes.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target Tests &amp;&amp; Builds/Tests "[posthard]" &amp;&amp; Builds/Tests "[gate][NoiseGate]"</automated>
  </verify>
  <acceptance_criteria>
    - Builds/Tests "[posthard]" exits 0
    - `rg "gain = 0\\.0f" source/NoiseGate.h` does not match a one-sample snap on close (ramp path only)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 15 ms chop + PreSoft + regression (CORE-12/13)</name>
  <files>tests/PostGateTimingTest.cpp</files>
  <behavior>
    - PostGateTimingTest still chops within 15 ms
    - PreSoft soft-floor case still green
    - Preserve [pressure-release] [oversized-block] [true-bypass] [midi-apvts]
    - Confirm [shipping-policy] still fails
  </behavior>
  <read_first>
    - tests/PostGateTimingTest.cpp
    - tests/NoiseGateTest.cpp PreSoft case
  </read_first>
  <action>
    1. Run PostGateTiming + PreSoft NoiseGate.
    2. Run Phase 20–22 regression tags.
    3. Confirm shipping-policy remains red (do not fix).
  </action>
  <verify>
    <automated>Builds/Tests "[gate][TEST-02]" &amp;&amp; Builds/Tests "[pressure-release]" &amp;&amp; Builds/Tests "[oversized-block]" &amp;&amp; Builds/Tests "[true-bypass]" &amp;&amp; Builds/Tests "[midi-apvts]"; Builds/Tests "[shipping-policy]"; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - Builds/Tests "[gate][TEST-02]" exits 0
    - Regression tags exit 0
    - shipping-policy tag exits non-zero
  </acceptance_criteria>
</task>

</tasks>

<verification>
Builds/Tests "[posthard]" && Builds/Tests "[input-anchors]" && Builds/Tests "[pressure-release]" && Builds/Tests "[oversized-block]" && Builds/Tests "[true-bypass]" && Builds/Tests "[midi-apvts]"
</verification>

<success_criteria>
- CORE-10…13 satisfied; [posthard] + [input-anchors] green; prior greens preserved; shipping red
</success_criteria>

<output>
After completion, create `23-03-SUMMARY.md`
</output>
