---
phase: 23-input-level-gate-truth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - source/ParameterCurves.h
  - source/ParameterSnapshot.h
  - source/SmoothedParameterBank.h
  - source/PluginProcessor.cpp
  - source/ui/AdvancedDrawer.cpp
  - tests/ParameterSnapshotTest.cpp
  - tests/ParallelWetMixerTest.cpp
autonomous: true
requirements:
  - CORE-06
  - CORE-07
  - CORE-08
  - CORE-09
must_haves:
  truths:
    - "Level wetGain = sin(halfPi * norm); dry contribution unity (no dry scaling)"
    - "dryGain / levelDryGain dead state removed from snapshot, bank, and processSpan drain"
    - "Gate Sens keeps input_threshold ID; display shows inputThresholdDb"
    - "LEVEL=0 leaves dry audible; wet return scales only"
  artifacts:
    - path: "source/ParameterCurves.h"
      provides: "ADR-V1-09 wet-only level mapping"
      contains: "dry = 1.0f"
    - path: "source/SmoothedParameterBank.h"
      provides: "No levelDryGain smoother"
      contains: "getNextLevelWetGain"
    - path: "source/ui/AdvancedDrawer.cpp"
      provides: "Gate Sens dB display"
      contains: "inputThresholdDb"
  key_links:
    - from: "levelNorm"
      to: "ParallelWetMixer wetGain"
      via: "snapshot wetGain / levelWetGain smoother"
      pattern: "getNextLevelWetGain"
    - from: "Gate Sens knob"
      to: "inputThresholdDb"
      via: "AdvancedDrawer formatter"
      pattern: "inputThresholdDb"
---

<objective>
Finalize wet-only Level (remove dead dry-gain state) and make Gate Sens display the canonical threshold dB while keeping the existing parameter ID.

Purpose: CORE-06…09 + ADR-V1-09.
Output: Clean Level path + Gate Sens dB readout; updated snapshot/routing tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/SENDBLOOM-23-input-level-gate-truth/23-CONTEXT.md
@.planning/phases/SENDBLOOM-23-input-level-gate-truth/23-RESEARCH.md
@.planning/phases/SENDBLOOM-23-input-level-gate-truth/23-UI-SPEC.md
@source/ParameterCurves.h
@source/ParameterSnapshot.h
@source/SmoothedParameterBank.h
@source/PluginProcessor.cpp
@source/ui/AdvancedDrawer.cpp
@tests/ParameterSnapshotTest.cpp
@tests/ParallelWetMixerTest.cpp
@tests/PluginBasics.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wet-only Level + remove dryGain state (CORE-08/09)</name>
  <files>source/ParameterCurves.h, source/ParameterSnapshot.h, source/SmoothedParameterBank.h, source/PluginProcessor.cpp, tests/ParameterSnapshotTest.cpp, tests/ParallelWetMixerTest.cpp</files>
  <behavior>
    - levelEqualPower sets dry=1.0f, wet=sin(halfPi*norm)
    - Remove dryGain field from ParameterSnapshot (or stop writing/using it)
    - Remove levelDryGain smoother + getNextLevelDryGain
    - Remove processSpan `(void) getNextLevelDryGain()` drain
    - Update tests that assert wet≈dry at 0.5 or dual-scaled equal-power identity
  </behavior>
  <read_first>
    - ADR-V1-09 in MILESTONE-SPEC
    - 23-RESEARCH.md Exact Bug Loci §3
    - PluginProcessor processSpan wetGainScratch fill
  </read_first>
  <action>
    1. Change `levelEqualPower` per ADR-V1-09.
    2. Delete dryGain / levelDryGain plumbing.
    3. Fix ParameterSnapshotTest + ParallelWetMixer dual-scaled helper expectations.
    4. Confirm PluginBasics `level scales wet return not dry tap` still passes.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target Tests &amp;&amp; Builds/Tests "[parm][snapshot]" &amp;&amp; Builds/Tests "[chain][routing]" &amp;&amp; Builds/Tests "level scales wet return"</automated>
  </verify>
  <acceptance_criteria>
    - `rg "levelDryGain|getNextLevelDryGain" source/` returns no matches
    - `rg "dryGain" source/ParameterSnapshot.h source/SmoothedParameterBank.h` returns no matches (or only comments if any)
    - Builds/Tests "[chain][routing][ParallelWet]" exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate Sens canonical dB display (CORE-06/07)</name>
  <files>source/ui/AdvancedDrawer.cpp</files>
  <behavior>
    - Parameter ID remains input_threshold (attachment unchanged)
    - Formatter calls ParameterCurves::inputThresholdDb and shows dB string
    - Knob stays advanced-drawer only
  </behavior>
  <read_first>
    - 23-UI-SPEC.md Gate Sens row
    - source/ui/AdvancedDrawer.cpp gateSensKnob formatter
    - source/ParameterCurves.h inputThresholdDb
  </read_first>
  <action>
    1. Include ParameterCurves.h.
    2. Replace Gate Sens formatter with `inputThresholdDb((float)value)` formatted as dB (2 decimals).
    3. Do not rename parameter ID or move control out of advanced drawer.
  </action>
  <verify>
    <automated>rg "inputThresholdDb" source/ui/AdvancedDrawer.cpp &amp;&amp; cmake --build Builds --config Release --target Tests &amp;&amp; Builds/Tests "[parm]"</automated>
  </verify>
  <acceptance_criteria>
    - `rg "inputThresholdDb" source/ui/AdvancedDrawer.cpp` matches
    - `rg "input_threshold|inputThreshold" source/ParameterIDs.h source/ui/AdvancedDrawer.cpp` shows ID preserved
  </acceptance_criteria>
</task>

</tasks>

<verification>
Builds/Tests "[parm][snapshot]" && Builds/Tests "[chain][routing]" && Builds/Tests "level scales wet return"
</verification>

<success_criteria>
- CORE-06…09 satisfied; dead dry Level state gone; Gate Sens shows dB
</success_criteria>

<output>
After completion, create `23-02-SUMMARY.md`
</output>
