---
phase: 21-realtime-span-engine-true-bypass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - source/PluginProcessor.h
  - source/PluginProcessor.cpp
  - tests/IntegrationAllocScanTest.cpp
  - tests/V1ContractRealtimeSpanTest.cpp
autonomous: true
requirements:
  - RT-01
  - RT-02
  - RT-03
  - RT-05
  - RT-15
must_haves:
  truths:
    - "processBlock performs no dry-buffer resize (setSize API) / resize / assign / push_back / make_unique heap path"
    - "Host block 2048 after prepare(48000,512) keeps nonzero wet and matches 4×512 within 2e-5"
    - "Control-rate reverb params (RT60/dark/authentic latch) update in spans of at most 128 samples"
    - "preparedMaxBlock_ <= 0 returns safely without scratch indexing or allocation"
    - "Dry-only oversized fallback branch is gone"
  artifacts:
    - path: "source/PluginProcessor.cpp"
      provides: "span loop + processSpan; no oversized dry fallback; no setSize in process"
      contains: "kControlQuantum"
    - path: "source/PluginProcessor.h"
      provides: "processSpan declaration / preparedMaxBlock_ safety"
    - path: "tests/IntegrationAllocScanTest.cpp"
      provides: "setSize banned in process bodies"
    - path: "tests/V1ContractRealtimeSpanTest.cpp"
      provides: "RT-05 span quantum + RT-15 unprepared"
    - path: "tests/V1ContractOversizedBlockTest.cpp"
      provides: "existing RT-02/03 contract flips green"
  key_links:
    - from: "processBlock span loop"
      to: "processSpan"
      via: "offset += span with span = min(remaining, preparedMaxBlock_, 128)"
      pattern: "kControlQuantum|processSpan"
    - from: "processSpan"
      to: "GatedBloomChain::processBlock"
      via: "span <= preparedMaxBlock_ so chain does not early-return"
      pattern: "chain.processBlock"
    - from: "prepareToPlay"
      to: "scratch capacity"
      via: "vectors/dryBuffer sized to samplesPerBlock only (no process grow)"
---

<objective>
Refactor PluginProcessor into a no-allocation span engine so oversized host blocks keep full wet processing and match chunked renders — without fixing true-bypass mix order, authentic snapshot edge, or MIDI purity.

Purpose: Flip `[oversized-block]` green and establish ADR-V1-05 span quantum (D-01…D-05).
Output: Span/processSpan production path, alloc-scan setSize ban, realtime span contract tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/SENDBLOOM-21-realtime-span-engine-true-bypass/21-CONTEXT.md
@.planning/phases/SENDBLOOM-21-realtime-span-engine-true-bypass/21-RESEARCH.md
@.planning/phases/SENDBLOOM-21-realtime-span-engine-true-bypass/21-VALIDATION.md
@.planning/MILESTONE-SPEC-v1.0-interaction-truth.md
@source/PluginProcessor.cpp
@source/PluginProcessor.h
@source/GatedBloomChain.h
@source/SchroederTank32.h
@tests/V1ContractOversizedBlockTest.cpp
@tests/IntegrationAllocScanTest.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alloc-scan setSize ban + unprepared safety scaffold (RT-01/15)</name>
  <files>tests/IntegrationAllocScanTest.cpp, tests/V1ContractRealtimeSpanTest.cpp, source/PluginProcessor.h, source/PluginProcessor.cpp</files>
  <behavior>
    - IntegrationAllocScan rejects `.setSize(` (and existing make_unique/resize/push_back/emplace_back) in scanned process bodies including PluginProcessor.cpp
    - Calling processBlock before prepare (preparedMaxBlock_==0) does not crash; output remains finite / extras cleared (RT-15)
    - Tests may fail red until Task 2 removes setSize and adds early return
  </behavior>
  <read_first>
    - 21-RESEARCH.md Exact Bug Loci §1 + Open Questions Q1/Q5/Q6
    - tests/IntegrationAllocScanTest.cpp requireNoAllocTokens + extractProcessBlockBody
    - source/PluginProcessor.cpp prepareToPlay / releaseResources / processBlock oversized branch
    - Milestone ADR-V1-05 + §10.4 preparedMaxBlock_ <= 0 guard
  </read_first>
  <action>
    Per D-01/D-05 and RT-01/15:

    1. Extend `requireNoAllocTokens` in `IntegrationAllocScanTest.cpp` to also fail when the stripped process body contains an AudioBuffer capacity-grow call (the JUCE member whose name is setSize). Keep existing token bans. Do not require absence of MIDI APVTS stores here.
    <!-- planner-discipline-allow: .setSize( -->
    2. Add `tests/V1ContractRealtimeSpanTest.cpp` tagged `[v1][contract][realtime][RT-15]` with an unprepared (or releaseResources then process) safety case: no throw; samples finite.
    3. In `PluginProcessor::processBlock`, add an early return when `preparedMaxBlock_ <= 0` after clearing unused output channels — before any scratch indexing or capacity-grow calls. Per research, safe identity/pass-through.
    4. Do not remove MIDI controller handling. Do not yet delete the oversized dry branch if that blocks compile — prefer Task 2 for the full span refactor; if early return alone is enough for RT-15 green, stop there for this task.
    5. CMake GLOBs tests — no CMakeLists edit.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target Tests && Builds/Tests "[realtime][static][integration]" ; Builds/Tests "[v1][contract][realtime][RT-15]"</automated>
  </verify>
  <done>Alloc scan bans setSize; RT-15 unprepared path is safe; MIDI store left intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Span loop + processSpan; flip oversized-block (RT-02/03/05)</name>
  <files>source/PluginProcessor.h, source/PluginProcessor.cpp, tests/V1ContractRealtimeSpanTest.cpp</files>
  <behavior>
    - No dry-only branch when numSamples > preparedMaxBlock_
    - No dry-buffer resize (setSize API) (or vector assign/resize) inside processBlock
    - prepare(48k,512) then one 2048 block: wetEnergyProxy > 1e-4 and maxAbsDiff vs 4×512 < 2e-5 (existing oversized contract)
    - Control-rate reverb latch changes at most every 128 samples within a host block (RT-05) — assert via observable span quantum constant and/or dual-instance hold test documented in RESEARCH
    - Pressure-release and DryPath remain green
  </behavior>
  <read_first>
    - 21-RESEARCH.md Architecture Patterns target processBlock / processSpan
    - Milestone §10.4–10.5 processSpan steps 1–5 (bypass order may still be pre-ADR until plan 02)
    - source/GatedBloomChain.h early-return when numSamples > maxBlockSize_
    - tests/V1ContractOversizedBlockTest.cpp tolerances
  </read_first>
  <action>
    Per D-01…D-04 / RT-02/03/05:

    1. Introduce `static constexpr int kControlQuantum = 128` (processor translation unit or header).
    2. Replace the single full-block DSP path and the entire `numSamples > preparedMaxBlock_` dry-fallback with an offset loop: `span = min(remaining, preparedMaxBlock_, kControlQuantum)` then `processSpan(buffer, offset, span, snapshot)` (private method). Do not include MIDI event distance in the min (Phase 22 / RT-04).
    3. `processSpan` must: copy only `[offset, offset+span)` into preallocated `dryBuffer` slots `[0, span)`; fill per-sample scratch for `span` using existing smoothers / PressureController; latch span-constant RT60/dark/authentic/gatePreSoft; call `chain.processBlock` with `span` (never larger than preparedMaxBlock_); write engaged mix back to `buffer` at `offset`. Scratch strategy per Q1: members sized in prepare only.
    4. Delete all dry-buffer capacity-grow calls from `processBlock`. Keep prepareToPlay allocation/assign.
    5. Authentic request may still use the existing smoother-edge logic temporarily if relocating it is awkward — Plan 03 owns ADR-V1-07 cleanup. Prefer moving authentic edge to block-start if it simplifies the span loop (acceptable early).
    6. Add RT-05 coverage in `V1ContractRealtimeSpanTest.cpp` tagged `[v1][contract][realtime][RT-05]` proving control quantum ≤128 (constant present + behavioral hold: e.g. size smoother mid-ramp produces distinct wet when comparing quantum-1 vs quantum-128 latch spacing within documented tolerance — or source-level guarantee plus integration that block>128 still processes wet continuously). Prefer a concrete audio assertion over comments-only.
    7. Do not change true-bypass Output/mix order yet if that expands scope — leave CORE-14 red for Plan 02. Do not touch PostHard/Input/shipping brand strings.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target Tests && Builds/Tests "[oversized-block]" && Builds/Tests "[v1][contract][realtime]" && Builds/Tests "[realtime][static][integration]" && Builds/Tests "[pressure-release]" && Builds/Tests "[DryPath]"</automated>
  </verify>
  <done>`[oversized-block]` green; no process setSize; span quantum ≤128; pressure/DryPath still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host → processBlock | Untrusted numSamples, channel counts, buffer contents |
| Host → MIDI buffer | Untrusted event stream (mutation deferred Phase 22) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Denial of Service | PluginProcessor::processBlock | high | mitigate | No heap in process; preparedMaxBlock_≤0 early return; span ≤ prepared capacity |
| T-21-02 | Tampering | Oversized / span path | medium | mitigate | Clamp span to preparedMaxBlock_ and kControlQuantum; never grow buffers on audio thread |
| T-21-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages in this plan |
</threat_model>

<verification>
Builds/Tests "[oversized-block]" passes; alloc scan passes with setSize ban; realtime RT-05/15 pass; pressure-release and DryPath still pass.
</verification>

<success_criteria>
Dry fallback and process setSize gone; 2048@512 wet parity green; control spans ≤128; unprepared safe.
</success_criteria>

<output>
Create `.planning/phases/SENDBLOOM-21-realtime-span-engine-true-bypass/21-01-SUMMARY.md` when done
</output>
