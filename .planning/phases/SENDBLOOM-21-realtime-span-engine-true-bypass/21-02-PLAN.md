---
phase: 21-realtime-span-engine-true-bypass
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - source/PluginProcessor.cpp
  - source/BypassCrossfade.h
  - tests/BypassCrossfadeTest.cpp
  - tests/V1ContractTrueBypassTest.cpp
autonomous: true
requirements:
  - CORE-14
  - CORE-15
  - CORE-16
  - CORE-17
  - CORE-18
must_haves:
  truths:
    - "Settled bypass preserves each input channel within 1e-6 despite Output +6 dB and non-unity Input/Level"
    - "Bypass dry side is original per-channel input; Output gain applies only on the engaged path before crossfade"
    - "Bypass transitions remain click-bounded over the existing 5 ms smoother"
    - "Engaged (non-bypass) mono-first dual-mono behavior is unchanged when extended_stereo is off"
    - "[true-bypass] contract green; [oversized-block] remains green"
  artifacts:
    - path: "source/PluginProcessor.cpp"
      provides: "ADR-V1-10 mix order inside processSpan"
    - path: "source/BypassCrossfade.h"
      provides: "mixSample(originalDry, processed, engagedMix) helper"
    - path: "tests/V1ContractTrueBypassTest.cpp"
      provides: "CORE-14..16 green proof"
    - path: "tests/BypassCrossfadeTest.cpp"
      provides: "CORE-17 click-bound coverage"
  key_links:
    - from: "engaged path"
      to: "OutputStage::processSample"
      via: "gain applied before bypass crossfade only"
      pattern: "OutputStage::processSample"
    - from: "BypassCrossfade::mixSample"
      to: "buffer writeback"
      via: "final = dry*(1-mix) + engaged*mix per channel"
      pattern: "mixSample|engagedMix|bypassWet"
---

<objective>
Implement ADR-V1-10 true bypass: compute output-gained engaged audio first, then crossfade against original per-channel dry so settled bypass is channel-preserving unity.

Purpose: Flip `[true-bypass]` green (D-06…D-08 / CORE-14…18) without regressing span/oversized or Phase 20 pressure.
Output: Correct processSpan mix order, BypassCrossfade helper, green true-bypass + click tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/SENDBLOOM-21-realtime-span-engine-true-bypass/21-CONTEXT.md
@.planning/phases/SENDBLOOM-21-realtime-span-engine-true-bypass/21-RESEARCH.md
@.planning/phases/SENDBLOOM-21-realtime-span-engine-true-bypass/21-01-PLAN.md
@.planning/MILESTONE-SPEC-v1.0-interaction-truth.md
@source/PluginProcessor.cpp
@source/BypassCrossfade.h
@source/OutputStage.h
@source/ParallelWetMixer.h
@tests/V1ContractTrueBypassTest.cpp
@tests/BypassCrossfadeTest.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ADR-V1-10 mix order — flip true-bypass (CORE-14…16,18)</name>
  <files>source/PluginProcessor.cpp, source/BypassCrossfade.h, tests/V1ContractTrueBypassTest.cpp</files>
  <behavior>
    - After settle (>5 ms / smoother not smoothing): stereo distinct L/R inputs reproduce within 1e-6 on each channel with bypass=1 and outputGain=+6 dB (existing contract)
    - Settled bypass ignores Input/Distn/Gate/Level/Output (contract setup already stresses Output)
    - When bypass engagedMix is mid-ramp, both dry and engaged contribute; when mix=0, dry only
    - With bypass off and extended_stereo off, engaged path remains mono-first dual-mono (same wet feed both outs) — do not invent full stereo wet engine (CORE-18)
  </behavior>
  <read_first>
    - 21-RESEARCH.md Exact Bug Loci §2 + ADR-V1-10 text
    - Milestone §10.5 step 6 and §10.6 BypassCrossfade::mixSample sketch
    - Current processSpan / mix loop in PluginProcessor.cpp after Plan 01
    - tests/V1ContractTrueBypassTest.cpp
  </read_first>
  <action>
    Per D-06/D-08 / CORE-14…16/18 and ADR-V1-10:

    1. Add `BypassCrossfade::mixSample(float originalDry, float processed, float engagedMix)` returning `originalDry * (1 - engagedMix) + processed * engagedMix`. Keep existing buffer helper or refactor it to call mixSample per sample/channel.
    2. In `processSpan` final writeback: build engaged sample(s) with ParallelWetMixer + OutputStage using outputGain scratch; then mix with original per-channel dry from dryBuffer using bypass/engagedMix scratch. Never apply OutputStage after the bypass crossfade.
    3. For extended_stereo on: per-channel dry taps into wet mix as today, then Output on engaged, then per-channel bypass mix.
    4. For extended_stereo off: engaged path may still mono-sum for the wet engine feed and dual-mono engaged output; bypass dry side must still use each channel's original dry sample (this is the true-bypass fix). Do not change non-bypass engaged mono-first character (CORE-18).
    5. Do not weaken V1ContractTrueBypassTest tolerances. Optionally add a second assert that L and R remain unequal under anti-phase/distinct inputs when settled.
    6. Do not modify MIDI purity, PostHard, Input curves, or shipping brand assets.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target Tests && Builds/Tests "[true-bypass]" && Builds/Tests "[oversized-block]" && Builds/Tests "[pressure-release]"</automated>
  </verify>
  <done>`[true-bypass]` green; oversized and pressure-release still green; Output not applied after bypass mix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Click-bounded bypass transitions (CORE-17)</name>
  <files>source/BypassCrossfade.h, tests/BypassCrossfadeTest.cpp, source/PluginProcessor.cpp</files>
  <behavior>
    - Existing BypassCrossfade 5 ms ramp adjacent-delta bound remains green
    - Optional integration: toggle bypass mid-stream keeps max adjacent delta bounded (same order as EngineCrossfade click metric, e.g. < 1.0 absolute on unit-ish signals)
    - Smoother configuration remains 5 ms in SmoothedParameterBank (do not shorten/lengthen)
  </behavior>
  <read_first>
    - tests/BypassCrossfadeTest.cpp
    - source/SmoothedParameterBank.h bypassWetMix reset(0.005)
    - tests/EngineCrossfadeTest.cpp click metric style for optional integration case
  </read_first>
  <action>
    Per D-07 / CORE-17:

    1. Update BypassCrossfadeTest to cover `mixSample` unit behavior (dry when mix=0, processed when mix=1, linear blend at 0.5) and keep the existing adjacent-delta ramp test green after any helper refactor.
    2. If the integration click case is cheap, add one `[parm][bypass]` or `[v1][contract]` case that engages/disengages bypass across a block and bounds adjacent sample delta; otherwise rely on existing unit ramp test — document choice in SUMMARY.
    3. Do not alter bypass smoother seconds. Do not regress true-bypass or oversized.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target Tests && Builds/Tests "[parm][bypass]" && Builds/Tests "[true-bypass]" && Builds/Tests "[oversized-block]"</automated>
  </verify>
  <done>CORE-17 click-bounded proof green; true-bypass and oversized still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host parameters → bypass mix | APVTS bypass drives engagedMix smoother |
| Audio buffer dry copy → output | Must not leak processed gain when bypassed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-03 | Tampering / Elevation | True bypass mix | medium | mitigate | Output gain only on engaged path; settled mix uses original dry per channel |
| T-21-04 | Denial of Service | Bypass transition | low | mitigate | Keep 5 ms smoother; bound adjacent deltas in tests |
| T-21-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
Builds/Tests "[true-bypass]" and "[parm][bypass]" pass; oversized and pressure-release remain green.
</verification>

<success_criteria>
Settled bypass is per-channel unity ignoring Output; transitions click-bounded; mono-first engaged unchanged.
</success_criteria>

<output>
Create `.planning/phases/SENDBLOOM-21-realtime-span-engine-true-bypass/21-02-SUMMARY.md` when done
</output>
