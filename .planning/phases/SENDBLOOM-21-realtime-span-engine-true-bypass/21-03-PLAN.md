---
phase: 21-realtime-span-engine-true-bypass
plan: 03
type: execute
wave: 3
depends_on:
  - 21-01
  - 21-02
files_modified:
  - source/PluginProcessor.h
  - source/PluginProcessor.cpp
  - source/SmoothedParameterBank.h
  - tests/V1ContractAuthenticTransitionTest.cpp
  - tests/RealtimeStressTest.cpp
  - tests/EngineCrossfadeTest.cpp
autonomous: true
requirements:
  - RT-08
  - RT-09
  - RT-10
  - RT-11
  - RT-12
  - RT-13
  - RT-14
must_haves:
  truths:
    - "Each authentic_color APVTS change requests exactly one engine crossfade toward the new target (ADR-V1-07)"
    - "getLatencySamples() remains 0 across authentic transitions (ADR-003 Path B)"
    - "Crossfade begins within the first processBlock after the parameter change; rapid toggles converge to final target"
    - "Crossfade completion resets only the idle engine and performs no heap allocation"
    - "10k-block stress with authentic toggles, bypass toggles, and oversized blocks stays finite"
  artifacts:
    - path: "source/PluginProcessor.cpp"
      provides: "requestedAuthenticColor_ snapshot-edge request at block start"
    - path: "tests/V1ContractAuthenticTransitionTest.cpp"
      provides: "RT-08/10/11 (+ latency) contract coverage"
    - path: "tests/RealtimeStressTest.cpp"
      provides: "RT-14 strengthened stress"
  key_links:
    - from: "snapshot.authenticColor"
      to: "chain.requestEngineCrossfade"
      via: "block-start inequality vs requestedAuthenticColor_"
      pattern: "requestedAuthenticColor_|requestEngineCrossfade"
    - from: "SchroederTank32 crossfade completion"
      to: "hostEngine.reset / fixedRate_.reset"
      via: "reset only idle side"
      pattern: "targetIsFixedEngine|reset\\(\\)"
    - from: "updateReportedLatency"
      to: "setLatencySamples(0)"
      via: "always Path B"
---

<objective>
Replace authentic smoothed-threshold edge detection with ADR-V1-07 once-per-change snapshot requests, prove latency/crossfade/idle-reset contracts, and keep 10k stress finite under authentic/bypass/oversized churn.

Purpose: Close RT-08…14 (D-09…D-10) after span + true bypass land.
Output: Snapshot-edge authentic wiring, authentic contract tests, strengthened RealtimeStressTest.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/SENDBLOOM-21-realtime-span-engine-true-bypass/21-CONTEXT.md
@.planning/phases/SENDBLOOM-21-realtime-span-engine-true-bypass/21-RESEARCH.md
@.planning/phases/SENDBLOOM-21-realtime-span-engine-true-bypass/21-01-PLAN.md
@.planning/phases/SENDBLOOM-21-realtime-span-engine-true-bypass/21-02-PLAN.md
@.planning/MILESTONE-SPEC-v1.0-interaction-truth.md
@source/PluginProcessor.cpp
@source/PluginProcessor.h
@source/SmoothedParameterBank.h
@source/SchroederTank32.h
@source/EngineCrossfade.h
@tests/EngineCrossfadeTest.cpp
@tests/RealtimeStressTest.cpp
@tests/LatencyTest.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ADR-V1-07 authentic snapshot edge + contracts (RT-08…13)</name>
  <files>source/PluginProcessor.h, source/PluginProcessor.cpp, source/SmoothedParameterBank.h, tests/V1ContractAuthenticTransitionTest.cpp, tests/EngineCrossfadeTest.cpp</files>
  <behavior>
    - Toggling authentic_color once between blocks causes exactly one requestEngineCrossfade toward the new bool target (observable via isCrossfading becoming true in the first subsequent processBlock, or a test seam / call counter if already present — prefer production-observable isCrossfading / wet divergence start)
    - Rapid block-to-block toggles leave the engine targeting the final snapshot value after settles (RT-11)
    - getLatencySamples() == 0 before/during/after toggle (RT-09)
    - No new heap tokens in process/crossfade completion paths (RT-13); idle engine reset path unchanged (RT-12)
    - Remove dependence on 15 ms authenticColorTarget 0.5 threshold for requesting crossfades
  </behavior>
  <read_first>
    - Milestone ADR-V1-07
    - 21-RESEARCH.md Exact Bug Loci §3 + already-good §5
    - source/PluginProcessor.cpp updateReportedLatency + current authentic edge
    - source/SmoothedParameterBank.h authenticColorTarget
    - source/SchroederTank32.h requestEngineCrossfade + completion reset
    - tests/LatencyTest.cpp LAT-03 cases
  </read_first>
  <action>
    Per D-09/D-10 / RT-08…13:

    1. Add `bool requestedAuthenticColor_` on PluginProcessor; initialize in prepareToPlay from APVTS authentic_color.
    2. At the start of processBlock (after snapshot capture, before span loop): if `snapshot.authenticColor != requestedAuthenticColor_`, call `chain.requestEngineCrossfade(snapshot.authenticColor)`, `updateReportedLatency(...)`, then assign `requestedAuthenticColor_ = snapshot.authenticColor`. Exactly one request per differing snapshot (RT-08). This begins crossfade in the first processed block after the change (RT-10).
    3. Remove per-sample / smoother 0.5-threshold authentic request logic and `crossfadeEdgeHandled` / `lastAuthenticColorSmoothed_` if obsolete. Stop using authenticColorTarget smoother as the request trigger. Discretion: may leave unused smoother members alone if removing them risks wide churn — but they must not drive requests. Prefer deleting dead authentic smoother usage from the process path.
    4. Add `tests/V1ContractAuthenticTransitionTest.cpp` tagged `[v1][contract][authentic][RT-08]` (and RT-09/10/11 as appropriate) covering the behaviors above. Keep `EngineCrossfadeTest` / LatencyTest green; extend only if needed for first-block-start assertion.
    5. Do not change EngineCrossfade fade seconds. Do not reset both engines on completion. Do not touch MIDI store.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target Tests && Builds/Tests "[v1][contract][authentic]" && Builds/Tests "[chain][latency]" && Builds/Tests "[verb][EngineCrossfade]" && Builds/Tests "[true-bypass]" && Builds/Tests "[oversized-block]"</automated>
  </verify>
  <done>Authentic snapshot-edge requests only; latency 0; true-bypass and oversized still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strengthen 10k stress — authentic/bypass/oversized (RT-14)</name>
  <files>tests/RealtimeStressTest.cpp</files>
  <behavior>
    - Existing 10k finite stress cases remain green
    - At least one stress case includes authentic toggles, bypass toggles, and host blocks larger than preparedMaxBlock_ (e.g. prepare 512, occasionally process 1024/2048) with all samples finite and peak bounded
    - Still-red Phase 19 contracts (midi-apvts, shipping-policy) remain failing when run — do not “fix” them
  </behavior>
  <read_first>
    - tests/RealtimeStressTest.cpp existing cases and tags
    - 21-VALIDATION.md RT-14 row + still-red policy
  </read_first>
  <action>
    Per D-10 / RT-14:

    1. Extend or add a `[realtime][integration][stress]` case that prepares at 512 (or 1024), runs ≥10_000 blocks with varying sizes including oversized (> prepared), toggles authentic_color and bypass periodically, and asserts finite samples + bounded peak (reuse existing peak<4 style).
    2. Keep runtime reasonable (< ~3–5 min). Do not disable other stress tests.
    3. Final verification must also confirm midi-apvts and shipping-policy filters still exit non-zero (still intentionally red). Confirm pressure-release still green.
  </action>
  <verify>
    <automated>cmake --build Builds --config Release --target Tests && Builds/Tests "[realtime][stress]" && Builds/Tests "[pressure-release]" && Builds/Tests "[true-bypass]" && Builds/Tests "[oversized-block]" && (Builds/Tests "[v1][contract][midi-apvts]"; test $? -ne 0) && (Builds/Tests "[v1][contract][shipping-policy]"; test $? -ne 0)</automated>
  </verify>
  <done>RT-14 stress covers authentic/bypass/oversized finite; Phase 21 contracts green; midi/shipping still red.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| APVTS authentic_color → engine select | Host/UI parameter edge triggers DSP crossfade |
| Audio thread during crossfade | Dual engines run; must stay allocation-free |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-05 | Denial of Service | Authentic crossfade request storm | medium | mitigate | One request per snapshot edge; EngineCrossfade retargets without heap |
| T-21-06 | Denial of Service | Realtime stress / oversized+toggles | medium | mitigate | Span capacity limits + finite/peak asserts in stress tests |
| T-21-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
Authentic contracts + latency + EngineCrossfade green; stress finite; oversized/true-bypass/pressure green; midi/shipping still red.
</verification>

<success_criteria>
ADR-V1-07 authentic requests; RT-08…14 proven; Phase 21 contract pair green without touching deferred defects.
</success_criteria>

<output>
Create `.planning/phases/SENDBLOOM-21-realtime-span-engine-true-bypass/21-03-SUMMARY.md` when done
</output>
